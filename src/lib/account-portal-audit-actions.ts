/**
 * Epic 15 — **`ops_audit_log.action`** values for **account portal** (`actor_role = account_portal`).
 * Must stay in sync with **`ops_audit_log_account_portal_insert`** policy whitelist in migrations.
 */
export const OPS_AUDIT_ACTION_ACCOUNT_COMMS_PREFERENCES_UPDATED =
	'account_comms_preferences_updated' as const
