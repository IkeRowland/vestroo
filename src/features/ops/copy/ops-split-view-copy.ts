/**
 * Strings for **`OpsSplitView`** / **`OpsDetailRail`** (Story 17.9 / FE.17.5) — NFR.17.8.
 */
export const opsSplitViewCopy = {
	closeDetailAriaLabel: 'Close detail',
	detailPanelAriaLabel: 'Record detail',
	/** Radix **`DialogTitle`** (visually hidden) for the **`< lg`** drawer shell. */
	detailSheetDialogTitle: 'Detail panel',
} as const
