/**
 * Shell copy for **`/ops/fleet`** layout (Drivers tab **`/ops/fleet/drivers`**, Vehicles **`/ops/fleet/vehicles`**, categories).
 */
export const opsFleetShellCopy = {
	pageTitle: 'Fleet',
	pageDescription:
		'Manage vehicles, planned driver shifts on the Drivers tab (scheduled coverage — not the same as trips on Trips), vehicle categories, plates, and operational status. Archive removes a vehicle from the public catalogue without deleting historical trips.',
} as const
