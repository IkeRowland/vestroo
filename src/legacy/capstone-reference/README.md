# Capstone reference (third-party)

TypeScript copied from `docs/capstone-reference/` for **reference while porting** to Vestroo. It is **not** part of the production Next.js bundle (see `tsconfig.json` / ESLint ignores).

Vestroo is a premium South African transport operator (corporate, VIP, tours) per `docs/Overview Vestroo-Pty-Ltd.pdf`. This tree may still contain legacy identifiers from the original import; prefer Vestroo naming and domain terms in all new code.

- `backend-modules/` — NestJS modules (reference)
- `backend-share/` — shared utilities (reference)
- `frontend-driver/` — React Native chauffeur app (reference)

Canonical import folder: `docs/capstone-reference/`.
