# Capstone backend — module traceability matrix (FE.5.10 / BE.6.1)

**SH.9.1 product gate (go / no-go for patterned or capacity-managed corporate shuttle):** authoritative record — **[Epic 9 — SH.9.1](epic-9.md#sh-9-1)**.

**Living artifact** for **FE.5.10** (Epic 5) and **BE.6.1** (Epic 6): maps each **domain** Nest module wired in vendored **[`docs/capstone-reference/backend/src/app.module.ts`](capstone-reference/backend/src/app.module.ts)** to **Vestroo** capabilities in **corporate shuttle** vocabulary (**NFR.5.4**), plus indexes **`cline_docs/`** topics and **`document/`** diagram assets. **Staff ops** data-owner checklist: **§ FE.5.11 / BE.6.7** (heading *staff ops data owners* below) — narrative owner **[Epic 6 — BE.6.7](epic-6.md#be67-data-and-server-actions-for-staff-ops-fe511)**. **Screen- and route-level** detail stays in **[FE.5.4 — admin → ops](capstone-admin-to-ops-mapping.md)**, **[FE.5.5 — manager](capstone-manager-to-vestroo-mapping.md)**, **[FE.5.6 — driver → field](capstone-driver-to-field-mapping.md)**.

**Integration posture** (auth, HTTP clients, storage, realtime — **no** duplicate tables here): **[Capstone reference — stack & integration](capstone-reference-stack-integration.md)** (**FE.5.9**, **NFR.5.3**).

**Nest REST → Vestroo entry points:** **[Capstone Nest REST → Vestroo mapping](capstone-nest-rest-to-vestroo-mapping.md)** (**BE.6.3** / Story 6.3) maps each reference **`@Controller`** group to Server Actions, Route Handlers, or **deferred** / **N/A** — without duplicating this matrix.

**Traveller search → checkout (CORE.6.4):** **[Core traveller flow parity](core-traveller-flow-parity.md)** (**Story 6.4**) describes **search → quote → booking → PayFast → confirmation** across **Search**, **Pricing**, **Booking**, **Trip**, and **Checkout** reference modules; behaviour detail stays in **[front-end API interaction](front-end-api-interaction.md)**.

**Auth / Keytoken / OTP (BE.6.5):** **[Capstone Auth, Keytoken, and OTP parity](capstone-auth-keytoken-otp-parity.md)** (**Story 6.5**) contrasts reference **Nest JWT + Mongo KeyToken + OTP verify** with **Supabase Auth**, **`src/lib/supabase/server.ts`** clients, and **ops/field** gates — **JWT-in-`localStorage` / browser refresh-token** defaults **forbidden** without **[ADR](adr/)**.

**Last updated:** 2026-04-18 (**SH.9.6** deferred capstone subsection; **`cline_docs`** / **`document/`** index); 2026-04-11 (**Story 7.6** / **RT.7.6**; **Story 7.5** / **RT.7.5**; **Story 7.4** / **RT.7.4**; **Story 7.3** / **RT.7.3**; **Story 7.2** / **RT.7.2**; **Story 7.1** / **RT.7.1**; **Story 6.7** / **BE.6.7**; **6.6** / **BE.6.6**; **6.5** / **BE.6.5**; **6.4** / **CORE.6.4**; **6.3** / **BE.6.3**; **6.2** / **BE.6.2**; **6.1** / **BE.6.1**; **FE.5.11** / Story 5.11)

---

## Deviation analysis (canonical path — AC1)

Before authoring, **`docs/epic-6.md`** (**BE.6.1**) and **`docs/index.md`** were checked: **no** separate matrix file existed. **`docs/capstone-backend-module-matrix.md`** is the **single canonical** doc for **domain module rows**, **`cline_docs`** indexing, and **`document/`** diagram coverage. **[Epic 5](epic-5.md)** (**FE.5.10**) and **[Epic 6](epic-6.md)** (**BE.6.1**) both point here so teams do not maintain **two competing** matrices.

---

<a id="sh-9-6-deferred-capstone-modules"></a>

## Deferred capstone modules (SH.9.6)

**Epic 9 — [SH.9.6](epic-9.md#sh-9-6)** is the normative home for traceability of the **six** Nest modules below. **[`docs/capstone-reference`](capstone-reference/)** (including Nest **`Bus*`** names and **`cline_docs`** filenames) is **reference material** for a **campus / line-haul demo stack** — **not** Vestroo product specifications (**reference ≠ product**; **[`capstone-reference-path-map.md`](capstone-reference-path-map.md)**). **[`#sh-9-1`](epic-9.md#sh-9-1)** records the optional **patterned / capacity-managed corporate shuttle** **go** / **no-go** decision and contextualises **SH.9.2–SH.9.5**; it is **not** a prerequisite for completing this mapping — **no-go** remains a **complete** outcome when rows below stay **honest** (**[Epic 9](epic-9.md)** rules **(c)**–**(d)** at **`#sh-9-1`**).

### Summary — six reference Nest modules (matrix rows)

| Nest module | Matrix row anchor | Status | **[`epic-9.md#sh-9-1`](epic-9.md#sh-9-1)** | One-line rationale (corporate shuttle) |
| ----------- | ----------------- | ------ | ---------------------------------------- | -------------------------------------- |
| `BusStopModule` | [Authoritative domain module list](#authoritative-domain-module-list-24-imports) — row **`BusStopModule`** | not applicable | Gate context for optional **patterned** work — **not** a blocker for this table | Vestroo targets **booked-by-job** and **approved pickup/drop-off** for clients — **not** a public **stop network** operator. |
| `BusRouteModule` | [Authoritative domain module list](#authoritative-domain-module-list-24-imports) — row **`BusRouteModule`** | not applicable | Same | **Line-haul** graph in the reference demo does **not** map to default **corporate shuttle** IA (**NFR.5.4**). |
| `BusScheduleModule` | [Authoritative domain module list](#authoritative-domain-module-list-24-imports) — row **`BusScheduleModule`** | not applicable | Same | **Published timetable** semantics in the capstone demo are **out of scope** unless product adopts a **patterned** offering under **`#sh-9-1`**. |
| `TicketModule` | [Authoritative domain module list](#authoritative-domain-module-list-24-imports) — row **`TicketModule`** | not applicable | Same | Default **B2B shuttle** does **not** ship capstone-style **transit ticketing**; **`ticketingSystem.md`** is a **traceability key** only. |
| `BusTrackingModule` | [Authoritative domain module list](#authoritative-domain-module-list-24-imports) — row **`BusTrackingModule`** | not applicable | Same | Live fleet context is **`vehicle_trackings`** + assignments — **not** “bus line” **operator** semantics (**NFR.5.4**). |
| `DriverBusScheduleModule` | [Authoritative domain module list](#authoritative-domain-module-list-24-imports) — row **`DriverBusScheduleModule`** | not applicable | Same | Chauffeur **roster / run** work maps to **`chauffeur_schedules`** and **dispatch** — **not** capstone **driver ↔ bus timetable** joins as a default story. |

**Optional (#sh-9-1 go — recorded 2026-04-17):** Where **[SH.9.2](stories/9.2.story.md)–[SH.9.5](stories/9.5.story.md)** shipped patterned work, see **[ADR 0002](adr/0002-patterned-shuttle-domain-sh9-2.md)**, **[ADR 0003](adr/0003-service-run-capacity-holds-sh9-3.md)**, **[ADR 0004](adr/0004-patterned-run-realtime-sh9-4.md)**, **[ADR 0005](adr/0005-patterned-checkout-sh9-5.md)**, **[patterned-run-live-ops-mapping.md](patterned-run-live-ops-mapping.md)**, **[patterned-checkout-vst6-delta.md](patterned-checkout-vst6-delta.md)** — **without** changing **`not applicable`** above into **implemented** for Nest **`Bus*`** / **`Ticket*`** names (those remain **reference keys**).

---

## Authoritative domain module list (24 imports)

The following **Nest `Module` classes** appear as **domain** `imports` in **`app.module.ts`** after infrastructure modules (count **= 24**). **Reference-only** names below trace the vendored repo; **Vestroo** descriptions use shuttle terminology.

**SH.9.6:** The six capstone transit-demo modules (**`BusStopModule`**, **`BusRouteModule`**, **`BusScheduleModule`**, **`TicketModule`**, **`BusTrackingModule`**, **`DriverBusScheduleModule`**) are summarized at **[§ Deferred capstone modules (SH.9.6)](#sh-9-6-deferred-capstone-modules)** ([Epic 9](epic-9.md#sh-9-6)); full rows remain in the table below.

**BE.6.1 status enum:** Each row uses **implemented**, **partial**, **not applicable**, or **deferred** (per **[Epic 6 — BE.6.1](epic-6.md)**). No legacy **not started** remains in the domain table.

| Reference module (Nest) | Vestroo capability (epic / VST / surface) | Status | Supabase (summary) | RLS summary | Server Actions / Route Handlers (examples) | Notes |
| ------------------------| -------------------------------------------| ------ | ------------------- | ----------- | ------------------------------------------- | ----- |
| `VehicleCategoryModule` | **Fleet:** vehicle classes/types for **corporate shuttle** ops — **FE.5.11** / **BE.6.7** | partial | `vehicle_categories` | Staff read on ops paths; write policies **TBD** for staffed category CRUD | **TBD** dedicated category mutations — today read in **`/ops/vehicles`** context | Reference admin **`/category`** → no dedicated **`/ops/category`**; see [admin mapping](capstone-admin-to-ops-mapping.md). **Migrations:** `supabase/migrations/20260402131358_vin_shuttle_vehicles_and_pricing.sql`, `20260402131457_vin_shuttle_rls_enable_and_is_staff.sql`, `20260402133646_vestroo_rls_policies_vestroo_domain.sql`. **Staff ops data owners:** § **FE.5.11 / BE.6.7 — staff ops data owners** (this doc); **[Story 6.7](stories/6.7.story.md)**. |
| `VehiclesModule` | **Fleet** records, utilisation context — **VST-7**, **FE.5.11** | partial | `vehicles`, `vehicle_pricings`, related joins | Staff/chauffeur policies per role | Ops fleet reads; **TBD** full CRUD vs list-only | **`/ops/vehicles`** — [admin mapping](capstone-admin-to-ops-mapping.md). **Not** Mongo **Mongoose** models — **Postgres + RLS** (**NFR.5.3**). **Migrations:** `20260402131358_vin_shuttle_vehicles_and_pricing.sql`, `20260402131457_vin_shuttle_rls_enable_and_is_staff.sql`, `20260402133646_vestroo_rls_policies_vestroo_domain.sql`. **Staff ops data owners:** § **FE.5.11 / BE.6.7 — staff ops data owners** (this doc); **[Story 6.7](stories/6.7.story.md)**. |
| `UsersModule` | **Staff** and **chauffeur** identities vs reference “users” grid — **BE.6.5**, **BE.6.7** | partial | `auth.users`, `profiles` | RLS by role; **no** client-trusted admin grid | **`requireOpsStaffPage`**, **`requireChauffeurPage`**; profile reads in shell | No **`/ops/users`** parity (**FE.5.11** row 1); Supabase Auth — **not** reference JWT-in-browser. **`auth.users`:** platform-managed (no app migration). **`public.profiles`:** `20260402131346_vin_shuttle_core_schema.sql`, `20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`, `20260412120000_vst12_compliance_incidents_documents_retention.sql`. **Staff ops data owners:** § **FE.5.11 / BE.6.7 — staff ops data owners** (this doc); **[Story 6.7](stories/6.7.story.md)**. |
| `AuthModule` | **Supabase Auth** session model — **BE.6.5** | partial | Supabase Auth + `profiles` | Server gates + RLS | **`/ops/login`**, **`/field/login`**; session via SSR clients | Reference **Nest JWT** patterns are **reference-only**; see [FE.5.9 appendix](capstone-reference-stack-integration.md). **`auth.*`:** Supabase-managed. **`profiles` + RLS:** `20260402131346_vin_shuttle_core_schema.sql`, `20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`. **BE.6.5 / Story 6.5:** [capstone-auth-keytoken-otp-parity.md](capstone-auth-keytoken-otp-parity.md). **Forbidden:** **JWT-in-`localStorage`** / **browser refresh-token-as-default** → **ADR** in **[adr/](adr/)**. **Staff ops data owners:** § **FE.5.11 / BE.6.7 — staff ops data owners** (this doc); **[Story 6.7](stories/6.7.story.md)** (invite/disable flows **TBD** in row 1). |
| `KeytokenModule` | Refresh / key rotation in reference | partial | **none** mapped 1:1 | **TBD** if product needs device/session table | **TBD** | Vestroo uses **Supabase session**; **do not** copy reference **Keytoken** semantics without ADR. **BE.6.5 / Story 6.5:** [capstone-auth-keytoken-otp-parity.md](capstone-auth-keytoken-otp-parity.md). **Forbidden:** **JWT-in-`localStorage`** / **header refresh-token port** as first-party default → **ADR**. |
| `OtpModule` | OTP / verification flows | partial | **TBD** provider tables or Supabase-native | Rate-limit / abuse **TBD** | **TBD** | Align with **BE.6.5**; no Nest OTP port as default. **Migrations:** **N/A** — no dedicated **`public.*`** OTP tables in repo yet; cite files when introduced. **BE.6.5 / Story 6.5:** [capstone-auth-keytoken-otp-parity.md](capstone-auth-keytoken-otp-parity.md) (**provider / rate-limit TBD**). **Forbidden:** **JWT-in-`localStorage`** / **browser refresh** patterns as default → **ADR**. |
| `PricingModule` | **Quote / pricing** for bookings — **CORE.6.4**, **VST-6** | partial | `vehicle_pricings`, quote-related inputs | Staff vs public read differences | **`calculateQuote`**, **`calculateHourlyQuote`**, **`calculateExperienceQuote`** (`src/actions/`) | Reference **`/money`**, **`/cal`** → booking + fleet context; see [admin mapping](capstone-admin-to-ops-mapping.md). **Migrations:** `20260402131358_vin_shuttle_vehicles_and_pricing.sql`, `20260402133646_vestroo_rls_policies_vestroo_domain.sql`. **CORE.6.4 / Story 6.4:** [core-traveller-flow-parity.md](core-traveller-flow-parity.md). **Quote reconcile** → [front-end-api-interaction.md](front-end-api-interaction.md) (**Quote integrity**). **Staff ops data owners:** § **FE.5.11 / BE.6.7 — staff ops data owners** (this doc); **[Story 6.7](stories/6.7.story.md)** (row **4** service-area / pricing config). |
| `ScenicRouteModule` | **Service routes**, corridors, **approved pickup/drop-off** geometry — **FE.5.11** / **BE.6.7** | partial | `service_routes`, `service_route_points`, `service_points` (see [data models](data-models.md)) | Staff vs customer read policies on network tables (**Epic 9** transit-demo tables remain **deferred** for product) | **TBD** map authoring **`/ops/*`** | Reference **`ScenicRoute`** names a capstone domain; Vestroo = **shuttle route/area** authoring — **not** a public fixed-line network (**NFR.5.4**). **Migrations (network graph, renamed from capstone bus vocabulary):** `20260402131410_vin_shuttle_bus_network.sql`, `20260402133631_vestroo_rename_tables_and_columns.sql`; **RLS** on these surfaces via historical **`vin_shuttle_rls_policies_*.sql`** chain + **`20260402133618_vestroo_drop_legacy_rls_policies.sql`** refresh — see **[data-models.md](data-models.md)**. **Staff ops data owners:** § **FE.5.11 / BE.6.7 — staff ops data owners** (this doc); **[Story 6.7](stories/6.7.story.md)**. |
| `DriverScheduleModule` | **Chauffeur schedules** / shifts — **VST-7**, **VST-8** | partial | `chauffeur_schedules`, `profiles` | Staff manage; chauffeur scoped read | Roster/calendar surfaces **`/ops/roster`**, **`/ops/calendar`** | **§ [RT.7.5](#rt-7-5)** — **[Story 7.5](stories/7.5.story.md)**: assignments/runs vs **`chauffeur_schedules`**, **`chauffeur_assignments`**, **`service_runs`**, **`trips`**; **no** shipped **`chauffeur_schedules`** Realtime subscriber. Maps manager/driver **schedule** UX ideas; data via Supabase — [manager](capstone-manager-to-vestroo-mapping.md) / [field](capstone-driver-to-field-mapping.md). **Migrations:** `20260402131427_vin_shuttle_driver_and_on_demand_trips.sql` (create **`driver_schedules`**), `20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql` (rename → **`chauffeur_schedules`**, policies). |
| `SearchModule` | **Booking / trip discovery** for staff — **CORE.6.4** | partial | `bookings`, `trips`, joins | Staff search policies | **`/ops/search`**; server queries | Reference **Nest search** shapes are **not** copy-paste API targets (**FE.5.9**). **Migrations:** `20260402131427_vin_shuttle_driver_and_on_demand_trips.sql`, `20260402140000_vestroo_bookings_web_columns.sql`, `20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`, `20260408120000_vst8_chauffeur_booking_rls_ops_audit_actor_role.sql`. **CORE.6.4 / Story 6.4:** [core-traveller-flow-parity.md](core-traveller-flow-parity.md) (traveller **wizard** discovery ≠ Nest **`/search`**). **Manage lookup:** **`searchBooking`** → [front-end-api-interaction.md](front-end-api-interaction.md). **Staff ops data owners:** § **FE.5.11 / BE.6.7 — staff ops data owners** (this doc); **[Story 6.7](stories/6.7.story.md)**. |
| `BookingModule` | **Bookings** lifecycle — **VST-6**, **CORE.6.4** | partial | `bookings`, `booking_trips`, related | RLS staff + traveller contexts | **`/ops/fulfil`**, **`/ops/trips`**, booking actions under `src/actions/` | **FE.5.11** data owner for staffed booking ops. **Migrations:** `20260402131427_vin_shuttle_driver_and_on_demand_trips.sql`, `20260402140000_vestroo_bookings_web_columns.sql`, `20260406120000_vst6_booking_intent_and_payment_audit.sql`, `20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`, `20260408120000_vst8_chauffeur_booking_rls_ops_audit_actor_role.sql`, `20260413130000_vst13_corporate_invoicing_hooks.sql`, `20260412120000_vst12_compliance_incidents_documents_retention.sql`. **CORE.6.4 / Story 6.4:** [core-traveller-flow-parity.md](core-traveller-flow-parity.md). Traveller persist: **`createBooking`**, **`processPayment`** — [front-end-api-interaction.md](front-end-api-interaction.md). **PayFast / webhook idempotency** — [integrations-and-payments.md](integrations-and-payments.md). **Staff ops data owners:** § **FE.5.11 / BE.6.7 — staff ops data owners** (this doc); **[Story 6.7](stories/6.7.story.md)**. |
| `TripModule` | **Trips** execution — **VST-7**, **VST-8**, **VST-9** | partial | `trips`, assignments, tracking | Staff vs chauffeur vs customer tiers | **`opsDispatch.ts`**, **`fieldChauffeur.ts`**, board/calendar pages | Realtime: [VST-9](realtime-and-notifications.md) — **not** reference **socket.io** drop-in. **Migrations:** `20260402131427_vin_shuttle_driver_and_on_demand_trips.sql`, `20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`, `20260407130000_vst7_ops_audit_trips_fulfilment.sql`, `20260408120000_vst8_chauffeur_booking_rls_ops_audit_actor_role.sql`, `20260409120000_vst9_realtime_notifications.sql`. **CORE.6.4 / Story 6.4:** [core-traveller-flow-parity.md](core-traveller-flow-parity.md) — **guest checkout** does **not** insert **`trips`** / **`booking_trips`**; **`assignBookingToRun`** (**VST-7**) — [front-end-api-interaction.md](front-end-api-interaction.md) **Data flow**. **Staff ops data owners:** § **FE.5.11 / BE.6.7 — staff ops data owners** (this doc); **[Story 6.7](stories/6.7.story.md)**. |
| `CheckoutModule` | **Payment handoff** — **VST-13** | partial | payment-related hooks per schema | **TBD** per integration | **PayFast** Route Handler (`/api/payfast/webhook`), booking payment steps | **CORE.6.4** end-to-end traveller flow. **Migrations:** `20260402140000_vestroo_bookings_web_columns.sql`, `20260406120000_vst6_booking_intent_and_payment_audit.sql`, `20260413130000_vst13_corporate_invoicing_hooks.sql`. **CORE.6.4 / Story 6.4:** [core-traveller-flow-parity.md](core-traveller-flow-parity.md). **`processPayment`**, **`GET /api/booking-confirmation`** — [front-end-api-interaction.md](front-end-api-interaction.md). **Webhook idempotency / failed payment** — [integrations-and-payments.md](integrations-and-payments.md). **Staff ops data owners:** § **FE.5.11 / BE.6.7 — staff ops data owners** (this doc); **[Story 6.7](stories/6.7.story.md)**. **Gate go — patterned checkout:** **[SH.9.5](stories/9.5.story.md)**, **[ADR 0005](adr/0005-patterned-checkout-sh9-5.md)**, **[patterned-checkout-vst6-delta.md](patterned-checkout-vst6-delta.md)** ([`#sh-9-5-checkout`](epic-9.md#sh-9-5-checkout)). |
| `BusStopModule` | Capstone **stop network** (reference transit demo) | not applicable | **none** for default shuttle MVP | N/A | N/A | **Epic 9** — **SH.9.1** gate; Vestroo **not** a public stop-network operator. Optional future: **service points** vocabulary only with product sign-off. **Gate go (2026-04-17):** patterned **waypoints** / network vocabulary — **[SH.9.2](stories/9.2.story.md)**, **[ADR 0002](adr/0002-patterned-shuttle-domain-sh9-2.md)** — **not** a capstone **`BusStop`** port. |
| `BusRouteModule` | Capstone **line-style** route network (reference) | not applicable | **none** default | N/A | N/A | **Epic 9** / **deferred**; do not map to **`/ops`** **fixed-line** IA (**NFR.5.4**). **Gate go:** **[SH.9.2](stories/9.2.story.md)**, **[ADR 0002](adr/0002-patterned-shuttle-domain-sh9-2.md)** — **not** **`BusRouteModule`** parity. |
| `BusScheduleModule` | Capstone **timetable / line schedules** | not applicable | **none** default | N/A | N/A | **Epic 9** optional **patterned shuttle** only — not capstone **BusSchedule** 1:1. **Gate go:** **[SH.9.2](stories/9.2.story.md)**, **[ADR 0002](adr/0002-patterned-shuttle-domain-sh9-2.md)**. |
| `TrackingModule` | **Live vehicle / assignment tracking** — **VST-9** | partial | `vehicle_trackings`, `trips` | Strict RLS by role; customer **vehicle_trackings** **not** MVP | **`publishChauffeurLocationAction`**, **`fieldLocation.ts`**, Realtime helpers `src/lib/supabase/realtime.ts` | Corporate shuttle **ETA** / location — not reference **BusTracking** product framing. **Migrations:** `20260402131447_vin_shuttle_tickets_and_social.sql` (create **`bus_trackings`**), `20260402133631_vestroo_rename_tables_and_columns.sql` (→ **`vehicle_trackings`**), `20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`, `20260409120000_vst9_realtime_notifications.sql`. |
| `RatingModule` | **Trip / service feedback** | partial | `ratings` | Customer/chauffeur/staff party policies per [data models](data-models.md) § **`public.ratings`**; **no** dedicated **`/ops/rating`** UX; **no** **`ratings`** Realtime subscriber or **`supabase_realtime`** publication row (**[RT.7.6](#rt-7-6)**, **NFR.5.5**) | **TBD** — **no** **`src/actions/*`** rating mutations or queries **grep-verified** (2026-04-11); default **batch** read / refetch when actions exist | **§ [RT.7.6](#rt-7-6)** — **[Story 7.6](stories/7.6.story.md)**. Reference Nest **`POST`** **`/rating/create-rating`**, **`GET`** **`get-rating-by-trip-id/:tripId`**, **`GET`** **`average-rating`**, **`GET`** **`get-rating-by-query`** — **`src/legacy/capstone-reference/backend-modules/rating/rating.controller.ts`** (and siblings); contrast **HTTP + reference stack** vs Vestroo **Postgres + RLS** (**[FE.5.9](capstone-reference-stack-integration.md)**). **Migrations:** `20260402131447_vin_shuttle_tickets_and_social.sql` (**`trip_id`**, **`rate`**, **`feedback`**, **`driver_id`**, **`customer_id`**), `20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql` (**`driver_id` → `chauffeur_id`**). **Manager-style grids:** deferred — **[capstone-manager-to-vestroo-mapping.md](capstone-manager-to-vestroo-mapping.md)**. |
| `NotificationModule` | **Operational notifications** — **VST-9** | partial | `notifications` (and related ops notification paths) | Staff/chauffeur/customer insert policies | **`insertOperationalNotifications`**, dispatch/field actions | **Not** reference **socket** notification hub; email/SMS per [integrations](integrations-and-payments.md). **§ RT.7.2** below. **Migrations:** `20260402131447_vin_shuttle_tickets_and_social.sql` (table), `20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql` (**`notifications_own`**), **`20260409120000_vst9_realtime_notifications.sql`** (**`notifications_chauffeur_customer_insert`**, columns; **`supabase_realtime`** adds **`vehicle_trackings`** + **`trips` only** — not **`notifications`**). |
| `ConversationModule` | In-app **chat** (reference) | not applicable | **none** for **`/field/*` web MVP** | N/A | N/A | **§ RT.7.3** — **not applicable** web MVP; approved **`tel:`** after policy + **`chauffeur_contact_intent`** audit (**[field-tools.md](field-tools.md)**, **[VST-8](stories/vst-8.story.md)** AC7/AC9); **staff** coordination via dispatch, **`notifications`** (**RT.7.2**), ops board — **not** reference **conversation** threads. **[FE.5.6](capstone-driver-to-field-mapping.md)**. |
| `SharedItineraryModule` | Shared **itinerary** socket domain (reference) | deferred | **TBD** if product needs shared run view | **TBD** | **TBD** | **§ RT.7.3** — **deferred** / **TBD**; **no** **`shared_itinerary`** / SharedItinerary **`postgres_changes`** builders in shipped **`src/lib/supabase/realtime.ts`** (verify **Story 7.3**); interim maps / trip / run context per **[FE.5.6](capstone-driver-to-field-mapping.md)** + authorised live data in **[realtime-and-notifications.md](realtime-and-notifications.md)** — **no** shared-itinerary **event** parity. |
| `DriverBusScheduleModule` | Capstone **driver ↔ bus timetable** joins | not applicable | **none** default | N/A | N/A | **§ [RT.7.5](#rt-7-5)** — **[Story 7.5](stories/7.5.story.md)**; **[Epic 9](epic-9.md)** (**SH.9.1** go/no-go, **SH.9.6** deferred mapping, **SH.9.4** patterned live ops **gated** same). **N/A** default **corporate shuttle**; **forbidden** narrative: **mass-transit** / **public timetable** / **fixed public-style** schedules as the **default** shuttle story (**[Epic 7](epic-7.md)** Goals). **Gate go:** **[SH.9.4](stories/9.4.story.md)**, **[patterned-run-live-ops-mapping.md](patterned-run-live-ops-mapping.md)**, **[ADR 0004](adr/0004-patterned-run-realtime-sh9-4.md)** — **not** a **`DriverBusSchedule`** Nest port. |
| `TicketModule` | Capstone **ticketing** (reference transit) | not applicable | **none** default | N/A | N/A | **Epic 9** / **not applicable** for default **B2B shuttle**; filename is **reference-only** (**NFR.5.4**). **Gate go:** per-departure **capacity / holds** — **[SH.9.3](stories/9.3.story.md)**, **[ADR 0003](adr/0003-service-run-capacity-holds-sh9-3.md)** — **not** a capstone **`TicketModule`** implementation. |
| `BusTrackingModule` | Capstone **bus fleet tracking** (reference) | not applicable | **none** mapped 1:1 | N/A | N/A | Vestroo **fleet** tracking via **`vehicle_trackings`** + **chauffeur assignments** — **not** “bus line” operator semantics (**NFR.5.4**). **Gate go:** **[SH.9.4](stories/9.4.story.md)**, **[patterned-run-live-ops-mapping.md](patterned-run-live-ops-mapping.md)**, **[ADR 0004](adr/0004-patterned-run-realtime-sh9-4.md)** — **not** capstone **`BusTracking`** gateway parity. |

---

## Migration citation status (BE.6.2 — Story 6.2)

**Closed:** The **authoritative domain module** table above cites **`supabase/migrations/<filename>.sql`** in **Notes** for every **partial** row with concrete **`public.*`** (or **`auth.users`**) surfaces. **Outstanding:** **`OtpModule`** — **no** OTP/provider tables in repo yet; add filenames when a migration introduces them. Epic **9** **not applicable** / **deferred** rows unchanged.

---

## RLS traceability appendix (BE.6.2)

**NFR.3.1:** Summaries here and in the **RLS summary** column reflect **Postgres RLS + server gates**, not **client-only** checks. **Authoritative** role names and policy behaviour: **[`docs/data-models.md`](data-models.md)** § **RLS overview** and § **Operations console (VST-7)**; **promotion review:** **[`docs/staging-and-promotion.md`](staging-and-promotion.md)**.

**Policy refresh spine (most domain tables):** early **`vin_shuttle_rls_*.sql`** + **`20260402133618_vestroo_drop_legacy_rls_policies.sql`** + **`20260402133646_vestroo_rls_policies_vestroo_domain.sql`** / **`20260402133655_vestroo_rls_policies_booking_social.sql`** / **`20260402133703_vestroo_rls_policies_tracking_drivers.sql`**, then **`20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`** (VST-5 **chauffeur** / **dispatcher**, column renames, recreated policies on **trips**, **bookings**, **booking_trips**, **notifications**, **vehicle_trackings**, **chauffeur_schedules**, **ratings**). **VST-7 / VST-8 / VST-9:** **`20260407130000_vst7_ops_audit_trips_fulfilment.sql`**, **`20260408120000_vst8_chauffeur_booking_rls_ops_audit_actor_role.sql`**, **`20260409120000_vst9_realtime_notifications.sql`**.

**Intent by actor (deny-by-default unless policy allows):**

| Actor | Typical **SELECT** | Typical **INSERT / UPDATE / DELETE** |
| ----- | ------------------ | ------------------------------------- |
| **Customer** (`profiles.role = customer`) | Own **`bookings`**, **`trips`** as customer party, **`booking_trips`** via booking, **`notifications`** as recipient, **`ratings`** as party | **`bookings`** / **`trips`** where policies allow customer lifecycle; **staff-only** writes elsewhere |
| **Chauffeur** | Own **`chauffeur_schedules`**, **`trips`** / **`vehicle_trackings`** / **`bookings`** (guest PII) per **VST-8** migration predicates | **`vehicle_trackings`** (chauffeur policies), **`ops_audit_log`** append-only (**VST-8** allow-list), assignment/trip updates as defined in SQL |
| **Dispatcher / admin** (`public.is_staff`) | Broad operational reads on fleet, bookings, trips, network (**as named in policies**) | Staff-managed tables per policy **`WITH CHECK`**; **not** a bypass for **anon** clients — **service role** is server-only |

---

<a id="rt-7-1"></a>

## RT.7.1 / Epic 7 — Tracking, Trip, BusTracking → Supabase Realtime

**Traceability:** **[Epic 7](epic-7.md)** (**RT.7.1**). **Canonical** channel tables, role matrix, intervals, and RLS notes: **[Realtime and notifications (VST-9)](realtime-and-notifications.md)**. **Shipped** ingestion, throttles, and staging themes: **[VST-9 story](stories/vst-9.story.md)**. **Reference Nest** **`BusTrackingModule`** — patterned live ops: **[Epic 9](epic-9.md)** (**SH.9.1** **go**, **[SH.9.4](stories/9.4.story.md)** Realtime + mapping), **SH.9.6** deferred full reference mapping.

| Reference module (Nest) | Vestroo table / relationship | Realtime (`postgres_changes`) | Polling / refresh | RLS summary (staff / chauffeur / customer) |
| ------------------------| ---------------------------- | ------------------------------ | ----------------- | ------------------------------------------ |
| **`TrackingModule`** | **`public.vehicle_trackings`** — rows keyed by **`chauffeur_assignment_id`**; **`service_run_id`** / **`vehicle_id`** align with **`trips`**; chauffeur writes via **`publishChauffeurLocationAction`** (`src/actions/fieldLocation.ts`) after resolving assignment for the **`trips`** row | **`subscribeVehicleTrackings`** — `event: '*'` on **`public.vehicle_trackings`** (`src/lib/supabase/realtime.ts`); RLS filters per JWT | **Ops board:** **`OpsBoardRealtimeBridge`** — **2 s** debounce then **`router.refresh()`** + ETA strip refetch from **`vehicle_trackings`**. **Field:** **`FieldLocationPublisher`** — **8 s** interval calling **`publishChauffeurLocationAction`** (under server cap). See **[realtime-and-notifications.md](realtime-and-notifications.md)** § *Update intervals and rate limits*. | **Staff:** broad read where **`is_staff`**. **Chauffeur:** **`SELECT`** only for rows tied to own **`chauffeur_assignments`**. **Customer:** **not** MVP on **`vehicle_trackings`** — no customer **`SELECT`** policy today; Realtime would deliver nothing until policies + product consent change (**[realtime-and-notifications.md](realtime-and-notifications.md)**; migrations **`20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`**). |
| **`TripModule`** | **`public.trips`** — status and window fields drive fulfilment UI | **`subscribeTripsBoard`** — `event: '*'` on **`public.trips`** (`src/lib/supabase/realtime.ts`) | Same **ops** debounce / **`router.refresh()`** pattern via **`OpsBoardRealtimeBridge`** (subscribes **`trips`**, **`vehicle_trackings`**, **`service_runs`**). **Field** trip UX does not require a separate **`trips`** Realtime subscriber for the publish path; board/staff path above. | **Staff** vs **chauffeur** vs **customer** per existing **`trips`** policies (customer sees own-party trips where policies allow). Align detail with **[realtime-and-notifications.md](realtime-and-notifications.md)** § *Channels and roles*. |
| **`BusTrackingModule`** (reference Nest name only) | **Traceability** — patterned live ops map to **`vehicle_trackings`**, **`trips`**, **`service_runs`**, **`chauffeur_assignments`** (**[SH.9.4](stories/9.4.story.md)**, **[patterned-run-live-ops-mapping.md](patterned-run-live-ops-mapping.md)**) — **not** a separate capstone gateway | **`subscribeVehicleTrackings`**, **`subscribeTripsBoard`**, **`subscribeServiceRuns`**, optional **`subscribeChauffeurAssignments`** (**[RT.7.1](#rt-7-1)**) | **N/A** — **no** capstone **socket.io** port (**NFR.5.4**) | **RLS** per role on each table; **`service_runs`** **no** longer open read for all authenticated (**SH.9.4** migration). **SH.9.6** remains **deferred** for full reference mapping beyond this slice. |

### Performance / reliability — Epic 7

**Reference:** **[`techContext.md`](capstone-reference/cline_docs/techContext.md)** (vendored capstone) cites **real-time tracking latency under 2 seconds** and **WebSocket**-style concerns.

- **Alignment (documented posture):** Ops UI reacts to **`postgres_changes`** within a **2 s** coalescing window (**`OPS_BOARD_REALTIME_DEBOUNCE_MS`** in **`src/features/ops/components/OpsBoardRealtimeBridge.tsx`**), then **`router.refresh()`** — consistent with treating **about 2 s** as an acceptable **board-level** update bound after DB change. Chauffeur location writes are **server-throttled** (minimum interval / **12**/min cap per assignment — **`src/lib/vehicle-tracking-throttle.ts`**, **`publishChauffeurLocationAction`**) with **8 s** client cadence (**`FieldLocationPublisher`**), matching **[realtime-and-notifications.md](realtime-and-notifications.md)**.
- **Gap (REST vs app path):** **`techContext`** **API under 500 ms** targets **reference HTTP**; Vestroo **Server Actions**, **RLS**, and **Realtime** are not directly comparable. **Measurement:** staging **browser Network** + **`docs/staging-and-promotion.md`** (Realtime / RLS checks). **Owner:** **Epic 7** follow-up or **ops performance** story **TBD** (until a named story exists).
- **Gap (automated SLA):** No CI assertion for **under 2 s** end-to-end tracking latency. **Measurement:** manual **VST-9** staging subscriber checks + Supabase dashboard. **Owner:** **[VST-9 story](stories/vst-9.story.md)** baseline / **RT.7.x** if automated probes are required.

---

<a id="rt-7-2"></a>

## RT.7.2 / Epic 7 — NotificationModule → Vestroo

**Traceability:** **[Epic 7](epic-7.md)** (**RT.7.2**). **Channels, RLS, operational kinds:** **[Realtime and notifications (VST-9)](realtime-and-notifications.md)**. **Baseline story:** **[VST-9](stories/vst-9.story.md)**. **Reference Nest** pushes events through **`NotificationModule`** + **socket** fan-out — contrast only in **[Capstone reference — stack & integration](capstone-reference-stack-integration.md)** (**FE.5.9**); Vestroo first-party path is **Postgres rows** + **RLS**, not a parallel **socket.io** hub.

| Reference (conceptual) | Vestroo (shipped) | Payload → `public.notifications` / behaviour |
| ---------------------- | ----------------- | ---------------------------------------------- |
| Dispatch / trip lifecycle alerts (reference **Nest** + **socket** hub) | **Server Actions** **`opsDispatch.ts`** (assign, status, delay, swap) and **`fieldChauffeur.ts`** (confirm / complete → customer **`trip_status`**) call **`insertOperationalNotifications`** (`src/lib/operational-notifications.ts`); **no** app-wide socket fan-out | **`recipient_id`**, **`title`**, **`body`**, **`kind`** (`assignment` \| `change` \| `no_show` \| `trip_status` \| `general`), **`metadata`** (jsonb, ids/status labels — avoid PII), **`channel`** default **`in_app`**, **`is_read`** default **`false`** on insert — per **[data models](data-models.md)** § **`public.notifications`** |
| Read / unread UX | **`is_read: false`** set in **`insertOperationalNotifications`**; **no** mark-read Server Action, route, or **`(ops)` / `(field)` / `(app)`** UI **grep-verified** (2026-04-10) | **Backlog:** mark-read API/UI + optional Realtime-driven inbox — **owner:** product / **Epic 7** follow-up (**TBD** story) |
| Live notification stream | **`src/lib/supabase/realtime.ts`** — **`subscribeVehicleTrackings`**, **`subscribeTripsBoard`**, **`subscribeServiceRuns`**, **`subscribeChauffeurAssignments`** (**SH.9.4**); **no** `notifications` channel builder | **Polling / refresh:** none dedicated for notifications; customers discover trip state via existing trip/ops surfaces or future inbox. **`supabase_realtime` publication:** VST-9 baseline adds **`vehicle_trackings`** + **`trips`**; **SH.9.4** adds **`service_runs`** + **`chauffeur_assignments`** — **`notifications`** remains **not** published; even a future client subscribe would need **DDL + RLS review** first |
| RLS (align **[realtime-and-notifications.md](realtime-and-notifications.md)**) | **`notifications_own`** (recipient **or** staff for operations on rows), **`notifications_chauffeur_customer_insert`** (chauffeur → **`recipient_id`** = linked **`trips.customer_id`** for own trip) | Customer **reads** own rows as **`recipient_id`**; **no** cross-recipient leakage (**NFR.3.1**) |

**MVP gaps & interim patterns (honest):**

- **No native Apple/Google push** — in-app **`notifications`** table only for this slice; see **[realtime-and-notifications.md](realtime-and-notifications.md)** § *Channels and roles*.
- **SMS** not wired to booking/dispatch for notifications — stub **`src/services/sms-stub.ts`**, env matrix **[environment-vars.md](environment-vars.md)** (**`SMS_PROVIDER_*`**), integration posture **[integrations-and-payments.md](integrations-and-payments.md)**; scope owner **[VST-13](stories/vst-13.story.md)**.
- **Interim:** operational comms rely on **in-app persistence** + **field** behaviour (maps **`tel:`**, deep links) per **[field-tools.md](field-tools.md)**.

### Performance / reliability — Epic 7 (notifications)

**Reference:** **[`techContext.md`](capstone-reference/cline_docs/techContext.md)** — **§ Performance** (**real-time tracking &lt; 2 s**, **API &lt; 500 ms**, **WebSocket** stability).

- **Alignment:** Notification rows are **write-on-event** via Server Actions with **same user-scoped Supabase client** as dispatch/field (**[VST-9](stories/vst-9.story.md)**); no extra **socket** hop — **insert latency** bounded by **Postgres + RLS** like other action paths.
- **Gap (no live delivery SLA):** **`techContext`** **sub-second / WebSocket** expectations apply to **reference** live hubs; shipped **notifications** have **no** Realtime subscriber and **no** publication row — **cannot** claim instant delivery or **&lt; 2 s** visibility to end users. **Measurement:** staging **manual** insert + DB read; future **EXPLAIN** / app **inbox** E2E when built. **Owner:** **Epic 7** / **product backlog** until a named story ships inbox + optional **`postgres_changes`** on **`notifications`**.
- **Gap (provider quotas):** High churn on **`notifications`** could stress **PostgREST**/**future Realtime**; **bounded payloads** (**metadata** discipline) per **[realtime-and-notifications.md](realtime-and-notifications.md)** — **Owner:** **NFR.1.1 / 1.2** reviews with **[realtime-and-notifications.md](realtime-and-notifications.md)** (Supabase Realtime caveats) and **[VST-9 story](stories/vst-9.story.md)** baseline.

---

<a id="rt-7-3"></a>

## RT.7.3 / Epic 7 — ConversationModule, SharedItineraryModule → Vestroo

**Traceability:** **[Epic 7](epic-7.md)** (**RT.7.3**). **Story:** **[7.3](stories/7.3.story.md)**. **Stack contrast (Redis / JWT WebSocket guard, socket.io vs Supabase):** **[Capstone reference — stack & integration](capstone-reference-stack-integration.md)** (**FE.5.9**) — **single high-level anchor**; **do not** duplicate the full reference **socket** enum here.

**Reference-only code pointers (not shipped Vestroo product paths):**

- **Legacy vendored Nest / RN samples:** `src/legacy/capstone-reference/backend-modules/conversation/*`, `src/legacy/capstone-reference/backend-modules/shared-itinerary/*` (gateways, services).
- **Reference driver hooks (docs tree):** [`docs/capstone-reference/frontend-driver/src/hook/useConversationSocket.ts`](capstone-reference/frontend-driver/src/hook/useConversationSocket.ts), [`useSharedItinerarySocket.ts`](capstone-reference/frontend-driver/src/hook/useSharedItinerarySocket.ts).
- **Optional customer sample (excluded from app build intent):** `src/features/capstone-reference/customer/service/conversation.service.ts`.

| Axis | Reference (capstone) | Vestroo (shipped / product) |
| ---- | -------------------- | ---------------------------- |
| **Transport** | **Nest** **`ConversationModule`** + **`SharedItineraryModule`** with **socket.io** hooks and REST under **`conversation/*`**; **Redis + JWT**-style **WS** guard expectations per **FE.5.9** | **Supabase-first** — **no** parallel **socket.io** gateway or Nest **AppGateway** port for first-party **`/ops/*`**, **`/field/*`**, **`(app)`** |
| **Realtime (`postgres_changes`)** | Reference live **conversation** / **shared itinerary** fan-out over sockets | **`src/lib/supabase/realtime.ts`** — shipped builders for **`vehicle_trackings`**, **`trips`**, **`service_runs`**, **`chauffeur_assignments`** (**SH.9.4** for patterned tables) — **no** **`conversations`**, **`messages`**, or **`shared_itinerary`** channel builders (**grep-verified** shipped **`src/`** vs **legacy** / **`capstone-reference`** paths — **Story 7.3**) |
| **Storage / attachments** | Reference may assume message media | **None** for web MVP **in-app chat** (no chat tables/buckets in scope); future **message media** = **TBD** backlog (**Supabase Storage** buckets + **RLS** design — **not** **7.3**) |
| **In-app chat** | Cross-party threads in reference | **Not applicable** for Vestroo **web MVP** — **approved alternative:** **`tel:`** after policy check, **`chauffeur_contact_intent`** audit (**[field-tools.md](field-tools.md)**), aligned with **[VST-8](stories/vst-8.story.md)** AC7/AC9; **staff** use dispatch, **`notifications`** (**RT.7.2**), and board — **not** thread-based **Conversation** parity |
| **Shared itinerary** | **`useSharedItinerarySocket`** + gateway emits (reference) | **Deferred** / **TBD** — **no** SharedItinerary **Realtime** surface in shipped **`realtime.ts`**; **interim:** **[FE.5.6 — driver → field](capstone-driver-to-field-mapping.md)** (maps deep link, trip/run, **VST-9** location context) + **[realtime-and-notifications.md](realtime-and-notifications.md)** for **authorised** live **`vehicle_trackings` / `trips`** data — **no** claim of **shared-itinerary event** parity |

**RLS + role visibility (operational comms today):**

- **Staff (`/ops/*`):** **`trips`**, board **Realtime** (**RT.7.1**), dispatch **Server Actions**, **`notifications`** inserts where policies allow (**RT.7.2**), **`ops_audit_log`** patterns (**VST-8** / **VST-7**).
- **Chauffeur (`/field/*`):** Assigned **`trips`**, **`vehicle_trackings`** writes/reads per policy, **`chauffeur_contact_intent`** audit, maps targets — **no** in-app chat UI.
- **Customer (`(app)` booking):** Own-party **`trips`** / **`notifications`** as recipient per RLS — **not** reference-style **cross-party messaging** or **conversation** Realtime.

### Performance / reliability — Epic 7 (conversation / shared itinerary)

**Reference:** **[`techContext.md`](capstone-reference/cline_docs/techContext.md)** — **§ Technical Constraints → Performance** (**real-time tracking &lt; 2 s**, **API &lt; 500 ms**, concurrent users) and **§ Technical Limitations → Real-time Updates** (**WebSocket** connection stability, network dependency).

- **Alignment:** Vestroo **does not** operate a first-party **socket.io** **conversation** or **shared-itinerary** hub; **NFR.1.1 / NFR.1.2** fan-out risk for those domains is **avoided** on shipped surfaces. Live ops data uses **bounded** **`postgres_changes`** subscriptions documented in **RT.7.1** / **[realtime-and-notifications.md](realtime-and-notifications.md)** — consistent with **no false socket parity** (**NFR.5.5**).
- **Gap (if product adds chat or shared-view sync later):** **`techContext`** **WebSocket** stability and **sub-second** reference assumptions would apply to any **new** hub — requires **ADR**, **RLS** review, and load model. **Measurement:** staging **connection** / **message** metrics + **`docs/staging-and-promotion.md`** checklist; optional automated probes **TBD**. **Owner:** **Epic 7** / product **backlog** until a named story (post-**7.3**) ships the feature.

---

<a id="rt-7-4"></a>

## RT.7.4 / Epic 7 — Realtime policies, abuse controls, observability

**Traceability:** **[Epic 7](epic-7.md)** (**RT.7.4**). **Authoritative** channels, privacy matrix, and interval table: **[Realtime and notifications (VST-9)](realtime-and-notifications.md)**. **Shipped** subscription builders: **`src/lib/supabase/realtime.ts`**. **Staging** checks: **[`docs/staging-and-promotion.md`](staging-and-promotion.md)** — **Staging: Realtime subscriber latency (VST-9)**.

### Epic 7 subsection summary (RT.7.1–RT.7.3)

| ID | Purpose (short) | Matrix subsection |
| -- | ---------------- | ----------------- |
| **RT.7.1** | Map **Tracking** / **Trip** / reference **BusTracking** to **`vehicle_trackings`**, **`trips`**, **`service_runs`**, **`chauffeur_assignments`** **`postgres_changes`**, RLS, polling/debounce patterns (**SH.9.4**) | [§ RT.7.1](#rt-7-1) |
| **RT.7.2** | Map **NotificationModule** to **`notifications`** inserts/reads, publication honesty vs **`supabase_realtime`**, gaps (no live stream, mark-read **TBD**) | [§ RT.7.2](#rt-7-2) |
| **RT.7.3** | **Conversation** / **SharedItinerary** vs shipped **`realtime.ts`** (**no** extra channel builders), **N/A** web chat, **`tel:`** interim | [§ RT.7.3](#rt-7-3) |
| **RT.7.6** | **`RatingModule`** → **`public.ratings`**, **`/ops/rating`** gap, **Realtime vs batch** (no subscriber / publication), **service type** vocabulary (**NFR.5.4**) | [§ RT.7.6](#rt-7-6) |

### Per-topic policy (shipped Realtime-related tables)

| Topic | Who may subscribe / consume | RLS / filter (pointer) | Rate limit / interval (client + server) | Privacy tier behaviour | Abuse / operational controls | Observability (today) |
| ----- | ---------------------------- | ---------------------- | ---------------------------------------- | ------------------------ | ----------------------------- | ---------------------- |
| **`vehicle_trackings`** (`subscribeVehicleTrackings` in **`src/lib/supabase/realtime.ts`**) | **Staff** (`is_staff`) — broad read per policies. **Chauffeur** — rows for own **`chauffeur_assignments`** only. **Customer** — **not** MVP: no customer **`SELECT`** on this table today; Realtime would deliver nothing until policies + product consent change. | **[realtime-and-notifications.md](realtime-and-notifications.md)** § *Channels and roles*; **[data-models.md](data-models.md)** § *`public.vehicle_trackings`*; migrations **`20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`**, **`20260409120000_vst9_realtime_notifications.sql`** (publication includes this table). | **Server:** max **12** successful writes/min per **`chauffeur_assignment_id`** (min **5 s** between updates) — **`src/lib/vehicle-tracking-throttle.ts`**, enforced in **`publishChauffeurLocationAction`** (`src/actions/fieldLocation.ts`). **Field client:** **`FIELD_LOCATION_PUBLISH_INTERVAL_MS`** = **8 s** — **`FieldLocationPublisher`**. **Ops board:** **`OPS_BOARD_REALTIME_DEBOUNCE_MS`** = **2 s** before **`router.refresh()`** after **`postgres_changes`** — **`OpsBoardRealtimeBridge`**. | **Staff:** full lat/lng + ETA from DB/Realtime. **Corporate / VIP** (when exposed in UI): coordinate rounding + ETA wording via **`roundCoordinatesForPrivacyTier`** and helpers in **`src/lib/maps.ts`** — align with **Privacy matrix** in **[realtime-and-notifications.md](realtime-and-notifications.md)**. **MVP:** customer path on this table disabled so precision cannot leak across tiers via Realtime. | **Throttle** (server) + **debounce** (ops client) + **bounded publish cadence** (field). **Provider:** Supabase Realtime **connection and message quotas** — see *Provider caveats* in **[realtime-and-notifications.md](realtime-and-notifications.md)** and [Realtime quotas](https://supabase.com/docs/guides/realtime/rate-limits); prefer **narrow filters** and **small payloads** under churn. **Polling fallback** (ops) documented in **[ops-console.md](ops-console.md)** if Realtime unavailable — higher load trade-off. | **Manual staging** only: **[staging-and-promotion.md](staging-and-promotion.md)** *Staging: Realtime subscriber latency (VST-9)* — board refresh within **≤ 30 s** end-to-end under normal staging load (target **≤ 10 s** when healthy); **not** a formal SLA or APM metric. **No** automated end-to-end **&lt; 2 s** assertion in CI. |
| **`trips`** (`subscribeTripsBoard` in **`src/lib/supabase/realtime.ts`**) | **Staff** — board subscription with staff JWT. **Chauffeur** — own trips per **`trips`** policies (field list is **not** a separate Realtime subscriber; board is staff path). **Customer** — own-party trips where **`trips`** RLS allows; **no** dedicated customer **`trips`** Realtime subscriber in shipped app code beyond what product wires (board is **`/ops/board`**). | **[realtime-and-notifications.md](realtime-and-notifications.md)** § *Channels and roles*; **`trips`** policies in migration chain cited in matrix **RLS appendix**. Publication: **`20260409120000_vst9_realtime_notifications.sql`**. | **Ops board:** same **`OPS_BOARD_REALTIME_DEBOUNCE_MS`** (**2 s**) coalescing with **`vehicle_trackings`** and **`service_runs`** in **`OpsBoardRealtimeBridge`**. **No** separate server “trip event” throttle — mutations go through Server Actions + RLS. | Trip window / status display follows role; customer-facing maps/ETA precision **TBD** per product surfaces — tier rules same doc **Privacy matrix** when UI exposes location-derived fields. | Same **debounce** + **Supabase** quota posture as **`vehicle_trackings`**; **RLS** is primary partition between roles. | Same **VST-9 staging** checklist as above; **honest** observational bounds only. |
| **`service_runs`** (`subscribeServiceRuns` in **`src/lib/supabase/realtime.ts`**, **SH.9.4**) | **Staff** — full read via **`service_runs_staff`**. **Chauffeur** — runs linked through own **`trips`**. **Customer** — runs linked through **`trips`**, **`tickets`**, or **`bookings`** per **`service_runs_select_party`** — **not** the former open **`USING (true)`** authenticated read. | **[realtime-and-notifications.md](realtime-and-notifications.md)**; migration **`20260418150000_sh94_patterned_run_realtime.sql`**; **[ADR 0004](adr/0004-patterned-run-realtime-sh9-4.md)**. | **Ops board:** same **2 s** debounce as **`trips`** / **`vehicle_trackings`**. **Capacity** updates remain **Postgres/RPC**-authoritative (**ADR 0003**) — **do not** infer seats from Realtime alone. | Run metadata only — **no** customer **`vehicle_trackings`** stream; map precision still **`roundCoordinatesForPrivacyTier`** when UI shows coords. | **RLS** before publication; optional **`id=eq.<run>`** filter on subscribe to limit churn. | Staging: confirm customer **cannot** see unrelated runs (**[realtime-and-notifications.md](realtime-and-notifications.md)** verification list). |
| **`chauffeur_assignments`** (`subscribeChauffeurAssignments` in **`src/lib/supabase/realtime.ts`**, **SH.9.4**) | **Staff** — broad read. **Chauffeur** — **`chauffeur_id = auth.uid()`** only. **Customer** — typically **no** unrelated rows (deny-by-default outside party policies). | Existing **`chauffeur_assignments_chauffeur_read`** + staff; publication **`20260418150000_sh94_patterned_run_realtime.sql`**. | Optional **`chauffeur_id=eq.<uid>`** Realtime filter for field; **no** client high-frequency publish — rows change on dispatch/ops actions. | Assignment rows are **not** live GPS — location tier rules apply to **`vehicle_trackings`** / maps, not roster identifiers. | Same quota + **RLS** posture as other topics. | Wire **TBD** on field shell; helper shipped for integration. |
| **`notifications`** (insert/read; **not** a shipped Realtime topic) | **Recipients** read own rows (**`recipient_id`**). **Staff** insert for ops (**`notifications_own`**). **Chauffeur** insert for linked trip customer (**`notifications_chauffeur_customer_insert`**). **No** **`postgres_changes`** subscriber in **`realtime.ts`**. | **[realtime-and-notifications.md](realtime-and-notifications.md)** § *Operational notifications*; **[RT.7.2](#rt-7-2)** table; migration **`20260409120000_vst9_realtime_notifications.sql`** — **`ALTER PUBLICATION`** adds **`vehicle_trackings`** + **`trips` only** (**`notifications`** **not** published). | **N/A** for Realtime (no live stream). Rows written **on dispatch/field events** via **`insertOperationalNotifications`** — bounded by action/DB latency, not a client publish interval. | **Minimise PII** in **`title`** / **`body`** / **`metadata`** — engineering stance in **[realtime-and-notifications.md](realtime-and-notifications.md)** + **VST-12** boundary in that doc. | **RLS** on insert/select; future **Realtime** on this table would need **DDL + RLS review** first. **Provider caveats** still apply if publication/subscribers are added later. | **No** Realtime latency metric; **manual** verify inserts + recipient reads in staging. **Mark-read** UI/API **TBD** per **RT.7.2**. |
| **`ratings`** (schema + RLS; **not** a shipped Realtime topic) | **Customer / chauffeur / staff** reads per **`ratings_parties`**; customer inserts per **`ratings_insert_customer`** — align **[data-models.md](data-models.md)** § **`public.ratings`** and migration **`20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`**. **No** **`postgres_changes`** subscriber in **`realtime.ts`**. | **[RT.7.6](#rt-7-6)**; **`20260409120000_vst9_realtime_notifications.sql`** — **`ratings`** **not** in **`supabase_realtime`** publication. | **N/A** for Realtime. Intended ingestion is **bounded writes** via future **Server Actions** (or explicit API) + **RLS** — same pattern as other social rows; **on-navigation refetch** / **batch** read after trip completion until product + **ADR** require live cross-role visibility. | **NFR.3.1** — party-scoped visibility only; **no** doc claim of cross-party **PII** beyond policy semantics. | **RLS** today; adding **Realtime** would require **publication + subscriber + RLS** review (**NFR.1.1 / NFR.1.2**, **NFR.5.5**). | **No** Realtime metric; **no** first-party **`(ops)` / `(field)` / `(app)`** rating UI **grep-verified** (2026-04-11) — see **RT.7.6** shipped-code notes. |

**RT.7.4 — Supabase provider / abuse controls (consolidated, matches shipped code + [realtime-and-notifications.md](realtime-and-notifications.md) *Provider caveats*):**

- **Ops board client debounce:** **`OPS_BOARD_REALTIME_DEBOUNCE_MS`** (**2 s**) — **`src/features/ops/components/OpsBoardRealtimeBridge.tsx`**.
- **Field location client cadence:** **`FIELD_LOCATION_PUBLISH_INTERVAL_MS`** (**8 s**) — **`src/features/field/components/FieldLocationPublisher.tsx`**.
- **Server throttle on `vehicle_trackings` writes:** **`VEHICLE_TRACKING_MAX_UPDATES_PER_MINUTE`** / **`VEHICLE_TRACKING_MIN_INTERVAL_MS`** — **`src/lib/vehicle-tracking-throttle.ts`**, applied in **`publishChauffeurLocationAction`**.
- **Supabase Realtime quotas and connection/message limits:** [Supabase Realtime](https://supabase.com/docs/guides/realtime) and [Realtime quotas](https://supabase.com/docs/guides/realtime/rate-limits); large payloads and high subscription churn remain **documented risk** (**NFR.1.1 / NFR.1.2**) — mitigate with **RLS**, **filter predicates**, and **payload discipline** per **[realtime-and-notifications.md](realtime-and-notifications.md)**.

### Load / soak vs staging latency (honest)

- **Formal load or soak tests** for Supabase Realtime (sustained concurrent subscribers, message rates, reconnect storms) are **not yet measured** in-repo: **no** dedicated doc, automated test, or runbook asserts Realtime load/soak results. **Owner:** **Epic 7** / platform engineering follow-up (**TBD** story) until named.
- **Partial / observational only:** **[`docs/staging-and-promotion.md`](staging-and-promotion.md)** *Staging: Realtime subscriber latency (VST-9)* defines **manual** steps and **staging** expectations (e.g. board refresh **≤ 30 s** end-to-end under normal staging load, **≤ 10 s** target when Realtime is healthy). These are **not** formal load/soak outcomes — they distinguish **“does it work on staging?”** from **capacity or endurance** proof.

### `techContext.md` align / partial / gap (vendored reference)

Source: **[`docs/capstone-reference/cline_docs/techContext.md`](capstone-reference/cline_docs/techContext.md)** — **§ Technical Constraints → Performance** and **§ Technical Limitations → Real-time Updates** (Nest + Mongo + WebSocket reference stack). Below maps those **claims** to **Vestroo** (Supabase Realtime + Next.js Server Actions).

| Claim | Status vs Vestroo | Measurement (when gap/partial) | Owner |
| ----- | ----------------- | ------------------------------ | ----- |
| **Real-time tracking latency &lt; 2 seconds** | **Partial.** Ops UI coalesces Realtime-driven refreshes on a **~2 s** debounce (**`OPS_BOARD_REALTIME_DEBOUNCE_MS`**), consistent with a **board-level** “about 2 s” bound **after** a qualifying DB change. **End-to-end** chauffeur publish → staff visibility includes **8 s** client interval + **5 s** server min gap + network; **not** asserted as **&lt; 2 s** in code or CI. | Staging: **[staging-and-promotion.md](staging-and-promotion.md)** VST-9 steps; manual subscriber / Network tab. | **VST-9** baseline / **Epic 7** if automated probes are required. |
| **API response time &lt; 500 ms** | **Gap** (different bar). **`techContext`** targets reference **HTTP** APIs; Vestroo **Server Actions**, **RLS**, and Postgres paths are **not** the same surface. | Staging **browser Network** on action calls; compare p95 informally — **no** committed product SLA in docs. | **Epic 7** follow-up or **ops performance** story **TBD**. |
| **WebSocket connection stability** (reference wording) | **Partial.** First-party app uses **Supabase Realtime** (WebSocket under the hood), **not** a bespoke **socket.io** gateway. Stability and reconnect behaviour depend on **browser**, **network**, and **Supabase** — **not** documented here as numeric SLOs. | Staging manual session + browser devtools; Supabase status/dashboard. | Platform / **Epic 7** + vendor runbooks. |
| **Concurrent users / scale** (horizontal scaling, load balancing) | **Gap** for **Realtime-specific** scale proof. **NFR.1.1 / NFR.1.2** require **bounded** subscriptions/payloads or **documented risk** — see **Provider caveats** above; **no** in-repo Realtime concurrency benchmark. | Future load test design **TBD**; until then treat **provider quotas** as upper-bound guidance. | **NFR** review + **Epic 7** / platform **TBD**. |

---

<a id="rt-7-5"></a>

## RT.7.5 / Epic 7 — DriverScheduleModule, DriverBusScheduleModule → assignments, runs, roster

**Traceability:** **[Epic 7](epic-7.md)** (**RT.7.5**). **[Epic 9](epic-9.md)** — **SH.9.1** (patterned/capacity shuttle **go**/**no-go**), **SH.9.4** (live ops for **patterned** runs), **SH.9.6** (deferred **DriverBusSchedule** reference mapping). **Schema / FK / RLS wording:** **[data-models.md](data-models.md)** (**`chauffeur_schedules`**, **`trips.schedule_id`**, **`service_runs`**, **`chauffeur_assignments`**). **Ops surfaces:** **[ops-console.md](ops-console.md)** (**`/ops/roster`**, **`/ops/calendar`**, board/fulfil). **Dispatch stories:** **[VST-7](stories/vst-7.story.md)**, **[VST-8](stories/vst-8.story.md)** — Server Actions + RLS (not a schedule socket). **Stack contrast (reference only):** **[capstone-reference-stack-integration.md](capstone-reference-stack-integration.md)** (**FE.5.9**) — **socket.io** / capstone broadcast vs Vestroo paths. **Realtime policy (thin pointer):** **[realtime-and-notifications.md](realtime-and-notifications.md)** — shipped **`postgres_changes`** topics; schedule table **not** subscribed. **Reference cline doc:** **[driverSchedule.md](capstone-reference/cline_docs/driverSchedule.md)** (Mongo / WebSocket-style **Real-time Features** — **≠** shipped Vestroo schedule path).

| Reference (Nest) | Vestroo primary mapping | Keys / relationships (per **[data-models.md](data-models.md)**) |
| ---------------- | ------------------------ | ---------------------------------------------------------------- |
| **`DriverScheduleModule`** | **`public.chauffeur_schedules`** | **Shift / roster** row: **`chauffeur_id`**, **`work_date`**, **`vehicle_id`**, hours/status. **`trips.schedule_id` → `chauffeur_schedules.id`**. |
| | **`public.profiles`** | **`chauffeur_id`** on schedule/trip rows references **`profiles.id`** (**`role = chauffeur`**). |
| | **`public.service_runs`** | Dispatch selects run (**`id`**, **`service_route_id`**, **`scheduled_start`**, **`scheduled_end`**, **`service_date`**, **`trip_number`**) before trip + assignment insert. |
| | **`public.chauffeur_assignments`** | Created on **`assignBookingToRun`** with window from run (**`start_time`**, **`end_time`**) + **`service_route_id`**, **`vehicle_id`**, **`chauffeur_id`** — ties chauffeur/vehicle to the **operational run window**; **`vehicle_trackings`** reference **`chauffeur_assignment_id`** (see data-models FK overview). |
| | **`public.trips`** | **`schedule_id`** (resolved schedule), **`service_run_id`** → **`service_runs`**, **`chauffeur_id`**, **`vehicle_id`**. |

**Assignments / runs (honest per code):** **`assignBookingToRun`** in **`src/actions/opsDispatch.ts`** loads **`service_runs`**, resolves **`work_date`** from **`service_runs.service_date`**, then **`resolveChauffeurScheduleId`**: **SELECT** matching **`chauffeur_schedules`** row (**`chauffeur_id`**, **`vehicle_id`**, **`work_date`**) or **INSERT** a default shift row if none exists. It inserts **`trips`** with **`schedule_id`** and **`service_run_id`**, then **`chauffeur_assignments`** aligned to the run’s **`scheduled_start` / `scheduled_end`**. **`/ops/roster`** reads **`chauffeur_schedules`** (server query); **`/ops/calendar`** is **trip**-centric (see ops-console). No separate “schedule repository” port of capstone **Nest** services — **Postgres + Server Actions + RLS**.

**Surfaces:** **`/ops/roster`** — chauffeur **`profiles`** + **`chauffeur_schedules`**. **`/ops/calendar`** — trips by **`time_start_estimate`**. **`/ops/board`** — **`trips`** / **`vehicle_trackings`** Realtime bridge (**not** roster schedule stream). **`/ops/fulfil`** — copy documents find-or-create **`chauffeur_schedules`** on assign. **Field** (**VST-8**): assignment/trip UX via **Server Actions** + RLS — **no** **`chauffeur_schedules`** **Realtime** client in shipped app.

**RLS summary (staff / chauffeur / customer):** **Staff** (`is_staff`): manage roster/dispatch paths per policies on **`chauffeur_schedules`**, **`trips`**, **`service_runs`**, assignments. **Chauffeur:** **own** **`chauffeur_schedules`** and **`chauffeur_assignments`** rows (**`chauffeur_id = auth.uid()`**); **`trips`** / **`vehicle_trackings`** per **VST-8** predicates — align **[data-models.md](data-models.md)** § **RLS overview** / **Operations console**. **Customer:** **no** direct **`chauffeur_schedules`** surface; party **`trips`** / **`notifications`** only where policies allow (**NFR.3.1**).

### Broadcast / socket vs Vestroo (explicit)

- **Shipped app:** **no** **`chauffeur_schedules`** **`postgres_changes`** subscriber — **`src/lib/supabase/realtime.ts`** also exposes **`subscribeServiceRuns`** / **`subscribeChauffeurAssignments`** (**SH.9.4**); **`chauffeur_schedules`** remains **off** Realtime (**[RT.7.2](#rt-7-2)**, **[realtime-and-notifications.md](realtime-and-notifications.md)**). **`supabase_realtime`** includes **`vehicle_trackings`**, **`trips`**, **`service_runs`**, **`chauffeur_assignments`** — **not** the roster table.
- Reference [`driverSchedule.md`](capstone-reference/cline_docs/driverSchedule.md) **Real-time Features** / status / notification lists assume a **live schedule hub** — **not** implemented for **`chauffeur_schedules`** in Vestroo; see **align-or-gap** bullets below.
- **Capstone AppGateway** / **socket.io** (**FE.5.9**): **reference-only** contrast — Vestroo uses **Postgres + Server Actions**, **`revalidatePath`**, and **ops** **`router.refresh()`** where applicable; **trips** / **tracking** live updates: **[RT.7.1](#rt-7-1)** / **[RT.7.4](#rt-7-4)** (**`trips`**, **`vehicle_trackings`** Realtime) — **not** a substitute for “every driver receives pushed schedule rows” unless code is added.

### DriverBusScheduleModule — Epic 9 optional patterned shuttle

**Not applicable** to the **default** **corporate shuttle** product (**[Epic 7](epic-7.md)**). **Optional** **patterned** / capacity shuttle **only** if **[Epic 9 — SH.9.1](epic-9.md)** is **go**. **SH.9.6:** deferred **DriverBusSchedule** reference → Vestroo mapping remains traceability-only. **SH.9.4:** live ops for **patterned** runs **gated** with the same product decision. **Forbidden** in product/docs narrative: **mass-transit**, **public timetable**, or **fixed public-style** line schedules as the **default** for corporate shuttle (**align [Epic 9 Description](epic-9.md)**).

### `driverSchedule.md` align-or-gap (AC7)

Reference file: **[driverSchedule.md](capstone-reference/cline_docs/driverSchedule.md)** (Mongo capstone).

- **Real-time Features / Status Updates (driver status, task progress, delays):** **Gap / not shipped** as a **schedule** **WebSocket** or **`chauffeur_schedules`** Realtime stream. **Partial:** **trip** status and board visibility via **`trips`** **`postgres_changes`** + debounced refresh — **[RT.7.1](#rt-7-1)** / **[RT.7.4](#rt-7-4)**; chauffeur location via **`vehicle_trackings`** (**same**). **Owner:** product / **Epic 7** backlog if schedule fan-out is required.
- **Notifications — schedule updates, assignment changes, trip updates:** **Align (partial):** **`insertOperationalNotifications`** from **`opsDispatch.ts`** / **`fieldChauffeur.ts`** — **[RT.7.2](#rt-7-2)** (**in-app** rows, **no** notifications Realtime subscriber). **Gap:** reference-style instant **socket** delivery to all drivers — **not** shipped.
- **Alert system (conflicts, break violations, overtime):** **Not shipped** as automated alerts in app scope documented here. **Owner:** backlog / compliance stories (**TBD**); do not imply capstone **Monitoring** / **Alert** services without implementation.

### `techContext.md` — schedule-adjacent align / partial / gap (AC5)

Source: **[techContext.md](capstone-reference/cline_docs/techContext.md)** — **Real-time Features: WebSocket**; **§ Technical Limitations → Real-time Updates** (WebSocket stability, network dependency).

| Reference claim | vs Vestroo (schedule path) |
| ----------------- | --------------------------- |
| **WebSocket / real-time hub for schedule** | **Gap.** **Schedule** data path is **Postgres + Server Actions + RLS** + **page**/**path** revalidation — **no** parallel **WebSocket schedule hub** in shipped **`/ops/*`** / **`/field/*`**. |
| **Real-time Updates** (connection stability, network dependency) | **Partial.** Applies to **Supabase Realtime** only for **shipped** topics (**`trips`**, **`vehicle_trackings`**) — **[RT.7.1](#rt-7-1)** / **[RT.7.4](#rt-7-4)** — **not** to **`chauffeur_schedules`**. |
| **Sub-second / fan-out** expectations | **Gap** for **schedule** visibility; **do not** equate **trips** board latency with **reference** schedule push semantics (**NFR.1.1 / NFR.1.2**, **NFR.5.5**). |

---

<a id="rt-7-6"></a>

## RT.7.6 / Epic 7 — RatingModule → feedback, ops visibility, Realtime vs batch

**Traceability:** **[Epic 7](epic-7.md)** (**RT.7.6**). **[Story 7.6](stories/7.6.story.md)**. **Schema + RLS:** **[data-models.md](data-models.md)** § **`public.ratings`**. **Stack contrast (reference HTTP / sockets vs Vestroo):** **[capstone-reference-stack-integration.md](capstone-reference-stack-integration.md)** (**FE.5.9**). **Shipped Realtime builders + publication:** **`src/lib/supabase/realtime.ts`**, **`supabase/migrations/20260409120000_vst9_realtime_notifications.sql`**. **Optional product line:** experiences / scenic-adjacent packages — **[VST-10 story](stories/vst-10.story.md)** when cited.

**Traceability paragraph:** Capstone **`RatingModule`** exposed **manager/customer** HTTP endpoints over the reference stack; Vestroo persists **`public.ratings`** with **Postgres RLS** party rules (**NFR.3.1**). First-party **Server Actions** for insert/read are **not** present under **`src/actions/`** today (**grep** 2026-04-11). **Supabase Realtime** does **not** include a **`ratings`** channel builder or **`supabase_realtime`** table entry — documented default is **batch** ingestion and **read on navigation** / **refetch** after trip completion (or staff refresh) until **product** + **ADR** explicitly require live cross-role delivery (**NFR.1.1 / NFR.1.2**, **NFR.5.5**).

| Axis | Reference (Nest **`RatingModule`**) | Vestroo (shipped / honest gap) |
| ---- | ------------------------------------- | ------------------------------ |
| **HTTP triggers** | **`POST`** **`/rating/create-rating`** (customer); **`GET`** **`/rating/get-rating-by-trip-id/:tripId`**; **`GET`** **`/rating/average-rating`** + **`get-rating-by-query`** (admin/manager) — **`src/legacy/capstone-reference/backend-modules/rating/rating.controller.ts`** | **Contrast only** — no Nest port; future **Server Actions** or **Route Handlers** **TBD** (**FE.5.9**). |
| **Storage** | Reference Mongo shapes (see **`rating.service.ts`** / repo in same folder) | **`public.ratings`**: **`trip_id`** → **`trips`**, **`chauffeur_id`** / **`customer_id`** → **`profiles`** (column **`driver_id`** renamed **`chauffeur_id`** in **`20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`**), **`rate`** (1–5), **`feedback`**, timestamps — create in **`20260402131447_vin_shuttle_tickets_and_social.sql`**. |
| **RLS** | Reference **JWT** roles in guards | **`ratings_parties`**, **`ratings_insert_customer`** (recreated in **`20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`**) — **[data-models.md](data-models.md)** § **RLS overview** + § **`public.ratings`**. |
| **Mutations / reads (app)** | HTTP to Nest | **Gap:** **no** **`ratings`** usage in shipped **`src/actions/`** **grep** (2026-04-11). |
| **Realtime vs batch** | Reference may pair with **socket** hubs elsewhere (**FE.5.9** only — **not** duplicated here) | **`subscribeVehicleTrackings`**, **`subscribeTripsBoard`**, **`subscribeServiceRuns`**, **`subscribeChauffeurAssignments`** in **`src/lib/supabase/realtime.ts`** — **no** **`ratings`** builder. **`supabase_realtime`:** VST-9 **`vehicle_trackings`** + **`trips`**; **SH.9.4** **`service_runs`** + **`chauffeur_assignments`** — **`ratings`** **not** published. **Default stance:** **batch** / **on-demand read** for **`ratings`**. |

### Service type mapping (reference hourly vs scenic → Vestroo lines) — **NFR.5.4**

| Reference / capstone dimension | Vestroo mapping (use **shuttle / corporate** vocabulary) |
| ------------------------------ | ---------------------------------------------------------- |
| **Hourly / on-demand style** (reference **`ServiceType`** hourly paths) | **`PricingModule`** quote paths — **`calculateHourlyQuote`**, **`calculateQuote`** under **`src/actions/`**; customer copy: **transfer** / **hourly hire** / **charter** — **not** a default **“bus product line”** label for these flows. |
| **Scenic / corridor / experience style** | **`ScenicRouteModule`** + **`service_routes`** / **`service_points`** narrative; **experiences** via **`experience_packages`** / **`booking_intent = experience_package`** — see **[tours-and-experiences.md](tours-and-experiences.md)** and optional **[VST-10](stories/vst-10.story.md)**. |
| **Forbidden** | **Do not** map the above to a default **bus** **product line** in **Epic 5** customer/shuttle copy — **NFR.5.4** (align **[Epic 5](epic-5.md)**, **[Epic 6](epic-6.md)** matrix rows). |

### Ops visibility — explicit gap + backlog owner

- Shipped **`/ops/*`** URLs include **no** **`/ops/rating`** (or **`/ops/ratings`**) route; **`OPS_NAV_GROUPS`** in **`src/features/ops/ops-nav-config.ts`** contains **no** ratings nav item (**grep** 2026-04-11). Route inventory: **[ops-console.md](ops-console.md)** § **Routes** — same conclusion.
- **Future** manager-style **ratings grids** / staffed analytics: **product** / **Epic 6** follow-up story **TBD** — **no** **7.6** implementation mandate.

### Shipped vs reference-only code (honest **grep**, 2026-04-11)

- **`src/app/(ops)`**, **`src/app/(field)`**, **`src/app/(app)`:** **no** matches for **`ratings`** table usage or obvious trip-rating UI strings in **first-party** route files.
- **`src/app/(marketing)/`:** homepage **testimonials** use a local **`rating?: number`** for **star display** only — **unrelated** to **`public.ratings`**.
- **Reference-only:** **`src/features/capstone-reference/customer/views/Trips/components/tripRatingView.tsx`**, **`tripRatingForm.tsx`**, **`src/legacy/capstone-reference/backend-modules/rating/*`** — **not** wired as **`(app)`** product routes unless explicitly integrated later.

### `techContext.md` — ratings-adjacent align / partial / gap (**AC5**)

Source: **[`docs/capstone-reference/cline_docs/techContext.md`](capstone-reference/cline_docs/techContext.md)** — **Performance** (**API &lt; 500 ms**, **real-time tracking &lt; 2 s**, **WebSocket** wording) and **Real-time Updates** limitations.

| Reference claim | vs Vestroo (**`public.ratings`**) |
| ---------------- | -------------------------------- |
| **HTTP API latency** | **Gap** for direct comparison — Vestroo path will be **Server Actions** + **RLS** when implemented; **not** reference Nest **`/rating/*`** timings. |
| **WebSocket / sub-second fan-out** (live hubs) | **Gap** for **ratings** — **no** shipped **socket.io**-style rating hub and **no** **`ratings`** **Realtime** subscription or publication row. **Do not** claim instant cross-role rating visibility (**NFR.5.5**). |
| **Bounded writes / reads** | **Align (posture):** when implemented, inserts/selects remain **Postgres + RLS**-bounded like other customer social rows — consistent with **batch** / **read-on-demand** until **ADR** expands scope. |

---

## Infrastructure & cross-cutting (AC3) — not domain matrix rows

Verified in **`app.module.ts`** alongside the **24** domain modules:

| Import / provider | Role in reference | Vestroo posture |
| ----------------- | ------------------ | ---------------- |
| `ConfigModule` | Nest config | Env validation per **[environment-vars](environment-vars.md)** — **not** Mongo URI as Vestroo source of truth. |
| `MongooseModule` | **MongoDB** ODM | **Reference-only**. Vestroo **Supabase Postgres** — **do not** treat Mongoose models as schema to port (**NFR.5.3**). |
| `MailerModule` | SMTP / templates | Align outbound mail with **Epic 8** / **VST-13** (Resend, etc.) — **not** automatic parity with Handlebars paths in reference. |
| `ScheduleModule.forRoot()` | Nest cron/schedule | Background jobs per **Epic 8** patterns — **TBD** per feature. |
| `AppGateway` (provider) | **WebSocket** gateway (**socket.io**-style in reference) | **Supabase Realtime** + **RLS** for first-party live data — see **[FE.5.9](capstone-reference-stack-integration.md)** and **[VST-9](realtime-and-notifications.md)**; **not** a drop-in gateway port. |
| **OpenAPI / BE.6.6** (Story 6.6) | Reference **Swagger** / public HTTP modules | **partial** — time-boxed **OpenAPI 3.x** for **external** **`src/app/api/**/route.ts`** handlers only ([`docs/openapi/vestroo-external-route-handlers.yaml`](openapi/vestroo-external-route-handlers.yaml)); **not** Nest Swagger path parity; **not** Server Actions or PostgREST; **NFR.3.1** disclaimer in spec; **Phase C** CI validation **TBD**. |

---

## FE.5.11 / BE.6.7 — staff ops data owners (canonical checklist)

**Single source of truth** for **FE.5.11** capability status (**Story 5.11**) and **[Epic 6 — BE.6.7](epic-6.md#be67-data-and-server-actions-for-staff-ops-fe511)**. **Do not** duplicate this table elsewhere; **[`docs/ops-console.md`](ops-console.md)** § **FE.5.11 — route & capability gaps** summarises gaps and links here.

**NFR.5.5:** Sidebar visibility ≠ authorization — **`requireOpsStaffPage`** on layout; **`getOpsStaffForAction`** / **`getOpsAdminForAction`** on mutations; RLS on reads. **AC4 (staff vs chauffeurs):** **Internal staff** (**dispatcher** / **admin**) are **distinct** from **chauffeur** accounts (**`/field/*`**, **`profiles.role = chauffeur`**). **`/ops/roster`** lists chauffeurs for **scheduling** only — not staff identity admin.

**Epic ↔ row map ([Epic 6 — BE.6.7](epic-6.md#be67-data-and-server-actions-for-staff-ops-fe511)):** The epic lists **five** bullets; this checklist has **six** rows. **Staff directory & roles** ↔ **row 1**. **Corporate clients** ↔ **row 2**. **Vehicles & vehicle categories** ↔ **row 3**. **Service routes / service areas** ↔ **row 4** — capstone **Bus**\* / transit-demo schema stays **out of** this row unless **[Epic 9 — SH.9.1](epic-9.md)** is **go**. **Bookings, trips, checkout** ↔ **row 5**, tying **`SearchModule`**, **`BookingModule`**, **`TripModule`**, and **`CheckoutModule`** to traveller parity in **[core-traveller-flow-parity.md](core-traveller-flow-parity.md)** (**CORE.6.4**); **staff** **create** / **edit** / **cancel** is **implemented only where product and code already allow**, otherwise **honest deferred** (this row + **[ops-console.md](ops-console.md)**). **Row 6** matches **[epic-5.md](epic-5.md)** **FE.5.11** narrative **item 6** only — **not** a sixth **BE.6.7** bullet; it **aggregates** adjacent **IA** / ops workflows **without** duplicating **rows 1–5**.

| Capability area | Status | Primary `/ops/*` routes | Supabase / Server Actions / Route Handlers (summary) | Owner | Capstone mapping cross-links |
| ----------------- | ------ | ------------------------ | ------------------------------------------------------ | ----- | ---------------------------- |
| **1. Staff users & roles** | partial | **none** dedicated (no **`/ops/users`**); shell shows email/role | `auth.users`, `profiles`; **`requireOpsStaffPage`**; **Supabase Auth** **invite** / **disable** (deactivate) **from `/ops/*`:** **TBD** — often **Dashboard** / manual today; admin-only **`exportDataSubjectAction`**, **`anonymiseDataSubjectAction`** in **`opsCompliance.ts`**. **Migrations (`profiles`):** `20260402131346_vin_shuttle_core_schema.sql`, `20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`, `20260412120000_vst12_compliance_incidents_documents_retention.sql` | **BE.6.5**, **BE.6.7**; follow-up story **TBD** (staff invite / role assignment / deactivate UI + policies) | [Admin → ops — login / profile rows](capstone-admin-to-ops-mapping.md); [Manager — profile](capstone-manager-to-vestroo-mapping.md) |
| **2. Corporate clients** | deferred | **none** (no **`/ops/clients`** / **`/ops/customers`**) | **TBD** — B2B **account** / **client** entity (distinct from traveller **`profiles`** where product requires); **no** staffed CRUD Server Actions yet; bookings discoverable via **`/ops/search`** only today | **Epic 6** schema + **`/ops/*`** story **TBD**; **FE.5.11** / **BE.6.7** gap explicit | [Manager — `/customers`](capstone-manager-to-vestroo-mapping.md) |
| **3. Vehicles & categories** | partial | **`/ops/vehicles`** (list + context; **no** per-vehicle detail route) | `vehicles`, `vehicle_categories`, `vehicle_pricings`; **staffed** **category** CRUD + **vehicle** create/update/**retire** + **Supabase Storage** fleet **media** UI: **TBD** / read-heavy today. **Migrations:** `20260402131358_vin_shuttle_vehicles_and_pricing.sql`, `20260402133646_vestroo_rls_policies_vestroo_domain.sql` | **FE.5.4**, **BE.6.7**; category/media/detail-route follow-up **TBD** | [Admin — vehicles / category / Firebase media rows](capstone-admin-to-ops-mapping.md) |
| **4. Service routes & service areas** | deferred | **none** (no **`/ops/service-areas`** / map authoring) | **TBD** — **`service_routes`** / graph tables exist; **corporate shuttle** corridor / zone / **approved pickup/drop-off** **geo authoring** Server Actions + staffed **`/ops/*`** surface: **none** yet; **not** Epic 9 capstone **Bus**\* transit-demo unless **SH.9.1** **go** | **Epic 6** + product spike **TBD** | [Admin — `/router`](capstone-admin-to-ops-mapping.md); [Manager — `/routes`](capstone-manager-to-vestroo-mapping.md) |
| **5. Bookings lifecycle** | partial | **`/ops/search`**, **`/ops/fulfil`**, **`/ops/trips`**, **`/ops/board`**, **`/ops/calendar`** | **`SearchModule`** → **`/ops/search`**; **`BookingModule`** / **`TripModule`** → **`opsDispatch.ts`**, fulfil/trip queries; **`CheckoutModule`** → PayFast **`/api/payfast/webhook`** + booking payment steps. **Traveller** **search → checkout:** [core-traveller-flow-parity.md](core-traveller-flow-parity.md) (**CORE.6.4** / Story **6.4**). **Staff** booking **create** / **edit** / **cancel** as dedicated **`/ops/*`** flows: **deferred** except existing **assign** / **modify** / fulfilment paths — see **`ops-console.md`**. **Migrations:** `20260402131427_vin_shuttle_driver_and_on_demand_trips.sql`, `20260402140000_vestroo_bookings_web_columns.sql`, `20260406120000_vst6_booking_intent_and_payment_audit.sql`, `20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`, `20260407130000_vst7_ops_audit_trips_fulfilment.sql`, `20260408120000_vst8_chauffeur_booking_rls_ops_audit_actor_role.sql`, `20260413130000_vst13_corporate_invoicing_hooks.sql` | **VST-6**, **VST-7**; **CORE.6.4** (traveller vs staff) | [Manager — bookings constants](capstone-manager-to-vestroo-mapping.md); [Admin — `/trip`](capstone-admin-to-ops-mapping.md) |
| **6. Related staff workflows** | partial | **Fulfilment:** board, calendar, fulfil, trips, staff booking search · **Fleet:** vehicles · **People:** roster · **Finance & compliance:** invoicing, compliance · **Engagements:** experiences (optional nav) | Matches **`OPS_NAV_GROUPS`** in **`src/features/ops/ops-nav-config.ts`** — **no** critical workflow **orphaned** without route or explicit deferral above. **Migrations:** see domain rows **BookingModule**, **TripModule**, **CheckoutModule**, **VehiclesModule**, **DriverScheduleModule**, **NotificationModule**, **TrackingModule** above; compliance: `20260412120000_vst12_compliance_incidents_documents_retention.sql`, experiences: `20260410120000_vst10_experience_packages.sql` | **FE.5.11** item **6** (**IA** bucket — **not** a sixth **BE.6.7** bullet); **VST-7**, **VST-8** (roster), **VST-12**, **VST-13**, **VST-10** | [Ops console — routes](ops-console.md#routes); [Manager mapping](capstone-manager-to-vestroo-mapping.md) |

**Bookings gaps:** Staff-assisted **create** / **edit** / **cancel** booking from **`/ops/*`** (vs **CORE.6.4** traveller actions), richer **filters**, and dedicated **booking detail** routes are **backlog** — track in matrix + **`ops-console.md`** (**FE.5.11** / **Story 5.11**) until implemented.

Epic narrative: **[Epic 5 — FE.5.11](epic-5.md)**.

---

## `cline_docs` topic index (AC6)

Files under **[`docs/capstone-reference/cline_docs/`](capstone-reference/cline_docs/)** describe the **campus / line-haul demo** reference stack (Mongo, Nest, socket-era assumptions). They are **documentation for that demo** — **not** Vestroo product specifications (**reference ≠ product**; **NFR.5.4**). **Legacy `bus*`-prefixed filenames** (and **`Bus*`** Nest modules) are **traceability keys** to the vendored tree only — **not** an assertion that Vestroo is a **public bus** or **mass-transit** operator.

**Indexed topic files (required names):** [`busOperations.md`](capstone-reference/cline_docs/busOperations.md), [`busTracking.md`](capstone-reference/cline_docs/busTracking.md), [`ticketingSystem.md`](capstone-reference/cline_docs/ticketingSystem.md), [`seatTracking.md`](capstone-reference/cline_docs/seatTracking.md), [`driverSchedule.md`](capstone-reference/cline_docs/driverSchedule.md).

**Legacy filenames** **`busOperations`**, **`busTracking`** appear below as **capstone file names only** — **not** Vestroo product terms.

| Topic file | Maps to matrix rows / subsection | Notes |
| -----------| ---------------------------------- | ----- |
| [productContext.md](capstone-reference/cline_docs/productContext.md) | Cross-cutting product background | **N/A** for Nest row mapping — historical capstone context; use **[Epic 4](epic-4.md)** for Vestroo product source of truth. |
| [systemPatterns.md](capstone-reference/cline_docs/systemPatterns.md) | `AuthModule`, `KeytokenModule`, infrastructure | Describes reference **JWT / layered** patterns — **reference-only** vs **[FE.5.9](capstone-reference-stack-integration.md)**. |
| [techContext.md](capstone-reference/cline_docs/techContext.md) | Infrastructure + **Tracking**, **Notification**, **`RatingModule`** (via **[RT.7.6](#rt-7-6)**) | Stack choices (Mongo, sockets) — contrast with **Supabase** matrix rows; **ratings** path = **batch** / **RLS**, **not** live **socket** fan-out (**NFR.5.5**). |
| [ticketingSystem.md](capstone-reference/cline_docs/ticketingSystem.md) | `TicketModule` | **Not applicable** default shuttle MVP — **Epic 9** gate. |
| [seatTracking.md](capstone-reference/cline_docs/seatTracking.md) | `TicketModule`, `BusTrackingModule` | **Not applicable** / **Epic 9** — capstone seat/ticket semantics. |
| [driverSchedule.md](capstone-reference/cline_docs/driverSchedule.md) | `DriverScheduleModule`, `DriverBusScheduleModule` | Chauffeur **shifts** ↔ Vestroo **`chauffeur_schedules`**; **DriverBusSchedule** row **N/A** default. |
| [projectStructure.md](capstone-reference/cline_docs/projectStructure.md) | All modules (layout) | Repo layout reference — verify paths on checkout. |
| [activeContext.md](capstone-reference/cline_docs/activeContext.md) | — | **N/A** — working notes; no module ownership. |
| [progress.md](capstone-reference/cline_docs/progress.md) | — | **N/A** — progress log. |
| [busOperations.md](capstone-reference/cline_docs/busOperations.md) | `BusStopModule`, `BusRouteModule`, `BusScheduleModule` | **Legacy filename.** **Epic 9** deferral; **not** Vestroo **bus operator** positioning (**NFR.5.4**). |
| [busTracking.md](capstone-reference/cline_docs/busTracking.md) | `BusTrackingModule`, `TrackingModule` | **Legacy filename.** Capstone **bus** tracking vs Vestroo **`vehicle_trackings`** — see **Tracking** row rationale. |

---

## `document/` diagram index (AC7, AC10)

### Coverage strategy

1. **Indexed:** All **`.jpg`**, **`.docx`**, **`.xlsx`**, and the primary **`VinShuttle.vpp`** Visual Paradigm project under **`docs/capstone-reference/document/`**, **except** explicit exclusions below.
2. **Excluded:** All **`VinShuttle.vpp.bak_*`** files (auto backups) — redundant with **`VinShuttle.vpp`** and noise for readers.

| Pattern | Rationale |
| ------- | ---------- |
| `VinShuttle.vpp.bak_*` | Visual Paradigm backup fragments; **omit** from traceability; use **`VinShuttle.vpp`** as the canonical model file. |

### Diagram & document inventory

**2026-04-18 inventory check:** Every file under **`docs/capstone-reference/document/`** matching the [Coverage strategy](#coverage-strategy) (all **`.jpg`**, **`.docx`**, **`.xlsx`**, **`VinShuttle.vpp`**, excluding **`VinShuttle.vpp.bak_*`**) appears in the table below — **no missing assets**; backup fragments remain intentionally omitted.

**Verification mapping** uses one of: **automated test** (path), **story ID**, or **`diagram only — no automated test yet`** + **owner**. Automated paths below **do not** assert pixel or shape parity with **`.jpg`** diagrams — they document **overlapping product code** only. Where **[`#sh-9-1`](epic-9.md#sh-9-1)** is **go** (2026-04-17) and **SH.9.2–SH.9.5** artefacts exist, selected rows cite **[ADR 0002](adr/0002-patterned-shuttle-domain-sh9-2.md)–[0005](adr/0005-patterned-checkout-sh9-5.md)** / stories **only** when factually related.

| Filename | Type | Purpose (short) | Verification mapping |
| -------- | ---- | ----------------- | --------------------- |
| `Admin Screen Flow.jpg` | Screen flow | Reference admin UI flow | diagram only — no automated test yet — **Owner: TBD** (Engineering) |
| `API architecture diagram.jpg` | Architecture | Reference API layering | diagram only — no automated test yet — **Owner: TBD** |
| `BookingHour Activity Diagram.jpg` | Activity | Booking-hour workflow (reference) | **Automated overlap (not diagram conformance):** `src/actions/__tests__/createBooking.test.ts` — **[CORE.6.4](core-traveller-flow-parity.md)** / **VST-6**; **diagram only** for image fidelity — **Owner: Engineering** |
| `Create BookingHour Sequence Diagram.jpg` | Sequence | Create booking-hour (reference) | **Automated overlap:** `src/actions/__tests__/createBooking.test.ts`; **diagram only** for sequence-vs-code traceability — **Owner: TBD** |
| `Create BookingScenicRoute Sequence Diagram.jpg` | Sequence | Booking + scenic route | **Automated overlap:** `src/actions/__tests__/createBooking.test.ts`; **`#sh-9-1` go:** patterned domain — **[SH.9.2](stories/9.2.story.md)**, **[ADR 0002](adr/0002-patterned-shuttle-domain-sh9-2.md)**; **diagram only** for diagram shape |
| `Create DriverSchedule Sequence Diagram.jpg` | Sequence | Create chauffeur schedule (reference) | **Automated overlap:** `src/lib/__tests__/ops-booking.test.ts` (dispatch/assign context — **not** schedule diagram); **§ [RT.7.5](#rt-7-5)**; **diagram only** for capstone sequence — **Owner: TBD** |
| `Create Pricing Sequence Diagram.jpg` | Sequence | Create pricing | **Automated overlap:** `src/actions/__tests__/calculateQuote.test.ts`, `src/actions/__tests__/calculateExperienceQuote.test.ts` — **VST-6** quote paths; **diagram only** for diagram fidelity — **Owner: TBD** |
| `Create ScenicRoute Sequence Diagram.jpg` | Sequence | Create scenic/route entity | diagram only — no automated test yet — **Owner: TBD** |
| `Create Vehicle Sequence Diagram.jpg` | Sequence | Create vehicle | diagram only — no automated test yet — **Owner: TBD**; **FE.5.4** fleet |
| `Customer Screen Flow.jpg` | Screen flow | Reference customer app | diagram only — no automated test yet — **Owner: TBD** |
| `Driver Checkin DriverSchedule Sequence Diagram.jpg` | Sequence | Driver check-in schedule | diagram only — no automated test yet — **Owner: TBD**; **FE.5.6** |
| `Driver Checkout DriverSchedule Sequence Diagram.jpg` | Sequence | Driver checkout schedule | diagram only — no automated test yet — **Owner: TBD** |
| `Driver Complete Trip Sequence Diagram.jpg` | Sequence | Complete trip | diagram only — no automated test yet — **Owner: TBD**; **VST-8** / **fieldChauffeur** |
| `Driver Execute Trip Activity Diagram.jpg` | Activity | Trip execution | diagram only — no automated test yet — **Owner: TBD**; **FE.5.6** |
| `Driver Pickup Trip Sequence Diagram.jpg` | Sequence | Pickup trip | diagram only — no automated test yet — **Owner: TBD** |
| `Driver Screen Flow.jpg` | Screen flow | Reference driver app | diagram only — no automated test yet — **Owner: TBD**; **FE.5.6** |
| `Driver Start Trip Sequence Diagram.jpg` | Sequence | Start trip | diagram only — no automated test yet — **Owner: TBD**; **VST-8** |
| `DriverSchedule Class Diagram.jpg` | Class | Driver schedule domain | diagram only — no automated test yet — **Owner: TBD** |
| `DriverSchedule State Machine Diagram.jpg` | State machine | Schedule states | diagram only — no automated test yet — **Owner: TBD** |
| `Manage Booking Class Diagram.jpg` | Class | Booking aggregate | **Automated overlap:** `src/actions/__tests__/createBooking.test.ts`, `src/actions/__tests__/processPayment.test.ts`; **`#sh-9-1` go:** capacity holds — **[SH.9.3](stories/9.3.story.md)**, **[ADR 0003](adr/0003-service-run-capacity-holds-sh9-3.md)**, `src/actions/__tests__/service-run-capacity.test.ts`, `src/lib/__tests__/sh93-migration-ac7.test.ts`; patterned checkout — **[SH.9.5](stories/9.5.story.md)**, **[ADR 0005](adr/0005-patterned-checkout-sh9-5.md)**, **[patterned-checkout-vst6-delta.md](patterned-checkout-vst6-delta.md)**; **diagram only** for UML vs schema |
| `Manage Pricing Class Diagram.jpg` | Class | Pricing aggregate | **Automated overlap:** `src/actions/__tests__/calculateQuote.test.ts`, `src/actions/__tests__/calculateExperienceQuote.test.ts`; **diagram only** — **Owner: TBD** |
| `Manage ScenicRoute Class Diagram.jpg` | Class | Route/scenic aggregate | diagram only — no automated test yet — **Owner: TBD** |
| `Manage Trip Class Diagram.jpg` | Class | Trip aggregate | **Automated overlap:** `src/lib/__tests__/chauffeur-trip-transitions.test.ts`, `src/lib/__tests__/ops-booking.test.ts`, `src/lib/__tests__/realtime-channels.test.ts`, `src/lib/__tests__/sh94-migration-ac6.test.ts`; **`#sh-9-1` go:** live ops — **[SH.9.4](stories/9.4.story.md)**, **[ADR 0004](adr/0004-patterned-run-realtime-sh9-4.md)**, **[patterned-run-live-ops-mapping.md](patterned-run-live-ops-mapping.md)**; **diagram only** for class picture |
| `Manage Vehicle Class Diagram.jpg` | Class | Vehicle aggregate | diagram only — no automated test yet — **Owner: TBD** |
| `Manager Screen Flow.jpg` | Screen flow | Reference manager app | diagram only — no automated test yet — **Owner: TBD**; **FE.5.5** |
| `Report1_Project Introduction.docx` | Document bundle | Project intro | diagram only — no automated test yet — **Owner: TBD** (PM); internal outline **TBD** |
| `Report2_Project Management Plan.docx` | Document bundle | PM plan | diagram only — no automated test yet — **Owner: TBD** |
| `Report3_Software Requirement Specification.docx` | Document bundle | SRS | diagram only — no automated test yet — **Owner: TBD** |
| `Report4_Software Design Document.docx` | Document bundle | SDD | diagram only — no automated test yet — **Owner: TBD** |
| `Report5_Test Documentation.docx` | Document bundle | Test documentation | diagram only — no automated test yet — **Owner: TBD** |
| `Report5_Test Report.xlsx` | Spreadsheet | Test report | diagram only — no automated test yet — **Owner: TBD** |
| `Report5_Unit Test.xlsx` | Spreadsheet | Unit test listing | diagram only — no automated test yet — **Owner: TBD** |
| `Report6_Software User Guides.docx` | Document bundle | User guides | diagram only — no automated test yet — **Owner: TBD** |
| `ScenicRoute State Machine Diagram.jpg` | State machine | Scenic route states | diagram only — no automated test yet — **Owner: TBD** |
| `Trip State Machine Diagram.jpg` | State machine | Trip lifecycle | **Automated overlap:** `src/lib/__tests__/chauffeur-trip-transitions.test.ts` (transition rules — **not** full state-machine vs **`.jpg`**); align narrative with **`trips.status`** / dispatch; **diagram only** for diagram |
| `Update DriverSchedule Sequence Diagram.jpg` | Sequence | Update schedule | **Automated overlap:** `src/lib/__tests__/ops-booking.test.ts` (roster/assign paths partial); **`#sh-9-1` go:** **[SH.9.4](stories/9.4.story.md)** / **[patterned-run-live-ops-mapping.md](patterned-run-live-ops-mapping.md)** for patterned run ops — **diagram only** for image |
| `Update Pricing Sequence Diagram.jpg` | Sequence | Update pricing | **Automated overlap:** `src/actions/__tests__/calculateQuote.test.ts`, `src/actions/__tests__/calculateExperienceQuote.test.ts`; **diagram only** — **Owner: TBD** |
| `Update ScenicRoute Sequence Diagram.jpg` | Sequence | Update route | diagram only — no automated test yet — **Owner: TBD** |
| `Update Vehicle Sequence Diagram.jpg` | Sequence | Update vehicle | diagram only — no automated test yet — **Owner: TBD** |
| `VehicleCondition State Machine Diagram.jpg` | State machine | Vehicle condition | diagram only — no automated test yet — **Owner: TBD** |
| `VehicleOperationStatus State Machine Diagram.jpg` | State machine | Vehicle ops status | diagram only — no automated test yet — **Owner: TBD** |
| `vin_shuttle_erd.jpg` | ERD | Entity relationship (variant) | diagram only — no automated test yet — **Owner: TBD**; compare **[data models](data-models.md)** |
| `Vinshuttle logical entity relationship diagram.jpg` | ERD | Logical ERD | diagram only — no automated test yet — **Owner: TBD** |
| `VinShuttle.jpg` | Image | Cover / overview graphic | diagram only — no automated test yet — **Owner: TBD** |
| `VinShuttleUC.jpg` | Use case | Use-case overview | diagram only — no automated test yet — **Owner: TBD** |
| `VinShuttle.vpp` | Visual Paradigm project | Master model (all diagrams) | diagram only — no automated test yet — **Owner: TBD** (Architecture); open with **VP** |
| `Web API package diagram.jpg` | Package diagram | API package structure | diagram only — no automated test yet — **Owner: TBD** |

---

## Related documentation

* **[Epic 5](epic-5.md)** — **FE.5.10** narrative requirements; **FE.5.11** checklist → § **FE.5.11 / BE.6.7** above
* **[Epic 6](epic-6.md)** — **BE.6.1** (this matrix), **BE.6.2** (migration filenames in **Notes** + **RLS appendix**), **[BE.6.3](capstone-nest-rest-to-vestroo-mapping.md)** (Nest **`@Controller`** → Server Actions / Route Handlers), **[CORE.6.4](core-traveller-flow-parity.md)** (traveller **search → checkout** flow), **[BE.6.5](capstone-auth-keytoken-otp-parity.md)** (Auth / Keytoken / OTP vs Supabase), **[BE.6.7](epic-6.md#be67-data-and-server-actions-for-staff-ops-fe511)** / **[Story 6.7](stories/6.7.story.md)** (staff ops data owners = § above)
* **[Data models](data-models.md)** — **`public.*`** mapping, **RLS overview** (must stay consistent with this file’s **RLS** column + appendix)
* **[Epic 9](epic-9.md)** — **SH.9.1** gate; **[SH.9.6](epic-9.md#sh-9-6)** traceability → **[§ SH.9.6 — Deferred capstone modules](capstone-backend-module-matrix.md#sh-9-6-deferred-capstone-modules)**
* **[Frontend API interaction](front-end-api-interaction.md)** — Server Actions vs Route Handlers
* **[Ops console](ops-console.md)**, **[Field tools](field-tools.md)**, **[Realtime and notifications](realtime-and-notifications.md)**
* **[Epic 7](epic-7.md)** — **RT.7.1** Tracking / Trip / reference BusTracking → Realtime: § **RT.7.1 / Epic 7 — Tracking, Trip, BusTracking → Supabase Realtime**; **RT.7.2** **NotificationModule** → § **RT.7.2 / Epic 7 — NotificationModule → Vestroo**; **RT.7.3** **ConversationModule** / **SharedItineraryModule** → § **RT.7.3 / Epic 7 — ConversationModule, SharedItineraryModule → Vestroo**; **RT.7.4** cross-topic policy / abuse / observability / **`techContext`** align-or-gap → [§ RT.7.4 — Realtime policies, abuse controls, observability](#rt-7-4); **RT.7.5** **DriverScheduleModule** / **DriverBusScheduleModule** → assignments, runs, roster / Epic 9 deferral → [§ RT.7.5 — DriverScheduleModule, DriverBusScheduleModule](#rt-7-5); **RT.7.6** **RatingModule** → **`public.ratings`**, **`/ops/rating`** gap, **Realtime vs batch**, **service type** vocabulary → [§ RT.7.6 — RatingModule → feedback, ops visibility, Realtime vs batch](#rt-7-6) (this doc)
