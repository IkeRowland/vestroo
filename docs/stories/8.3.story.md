# Story 8.3: Payments — Momo / PayOS vs Vestroo PayFast (INT.8.3)

## Status: Done

**Dependencies:** **[`docs/epic-8.md`](../epic-8.md)** — **INT.8.3** source (reference **Momo** + **PayOS** vs authoritative **Vestroo PayFast**; **checkout**, **webhooks**, **refunds**, **reconciliation**, **deposit** rules; **provider substitution** for **developers** and **support**; **test** vs **production** keys per **[`docs/environment-vars.md`](../environment-vars.md)**). **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** (VST-13) — existing **PayFast webhook lifecycle**, **Security**, **failed/cancelled recovery**, **sub-processors** — **do not** wholesale duplicate; **INT.8.3** **extends** with **reference vs PayFast** mapping and a **provider-agnostic** matrix (**parity of outcomes**, not copy-paste). **[`docs/environment-vars.md`](../environment-vars.md)** — **`PAYFAST_*`**, **`NEXT_PUBLIC_APP_URL`** (notify prefix); **per-environment** table (**sandbox vs live**). **[`docs/data-models.md`](../data-models.md)** — **`bookings.payment_reference`**, **`trans_id`**, **`payment_status`** / **`status`** lifecycle vs PayFast (**§ Web bookings**); **grep** **`supabase/migrations/`** if column semantics need a second source. **[`docs/front-end-api-interaction.md`](../front-end-api-interaction.md)** — **`processPayment`**, **quote integrity**, **idempotency** (new checkout row vs webhook dedupe). **[`docs/capstone-reference-stack-integration.md`](../capstone-reference-stack-integration.md)** — **FE.5.9** outbound HTTP / Nest contrast (**thin**). **[`docs/compliance-and-safety.md`](../compliance-and-safety.md)** (VST-12) — **PCI** / **sub-processor** pointer (**short** cross-link; VST-13 already defers depth). **[`docs/stories/8.1.story.md`](8.1.story.md)** / **[`docs/stories/8.2.story.md`](8.2.story.md)** — **INT.8.1** / **INT.8.2** only where payment-adjacent (**booking paid → email**; **SMS stub** after booking) — **one-line** cross-links, **no** template re-inventory. **[`docs/stories/vst-13.story.md`](vst-13.story.md)** — VST-13 story (**Done**); add a **thin** reciprocal pointer to **`#int-8-3`** when that section ships (**optional** one-line in VST-13 story *Related* if maintainers want bidirectional traceability). **Reference code (pattern / traceability — not production Vestroo):** **`src/legacy/capstone-reference/backend-modules/checkout/checkout.service.ts`** — **`@payos/node`**, **`createPaymentLink`**, **`CheckoutBookingMomo`**, **`momoCalbackReturn`**, **`momoTransferTripCalbackReturn`** (**grep** `payos`, `momo`, `CheckoutBookingMomo`). **`src/legacy/capstone-reference/backend-modules/checkout/checkout.controller.ts`** — **`momo/return-booking-payment`**, **`momo/return-transfer-trips`**. **`src/legacy/capstone-reference/backend-share/momo.ts`**, **`payos.ts`**. **`src/legacy/capstone-reference/backend-modules/booking/booking.service.ts`** — **`checkoutService.CheckoutBookingMomo`** (**grep** `CheckoutBookingMomo`). **`src/legacy/capstone-reference/backend-modules/trip/trip.service.ts`** — **`initiateRefund`**, **`handleRefundForTrip`**, Momo transfer / payment link paths (**grep** `initiateRefund`, `handleRefundForTrip`, `momo`). **Shipped PayFast (verify, cite paths not fragile line numbers):** **`src/actions/processPayment.ts`**, **`src/lib/payfast.ts`**, **`src/lib/payfast-client.ts`**, **`src/app/api/payfast/webhook/route.ts`**, **`src/lib/__tests__/payfast-signature.test.ts`**, **`src/app/api/payfast/webhook/__tests__/route.test.ts`**. **Honest prep grep (2026-04-14):** shipped **`src/`** (excluding legacy) showed **no** `deposit` symbol hits; **no** automated **PayFast refund** API in app **`src/`**; **reconciliation** language appears for **quotes/Maps** (`src/lib/maps.ts`, `src/lib/experience-package-data.ts`) — **not** gateway settlement reconciliation — state **implemented / gap / N/A** honestly in the matrix.

**Explicit out of scope:** **INT.8.4** cron / scheduled jobs (**defer** unless a **grep** ties payment settlement to cron — then **one** footnote). **INT.8.5** full **`share/`** secrets matrix (**one-line defer** to that story). **INT.8.6** HTTP client / timeout / retry treatise — **one** cross-link to **[`docs/front-end-api-interaction.md`](../front-end-api-interaction.md)** + capstone doc is enough. **No** implementing **Momo** or **PayOS** in Vestroo. **No** changing **PayFast** integration **code** in this story — **documentation only**.

**Constraining NFRs:**

- **NFR.3.1:** **Webhook signatures** and **server-only secrets** — **single verification path** already documented in VST-13 (**`verifyPayFastWebhookSignature`** in **`src/lib/payfast.ts`** for **`POST /api/payfast/webhook`**). **INT.8.3** MUST **cite** and **not contradict**; reference Momo/PayOS callbacks described as **pattern contrast** only.
- **NFR.1.3:** Document **PayFast** **failure modes** and **operator/customer** recovery (**extend** VST-13 “Failed / cancelled” slice or add an **INT.8.3** subsection) — align with **`integrations-and-payments.md`** and **`front-end-api-interaction.md`** idempotency notes.
- **NFR.4.1:** **TypeScript** at boundaries — cite **`PayFastPaymentParams`**, signature helpers, and **webhook payload** handling types in **`src/lib/payfast.ts`** / **`src/app/api/payfast/webhook/route.ts`** (**grep** `PayFast`, `Record`, `z`); reference legacy **`any`** callbacks only as **anti-pattern** contrast.

## Story

- As a **platform, engineering, or support** owner of **Epic 8** payment traceability
- I want a **grep-backed** canonical section **`#int-8-3`** that maps **reference Momo / PayOS** checkout, callbacks, refunds, and reconciliation concepts to **Vestroo PayFast** outcomes — including **staging vs production** keys per **`environment-vars.md`**, **deposit** posture (**TBD** if absent), and a **provider substitution** guide — with **backlog** rows for **unmapped** reference flows
- so that **developers** and **support** never confuse **VN reference** mechanics with **ZA PayFast** production truth, **PCI / sub-processor** boundaries stay visible, and **NFR.3.1 / 1.3 / 4.1** remain enforceable at the **documentation** layer

## Acceptance Criteria (ACs)

1. **Canonical doc home for INT.8.3:** Add **`## INT.8.3 — Payments: reference Momo / PayOS vs Vestroo PayFast`** (or equivalent) with stable anchor **`#int-8-3`** to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** — **`<a id="int-8-3"></a>`** immediately above the heading (**same pattern** as **`#int-8-1`** / **`#int-8-2`**). Section MUST include a **provider-agnostic matrix** (sortable-by-eye Markdown table) with columns at minimum: **Business event** (checkout initiate, webhook/callback confirm, refund, reconciliation / ops); **Reference (Momo / PayOS) path** (file paths under **`src/legacy/capstone-reference/`** — **grep**, cite paths); **Vestroo PayFast equivalent** (e.g. **`processPayment`** + hosted form, **`/api/payfast/webhook`**, **N/A** where honest); **Checkout steps summary** (numbered short list per column cell or footnote key); **Webhook / callback model** (server-to-server vs return URL — **contrast** reference vs PayFast ITN); **Refunds row** (reference **`momo.ts` / `trip.service.ts`** vs Vestroo **manual / gateway UI / gap** — **grep** shipped `src/` for `refund`); **Reconciliation / ops row** (DB fields **`payment_reference`**, **`trans_id`**, **`payment_status`**; ops filters — vs reference trip totals / transfer logic); **Staging vs prod**; **Env vars pointer** (link **[`docs/environment-vars.md`](../environment-vars.md)** **`PAYFAST_*`** matrix); **Status + rationale** (**implemented / pattern-only / gap / N/A**) for **support**. **Update** the VST-13 **integration matrix** **PayFast** row (and/or **Per-environment** PayFast cell) to point at **`#int-8-3`** when that section becomes the **Momo/PayOS parity** home (**if** the row is still one-line-only).

2. **Reference inventory — Momo / PayOS / callbacks:** **Grep** **`src/legacy/capstone-reference/`** for **`momo`**, **`Momo`**, **`payos`**, **`PayOS`**, **`@payos/node`**, **`createPaymentLink`**, **`CheckoutBookingMomo`**, **`callback`**, **`return-booking`**, **`IPN`**, **`webhook`**. Summarise **distinct flows** (booking checkout vs transfer-trip paths). **Do not** imply these run in production Vestroo.

3. **Vestroo inventory — PayFast + booking payment lifecycle:** **Grep** shipped **`src/`** (exclude **`src/legacy/`** unless contrasting) for **`payfast`**, **`PayFast`**, **`processPayment`**, **`verifyPayFastWebhookSignature`**, **`payment_status`**, **`trans_id`**, **`payment_reference`**, **`pf_payment_id`**. Document the **chain**: reconcile quote → insert pending booking → **ITN** updates → customer **return URL** **`/confirmation?id={bookingId}`** (see **`processPayment`** `return_url`) and page data via **`GET /api/booking-confirmation`** — cite **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** webhook lifecycle **by link**, duplicate only **mapping** sentences needed for INT.8.3.

4. **Deposit rules:** **Grep** **`src/`**, **`supabase/migrations/`**, and **`docs/data-models.md`** for **`deposit`**, **`payment_amount`**, **`partial`**, **`split`**. If **zero** relevant hits, matrix MUST show **deposit / partial capture** as **TBD** with **honest** “**not evidenced in schema**” note (do **not** invent **Momo/PayOS** deposit parity).

5. **Provider substitution guide:** Include a **compact second table** or **sidebar-style bullet map** (“If you know **reference X** → in **Vestroo** it is **Y**”) covering at minimum: **payment link creation**, **return/callback URL**, **server confirmation**, **customer-visible reference code**, **gateway transaction id** — **Momo/PayOS** vs **PayFast** naming.

6. **NFR.3.1 — Signatures / secrets:** Explicit bullet: **PayFast** passphrase + merchant key **server-only**; **one** signature verification implementation; **no** `NEXT_PUBLIC_PAYFAST_*` — **quote** VST-13 Security + **`environment-vars.md`**; **contrast** with reference env patterns **without** prescribing new secret names.

7. **NFR.1.3 — Failure modes:** Extend VST-13 or INT.8.3 with **PayFast-specific** bullets: **user cancel** at gateway, **`FAILED`/`CANCELLED` ITN**, **duplicate ITN**, **signature failure** → expected HTTP + DB state + **customer** next step (**new checkout row** per **`front-end-api-interaction.md`**). **Operator** guidance: where to confirm payment (**Supabase** / ops UI) — **TBD** if no runbook.

8. **NFR.4.1 — Types at boundaries:** Footnote or row citing **`PayFastPaymentParams`**, **`generatePayFastSignature`**, and webhook field handling; point to **`src/lib/__tests__/payfast-signature.test.ts`** and **`src/app/api/payfast/webhook/__tests__/route.test.ts`**.

9. **Index / Related thin link:** Add **`#int-8-3`** to **[`docs/index.md`](../index.md)** and/or VST-13 **Related** / cross-links (**same spirit** as **INT.8.1** / **INT.8.2** — **no** wholesale restructure).

10. **Backlog for unmapped reference flows:** Any reference **Momo/PayOS** capability **without** a truthful Vestroo **implemented** equivalent (e.g. **programmatic refund** in legacy **`momo.ts`**) MUST be a **backlog** row with **priority** legend (**P0/P1/P2** or **High/Medium/Low** — **one** scheme, consistent with **INT.8.1** if possible).

## Tasks / Subtasks

- [x] **Task 1 — AC1, AC9:** Add **`<a id="int-8-3"></a>`** + **`## INT.8.3 …`** to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** with provider-agnostic matrix skeleton; patch **integration matrix** / **per-environment** PayFast pointer; **thin** **[`docs/index.md`](../index.md)** link. (AC: #1, #9)
- [x] **Task 2 — AC2:** **Grep** **`src/legacy/capstone-reference/`** for Momo/PayOS/callback symbols; fill **reference** column + **pattern-only** / **N/A** rows. (AC: #2)
- [x] **Task 3 — AC3, AC8:** **Grep** shipped **`src/`** for PayFast + booking payment fields; link lifecycle to **tests** + **types**. (AC: #3, #8)
- [x] **Task 4 — AC4:** **Grep** **`deposit`**, **`payment_amount`**, migrations; document **TBD** vs evidenced **partial payment**. (AC: #4)
- [x] **Task 5 — AC5:** Author **provider substitution** map table / sidebar bullets (**dev + support**). (AC: #5)
- [x] **Task 6 — AC6:** Write **NFR.3.1** bullets — **cite** VST-13 **single path**; **no** new secret patterns. (AC: #6)
- [x] **Task 7 — AC7:** Document **NFR.1.3** **PayFast** failure modes + recovery + **operator** notes. (AC: #7)
- [x] **Task 8 — AC1 housekeeping:** Align **`PAYFAST_*`** / **`NEXT_PUBLIC_APP_URL`** prose with **[`docs/environment-vars.md`](../environment-vars.md)** and **`.env.example`** (**grep**, **do not** assume drift). (AC: #1)
- [x] **Task 9 — VST-12 / PCI:** Short cross-link to **[`docs/compliance-and-safety.md`](../compliance-and-safety.md)** + VST-13 **Sub-processors** (no duplicate DSR). (AC: #1 narrative)
- [x] **Task 10 — AC10:** **Backlog** rows for **refund API**, **deposit**, **settlement export**, or other **unmapped** reference flows. (AC: #10)
- [x] **Task 11 — Sibling docs:** **Thin** pointers to **[`docs/stories/8.1.story.md`](8.1.story.md)** (**email on paid**) and **[`docs/stories/8.2.story.md`](8.2.story.md)** (**SMS stub**); optional reciprocal line in **[`docs/stories/vst-13.story.md`](vst-13.story.md)** to **`#int-8-3`**. (AC: #1)

## Dev Technical Guidance

### Primary sources

- **Epic requirement:** [`docs/epic-8.md`](../epic-8.md) — **INT.8.3**
- **Canonical integration doc:** [`docs/integrations-and-payments.md`](../integrations-and-payments.md) — PayFast lifecycle, Security, INT.8.1/8.2 anchors
- **Env + tiers:** [`docs/environment-vars.md`](../environment-vars.md)
- **Booking / payment columns:** [`docs/data-models.md`](../data-models.md) — **`bookings`** § lifecycle
- **Idempotency + actions:** [`docs/front-end-api-interaction.md`](../front-end-api-interaction.md)
- **Compliance / PCI pointer:** [`docs/compliance-and-safety.md`](../compliance-and-safety.md)
- **Stack contrast (optional):** [`docs/capstone-reference-stack-integration.md`](../capstone-reference-stack-integration.md)
- **Sibling stories:** [`docs/stories/8.1.story.md`](8.1.story.md), [`docs/stories/8.2.story.md`](8.2.story.md)

### How to inventory (reference vs Vestroo)

1. **Reference first:** **grep** capstone-reference **checkout** + **share** Momo/PayOS modules → note **callbacks** vs **server APIs** (payment link, refund stub).
2. **Vestroo second:** **grep** **`src/actions/processPayment.ts`**, **`src/app/api/payfast/webhook/route.ts`**, **`src/lib/payfast.ts`** — map **ITN** states to **`bookings.payment_status`** / **`status`** per **`data-models.md`**.
3. **Map by business event** (matrix rows) — emphasise **outcome parity** (paid, failed, id, customer reference), **not** identical HTTP shapes.
4. **Staging clarity:** **`PAYFAST_URL`** sandbox vs live rows in **`environment-vars.md`**; **`notify_url`** must match **`NEXT_PUBLIC_APP_URL`** tier.

## Testing / verification

- **Relative links:** From **`docs/stories/8.3.story.md`**, click-test **`../epic-8.md`**, **`../integrations-and-payments.md#int-8-3`** (after edit), **`../environment-vars.md`**, **`../data-models.md`**, **`../front-end-api-interaction.md`**, **`../compliance-and-safety.md`**, **`../capstone-reference-stack-integration.md`**, **`../index.md`**, **`8.1.story.md`**, **`8.2.story.md`**.
- **Grep bundle (reference):** `Momo`, `momo`, `payos`, `PayOS`, `@payos/node`, `CheckoutBookingMomo`, `createPaymentLink`, `return-booking`, `initiateRefund`, `handleRefundForTrip`
- **Grep bundle (shipped):** `payfast`, `PayFast`, `processPayment`, `verifyPayFastWebhookSignature`, `payment_status`, `payment_reference`, `trans_id`, `pf_payment_id`, `notify_url`
- **Grep bundle (deposit / schema):** `deposit`, `payment_amount`, `partial`
- **Consistency:** **No** row claims **Momo/PayOS** runs in Vestroo **prod**; **Vestroo PayFast** remains authoritative.

## Story draft self-check

_Checklist inspiration: `bmad-agent/checklists/story-draft-checklist.md`_

| Category | Status | Notes |
| -------- | ------ | ----- |
| 1. Goal & Context Clarity | PASS | **INT.8.3** scoped; **Epic 8** payment mapping; **VST-13** extends |
| 2. Technical Implementation Guidance | PASS | **grep** plans, **legacy vs shipped** paths, **honest gaps** |
| 3. Reference Effectiveness | PASS | **checkout.service**, **checkout.controller**, **momo.ts**, **payos.ts**, **booking.service**, **trip.service** |
| 4. Self-Containment Assessment | PASS | **Out of scope** for **8.4–8.6** implementation + **no PayFast code** |
| 5. Testing Guidance | PASS | Link check + **grep** bundles; **Vitest** pointers |

**Final Assessment: READY** — Developer agent can add **`#int-8-3`**, complete **grep-backed** matrices, **substitution** map, **NFR** slices, and **backlog** without implementing **Momo/PayOS** or changing **PayFast** code.

## Story Progress Notes

### Agent Model Used:

Story preparation (**sm** subagent) — **created** **`docs/stories/8.3.story.md`** only (**no** edits to **`integrations-and-payments.md`** / **`index.md`** in this step).

**Implementation (dev agent):** **`#int-8-3`** section in **`docs/integrations-and-payments.md`**, **`docs/index.md`**, **`docs/epic-8.md`**, reciprocal pointer in **`docs/stories/vst-13.story.md`**, tasks **1–11** complete (**docs-only**).

### Completion Notes List

- **Canonical home:** **[`docs/integrations-and-payments.md`](../integrations-and-payments.md#int-8-3)** — provider matrix, substitution map, **NFR.3.1 / 1.3 / 4.1**, backlog, **INT.8.1** / **INT.8.2** / **front-end-api-interaction** / **compliance** cross-links.
- **Integration matrix** PayFast row + **per-environment** footnote point to **`#int-8-3`**; **Cross-links** list includes INT.8.3.
- **Reference inventory:** **`checkout.service.ts`**, **`checkout.controller.ts`**, **`payos.ts`**, **`momo.ts`**, **`booking.service.ts`**, **`trip.service.ts`** under **`src/legacy/capstone-reference/`** — **pattern-only** vs **Vestroo** **`processPayment`**, **`payfast.ts`**, **`payfast-client.ts`**, **`/api/payfast/webhook`**.
- **Deposit / partial capture:** **TBD** — no evidence in shipped **`src/`**, **`supabase/migrations/`**, or prep **`data-models`** grep at delivery time.
- **Reciprocal:** **[`vst-13.story.md`](vst-13.story.md)** **Dependencies** line links **`../integrations-and-payments.md#int-8-3`** and **[Story 8.3](8.3.story.md)**.

### Change Log

| Date | Change |
| ---- | ------ |
| 2026-04-14 | **Ready** story file created: **INT.8.3** Momo/PayOS vs PayFast — **`#int-8-3`** spec, matrix columns, **NFR.3.1 / 1.3 / 4.1**, **substitution** map, **grep** verification plan, **docs-only** scope. |
| 2026-04-14 | Corrections: **`vst-13.story.md`** dependency + Progress Notes; AC3 chain — **`/confirmation`** return URL vs **`/api/booking-confirmation`**. |
| 2026-04-14 | **Documentation delivered:** **`#int-8-3`** in **`integrations-and-payments.md`**, **`index.md`**, **`epic-8.md`**, **`vst-13.story.md`** reciprocal link; tasks **1–11** marked complete. |
