# Story 14.8: "Ready to assign" filter chip on `/ops/bookings` with live count + row deep-link (Epic 14 / Theme E)

## Status: Done

**Dependencies:** **[`docs/epic-14.md`](../epic-14.md)** — **§3 Phase 4 — Ops queue integration** (**14.8** chip on **`/ops/bookings`**; **14.8** parallel after **14.1** per phased table; **14.9** after **14.8** for **`/ops/fulfil?queue=paid`** refactor); **§4 Theme E — US-E1** (lines ~170–174 — **"Ready to assign"** chip / preset, **`status='ready_to_assign'`**, live count, row deep-link to Fulfil assign); **product decision Q18** (table ~line 49 — **`/ops/bookings`** chip and later **`/ops/fulfil`** sync to **one** predicate; **14.8** implements **only** the bookings **chip** + **count** + **row** deep-link — **not** the fulfil query refactor); **§4 Theme G** catalogue **14.8** (**2 pts** \| Depends **14.1**); **§7** Definition of Done — paid walk-ins **discoverable** on **`/ops/bookings`** (**chip**); **§8** — `src/app/(ops)/ops/bookings/`, `src/features/ops/components/` (filter **chip** config). **[`docs/epic-12.md`](../epic-12.md)** — **Phase 2** stories **12.2** / **12.3** (**unified `/ops/bookings`**, **filter chips**; epic-14 **US-E1** cites the **12.2** **count** pattern next to chip labels — **survey** code to map epic wording to the implementing story/module); Epic **12** **US-B1** chip semantics and empty states (**§4 Theme B**). **[`docs/stories/14.1.story.md`](../stories/14.1.story.md)** — **blocking:** **`ready_to_assign`** in **`bookings_status_check`** + **Q19** trigger on **`payment_status` → paid** for **`walk_in`** (**14.8** does **not** add DB migrations by default). **[`docs/stories/14.6.story.md`](../stories/14.6.story.md)**, **[`docs/stories/14.7.story.md`](../stories/14.7.story.md)** — **optional** funnel context only (quote → pay → **ready**).

**Upstream (blocking):** Story **14.1** **Done** — **`ready_to_assign`** **status** value and **trigger** semantics are **live** (otherwise the chip matches **zero** rows and is useless). Epic **12** — **`/ops/bookings`** **chip** / **filter** infrastructure exists (**12.3**+).

**Explicit out of scope (do not implement in 14.8):**

- **Story 14.9** — **`/ops/fulfil?queue=paid`** query refactors, shared predicate with **`/ops/bookings`**, or URL/query-param changes to Fulfil buckets (**Q18** **second surface**).
- **`docs/fulfil-queue-buckets.md`** — updates (**14.9** / **US-E2**); **`docs/ops-console.md`**, **`docs/integrations-and-payments.md`**, and other **US-F2** files — **14.12** unless team policy says otherwise.
- **Stories 14.10–14.12** — golden-path regression, E2E / smoke, full doc sweep.
- **Playwright** coverage — **14.11** (US-F1); **14.8** may add **unit** / **light integration** tests only as listed in **Tasks**.

**Constraining principles (from epic + scope split):**

- **Traceability — Theme E / catalogue 14.8 / Q18 (bookings only):** Delivers **[`docs/epic-14.md`](../epic-14.md)** **§3 Phase 4 (14.8)**, **§4 Theme E (US-E1)**, **Q18** for **`/ops/bookings`** only, **§7** discoverability on **bookings** chip, **§8** paths.
- **Single predicate (default):** Chip / preset filters **`bookings.status = 'ready_to_assign'`** (walk-in PayFast-paid path per **Q19** / **14.1**). **Do not** add a **parallel** **`account_client`**-specific predicate in **14.8** unless **product** explicitly requires it (epic first AC allows “plus whatever account equivalent” — by Epic **14** ship, **account** rows at **`ready_to_assign`** are **likely none**; **document** **zero count** and **empty state**).
- **Count + deep-link — survey first:** Reuse the **Epic 12** live **count** pattern beside the chip label (**US-E1** names **12.2** — verify against **`/ops/bookings`** implementation, **e.g.** **12.3** data hooks); **document** which **query** or **API** supplies the count in **Progress Notes**. **Row click** from this view must match the **same** destination as the existing Fulfil **"Assign"** action (**path** / **query** / **helper** — **TBD** in code survey).
- **No migrations in 14.8** by default — add a migration **only** if a **DB view** is **required** for the chip (unlikely; prefer app-layer filter like other chips).

## Story

- As **ops staff**, I need a **"Ready to assign"** **filter chip** on **`/ops/bookings`**, so that **paid walk-ins** that have landed in **`ready_to_assign`** are **one click** away from dispatch **—** **US-E1**, **Q18** (bookings surface) (**[`docs/epic-14.md`](../epic-14.md)** **§4 Theme E**).

## Acceptance Criteria (ACs)

### US-E1 — Ops queue integration (Theme E) + Q18 (`/ops/bookings` only)

_(Epic **[`docs/epic-14.md`](../epic-14.md)** **§4 Theme E — US-E1** — lines ~170–174; **Q18** ~line 49; **§3 Phase 4**; **§7**; **§8**.)_

1. **Chip / preset — `ready_to_assign`:** The **Epic 12** **chip** set on **`/ops/bookings`** gains a new **chip** or **saved preset** labelled **"Ready to assign"** (exact copy **TBD** UX) that applies **`bookings.status = 'ready_to_assign'`** (PayFast handoff for **walk-ins** per **Q19** / **14.1**). Implementation follows existing **filter chip** configuration patterns under **`src/app/(ops)/ops/bookings/`** and **`src/features/ops/components/`**.

2. **Account / `client_type` note (no extra predicate by default):** The **default** **14.8** filter is **only** **`status = 'ready_to_assign'`** (per **US-E1** first AC — epic text mentions account equivalent **"likely nothing"**). **Document** in UI copy or **Progress Notes** that **`account_client`** rows at **`ready_to_assign`** are **not expected** at Epic **14** ship; **empty state** / **zero count** behaviour must be **clear** (no misleading “error”). **Do not** add a second parallel **`account_client`**-only predicate **unless** product explicitly approves a follow-up (out of default scope).

3. **Live count:** The chip (or its label area) shows a **live** **count** of rows matching the **same** predicate, reusing the **Epic 12** **count** pattern next to the label (**US-E1** / epic reference to **12.2** — **survey** `docs/epic-12.md` **§3** Phase 2 and **`/ops/bookings`** code for the actual **hook** / **query** / **API**). **Record** the chosen data source in **Story Progress Notes** (**TBD** until implementation).

4. **Row action — Fulfil assign parity:** **Clicking** a **booking** **row** while this **chip** / **view** is active navigates to the **same** **destination** as the existing Fulfil **"Assign"** **button** for that booking (deep-link or router helper — **TBD** survey: **`/ops/fulfil`** with **query** params vs existing **ops** **navigation**). No **new** assign semantics — **link** to **existing** behaviour.

5. **Migrations / schema — default none:** **No** new **migration** in **14.8** **unless** the chip **cannot** be implemented without a **DB** **view** (unlikely). If a migration is **unavoidable**, **justify** in **PR** and **Progress Notes** with a one-line **why** app-layer **filter** is insufficient.

6. **Traceability:** Maps to **[`docs/epic-14.md`](../epic-14.md)** **§3 Phase 4 (14.8)**, **§4 Theme E (US-E1)**, **Q18** (bookings half), **§4 Theme G 14.8**, **§5–§8** (paths above).

## Tasks / Subtasks

- [x] **Task 0 — Read sources:** **[`docs/epic-14.md`](../epic-14.md)** **§3 Phase 4**, **§4 Theme E (US-E1)**, **Q18**, **§4 Theme G 14.8**, **§7–§8**; **[`docs/epic-12.md`](../epic-12.md)** **§3** Phase 2 / **Theme B (US-B1, US-B2)**; **[`docs/stories/14.1.story.md`](../stories/14.1.story.md)**; optional **[`../stories/14.6.story.md`](../stories/14.6.story.md)**, **[`../stories/14.7.story.md`](../stories/14.7.story.md)** for funnel context.

- [x] **Task 1 — Survey `/ops/bookings`:** Map **filter chip** config, **list** **query** / **server** **props**, and where **per-chip** **counts** are **fetched** (Epic **12.2** vs **12.3** naming — **align** with code). List **files** in **Progress Notes**.

- [x] **Task 2 — Add chip / preset:** Register **"Ready to assign"** with predicate **`status === 'ready_to_assign'`** (or **SQL**-equivalent in shared filter builder — match repo **patterns** for **status** chips).

- [x] **Task 3 — Live count:** Wire **count** for **`ready_to_assign`** using the **same** **mechanism** as other **chips** (no **one-off** **polling** unless **unavoidable**). **Handle** **zero** gracefully.

- [x] **Task 4 — Row deep-link:** On **row** **click** from this **view**, navigate to the **identical** **target** as **Fulfil** **"Assign"** (reuse **existing** **href** / **onClick** / **router** helper after **survey** — **no** **duplicate** **route** **strings** if a **shared** **helper** exists).

- [x] **Task 5 — Empty / edge states:** **Copy** for **no** **rows** **matches** **Epic 12** **E2** **empty** **pattern**; **note** **account** **unlikely** **count** in **Progress Notes** if **not** **shown** in **UI**.

- [x] **Task 6 — Tests:** **Unit** or **component** **tests** for **filter** **mapping** and **(if** **feasible**) **count**; **no** **Playwright** (**14.11**). **Optional:** **light** **integration** **test** for **click** **navigation** if **harness** **exists**.

- [x] **Task 7 — Verification:** **Manual** — with **14.1** **live**, **pay** a **walk-in** **(or** **seed** **row** **`ready_to_assign`**) **→** **chip** **shows** **count** **≥** **1** **→** **row** **click** **lands** **same** **as** **Assign** — **staged env / PO** (code paths verified: count query + Fulfil **Assignment (paid)** URL + pre-select).

## Dev Technical Guidance

### Primary sources

- **Epic:** [`docs/epic-14.md`](../epic-14.md) — **§3–§4** Theme E, **Q18**, **§7–§8**.
- **Epic 12 / ops queue:** [`docs/epic-12.md`](../epic-12.md) — **Theme B** **US-B1** / **US-B2**; **§3** Phase 2 **12.2** / **12.3**.
- **Schema / trigger:** [`docs/stories/14.1.story.md`](../stories/14.1.story.md) — **`ready_to_assign`**, **Q19**.

### Likely code areas (paths only) — from epic **§8** + prior stories

- **`/ops/bookings`:** `src/app/(ops)/ops/bookings/` (page, layout, data loaders).
- **Chips / ops UI:** `src/features/ops/components/` (filter **chip** config, badges — **survey** for exact files).
- **Data layer:** e.g. `src/lib/ops-booking-grid-query.ts`, `src/lib/ops-bookings-queue-query.ts`, or **route** **handlers** — **TBD** **Task 1** **survey**.
- **Fulfil / assign:** `src/app/(ops)/ops/fulfil/` (or **shared** **ops** **navigation** — **Task 4** **survey**; **do not** **change** **fulfil** **queue** **queries** in **14.8** — **14.9**).

### Implementation notes

- **Blocking dependency:** If **14.1** is **not** **deployed**, **chip** is **inert** — coordinate **staging** **order** with **release** **notes**.
- **Single** **source** **of** **truth** **for** **Assign** **URL:** Prefer **one** **helper** **used** **by** **Fulfil** **and** **bookings** **row** **link** to **avoid** **drift** (**Q18** **predicate** **alignment** is **14.9** for **fulfil** **list** — not **this** **story**).

## Testing / verification

- **Automated:** **Unit** / **light** **integration** as in **Task 6**; **no** **full** **US-F1** **Playwright** in **14.8** (**14.11**).
- **Manual:** **Chip** **+** **count** **+** **row** **navigation** per **AC** **and** **Task 7**; **zero** **rows** **empty** **state**.

## Story Progress Notes

### Agent Model Used:

Cursor dev agent (Composer) — 2026-04-24

### Completion Notes List

**Files surveyed / touched**

- `src/lib/ops-bookings-queue-query.ts` — `ready_to_assign` in `OPS_BOOKINGS_QUEUE_STATUS_ORDER` (whitelist for URL + dropdowns); `isReadyToAssignPreset`, `OPS_BOOKINGS_READY_TO_ASSIGN_HREF` (chip href = `status=ready_to_assign` only, no `client`/`account_client` predicate per AC2).
- `src/app/(ops)/ops/bookings/page.tsx` — `Promise.all` list query + head **count** on `bookings` with `.eq('status', 'ready_to_assign')` (PostgREST `count: 'exact'`, `head: true` — same pattern as `load-ops-dashboard-kpis` / `src/app/(ops)/ops/search/page.tsx`).
- `src/features/ops/components/OpsBookingsQueuePresetChips.tsx` — **"Ready to assign"** quick-view chip, live count in badge, toggle clears to all bookings; product note in UI when active.
- `src/lib/ops-fulfil-nav.ts` — **single** builder for Fulfil URLs: `opsFulfilQueueHref` / `opsFulfilAssignBookingHref` (row deep-link); **FulfilQueueTabs** reuses it so tab changes preserve optional `bookingId`.
- `src/app/(ops)/ops/fulfil/page.tsx` + `AssignBookingPanel` — `bookingId` query param (UUID) → pre-selects booking in **Assignment (paid)** form; validates UUID shape via `isUuidShaped` (rejects unsafe values).
- `src/features/ops/components/BookingsQueueTableRow.tsx` — optional `navigateHref` for row navigation when the **Ready to assign** preset is active.
- `src/lib/__tests__/ops-bookings-queue-query.test.ts`, `src/lib/__tests__/ops-fulfil-nav.test.ts` — preset + URL helpers.
- **Migrations:** none (AC5).

**Count data source (AC3):** Server-side on each `/ops/bookings` request: Supabase/PostgREST on `bookings` with `status = 'ready_to_assign'`, RLS as for the list. If the count query errors, the chip shows `—` (not an error state — AC2).

**Fulfil / "Assign" parity (AC4):** `opsFulfilAssignBookingHref(bookingId)` → `/ops/fulfil?queue=paid&bookingId=…` — the **Assignment (paid)** / `AssignBookingPanel` surface (Fulfil tab **Assignment (paid)**; primary dispatch form). **14.9 note:** the paid queue list on Fulfil still uses `status = 'paid'` in the page query; a `ready_to_assign` walk-in may not appear in the **dropdown** until 14.9 refactors the bucket — the URL and panel match the Fulfil assignment destination; pre-select applies when the booking is present in the list.

**Commands run:** `pnpm test run` (all pass), `pnpm lint` (no new issues in changed files; existing repo warnings only).

### Story DoD Checklist Report

- **TypeScript / patterns:** new code in TS, strict; no new `any` introduces; RLS unchanged (app-layer filter only). **JSDoc:** brief on new exports where useful; not every trivial helper documented per project variance.
- **Tests:** `ops-fulfil-nav` + `ops-bookings-queue-query` (preset + URLs); all suite green.
- **Docs:** no epic/docs sweep (out of scope); story + progress notes updated.
- **Migrations:** none; justified in AC5.
- **N/A / waived:** 100% pricing coverage N/A; Playwright N/A (14.11); manual E2E sign-off in staging (Task 7) for paid walk-in left to team.

## Change Log

| Date       | Change |
| ---------- | ------ |
| 2026-04-24 | **Implemented:** **Ready to assign** chip, **count** query, **Fulfil** **deep-link** via **`opsFulfilAssignBookingHref`**, **`Status: Done`**. |
| 2026-04-20 | Initial **14.8** **Draft**: **US-E1** / **Theme** **E**; **`/ops/bookings`** **"Ready to assign"** **chip** + **`status='ready_to_assign'`**; **Epic** **12** **count** **pattern** (**survey**); **row** **deep-link** **=** **Fulfil** **Assign**; **Q18** **bookings** **only**; **OUT** **14.9** **fulfil** **+** **`docs/fulfil-queue-buckets.md`**, **14.10–12**, **14.11** **Playwright**; **Depends** **14.1** **+** **Epic** **12**; **migrations** **default** **none**; **all** **tasks** **unchecked**; **Status: Draft**. |
