# Story 8.5: Secrets, config, and shared modules (`share/`) — reference patterns vs Vestroo (INT.8.5)

## Status: Done

**Dependencies:** **[`docs/epic-8.md`](../epic-8.md)** — **INT.8.5** source (document reference **`share/`** concerns — **JWT**, **Redis**, **SMS**, **Momo**, **PayOS** — as **patterns only**; map to **Vestroo** **Supabase JWT/session**, **Realtime/Redis** (if any), and **env** vars; **Redis** MUST **not** be introduced solely to mimic the reference unless an **ADR** approves cost and hosting; **JWT in browser storage** forbidden for first-party **staff** flows unless **explicitly** revised by **ADR**; **parity of outcomes**, not copy-paste). **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** (VST-13) — default **canonical home** for **`#int-8-5`** (**`<a id="int-8-5"></a>`** immediately above **`## INT.8.5 — Secrets, config, and shared modules (`share/`)`** — **same anchor pattern** as **`#int-8-1`**–**`#int-8-4`**). **[`docs/environment-vars.md`](../environment-vars.md)** — **server-only** secrets, **`NEXT_PUBLIC_*`** boundaries, naming for provider keys. **[`docs/architecture.md`](../architecture.md)** — **thin** pointer only if **Progress Notes** justify moving **INT.8.5** out of VST-13 (**default** remains **integrations-and-payments.md**). **[`docs/realtime-and-notifications.md`](../realtime-and-notifications.md)** — when citing **Supabase Realtime** vs reference **socket + Redis** fan-out. **[`docs/capstone-auth-keytoken-otp-parity.md`](../capstone-auth-keytoken-otp-parity.md)** — **Supabase Auth** vs reference **JWT** / **OTP** patterns (**living** doc; **grep** before citing section anchors). **[`docs/stories/8.2.story.md`](8.2.story.md)** — **INT.8.2** **SMS** / **`share/`** gate (**cross-link**, **no** duplicate matrix). **[`docs/stories/8.3.story.md`](8.3.story.md)** — **INT.8.3** **Momo/PayOS vs PayFast** (**substitution** narrative — **one-line**, **no** re-documentation of payment steps). **[`docs/stories/8.4.story.md`](8.4.story.md)** — **INT.8.4** **cron/server-only secrets** posture (**one-line** tie-in where **`share/`** tokens surface in scheduled work). **[`docs/compliance-and-safety.md`](../compliance-and-safety.md)** (VST-12) — **PII** / secrets handling tone if **SMS** or **JWT** narratives touch personal data. **[`docs/stories/vst-12.story.md`](vst-12.story.md)** — **optional** story-level **VST-12** reciprocity for **secrets/PII** where cited. **`docs/adr/`** — **grep** **`Redis`**, **`redis`**, **`JWT`**, **`jwt`**, **`localStorage`**, **`sessionStorage`**; **prep grep 2026-04-14:** **no** matches under **`docs/adr/`** — matrix **ADR / gate** column MUST state **backlog: ADR required before Redis for mimicry** (and any **staff JWT-in-browser** policy change) unless new ADRs land. **Reference `share/` tree (vendored legacy — pattern only):** **`src/legacy/capstone-reference/backend-share/share.module.ts`** — **`JwtTokenService`** via **`src/share/jwt`** path alias (**physical:** **`backend-share/jwt.ts`**), **`ioredis`** **`Redis`** factory (**`REDIS_HOST`**, **`REDIS_PORT`**, **`REDIS_PASSWORD`**), wires **`SmsService`**, **`MomoService`**, **`PayosService`**; **`backend-share/share.port.ts`** — **`ITokenProvider`**, **`IRedisService`**, **`ISMSProvider`**, **`IMomoService`**, **`IPayosService`**; **`backend-share/redis.ts`**, **`di-token.ts`** (**`TOKEN_PROVIDER`**, **`REDIS_PROVIDER`**, **`MOMO_PROVIDER`**, **`PAYOS_PROVIDER`**, **`SMS_PROVIDER`**). **`src/legacy/capstone-reference/backend-modules/`** — gateways inject **`IRedisService`** + **`ITokenProvider`** (prep grep): **`tracking/tracking.gateway.ts`**, **`conversation/conversation.gateway.ts`**, **`notification/notification.gateway.ts`**, **`shared-itinerary/shared-itinerary.gateway.ts`**, **`driver-schedule/driver-schedule.gateway.ts`**, **`trip/trip.gateway.ts`** (**`REDIS_PROVIDER`**, **`TOKEN_PROVIDER`**); **`trip/trip.service.ts`** — **`IMomoService`**, **`IRedisService`**; **`auth`**, **`keytoken`**, **`OTP`**, **`wsAuth.guard.ts`** — **`TOKEN_PROVIDER`** / **`SMS_PROVIDER`** patterns. **Shipped prep hints (re-verify in AC):** **`ioredis` / `Redis` imports** — **only** under **`src/legacy/capstone-reference/`** in prep **`src/**/*.ts`/`*.tsx`** grep (**2026-04-14**); **no** matches in **`src/features/ops`**, **`src/app/(ops)`**, **`src/features/field`**, **`src/app/(field)`** for **`localStorage`** / **`sessionStorage`**; **`src/lib/supabase/server.ts`** — **`createUserServerClient()`** uses **`@supabase/ssr`** + **`cookies()`** (**httpOnly-capable** cookie transport — cite vs **legacy `jwt.sign`**); **`src/lib/supabase/realtime.ts`** — **Supabase Realtime** subscriptions (**contrast** reference **Redis** socket fan-out). **`src/features/capstone-reference/**`** (non-production reference UI) — **contains** **`localStorage`** token patterns (**do not** conflate with **`(ops)`** / **`(field)`** product surfaces — call out in **`#int-8-5`** narrative).

**Explicit out of scope:** **INT.8.6** full HTTP client / timeout / retry treatise — **one** cross-link to **[`docs/front-end-api-interaction.md`](../front-end-api-interaction.md)** where outbound idempotency matters. **No** wiring vendored **`share/`** into **production** Vestroo routes. **No** adding **Redis** infrastructure, **Upstash**, or **ioredis** dependencies for **reference mimicry** in this story. **No** editing **`docs/epic-8.md`** unless a **separate** housekeeping task authorises it (same spirit as **INT.8.4** story).

**Constraining NFRs:**

- **NFR.3.1:** **Secrets** MUST remain **server-only**; **`NEXT_PUBLIC_*`** MUST **not** carry privileged **JWT signing** material, **webhook** secrets, or **provider** keys — align prose with **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** **Security** and **[`docs/environment-vars.md`](../environment-vars.md)**.
- **NFR.4.1:** Integration and **`share/`** mapping prose MUST respect **TypeScript** boundaries — cite **narrow** types at **HTTP** / **webhook** / **Realtime** handlers when pointing at shipped code (**honest N/A** where only docs change).

## Story

- As a **platform, engineering, or security** owner of **Epic 8** integration traceability
- I want a **grep-backed** canonical section **`#int-8-5`** that maps each reference **`share/`** concern (**JWT**, **Redis**, **SMS**, **Momo**, **PayOS**) to **Vestroo** primitives (**Supabase** session/JWT, **Realtime**, **env** vars, **PayFast** substitution, **N/A**) — with an **ADR / gate** column (**Redis mimicry**, **staff JWT in browser storage**) — plus **honest** shipped vs **legacy-only** inventory
- so that **parity of outcomes** is documented **without** copy-paste of the **VN reference** stack, **no Redis** is implied for **socket fan-out** alone, and **first-party staff** auth transport stays **cookie/server-session** aligned unless an **ADR** explicitly revises policy

## Acceptance Criteria (ACs)

1. **Canonical doc home for INT.8.5 (default VST-13):** Add **`## INT.8.5 — Secrets, config, and shared modules (`share/`)`** with stable anchor **`#int-8-5`** to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** — **`<a id="int-8-5"></a>`** immediately above the heading (**same pattern** as **`#int-8-1`**–**`#int-8-4`**). Section MUST include a **matrix** (Markdown table, **no** blank lines between data rows) with columns at minimum: **Concern** (**JWT**, **Redis**, **SMS**, **Momo**, **PayOS**); **Reference `share/` / legacy files** (paths under **`src/legacy/capstone-reference/backend-share/`** and key **`backend-modules/`** consumers — **grep-backed**); **Vestroo mapping** (**Supabase JWT/session**, **Realtime**, **env vars**, **PayFast** / **N/A** — **honest**); **ADR / gate** (**Redis mimicry requires ADR**; **staff JWT in `localStorage`/`sessionStorage` forbidden** unless **ADR** — cite **epic** rule); **Status + rationale** (**pattern-only**, **implemented**, **stub**, **gap**, **N/A**). **Optional:** extend the VST-13 **integration matrix** with a row **Secrets / `share` patterns** → **`#int-8-5`**. If content **clearly** belongs outside VST-13, **Progress Notes** MUST justify **`docs/architecture.md`** — **pick one** canonical file.

2. **Reference inventory — `share.port`, DI tokens, gateways:** **Grep** **`src/legacy/capstone-reference/`** for **`share.port`**, **`backend-share`**, **`TOKEN_PROVIDER`**, **`REDIS_PROVIDER`**, **`MOMO_PROVIDER`**, **`PAYOS_PROVIDER`**, **`SMS_PROVIDER`**, **`IRedisService`**, **`ITokenProvider`**. Narrative MUST list **gateway** consumers that inject **Redis + token** providers (**prep:** **tracking**, **conversation**, **notification**, **shared-itinerary**, **driver-schedule**, **trip** gateways — **re-verify**). MUST explain **`src/share/...` import paths** in legacy sources vs on-disk **`backend-share/`** (**vendored** layout — **not** a shipped **`src/share/`** product folder in Vestroo).

3. **Shipped inventory — Redis / JWT signing:** **Grep** shipped **`src/`** excluding **`src/legacy/`** for **`ioredis`**, **`from 'redis'`**, **`Redis`**, **`jwt.sign`**, **`jsonwebtoken`**, **`jose`** (disambiguate **JWT** as product term in copy vs **library**). Report in **`#int-8-5`** (**prep 2026-04-14:** **`jwt.sign`** hits **only** **`src/legacy/capstone-reference/backend-share/jwt.ts`**; **`ioredis`** hits **only** legacy paths — **re-verify**). If **new** server signing appears, classify **server-only** vs **forbidden client** explicitly.

4. **Epic rule — no Redis for mimicry:** **`#int-8-5`** MUST state **non-negotiable**: **Redis** (or equivalent **socket-state** store) MUST **not** be introduced **solely** to mimic the reference unless an **ADR** approves **cost** and **hosting**; **prep** found **no** **`docs/adr/`** hits for **Redis** — cite **backlog ADR** until one exists.

5. **Epic rule — staff JWT in browser storage:** **`#int-8-5`** MUST state **first-party staff** (**`(ops)`**, chauffeur **`(field)`** tooling) MUST **not** persist **JWT** / session secrets in **`localStorage`** or **`sessionStorage`** unless **explicitly** revised by **ADR**; **verify** actual transport (**`createUserServerClient`**, **`cookies()`**, middleware refresh) with **grep** evidence — **prep:** **no** **`localStorage`** in **`src/app/(ops)`**, **`src/features/ops`**, **`src/app/(field)`**, **`src/features/field`** — **re-verify**; note **`src/features/capstone-reference/**`** **`localStorage`** as **non-product** reference UI only.

6. **Cross-links INT.8.2 / INT.8.3:** **`#int-8-5`** MUST **one-line** point to **`#int-8-2`** for **SMS** matrix / gate and **`#int-8-3`** for **Momo/PayOS → PayFast** substitution — **no** duplicate **SMS** or **payment** step documentation.

7. **Backlog for gaps:** Any **concern** row that cannot be mapped with **evidence** MUST get a **backlog** line (**P0/P1/P2** or **High/Medium/Low** — **one** scheme, consistent with **INT.8.1**–**INT.8.4** where possible), including **missing ADR** for **Redis mimicry** or **browser JWT** policy change.

8. **Index / Related thin links:** Add **`#int-8-5`** to **[`docs/index.md`](../index.md)** and/or VST-13 **Related** (**no** wholesale restructure).

9. **Epic / VST-13 housekeeping:** **Do not** edit **`docs/epic-8.md`** in this story’s implementation pass unless a **separate** maintainer task is opened; **optional** reciprocal line in **[`docs/stories/vst-13.story.md`](vst-13.story.md)** to **`#int-8-5`** (**same optional pattern** as **INT.8.3** / **INT.8.4**).

## Tasks / Subtasks

- [x] **Task 1 — AC1, AC8:** Add **`<a id="int-8-5"></a>`** + **`## INT.8.5 …`** to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** (or **justified** alternate per **AC1**); **thin** **[`docs/index.md`](../index.md)** link. (AC: #1, #8)
- [x] **Task 2 — AC2:** Re-**grep** **`src/legacy/capstone-reference/`** for **`share.port`**, **`TOKEN_PROVIDER`**, **`REDIS_PROVIDER`**, gateway injections; document **`src/share/`** path alias vs **`backend-share/`** on disk. (AC: #2)
- [x] **Task 3 — AC3:** Re-**grep** shipped **`src/`** (exclude **`src/legacy/`**) for **Redis** / **JWT signing** libraries; record **findings** in **`#int-8-5`**. (AC: #3)
- [x] **Task 4 — AC4:** Write **ADR / gate** column + narrative: **no Redis for mimicry** without **ADR**; **link** **`docs/adr/`** policy if added later. (AC: #4)
- [x] **Task 5 — AC5:** Document **staff session transport** (**Supabase SSR cookies** — **`createUserServerClient`** in **`src/lib/supabase/server.ts`**); **grep** **`(ops)`** / **`(field)`** for **browser storage**; contrast **legacy `jwt.ts`**. (AC: #5)
- [x] **Task 6 — AC6:** Add **thin** pointers to **`#int-8-2`**, **`#int-8-3`**; **one-line** **`#int-8-4`** cron secrets cross-link. (AC: #6, sibling **8.4**)
- [x] **Task 7 — AC7:** Add **backlog** rows for **gaps** (e.g. **missing ADR**, **unsettled Realtime vs Redis** wording). (AC: #7)
- [x] **Task 8 — AC1 matrix:** Fill **Vestroo mapping** + **status** for **all five** concerns — **no** implied production **Momo/PayOS/Redis**. (AC: #1)
- [x] **Task 9 — NFR.3.1 / NFR.4.1:** Footnote **server-only secrets** + **TS boundaries** at cited shipped files (**`realtime.ts`**, **`server.ts`**, payment libs if touched **by reference** only). (AC: **NFRs**)
- [x] **Task 10 — AC9:** Confirm **`docs/epic-8.md`** unchanged **or** file follow-up for **INT.8.5** deep link parity; optional **`vst-13.story.md`** reciprocal. (AC: #9)

## Dev Technical Guidance

### Primary sources

- **Epic requirement:** [`docs/epic-8.md`](../epic-8.md) — **INT.8.5**
- **Canonical integration doc (default):** [`docs/integrations-and-payments.md`](../integrations-and-payments.md) — **`#int-8-1`**–**`#int-8-4`** anchor pattern
- **Env + server-only posture:** [`docs/environment-vars.md`](../environment-vars.md)
- **Supabase Realtime product context:** [`docs/realtime-and-notifications.md`](../realtime-and-notifications.md)
- **Auth parity doc:** [`docs/capstone-auth-keytoken-otp-parity.md`](../capstone-auth-keytoken-otp-parity.md) — reference **JWT** / **OTP** vs **Supabase** (**grep** sections as needed)
- **Sibling stories:** [`docs/stories/8.2.story.md`](8.2.story.md), [`docs/stories/8.3.story.md`](8.3.story.md), [`docs/stories/8.4.story.md`](8.4.story.md)
- **Compliance tone (PII):** [`docs/compliance-and-safety.md`](../compliance-and-safety.md)

### How to map (`share/` → Vestroo)

1. **Reference first:** Read **`backend-share/share.module.ts`**, **`share.port.ts`**, **`jwt.ts`**, **`redis.ts`**, **`sms.ts`**, **`momo.ts`**, **`payos.ts`** — capture **env vars** and **DI tokens**.
2. **Consumers second:** **grep** **`backend-modules/**/*.gateway.ts`** (and **`trip.service.ts`**, **`auth`**) for **`REDIS_PROVIDER`** / **`TOKEN_PROVIDER`**.
3. **Vestroo third:** **`src/lib/supabase/server.ts`**, **`src/lib/supabase/realtime.ts`**, **ops/field auth** (**`src/lib/ops-auth.ts`**, **`src/lib/field-auth.ts`** — **grep**); **PayFast** files only as **INT.8.3** cross-reference.
4. **ADR fourth:** **grep** **`docs/adr/`**; if **empty**, state **backlog ADR** for **Redis mimicry** and any **browser JWT** exception.

## Testing / verification

- **Relative links:** From **`docs/stories/8.5.story.md`**, click-test **`../epic-8.md`**, **`../integrations-and-payments.md#int-8-5`** (after edit), **`../environment-vars.md`**, **`../architecture.md`** (if cited), **`../realtime-and-notifications.md`**, **`../index.md`**, siblings **`8.2`–`8.4`**, optional **`vst-13.story.md`**, **`../compliance-and-safety.md`**.
- **Grep bundle (reference):** `share.port`, `TOKEN_PROVIDER`, `REDIS_PROVIDER`, `backend-share`, `IRedisService`, `ITokenProvider`, `ioredis`
- **Grep bundle (shipped):** `ioredis`, `from 'redis'`, `jwt.sign`, `jsonwebtoken`, `localStorage`, `sessionStorage`, `createUserServerClient`, `createServerClient`
- **Consistency:** **No** row claims **vendored `share/`** runs in **Vestroo prod**; **reference** is **pattern-only**.

## Story draft self-check

_Checklist inspiration: `bmad-agent/checklists/story-draft-checklist.md`_

| Category | Status | Notes |
| -------- | ------ | ----- |
| 1. Goal & Context Clarity | PASS | **INT.8.5** scoped; **Epic 8**; default **VST-13** **`#int-8-5`** |
| 2. Technical Implementation Guidance | PASS | **grep** plans, **legacy vs shipped**, **Supabase** cookie session |
| 3. Reference Effectiveness | PASS | **`share.module`**, **`share.port`**, gateways, **`src/share/`** alias note |
| 4. Self-Containment Assessment | PASS | **Out of scope** **INT.8.6**, **no Redis infra**, **no** **`share/`** wiring |
| 5. Testing Guidance | PASS | Link check + **grep** bundles |

**Final Assessment: READY** — Developer agent can add **`#int-8-5`**, **grep-backed** matrix, **NFR.3.1 / NFR.4.1**, **ADR backlog**, and **cross-links** **without** implementing **Redis** or **reference payments**.

## Story Progress Notes

### Agent Model Used:

Story preparation (**sm** subagent) — **created** **`docs/stories/8.5.story.md`** only (**no** edits to **`integrations-and-payments.md`**, **`index.md`**, **`epic-8.md`**, or code in this step).

**Implementation (dev agent):** **`#int-8-5`** in **`integrations-and-payments.md`**, integration matrix row, **`index.md`**, **`vst-13.story.md`** reciprocal; **`docs/epic-8.md`** **unchanged** per **AC9**.

### Completion Notes List

- **Corrections (2026-04-14):** **[`docs/capstone-auth-keytoken-otp-parity.md`](../capstone-auth-keytoken-otp-parity.md)** exists; Dependencies **`backend-modules/`** markdown fixed; **`vst-12.story.md`** link clarified.
- **Canonical home:** **[`integrations-and-payments.md#int-8-5`](../integrations-and-payments.md#int-8-5)** — matrix (**JWT**, **Redis**, **SMS**, **Momo**, **PayOS**), **`src/share/`** alias note, gateway list, **NFR.3.1 / 4.1**, **INT.8.6** defer, backlog (**ADR** for Redis mimicry + staff browser JWT exception).
- **Grep (delivery):** **`ioredis`** / **`jsonwebtoken`** / **`jwt.sign`** only **`src/legacy/capstone-reference/`**; **no** `localStorage`/`sessionStorage` in **`(ops)`** / **`(field)`** first-party trees; **`docs/adr/`** — **no** Redis/JWT policy hits; six gateways inject **`REDIS_PROVIDER`** + **`TOKEN_PROVIDER`** as documented.
- **INT.8.3** pointer updated from “defer INT.8.5 to epic only” to **§ INT.8.5** link.

### Change Log

| Date | Change |
| ---- | ------ |
| 2026-04-14 | **Ready** story file created: **INT.8.5** **`share/`** secrets/config — **`#int-8-5`** spec, matrix columns (**ADR / gate**), **NFR.3.1 / NFR.4.1**, **Redis** + **staff JWT storage** epic rules, **grep** verification plan, **docs-only** scope, **INT.8.6** deferral. |
| 2026-04-14 | Dependencies cleanup: **capstone-auth** doc confirmed; **`backend-modules/`** path formatting; **VST-12** note. |
| 2026-04-14 | **Documentation delivered:** **`#int-8-5`**, integration matrix row, **`index.md`**, **`vst-13.story.md`**; tasks **1–10** complete; **`epic-8.md`** unchanged. |
