# Story 15.26: E2E + compliance review checklist (Epic 15 / 15C.8)

## Status: Done

**Dependencies:**

- **Catalogue:** **[`docs/epic-15.md`](../epic-15.md)** **Theme E — `15C.8`**: *E2E + compliance review checklist* — **15C** — **2** pts; table **Depends: `15C.1`–`15C.7`** (per **[`docs/epic-15.md`](../epic-15.md)** Theme E).
- **Epic cross-cutting (quality + compliance):** **[`docs/epic-15.md`](../epic-15.md)** **§7** Definition of Done — *“POPIA / CAN-SPAM checklist complete for comms matrix”*; *“Sub-epic-specific E2E specs green in CI”*; *“transactional-email unsubscribe bypass asserted”* (browser or **documented** equivalent test level). **[`docs/epic-15.md`](../epic-15.md)** **§6** — risk row *“Comms template rendering breaks with an unset variable (`{{ customer_name }}` literal)”*: mitigation includes template preview (**`15C.4`**) and Playwright asserting **no** `{{` in rendered HTML; **this story narrows scope** to **customer-facing** / **matrix-rendered** paths **15C** owns (not the entire product catalogue of templates).

**Prerequisite stories (must be complete or stable before implementation):**

- **[`docs/stories/15.19.story.md`](15.19.story.md)** (**`15C.1`**): `comms_templates` / `comms_dispatch_rules` registry, **`CommsEventKey`**, RLS — foundation for matrix data and types.
- **[`docs/stories/15.20.story.md`](15.20.story.md)** (**`15C.2`**): Matrix-driven dispatch (`sendEmail` / SMS entrypoints), **`comms_no_rule_matched`** / **`ops_audit_log`** — required for transactional and HTML assembly assertions.
- **[`docs/stories/15.21.story.md`](15.21.story.md)** (**`15C.3`**): **`/ops/comms`** staff UI — E2E staff path target.
- **[`docs/stories/15.22.story.md`](15.22.story.md)** (**`15C.4`**): Template preview with seeded variables — optional **faster** surface for **`{{`** guard if outbound send is heavy to fixture.
- **[`docs/stories/15.23.story.md`](15.23.story.md)** (**`15C.5`**): **`/account/preferences`** preference centre — E2E member path target.
- **[`docs/stories/15.24.story.md`](15.24.story.md)** (**`15C.6`**): Unsubscribe links, footers, **`List-Unsubscribe`** / category mapping — compliance checklist and preference deep-links.
- **[`docs/stories/15.25.story.md`](15.25.story.md)** (**`15C.7`**): Scheduled **`invoice_due_reminder`** job, idempotency / **`ops_audit_log`** dedupe — checklist **cron** row and transactional-class **reminder** path for bypass tests.

**Theme E — catalogue `15C.8`:** **“E2E + compliance review checklist”** — **2** pts, depends **`15C.1`–`15C.7`**.

**Reconciliation (mandatory):** Closes **sub-epic 15C** with **Playwright** coverage aligned to **[`docs/epic-15.md`](../epic-15.md)** **§6** (template literals; transactional bypass) and **§7** (15C-specific E2E green; POPIA/CAN-SPAM checklist). **Does not** replace **15A.10** portal suite or **15B.8** rider suite — **complements** them on **comms matrix** surfaces only.

**Explicit out of scope (defer to other stories or non-goals):**

- **`15B.8`** — rider tracking E2E / privacy scenarios (**[`docs/stories/15.18.story.md`](15.18.story.md)** owns **15B** browser coverage).
- **`15D`** — dispatch intelligence E2E and calibration UI.
- **Full** **`15A`** regression beyond **`/account/preferences`** — **do not** re-run the **15A.10** golden-path suite unless a **single** **import**-level reuse from **`epic15-15a10-portal-golden-paths.spec.ts`** (or shared helper) is **trivial** (zero new portal AC duplication).
- **External penetration test** execution — this story delivers **automated** tests + **internal** **checklist** artifact, not third-party pentest.
- **Whole-product** “all 10+ templates” **`{{`** sweep — epic mitigation text is **broader**; **this** story **scopes** to **≥1** **15C**-owned **seeded** template / preview / outbound path (**AC** below).

## Story

- As **engineering / QA / PO**, we need **Playwright E2E** for **15C** comms matrix surfaces (**ops** rules/templates UI, **preference centre**, transactional vs marketing behaviour, and **no** raw **`{{`** in matrix-rendered HTML on at least one representative path), plus a **short POPIA / CAN-SPAM–oriented checklist** the team can tick at PR/release review, so that **sub-epic 15C** meets **[`docs/epic-15.md`](../epic-15.md)** **§6–§7** expectations **without** duplicating **15A.10** or **15B.8** suites.

## Acceptance Criteria (ACs)

1. **E2E suite entrypoint:** Add **one** primary spec (or a **justified** split) under **`tests/e2e/`** — e.g. **`epic15-15c8-comms-matrix-e2e.spec.ts`** (name **survey** for consistency with **`epic15-15a10-*`**, **`epic15-15b8-*`**). Use **`@playwright/test`**, match **[`playwright.config.ts`](../../playwright.config.ts)** (**`testDir: './tests/e2e'`**, **`.env.test`** loading, **`workers: 1`**, **`fullyParallel: false`**, **Chromium** project). **`test.skip`** with a **clear message** when **Supabase** URL/keys, **staff** credentials, or **portal** member fixtures are missing — **same spirit** as **[`docs/stories/15.10.story.md`](15.10.story.md)** / **[`docs/stories/15.18.story.md`](15.18.story.md)** (**no** false green on empty env).

2. **`/ops/comms` (staff):** With **ops staff** session (reuse **`tests/e2e/helpers/ops-staff-login.ts`** or successor), assert **non-error** load of the **comms** surface (**list** of templates/rules or **documented** empty state). **Minimum:** **read-only** smoke **or** **one** **non-destructive** **toggle** (active/inactive) with **cleanup** / revert **in the same test** if CI DB policy allows — **document** chosen strategy in **Progress Notes** so operators are not left with dirty flags.

3. **`/account/preferences` (member):** With **customer** auth (reuse **`tests/e2e/helpers/portal-customer-login.ts`** patterns from **15.10** if applicable), assert **informational** / **marketing** toggles render and **transactional** category **cannot** be turned off (UI +/or server behaviour per **`15.23`** / **`15.24`**). Assert **`?category=`** (or shipped query param) **highlights** / focuses the intended section per preference-centre UX from **`15C.5`–`15C.6`**.

4. **Transactional bypass (§6 / epic risk row):** Prove **`invoice_due_reminder`-class** **or** **`payment_received`** email path **still** sends (or reaches the **matrix dispatch** layer **without** marketing suppression) when **marketing** (and any other non-transactional categories per product rules) is **off**. **Allowed:** **API-level** test, **mocked** Resend, or **dev-only** outbox hook — **document** the **observable** assertion in **Dev Technical Guidance** / **Progress Notes**; **do not** require a **real** inbox in CI.

5. **`{{` literal guard (15C-scoped, §6):** On **outbound** or **preview** HTML for **≥1** seeded **`comms_templates`** row (or **preview** endpoint / server action from **`15.22`** if easier to stabilise than live send), assert the **final** assembled string **does not** contain the literal substring **`{{`**. Scope: **matrix-rendered** / **customer-facing** paths **15C** owns — **not** an exhaustive scan of every email in the repo.

6. **Compliance checklist artifact — path (post-survey):** **Task 0** must **`glob`** / list **`docs/**`** for **`compliance/`** or similar. **If** **`docs/compliance/`** (or agreed equivalent) **already exists**, add **`docs/compliance/epic15-15c-comms-checklist.md`**. **Else** (current repo survey: **no** `docs/compliance/`), add **`docs/epic-15-15c-compliance-checklist.md`**. The story **AC** must name the **chosen** path after survey. Checklist: **short**, **markdown**, **checkbox** rows suitable for **PR** / release review.

7. **Checklist content mapping (mandatory rows):** The checklist **must** include explicit items (tick boxes) mapping to: **Q23** (template body = PR; recipient rules = **`/ops/comms`**); **Q24** (preference centre per member; transactional vs informational vs marketing); **§6** transactional **unsubscribe** bypass (cannot suppress **transactional** sends via marketing toggle); **List-Unsubscribe** (where **non-transactional** / marketing emails use it per **`15.24`**); **preference centre** URL behaviour and **`?category=`**; **footer** / unsubscribe link elements from **`15.24`**; **cron** / **`15.25`** idempotency note (no duplicate **same-phase** **same-day** reminder without justified product change).

8. **§7 DoD traceability:** Update or add **Progress Notes** / **Tasks** so reviewers can see mapping to **[`docs/epic-15.md`](../epic-15.md)** **§7** — **15C** checklist complete; **15C**-scoped Playwright spec(s) **green** in CI when env is provisioned (skipped **with message** when not — consistent with **15.10**).

9. **Env / docs:** Extend **[`.env.test.example`](../../.env.test.example)** (and **Progress Notes**) with any **new** **`E2E_15C8_*`** or reused **`E2E_OPS_*` / `E2E_PORTAL_*`** placeholders **required** for this suite — **no** committed secrets.

10. **Traceability:** Story text and **DoD** reference **[`docs/epic-15.md`](../epic-15.md)** **Theme E `15C.8`**, **§6** (template **`{{`** risk; transactional bypass), **§7** (checklist + sub-epic E2E); defers **15B**, **15D**, **full 15A** per **Out of scope**.

## Tasks / Subtasks

- [x] **Task 0 — Survey:** `glob` / list **`docs/**`** for **`compliance/`**; confirm checklist path (**AC6**). Read **`playwright.config.ts`**, **`.env.test.example`**, **`tests/e2e/helpers/ops-staff-login.ts`**, **`portal-customer-login.ts`**, **`supabase-e2e-service-client.ts`**; grep **`/ops/comms`**, **`/account/preferences`**, **`category=`**, preview routes from **`15.22`**; confirm **`payment_received`** / **`invoice_due_reminder`** dispatch hooks for **AC4**.

- [x] **Task 1 (AC: 1, 2, 9):** Add spec file(s) under **`tests/e2e/`**; staff login + **`/ops/comms`** smoke (read or toggle+cleanup); env placeholders.

- [x] **Task 2 (AC: 1, 3, 9):** Portal login + **`/account/preferences`** toggles + **`?category=`** highlight assertion.

- [x] **Task 3 (AC: 4, 5):** Transactional bypass assertion (Playwright **or** API/integration — **document**); **`{{`** guard on **≥1** template/preview/outbound path.

- [x] **Task 4 (AC: 6, 7, 8):** Author compliance checklist at **survey-chosen** path; ensure **Q23**, **Q24**, **§6**, **List-Unsubscribe**, preference centre, **15.24** footers, **15.25** idempotency rows present.

- [x] **Task 5 (AC: 6, 10):** **Progress Notes** (minimal), **change log** row **2026-04-26**; **Status** and DoD (per **`.cursorrules`** and **story-completion** rule).

## Dev Technical Guidance

### Primary sources

- **[`docs/epic-15.md`](../epic-15.md)** — **Theme E `15C.8`**, **§6** (risks: transactional unsubscribe; **`{{`** literals), **§7** (DoD: checklist + 15C E2E), **Q23**, **Q24**, **Theme B US-B2–B4** as context only.
- **[`docs/stories/15.10.story.md`](15.10.story.md)** — Playwright **`testDir`**, **skip-with-message**, **`.env.test`** contract.
- **[`docs/stories/15.18.story.md`](15.18.story.md)** — coherent sub-epic E2E file pattern; **no** cross-sub-epic duplication.
- **Prereq stories —** **[`15.19`](15.19.story.md)**–**[`15.25`](15.25.story.md)** ( **`15C.1`–`15C.7`** ).

### Likely code & test areas (paths only — survey may adjust)

- **Ops UI:** `src/app/(ops)/ops/comms/` — list, toggles, preview links per **`15.21`–`15.22`**.
- **Preferences:** `src/app/(account)/account/(portal)/preferences/` — toggles, query highlight per **`15.23`–`15.24`**.
- **Dispatch / templates:** `src/lib/comms/` (e.g. dispatch-email, event category), **`comms_templates`** seeds — **`{{`** substitution and transactional gating.
- **Cron / reminder:** **`/api/cron/daily-maintenance`** (or documented **`15.25`** entry) — reference for **checklist** idempotency wording only; E2E **need not** trigger real cron if **AC4** is satisfied via **matrix** unit/API path.
- **E2E:** `tests/e2e/` — new **`epic15-15c8-*.spec.ts`**; helpers under **`tests/e2e/helpers/`**.

### Implementation notes

- **Serialisation:** Keep tests **serial**-safe with existing **`workers: 1`** configuration.
- **15A.10 reuse:** Prefer **importing** a **login** or **wait** helper **only** if it avoids copy-paste and does **not** pull in **full** portal golden-path execution.
- **Resend:** Use **`re_test_playwright_skip`** or mocks as already established in **`playwright.config.ts`** / Epic 13 patterns — **document** if **AC4** uses **mock** only.

## Testing / verification

- **Targeted:** `npx playwright test tests/e2e/epic15-15c8-comms-matrix-e2e.spec.ts` (adjust to final filename).
- **Full E2E:** `npm run test:e2e` with **`.env.test`** populated for **staff** + **portal** personas when asserting non-skip paths locally/CI.
- **Unit / integration:** Optional additions for **AC4**/**AC5** if Playwright alone is flaky — cite in **Progress Notes**.

## DoD (story)

- [x] **AC1–AC10** satisfied or **explicitly** skipped with **documented** env gates (**no** false green).
- [x] Compliance checklist merged at **survey-chosen** path (**`docs/epic-15-15c-compliance-checklist.md`** — **`docs/compliance/`** did not exist).
- [x] **`.env.test.example`** updated for any **new** E2E variables.
- [x] **No** **15B.8**, **15D**, or **full 15A.10** scope creep.

## Story Progress Notes

- **AC6 path:** `glob` on **`docs/**/compliance/**`** found **0** results → checklist at **`docs/epic-15-15c-compliance-checklist.md`** (if **`docs/compliance/`** is added later, a sibling **`docs/compliance/epic15-15c-comms-checklist.md`** is the story’s **alternate**; current merge uses the flat path per AC6).
- **E2E:** **`tests/e2e/epic15-15c8-comms-matrix-e2e.spec.ts`**. **Gate:** `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` + `E2E_OPS_STAFF_EMAIL`/`PASSWORD` + `E2E_PORTAL_BOOKER_EMAIL`/`PASSWORD` (clear **`test.skip` message** when missing). **§7:** Playwright is **green** in CI with env; without env, all three tests are **skipped** (not a false pass).
- **§7 / 15C E2E:** Complements 15.10/15.18; matrix-only; Chromium / `workers: 1` / `fullyParallel: false` via shared **`playwright.config.ts`**.
- **AC2 strategy:** **Read-only** on **`/ops/comms`** (no **Active** checkboxes) so CI/shared DBs are not left with changed flags; documented here as the chosen operator-safe approach.
- **AC4 (transactional bypass, no real inbox):** **Vitest** in **`src/lib/comms/__tests__/dispatch-email.test.ts`** — mocked **`sendEmail`**: `payment_received` with template + **`templateVariableMap`** proves assembled outbound HTML has **no** raw `{{` and dispatch does not consult member `comms_preferences` (see **`comms-event-category.ts`** and **`resolveCommsEmailRecipient`** — no marketing gate). **`comms-event-category.test.ts`**: `invoice_due_reminder` → **transactional** (with **`getAccountPrefsDeepLinkCategory` → `null`**).
- **AC5:** **E2E** checks **iframe** HTML from **ops template preview** (avoids false positives from **Template preview** help text that mentions `{{`). Fallback when DB has no HTML: **Subject** line only. **Vitest** in **`dispatch-email.test.ts`** (above) is the **stable** CI proof for **matrix substitution** without `{{` on outbound.
- **Env:** Reused **`E2E_OPS_*`**, **`E2E_PORTAL_*`**; **`.env.test.example`** 15.26 block documents reuse — **no new `E2E_15C8_` secrets** (optional explicit vars reserved for later gates only).
- **Gates run (2026-04-26):** `npm run test -- --run` (**425** tests, pass); `npm run lint` (exit 0, existing project warnings only); `npx playwright test tests/e2e/epic15-15c8-comms-matrix-e2e.spec.ts` (**3 skipped** in empty-env agent — **exit 0**; skip message verified).

## Change log

| Date       | Change |
| ---------- | ------ |
| 2026-04-26 | **15.26 (`15C.8`) done:** E2E **`epic15-15c8-comms-matrix-e2e.spec.ts`**; compliance checklist **`docs/epic-15-15c-compliance-checklist.md`**; `.env.test.example` 15C.8; Vitest AC4/AC5 in **`dispatch-email` / `comms-event-category`** tests. |

## Story DoD Checklist Report

- [x] **AC1** — One spec, naming aligned; skip message when Supabase/ops/portal env incomplete. **N/A** — split file not required.
- [x] **AC2** — Ops staff session + **read-only** **`/ops/comms`** (Strategy in Progress Notes; no rule/template mutations).
- [x] **AC3** — Booker + **`/account/preferences`**: informational / marketing / transactional section headings; **`#prefs-transactional` checkbox** disabled+checked; **`?category=marketing` / `informational`** **ring-2** highlight.
- [x] **AC4** — **Vitest** (mocked **`sendEmail`**) for **`payment_received`**; category tests for **`invoice_due_reminder`**. No real inbox. Observable: **`not.toMatch(/\{\{/)`** on assembled payload + `send` outcome **`sent`**. Documented: no **`comms_preferences` read in dispatch** path.
- [x] **AC5** — E2E iframe (or subject fallback) + **`dispatch-email`** unit test; dialog **help text** excluded (iframe is rendered-email-only).
- [x] **AC6** — **`docs/compliance/`** absent in repo; artifact **`docs/epic-15-15c-compliance-checklist.md`**. Story names path.
- [x] **AC7** — Checklist table rows: Q23, Q24, §6 bypass, List-Unsubscribe, preference URL + `?category=`, 15.24 footers, 15.25 idempotency.
- [x] **AC8** — Progress Notes map to epic **§7** (E2E + checklist + env skip).
- [x] **AC9** — **`.env.test.example`**; documents reuse; no new secrets; no **`E2E_15C8_`** required.
- [x] **AC10** — This report + **Theme E `15C.8`**, **§6–§7** references; out-of-scope unchanged.

**Traceability (Theme E, §6, §7):** **[`docs/epic-15.md`](../epic-15.md)** **Theme E `15C.8`**, **§6** (literal guards + transactional class / bypass in tests), **§7** (compliance checklist + 15C Playwright in CI with env, skip otherwise).

**Migrations:** **None** (this story is tests + docs only).

**Supabase MCP:** **N/A** — no schema changes.

---

**Story reference:** Reconciles with **[`docs/checklists/story-dod-checklist.txt`](../../checklists/story-dod-checklist.txt)** items applicable to a **tests + documentation** change (ACs, env sample, no secrets, lint/test gates as project scripts).
