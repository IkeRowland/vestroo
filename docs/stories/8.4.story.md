# Story 8.4: Scheduled jobs and background work — reference ScheduleModule / `@Cron` vs Vestroo (INT.8.4)

## Status: Done

**Dependencies:** **[`docs/epic-8.md`](../epic-8.md)** — **INT.8.4** source (list reference **ScheduleModule** / **`@Cron`** jobs with **cron expressions** and **purposes**; map each to **Vestroo**: **implemented automation**, **manual ops procedure**, or **not applicable**; **no** new scheduled work with **client secrets**; **server** or **hosted cron** only; **monitoring** hooks tied to **hardening** docs where they exist; design goal **no silent cron** — **named owner** + **monitoring** note per mandatory schedule). **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** (VST-13) — default **canonical home** for **`#int-8-4`** (**same anchor pattern** as **`#int-8-1`**–**`#int-8-3`**: **`<a id="int-8-4"></a>`** immediately above the **`## INT.8.4 …`** heading). **[`docs/environment-vars.md`](../environment-vars.md)** — **server-only** secrets posture for any future **cron** / **Route Handler** auth headers (**no** `NEXT_PUBLIC_*` for scheduled job credentials — align with existing **PayFast** / **Maps** server-key narrative). **[`docs/architecture.md`](../architecture.md)** — **only** if **Progress Notes** justify moving **INT.8.4** out of VST-13 (**default** remains **integrations-and-payments.md** for **Epic 8** cohesion). **[`docs/index.md`](../index.md)** — thin **`#int-8-4`** pointer (**same spirit** as **INT.8.1**–**INT.8.3**). **Sibling stories:** **[`docs/stories/8.1.story.md`](8.1.story.md)**, **[`docs/stories/8.2.story.md`](8.2.story.md)**, **[`docs/stories/8.3.story.md`](8.3.story.md)** — **one-line** cross-links where **email/SMS/payment** triggers relate to **time-based** behaviour (**do not** re-open those inventories). **[`docs/stories/vst-13.story.md`](vst-13.story.md)** — optional **reciprocal** line to **`#int-8-4`** when the section ships. **Hardening / monitoring (prep grep 2026-04-14):** **`docs/`** — **no** hits for **`hardening`**, **`monitoring`**, **`SLO`**, or **`observability`** (case-insensitive); matrix **Monitoring** column MUST use **`TBD`** or point to **future** runbook path — **do not** invent doc titles. **Reference code (Nest `@nestjs/schedule` — pattern / traceability — not production Vestroo):** **`src/legacy/capstone-reference/backend-modules/driver-schedule/driver-schedule.service.ts`** — **`@Cron('0 20 * * * *', { name: 'autoCheckoutPendingSchedules' })`** → **`autoCheckoutPendingSchedules()`** (auto **checkout** for **IN_PROGRESS** / **NOT_STARTED** driver schedules past shift end; vehicle status + gateway hooks). **`src/legacy/capstone-reference/backend-modules/trip/trip.service.ts`** — **`@Cron(CronExpression.EVERY_MINUTE, { name: 'handleTripStartTimeout' })`** → **`handleTripStartTimeout()`** (**CONFIRMED** trips past **5-minute** start window → **`DROPPED_OFF`**, share-itinerary cancel, **notifications**, **conversation** cancel, socket emits). **`src/legacy/capstone-reference/backend-modules/conversation/conversation.service.ts`** — **`@Cron(CronExpression.EVERY_MINUTE, { name: 'checkTimeToOpenAndClose' })`** → **`checkTimeToOpenAndClose()`** (**PENDING** → open when **`timeToOpen`** due; **OPENED** → close when trip terminal or **`timeToClose`**). **`ScheduleModule` / `@nestjs/schedule` grep (prep 2026-04-14):** under **`src/legacy/capstone-reference/`**, **`@Cron`** appears **only** in the **three** files above; **`ScheduleModule.forRoot`** / **`import { ScheduleModule } from '@nestjs/schedule'`** — **zero** hits (**domain** **`DriverScheduleModule`** in **`driver-schedule.module.ts`** is **unrelated** to Nest’s **`ScheduleModule`** — **do not** conflate in the doc matrix). **Shipped inventory (prep grep 2026-04-14, exclude legacy):** **`src/`** (paths **not** under **`src/legacy/`**) — **no** **`@Cron`**, **`CronExpression`**, **`/api/cron`**, or **`vercel.json`** string hits tied to platform cron; **`vercel.json`** — **zero** files at **repo root** (**verify** again before delivery — add **Vercel project → Cron Jobs** UI **TBD** if still absent). **Supabase:** **`supabase/migrations/`** — **no** **`pg_cron`**, **`cron.schedule`**, **`pg_net`**, or **`http` extension** cron patterns in prep grep — state **N/A** unless migrations gain scheduled SQL.

**Explicit out of scope:** **INT.8.5** full **`share/`** secrets matrix (**one-line defer**). **INT.8.6** HTTP client / timeout / retry treatise — **one** cross-link to **[`docs/front-end-api-interaction.md`](../front-end-api-interaction.md)** if outbound retries matter for a **future** job (**no** deep treatise here). **No** implementing **Nest**-style **`@Cron`** inside **Next.js** app code in this story — **documentation + inventory + rules** only. **No** adding **`vercel.json`** or **Vercel cron routes** unless a **separate** product story authorises it. **No** editing **`docs/epic-8.md`** to add **`#int-8-4`** in this task — if maintainers want **`epic-8.md`** line **28–30** to mirror **INT.8.1**–**INT.8.3** deep links, that is **future housekeeping** (call out in **AC** only).

**Constraining NFRs:**

- **NFR.3.1:** New **scheduled** or **hosted-cron** work MUST **not** use **client** or **`NEXT_PUBLIC_*`** secrets — **server env** / **Vercel** project env / **Supabase** secrets only; align prose with **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** § **Security** and **[`docs/environment-vars.md`](../environment-vars.md)**.
- **NFR.1.3:** Any **automation** row that is **implemented** MUST document **failure visibility** — **logs** (e.g. Vercel function logs), **operator** check, **alerting** (**TBD** until **`docs/`** gains **monitoring** / **hardening** anchors — prep grep found **none**).
- **NFR.4.1:** When **TypeScript** **cron** or **scheduled Route Handlers** ship, keep **narrow** types at **HTTP** / **payload** boundaries (**honest N/A** until code exists).

## Story

- As a **platform, engineering, or ops** owner of **Epic 8** integration traceability
- I want a **grep-backed** canonical section **`#int-8-4`** that lists every reference **`@Cron`** job (**cron expression**, **Nest job `name`**, **purpose**, **file path**), maps each to **Vestroo** (**automation / manual ops / N/A**), assigns a **named owner**, records a **monitoring** note (**or TBD**), states **secrets posture** (**server-only / no client secrets**), and captures **backlog** for gaps — plus an honest inventory of **shipped** `src/`, **Vercel**, and **Supabase** scheduling
- so that **no silent cron** exists: anything that **must** run on a schedule has **visible** documentation, **no** **`NEXT_PUBLIC_*`** auth, and **parity of outcomes** vs the **reference** stack is explicit (**manual** vs **missing** vs **N/A**)

## Acceptance Criteria (ACs)

1. **Canonical doc home for INT.8.4 (default VST-13):** Add **`## INT.8.4 — Scheduled jobs and background work`** with stable anchor **`#int-8-4`** to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** — **`<a id="int-8-4"></a>`** immediately above the heading (**same pattern** as **`#int-8-1`** / **`#int-8-2`** / **`#int-8-3`**). Section MUST include a **matrix** (Markdown table, **no** blank lines between data rows) with columns at minimum: **Reference job** (**Nest** `name:` if present, **cron expression** literal or `CronExpression.*` symbol, **1-line purpose**, **file path** under **`src/legacy/capstone-reference/`**); **Vestroo mapping** (**implemented automation** / **manual ops procedure** / **not applicable** — **honest**); **Named owner** (person **or** role, e.g. **Head of Ops — Fleet** — **no** generic “the team”); **Monitoring** (concrete hook, **or** **`TBD`** with **why**); **Secrets posture** (**server-only** / **no client secrets** / **`NEXT_PUBLIC_*` forbidden** for job auth). **Optional:** extend the VST-13 **integration matrix** with a **Scheduled / background** row pointing at **`#int-8-4`** when useful (**do not** remove existing rows). If INT.8.4 content **clearly** belongs outside VST-13, **Progress Notes** in the **delivering** PR MUST justify **`docs/architecture.md`** instead — **pick one** canonical file in the **delivered** docs (**prefer VST-13**).

2. **Reference inventory — exhaust `@Cron` and `@nestjs/schedule`:** Matrix MUST cover **all** **`@Cron`** decorators under **`src/legacy/capstone-reference/`** (prep proves **exactly three**: **`driver-schedule.service.ts`**, **`trip.service.ts`**, **`conversation.service.ts`**). Narrative MUST note **Nest** **6-field** cron (`second minute hour day month weekday`) for **`'0 20 * * * *'`** (**minute 20** each hour at **second 0** — **verify** against `@nestjs/schedule` docs if prose is disputed). Document **`CronExpression.EVERY_MINUTE`** jobs by **name** + **intent** (**trip start timeout** vs **conversation open/close**). **ScheduleModule:** state **explicitly** that **`ScheduleModule.forRoot()`** (Nest **`@nestjs/schedule`** module) is **not** present in the **vendored** legacy tree (**grep**); **`DriverScheduleModule`** is a **domain** module — **clarify** to avoid reader confusion.

3. **Vestroo + platform inventory:** **Grep** shipped **`src/`** excluding **`src/legacy/`** for **`@Cron`**, **`CronExpression`**, **`cron`**, **`schedule`** (disambiguate **business** “schedule” UI/API from **platform cron**), **`vercel.json`**, **`/api/cron`**. Report **findings** in **`#int-8-4`** (prep: **no** **`@Cron`** / **`/api/cron`**; **`vercel.json`** **absent** at repo root — **re-verify**). Add **explicit TBD** row for **Vercel Dashboard → Cron Jobs** / project settings if **not** evidenced in-repo. **Supabase:** **grep** **`supabase/migrations/`** for **`pg_cron`**, **`cron.schedule`**, **`extensions`**, **`edge`**, **`pg_net`** — prep found **none**; matrix may show **N/A** unless new evidence appears.

4. **NFR.3.1 — Client secrets forbidden for cron:** **`#int-8-4`** MUST include a **non-negotiable** rule: new scheduled work **never** reads **`NEXT_PUBLIC_*`** or **browser-held** secrets for **auth**; cron **must** use **server** env, **hosted** scheduler secrets, or **Supabase** server contexts only — **cross-link** **[`docs/environment-vars.md`](../environment-vars.md)** and VST-13 **Security** bullets.

5. **NFR.1.3 — Job failure visibility:** For each **implemented automation** row (if any after honest mapping), document **how failures surface** (**HTTP** status for **Route Handler** cron, **Vercel** logs, **Supabase** logs, **operator** procedure). Where **no** automation exists, **manual ops** row MUST state **how** ops detect missed runs (**TBD** allowed with **gap** call-out).

6. **NFR.4.1 — Types at boundaries (conditional):** If **no** TS cron handlers exist in **shipped** code, state **N/A**; if **`/api/cron`** or similar ships later, cite **Zod** / **types** / tests in the matrix footnote.

7. **Backlog for unmapped reference jobs:** Each reference **`@Cron`** job **without** an **honest implemented automation** mapping MUST have a **backlog** row (**P0/P1/P2** or **High/Medium/Low** — **one** scheme, consistent with **INT.8.1** if possible) describing the **smallest** Vestroo-appropriate outcome (**manual runbook** vs **future Route Handler** vs **N/A** for **ZA** product).

8. **Index / Related thin links:** Add **`#int-8-4`** to **[`docs/index.md`](../index.md)** and/or VST-13 **Related** / cross-links (**no** wholesale restructure).

9. **Epic / VST-13 housekeeping:** **Do not** edit **`docs/epic-8.md`** in this story’s implementation pass unless a **separate** maintainer task is opened; **optional** reciprocal line in **[`docs/stories/vst-13.story.md`](vst-13.story.md)** to **`#int-8-4`** (**same optional pattern** as **INT.8.3**).

## Tasks / Subtasks

- [x] **Task 1 — AC1, AC8:** Add **`<a id="int-8-4"></a>`** + **`## INT.8.4 …`** to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** (or **justified** alternate per **AC1**); **thin** **[`docs/index.md`](../index.md)** link. (AC: #1, #8)
- [x] **Task 2 — AC2:** Re-**grep** **`src/legacy/capstone-reference/`** for **`@Cron`**, **`CronExpression`**, **`@nestjs/schedule`**, **`ScheduleModule`**; confirm **three** jobs; document **6-field** cron + **`ScheduleModule.forRoot`** absence + **`DriverScheduleModule`** disambiguation. (AC: #2)
- [x] **Task 3 — AC3:** Re-**grep** shipped **`src/`** (exclude **`src/legacy/`**), repo-wide **`vercel.json`**, **`/api/cron`**; record **Supabase** migration grep for **`pg_cron`** / **`cron.schedule`** / **`pg_net`**. (AC: #3)
- [x] **Task 4 — AC4:** Write **secrets posture** column + **`NEXT_PUBLIC_*`** prohibition; link **`environment-vars.md`** + VST-13 **Security**. (AC: #4)
- [x] **Task 5 — AC5:** Write **NFR.1.3** **failure visibility** bullets per row type (**automation** vs **manual** vs **N/A**). (AC: #5)
- [x] **Task 6 — AC6:** Add **NFR.4.1** footnote (**N/A** today or cite future types). (AC: #6)
- [x] **Task 7 — AC7:** Add **backlog** rows for **`autoCheckoutPendingSchedules`**, **`handleTripStartTimeout`**, **`checkTimeToOpenAndClose`** unless **honest automation** exists after full grep. (AC: #7)
- [x] **Task 8 — AC1 matrix:** Fill **Named owner** + **Monitoring** (**TBD** ok) for **every** row — **no silent cron**. (AC: #1)
- [x] **Task 9 — Sibling docs:** **Thin** pointers to **[`8.1.story.md`](8.1.story.md)** / **[`8.2.story.md`](8.2.story.md)** / **[`8.3.story.md`](8.3.story.md)** where **time-based** behaviour touches **email/SMS/pay**; optional **`vst-13.story.md`** reciprocal. (AC: #1, #9)
- [x] **Task 10 — AC9:** Confirm **`docs/epic-8.md`** left unchanged **or** file follow-up issue for **INT.8.4** deep link parity. (AC: #9)

## Dev Technical Guidance

### Primary sources

- **Epic requirement:** [`docs/epic-8.md`](../epic-8.md) — **INT.8.4**
- **Canonical integration doc (default):** [`docs/integrations-and-payments.md`](../integrations-and-payments.md) — **INT.8.1**–**INT.8.3** anchor pattern
- **Env + server-only posture:** [`docs/environment-vars.md`](../environment-vars.md)
- **Optional alternate home:** [`docs/architecture.md`](../architecture.md) — **only** with **Progress Notes** justification
- **Sibling stories:** [`docs/stories/8.1.story.md`](8.1.story.md), [`docs/stories/8.2.story.md`](8.2.story.md), [`docs/stories/8.3.story.md`](8.3.story.md)
- **Outbound HTTP idempotency (thin):** [`docs/front-end-api-interaction.md`](../front-end-api-interaction.md)

### How to inventory (reference → Vestroo → platform)

1. **Reference first:** **grep** **`@Cron`** under **`src/legacy/capstone-reference/`** → capture **expression**, **`name:`**, **method** body **one-line** summary, **path**.
2. **Nest vs domain modules:** **grep** **`ScheduleModule`** — separate **`@nestjs/schedule`** (missing in prep) from **`DriverScheduleModule`** / **`BusScheduleModule`** (**domain**).
3. **Vestroo second:** **grep** shipped **`src/`** for **platform** scheduling; **confirm** **`vercel.json`** presence **globally**.
4. **Supabase:** **grep** **`supabase/migrations/`** for **database-level** schedulers.
5. **Map honestly:** Prefer **manual ops** + **runbook TBD** over implying **hidden** cron.

## Testing / verification

- **Relative links:** From **`docs/stories/8.4.story.md`**, click-test **`../epic-8.md`**, **`../integrations-and-payments.md#int-8-4`** (after edit), **`../environment-vars.md`**, **`../architecture.md`** (if cited), **`../index.md`**, sibling **`8.1`–`8.3`**, optional **`vst-13.story.md`**.
- **Grep bundle (reference):** `@Cron`, `CronExpression`, `@nestjs/schedule`, `ScheduleModule`, `DriverScheduleModule`
- **Grep bundle (shipped):** `@Cron`, `CronExpression`, `vercel.json`, `/api/cron`, `cron` (filter false positives)
- **Grep bundle (Supabase):** `pg_cron`, `cron.schedule`, `pg_net`, `extensions`
- **Consistency:** **No** row claims **Nest** cron runs in **Vestroo prod**; **reference** is **pattern-only**.

## Story draft self-check

_Checklist inspiration: `bmad-agent/checklists/story-draft-checklist.md`_

| Category | Status | Notes |
| -------- | ------ | ----- |
| 1. Goal & Context Clarity | PASS | **INT.8.4** scoped; **Epic 8** scheduling; default **VST-13** **#int-8-4** |
| 2. Technical Implementation Guidance | PASS | **grep** plans, **6-field** cron note, **legacy vs shipped** paths |
| 3. Reference Effectiveness | PASS | **three** **`@Cron`** files + **`ScheduleModule`** honesty |
| 4. Self-Containment Assessment | PASS | **Out of scope** for **8.5–8.6**, **Nest-in-Next**, **`vercel.json`**, **epic** edit |
| 5. Testing Guidance | PASS | Link check + **grep** bundles |

**Final Assessment: READY** — Developer agent can add **`#int-8-4`**, **grep-backed** matrix, **NFR** rules, **backlog**, and **platform TBD**s **without** implementing **cron** code.

## Story Progress Notes

### Agent Model Used:

Story preparation (**sm** subagent) — **created** **`docs/stories/8.4.story.md`** only (**no** edits to **`integrations-and-payments.md`**, **`index.md`**, **`epic-8.md`**, or code in this step).

**Implementation (dev agent):** **`#int-8-4`** in **`integrations-and-payments.md`**, integration matrix row, **`index.md`**, **`vst-13.story.md`** reciprocal; **`docs/epic-8.md`** intentionally **unchanged** per **AC9** / story scope.

### Completion Notes List

- **Canonical home:** **[`docs/integrations-and-payments.md`](../integrations-and-payments.md#int-8-4)** — reference **`@Cron`** matrix (3 jobs), **`ScheduleModule`** vs **`DriverScheduleModule`**, shipped / **Vercel** / **Supabase** inventory, **NFR.3.1 / 1.3 / 4.1**, backlog, **INT.8.5**/**8.6** one-line deferrals.
- **Grep (delivery):** **`@Cron`** only in **`driver-schedule.service.ts`**, **`trip.service.ts`**, **`conversation.service.ts`** under legacy; **no** **`ScheduleModule.forRoot`** in vendored tree; **`src/app`** + **`src/actions`** — **no** **`@Cron`**; **no** repo-root **`vercel.json`**; **no** **`pg_cron`** / **`pg_net`** in **`supabase/migrations/`**.
- **`docs/epic-8.md`:** **not edited** (deep link parity to **INT.8.4** deferred per story).
- **Reciprocal:** **`vst-13.story.md`** **Dependencies** extended with **`#int-8-4`** + **[Story 8.4](8.4.story.md)**.

### Change Log

| Date | Change |
| ---- | ------ |
| 2026-04-14 | **Ready** story file created: **INT.8.4** scheduled jobs — **`#int-8-4`** spec, matrix columns (**owner** + **monitoring**), **NFR.3.1 / 1.3 / 4.1**, **grep** verification plan, **docs-only** scope, **epic-8** edit explicitly **out of scope**. |
| 2026-04-14 | AC3 / Task 3 wording: fixed **`src/legacy/`** markdown; **vercel.json** search repo-wide. |
| 2026-04-14 | **Documentation delivered:** **`#int-8-4`**, integration matrix row, **`index.md`**, **`vst-13.story.md`**; tasks **1–10** complete; **`epic-8.md`** unchanged. |
