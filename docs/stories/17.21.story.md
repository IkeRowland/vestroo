# Story 17.21: Wheelzie layout & token parity pass (Epic 17 — follow-up)

## Status: Done

**Story provenance:** **[`docs/epic-17.md`](../epic-17.md)** — suggested child story **21**; **[`docs/design/visual-redesign-references.md`](../design/visual-redesign-references.md)** — Gap analysis; **[`docs/design/visual-redesign-tokens.md`](../design/visual-redesign-tokens.md)** — §1.1a.

## Goal

Close the **visual gap** between shipped stories **17.1–17.10** and the **Wheelzie** PNGs in [`docs/design/wheelzie-reference/README.md`](../design/wheelzie-reference/README.md), **without** rewriting domain logic.

## Scope (prioritised)

- [x] **Sidebar active state:** Replace green-tint **`--ops-surface-active`** usage on nav links with **§1.1a** sky / soft-blue selection (`--ops-nav-active` or equivalent); keep **`vest.rust`** for icons and badges unless brand requests otherwise.
- [x] **`/ops/bookings`:** Add **KPI scorecard row** + **`OpsBarChart`** (or stacked booking overview) **above** existing filters/table per **`06-bookings.png`** — Vestroo metrics and copy (**NFR.17.7**).
- [ ] **`/ops` dashboard (optional):** Right-column widgets / fleet utilisation list per **`10-dashboard.png`** if product confirms value — **not shipped** (remains deferred per scope note).

## Acceptance criteria (draft)

1. Side-by-side screenshot: **`/ops`** sidebar active item vs **`01-drivers.png`** or **`10-dashboard.png`** — selection **hue** matches §1.1a (blue family, not green).
2. Side-by-side: **`/ops/bookings`** shows **three bands** — KPI, chart (if data available or justified placeholder), table — vs **`06-bookings.png`**.
3. **`npm run lint`** / **`npm run test`** green; **axe** on touched routes.
4. Parity doc updated for any new token names.

## Dependencies

Stories **17.1–17.10** **Done**; primitives **`OpsKpiCard`**, **`OpsBarChart`**, **`OpsPagination`** available.

---

**Related:** Stories **17.1**, **17.3**, **17.6**, **17.10** — post-ship alignment notes.

## Story progress notes

- **Shipped:** `globals.css` — `--ops-nav-active` on **`[data-ops-theme=light|dark]`**; Tailwind `bg-ops-nav-active`. **`OpsSidebar`** active link uses `bg-ops-nav-active` (replaces `bg-ops-surface-active` for nav). **`/ops/bookings`** — **`OpsBookingsQueueOverviewBand`**: three **`OpsKpiCard`**s (in-view total, ready-to-assign with drill to preset href, completed 7d UTC) + **`OpsBarChart`** (completed vs cancelled by UTC day, last 7 days) above filters; data from Supabase in parallel with existing queue counts. Copy in **`ops-bookings-queue-copy`**. Parity: **`docs/ops-design-system-parity.md`** (Token scope) documents `--ops-nav-active` / `bg-ops-nav-active`.
- **Tests:** `npm run lint` (0 errors; pre-existing warnings only); `npm run test` — all green; new **`OpsBookingsQueueOverviewBand.test.tsx`** (jest-axe); **`ops-bookings-queue-overview-chart.test.ts`** for bar bucketing.
- **Model:** implementation agent: GPT-5.1-based coding agent (Cursor Agent).
