# Story 8.2: SMS stub and policy parity (INT.8.2)

## Status: Done

**Dependencies:** **[`docs/epic-8.md`](../epic-8.md)** — **INT.8.2** source (document **which** events send SMS, **stub vs provider**, **opt-in**, **rate limits**; **`share/`** SMS helpers **not** wired without **product** + **legal**; matrix **implemented / stub / N/A** + **rationale**). **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** (VST-13) — existing **SMS** matrix row (`src/services/sms-stub.ts`); extend with **`## INT.8.2 …`** and anchor **`#int-8-2`** as canonical home (**mirror** **INT.8.1** pattern **`#int-8-1`**). **[`docs/environment-vars.md`](../environment-vars.md)** — **grep** `SMS_` / `sms` (optional **`SMS_PROVIDER_*`** placeholders; **verify** alignment with **`.env.example`** — **grep**, do **not** assume zero rows). **[`docs/compliance-and-safety.md`](../compliance-and-safety.md)** (**VST-12**) — phone numbers as **PII**, consent, minimisation; **cross-link** only — **no** new legal claims. **[`docs/stories/vst-12.story.md`](vst-12.story.md)** — story-level consent / PII where SMS touches **`customer_phone`**. **[`docs/stories/8.1.story.md`](8.1.story.md)** — **INT.8.1** complete; **do not** re-run email template inventory — **one** cross-reference if a row overlaps (e.g. booking paid **email** vs **SMS**). **[`docs/capstone-auth-keytoken-otp-parity.md`](../capstone-auth-keytoken-otp-parity.md)** — **OTP** parity; reference stack delivers OTP via **SMS** in **`auth.service.ts`** — **verify** overlap with **INT.8.2** matrix (**do not** conflate **transactional booking SMS** with **auth OTP** without separate rows). **[`docs/capstone-reference-stack-integration.md`](../capstone-reference-stack-integration.md)** (**FE.5.9**) — **grep** for SMS / outbound HTTP context if needed for narrative glue. **Reference code (traceability, not production Vestroo):** **`src/legacy/capstone-reference/backend-modules/auth/auth.service.ts`** — **`smsService.sendSmsWithoutBrandname`** after **`otpService.create`** (**grep** `sendSmsWithoutBrandname`, `SMS_PROVIDER`). **`src/legacy/capstone-reference/backend-share/sms.ts`**, **`share.port.ts`** (`ISMSProvider`), **`di-token.ts`** (`SMS_PROVIDER`) — **pattern only**; **forbidden** to wire this **`share/`**-style SMS into first-party app without **product** + **legal** sign-off (**epic**). **Shipped stub path (verify call sites):** **`src/services/sms-stub.ts`** — **`notifyBookingCreatedSmsStub`**, **`BookingSmsStubPayload`**, VST-6 / VST-9 comments, **no-op**. **`src/actions/createBooking.ts`**, **`src/actions/processPayment.ts`** — import + **`await notifyBookingCreatedSmsStub`** after booking insert (**grep** `notifyBookingCreatedSmsStub` — **do not** cite fragile line numbers). **`src/actions/__tests__/createBooking.test.ts`** — mocks stub (**grep** `sms-stub`).

**Explicit out of scope:** **INT.8.1** email matrix / Mailer inventory (**cross-link** [`#int-8-1`](../integrations-and-payments.md#int-8-1) only). **INT.8.3** PayFast deep dive (**one** pointer to VST-13 webhook / checkout is enough). **INT.8.4** cron / scheduled jobs (**defer** unless a **grep** finds SMS tied to cron — then **one** footnote). **INT.8.5** full **`share/`** secrets / JWT / Redis matrix (**one-line defer** to that story). **No** implementing **Twilio** (or any live SMS provider), **no** new env consumption in **`sms-stub.ts`** beyond documentation — **separate product story** if wiring proceeds.

**Constraining NFRs:**

- **NFR.3.1:** Any future SMS API keys (**e.g.** `SMS_PROVIDER_*` in **[`docs/environment-vars.md`](../environment-vars.md)**) remain **server-only**; **no** SMS secrets in **`NEXT_PUBLIC_*`** or client bundles — align prose with VST-13 **Security** section.
- **NFR.1.3:** Document **failure modes** for the **stub** path (**silent success** / **no-op**, **no** gateway failure, **no** operator alert) vs a **future provider** path (**TBD** — timeouts, 429, invalid key). **Operator expectation:** today **no SMS leaves the platform** from **`notifyBookingCreatedSmsStub`** — state explicitly after **grep**-verifying implementation.
- **NFR.4.1:** **`BookingSmsStubPayload`** and any future provider payload types stay **TypeScript**-narrowed at the boundary — matrix or footnote MUST cite **`src/services/sms-stub.ts`** exports; add **test** pointer if **`createBooking`** / **`processPayment`** tests assert mock behaviour (**grep** `vi.mock('@/services/sms-stub'`).

## Story

- As a **platform or engineering team member** owning **Epic 8** integration traceability
- I want a **grep-backed** **SMS** policy and inventory: **which** business events invoke SMS (stub or future provider), **opt-in / consent** notes for **phone** use, **rate-limit** posture (**explicit TBD** where none), **staging vs production** behaviour, and **reference** **`share/`** / capstone **SpeedSMS**-style helpers mapped as **patterns only** — with **product + legal** gate for any real provider wiring
- so that we **do not** silently assume SMS is sent, **parity** with the reference stack is **documented** (outcomes, not copy-paste), and **VST-12** boundaries for **phone PII** stay visible next to **INT.8.1** email work

## Acceptance Criteria (ACs)

1. **Canonical doc home for INT.8.2:** Add **`## INT.8.2 — SMS stub and policy parity`** (or equivalent title) with stable anchor **`#int-8-2`** to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** (**`<a id="int-8-2"></a>`** above the heading, **same pattern** as **`#int-8-1`**) **unless** **Progress Notes** justify a split file — **default** is **extend VST-13**. Section MUST include a **compact matrix** with columns at minimum: **Business event**; **Reference trigger** (file path(s) under **`src/legacy/capstone-reference/`** — locate via **grep**, cite paths not fragile line numbers unless verified at edit time); **Vestroo implementation** (e.g. **`src/services/sms-stub.ts`** **`notifyBookingCreatedSmsStub`** for booking-create paths); **Opt-in / consent note** (link **VST-12**; state **TBD** if product copy / checkbox not defined — **honest**); **Rate limit policy** (**explicit TBD** if none — reference doc already flags OTP abuse as **TBD**); **Staging vs prod**; **Status** (**implemented** | **stub** | **N/A**) + **short rationale**; **Product + legal gate** column or footnote: **no** wiring **`share/`** SMS / external provider without approval. Update or extend the **integration matrix** **SMS** row if the new section supersedes one-liner detail.

2. **Reference inventory — SMS / OTP / providers:** **Grep** **`src/legacy/capstone-reference/`** for **`sendSms`**, **`sendSmsWithoutBrandname`**, **`SMS_PROVIDER`**, **`ISMSProvider`**, **`sms.ts`**, **`SpeedSMS`**, **`api.speedsms`**, **`ESMS_`**, **`Twilio`**, **`twilio`**, **`OTP`**, **`otpService`**. Summarise **each** distinct **delivery channel** (e.g. **OTP via SMS** in **`auth.service.ts`** vs **`SmsService`** implementation in **`backend-share/sms.ts`**). Matrix rows MUST **not** label Vestroo as “implemented SMS” without a **real** provider callsite under **`src/`** (excluding **`src/legacy/`**).

3. **Vestroo inventory — stub and phone fields:** **Grep** shipped **`src/`** (exclude **`src/legacy/`** unless comparing) for **`sms-stub`**, **`notifyBookingCreatedSmsStub`**, **`BookingSmsStubPayload`**, **`customer_phone`**, **`customerPhone`**. Document **at least**: **`createBooking`** and **`processPayment`** invoking the stub after **`bookings`** insert; **`customer_phone`** persisted on the row. Optionally note **UI / API** surfaces that display phone (**grep** results — **confirmation**, **field trip**, **search**, **cancel** — **verify** before listing).

4. **`share/` / capstone SMS — prohibition:** In **INT.8.2** prose, state clearly that **reference** **`backend-share`** SMS (**`SmsService`**, **`SMS_PROVIDER`**) is **not** part of production Vestroo routing; **first-party** app MUST **not** import or **DI**-wire that module tree without **product** + **legal** approval and env / sub-processor review. Align wording with **[`docs/epic-8.md`](../epic-8.md)** **INT.8.2** and **INT.8.5** deferral.

5. **Backlog rows for gaps:** Any **reference** SMS event without a **truthful** Vestroo **implemented** row, and any **Vestroo** **stub** that needs **opt-in**, **templates**, **rate limits**, or **provider choice**, MUST appear as **backlog** with **priority** (**P0/P1/P2** or **High/Medium/Low** — **one** scheme + **one-line** legend, consistent with **INT.8.1** if possible).

6. **NFR.1.3 — Stub failure modes:** Subsection or bullets under **INT.8.2**: **`notifyBookingCreatedSmsStub`** resolves **`void`**, **no** throw, **no** external I/O — **grep** `sms-stub.ts` body to confirm. Document **operator** expectation (**no** customer SMS; rely on **email** / **UI** for confirmations per product). Contrast with **future** provider row (**TBD** — retries, dead letter, **429**).

7. **NFR.4.1 — TypeScript boundaries:** Cite **`BookingSmsStubPayload`** (and **`notifyBookingCreatedSmsStub`** signature) from **`src/services/sms-stub.ts`**; cite **test** mock in **`src/actions/__tests__/createBooking.test.ts`** (**grep**); add **`processPayment`** test pointer if present (**grep** `sms-stub` under **`src/actions/__tests__/`**).

8. **Index / Related links:** **Thin** update: **[`docs/index.md`](../index.md)** and/or **VST-13** *Related* / cross-links so Epic **8** discovery hits **`#int-8-2`** (same spirit as **INT.8.1** — **no** wholesale restructure).

## Tasks / Subtasks

- [x] **Task 1 — AC1, AC8:** Add **`#int-8-2`** subsection to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** with matrix + **Related** / **[`docs/index.md`](../index.md)** link. (AC: #1, #8)
- [x] **Task 2 — AC2:** **Grep** **`src/legacy/capstone-reference/`** for SMS / OTP / provider strings; fill **reference** column + **N/A** vs **pattern-only** rows. (AC: #2)
- [x] **Task 3 — AC3, AC7:** **Grep** shipped **`src/`** for stub + phone fields; document **Server Action** triggers; cite **types** + **tests**. (AC: #3, #7)
- [x] **Task 4 — AC4:** Author **`share/`** / capstone **prohibition** + **product + legal** gate prose. (AC: #4)
- [x] **Task 5 — AC5:** **Backlog** rows + priority legend for **OTP**, **transactional booking SMS**, **rate limits**, **opt-in** gaps. (AC: #5)
- [x] **Task 6 — AC6:** Document **stub** **NFR.1.3** behaviour vs **future** provider **TBD**. (AC: #6)
- [x] **Task 7 — AC1 housekeeping:** Align **INT.8.2** with **[`docs/environment-vars.md`](../environment-vars.md)** — **grep** `SMS_` and **`.env.example`**; footnote **stub** does **not** read env yet if still true after **grep**. Cross-link **[`docs/capstone-auth-keytoken-otp-parity.md`](../capstone-auth-keytoken-otp-parity.md)** for **OTP** / SMS overlap. (AC: #1)
- [x] **Task 8 — VST-12:** Short bullets in **INT.8.2** tying **phone** fields to **minimisation** / consent themes; link **[`docs/compliance-and-safety.md`](../compliance-and-safety.md)** and **[`docs/stories/vst-12.story.md`](vst-12.story.md)**. (AC: #1 matrix column / footnote)

## Dev Technical Guidance

### Primary sources

- **Epic requirement:** [`docs/epic-8.md`](../epic-8.md) — **INT.8.2**
- **Integration + env:** [`docs/integrations-and-payments.md`](../integrations-and-payments.md), [`docs/environment-vars.md`](../environment-vars.md)
- **Compliance:** [`docs/compliance-and-safety.md`](../compliance-and-safety.md), [`docs/stories/vst-12.story.md`](vst-12.story.md)
- **Sibling story:** [`docs/stories/8.1.story.md`](8.1.story.md) — **INT.8.1** (**do not** duplicate email inventory)
- **OTP / SMS overlap (verify):** [`docs/capstone-auth-keytoken-otp-parity.md`](../capstone-auth-keytoken-otp-parity.md) — **OTP** §; legacy **`auth.service.ts`** sends **SMS** body for OTP
- **Stack contrast (optional grep):** [`docs/capstone-reference-stack-integration.md`](../capstone-reference-stack-integration.md)
- **Shipped stub:** `src/services/sms-stub.ts`
- **Call sites:** `src/actions/createBooking.ts`, `src/actions/processPayment.ts`

### How to inventory (reference vs Vestroo)

1. **Reference first:** **grep** capstone-reference for **SMS** symbols and **OTP** → **SMS** delivery paths; capture **provider names** (e.g. **SpeedSMS** in vendored **`sms.ts`**) as **historical / pattern**, not Vestroo choices.
2. **Vestroo second:** **grep** **`src/actions/`**, **`src/app/api/`**, **`src/services/`** for **`sms-stub`** and **`customer_phone`** / **`customerPhone`** — distinguish **persistence / UI** from **SMS send**.
3. **Map by business event:** e.g. “**Booking created (pending payment)**” → **stub** **`notifyBookingCreatedSmsStub`**; “**Reference phone OTP**” → **Vestroo** **N/A** or **gap** until product selects **Supabase phone** vs **third-party** (**per** capstone-auth doc).
4. **Staging clarity:** stub behaves **identically** in dev/staging/prod (**no** send); when a provider exists, align tiers with **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** per-environment table (extend **SMS** column if missing).

## Testing / verification

- **Relative links:** From **`docs/stories/8.2.story.md`**, click-test **`../epic-8.md`**, **`../integrations-and-payments.md#int-8-2`**, **`../environment-vars.md`**, **`../compliance-and-safety.md`**, **`vst-12.story.md`**, **`8.1.story.md`**, **`../capstone-auth-keytoken-otp-parity.md`**, **`../capstone-reference-stack-integration.md`**, **`../index.md`**.
- **Grep bundle (reference):** `sendSms`, `sendSmsWithoutBrandname`, `SMS_PROVIDER`, `ISMSProvider`, `speedsms`, `ESMS`, `twilio`, `otpService`, `OtpModule`
- **Grep bundle (shipped):** `sms-stub`, `notifyBookingCreatedSmsStub`, `BookingSmsStubPayload`, `customer_phone`, `customerPhone`
- **Consistency:** **No** matrix row **implemented** (real SMS) without a **non-legacy** **`src/`** provider integration and **ADR / product** traceability.

## Story draft self-check

_Checklist inspiration: `bmad-agent/checklists/story-draft-checklist.md`_

| Category | Status | Notes |
| -------- | ------ | ----- |
| 1. Goal & Context Clarity | PASS | **INT.8.2** scoped; epic **SMS** policy; **VST-12** phone / consent |
| 2. Technical Implementation Guidance | PASS | **Stub** path, **grep** plans, **`share/`** gate |
| 3. Reference Effectiveness | PASS | **`auth.service`**, **`backend-share/sms.ts`**, **`share.port`**, **`di-token`** |
| 4. Self-Containment Assessment | PASS | **Out of scope** for **8.1, 8.3–8.5** implementation stories |
| 5. Testing Guidance | PASS | Link check + **grep** bundles; **mock** test pointer |

**Final Assessment: READY** — Developer agent can add **`#int-8-2`**, complete **grep-backed** matrix, **VST-12** phone bullets, **stub failure modes**, **backlog** priorities, and **env** cross-checks **without** wiring **Twilio** or **`share/`** SMS.

## Story Progress Notes

### Agent Model Used:

Story preparation (**sm** subagent) — **documentation requirements only** (**no** edits outside **`docs/stories/8.2.story.md`** in this step).

### Completion Notes List

- Added **`<a id="int-8-2"></a>`** and **`## INT.8.2 — SMS stub and policy parity`** to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** with compact matrix, **VST-12** bullets, **NFR.1.3** / **NFR.4.1**, backlog + **P0/P1/P2** legend (same wording as **INT.8.1**), **`share/`** gate + **INT.8.5** deferral, env footnote (**`SMS_PROVIDER_*`** documented; stub does not read env), and **Story 8.2** traceability line.
- Extended VST-13 **integration matrix** SMS row and **per-environment** table **SMS** column (**stub** = **no outbound** all tiers); **Cross-links** includes **`#int-8-2`**.
- **`docs/index.md`**: Epic **8** and **VST-13** bullets link **`integrations-and-payments.md#int-8-2`** alongside **INT.8.1**.
- **`docs/epic-8.md`**: **INT.8.2** subsection links canonical **[`integrations-and-payments.md#int-8-2`](../integrations-and-payments.md#int-8-2)**.
- **`processPayment`** + **`sms-stub`**: documented as **TBD** test harness (**`createBooking.test.ts`** mocks present; no **`processPayment`** test under **`src/actions/__tests__/`** per prep glob).

### Change Log

| Date | Change |
| ---- | ------ |
| 2026-04-14 | **Ready** story file created: **INT.8.2** SMS stub + policy parity — VST-13 **`#int-8-2`**, matrix columns, **`share/`** gate, **NFR.3.1 / 1.3 / 4.1**, **VST-12** phone, **grep** verification plan. |
| 2026-04-14 | Formatting normalised (spacing + markdown table rows). |
| 2026-04-14 | **Documentation delivered:** **`#int-8-2`** section in **`integrations-and-payments.md`**, **`index.md`** / **`epic-8.md`** links, tasks **1–8** complete (**docs-only**, no app code). |
