# Story 7.6: Ratings and feedback events — storage, ops visibility, Realtime vs batch (RT.7.6)

## Status: Done

**Dependencies:** **[Stories 7.1–7.5](7.1.story.md)** through **[7.5](7.5.story.md)** (**Done**) — prior **RT.7.x** matrix work through **DriverSchedule** / **DriverBusSchedule** (**RT.7.5**). **7.6** extends **[`docs/capstone-backend-module-matrix.md`](../capstone-backend-module-matrix.md)** with **`## RT.7.6 / Epic 7 — RatingModule → feedback, ops visibility, Realtime vs batch`** (or equivalent) **only** — **no** second matrix file. **[`docs/epic-7.md`](../epic-7.md)** — **RT.7.6** source (**Rating** flows, **service type** dimensions, **ops** visibility, **Realtime** vs **batch**). **[`docs/epic-5.md`](../epic-5.md)** — **FE.5.x**, reference driver **trip rating** / service-type context (**high-level**); **NFR.5.4** corporate shuttle vocabulary (**no** default **bus** product language in customer/shuttle copy when mapping **hourly** vs **scenic**). **[`docs/epic-6.md`](../epic-6.md)** — **BE.6.x** / staff ops narrative where **NFR.5.4** applies to matrix and **FE.5.11** rows. **[`docs/data-models.md`](../data-models.md)** — **`public.ratings`** (grep **migrations** and **RLS overview**; add or extend a short **`ratings`** subsection if the epic table omits column-level truth — **verify** before claiming parity). **[`docs/realtime-and-notifications.md`](../realtime-and-notifications.md)** — **`supabase_realtime`** publication scope vs **`src/lib/supabase/realtime.ts`** subscribers (**grep** **`ratings`** **not** listed). **[`docs/ops-console.md`](../ops-console.md)** — whether **`/ops/*`** exposes ratings (matrix domain row today: **no** dedicated **`/ops/rating`** — **re-verify** routes and **Related** links). **[`docs/capstone-driver-to-field-mapping.md`](../capstone-driver-to-field-mapping.md)** — reference **tripRating** / rating UI row **if present** (may be **absent**; record **grep** result honestly). **[`docs/capstone-reference-stack-integration.md`](../capstone-reference-stack-integration.md)** (**FE.5.9**) — **socket.io** / reference **HTTP** rating API contrast **only**. **[`docs/capstone-reference/cline_docs/techContext.md`](../capstone-reference/cline_docs/techContext.md)** — **align-or-gap** for **WebSocket** / **real-time** / performance bullets vs **ratings** path (**batch** / **read on demand** default). **Reference code (traceability, not shipped product):** `src/legacy/capstone-reference/backend-modules/rating/*`, `src/features/capstone-reference/customer/views/Trips/components/tripRatingView.tsx`, `src/features/capstone-reference/customer/views/Trips/components/tripRatingForm.tsx`, manager **`getAllRating`** / **`/rating`** sample under **`src/features/capstone-reference/manager/**` **if** still accurate (**verify** paths). **Optional:** **[`docs/stories/vst-10.story.md`](vst-10.story.md)** when citing **experiences** / **scenic routes** product line for **service type** mapping.

**Explicit out of scope:** **No** new **Epic 9** **bus** product or **Bus**\* transit-demo surfaces. **No** second **`docs/*matrix*.md`**. **No** mandatory **Realtime** on **`public.ratings`** unless **product** + **ADR** explicitly require it — **default documentation stance:** **batch** ingestion + **read on navigation** / **refetch** suffices unless evidence shows otherwise (**grep** **`realtime.ts`**, **`20260409120000_vst9_realtime_notifications.sql`**). **No** full **Epic 5** UI redesign — **wording**, **matrix**, and **cross-links** only unless verification finds a **blocking** doc bug. **No** implementing customer rating capture UI in **`(app)`** as part of this story unless a separate product story says so (this story is **traceability** / **docs**).

**Constraining NFRs:**

- **NFR.3.1:** **`ratings`** rows tie to **`trips`** and party **`profiles`**; matrix and docs MUST **not** imply cross-party **PII** leakage beyond **`ratings_parties`** / **`ratings_insert_customer`** semantics (**grep** latest migration policies).
- **NFR.1.1 / NFR.1.2:** **Do not** claim **unbounded** **socket** fan-out or **Realtime** subscriptions for **`ratings`** without **DDL** + client code; default posture is **bounded** reads/writes via **Server Actions** (when implemented) or **explicit** backlog.
- **NFR.5.5:** UI copy and diagrams MUST **match** **server** enforcement: **no** ops/chauffeur/customer doc claims they **subscribe** to **`ratings`** via **Supabase Realtime** unless **`realtime.ts`** and **`supabase_realtime`** publication **both** support it (**today:** **neither** lists **`ratings`** — **verify**).
- **NFR.5.4** (**[`docs/epic-5.md`](../epic-5.md)** / **[`docs/epic-6.md`](../epic-6.md)**): When mapping reference **hourly** vs **scenic** (or similar) to Vestroo product lines, use **Vestroo** terms — e.g. **hourly / transfer** vs **experiences** / **scenic routes** per existing **`PricingModule`**, **`ScenicRouteModule`**, and **[`docs/stories/vst-10.story.md`](vst-10.story.md)** where cited — **forbidden:** forcing **bus** product vocabulary into **Epic 5** customer/shuttle copy for those mappings.

## Story

- As a **platform or engineering team member** tracing **capstone** **`RatingModule`** to **Vestroo**
- I want **`public.ratings`** storage, **RLS**, **Server Actions** (or honest **TBD**), **ops** visibility gaps, and **Realtime vs batch** decisions **documented** with **grep-backed** evidence from **`realtime.ts`**, **VST-9** publication migration, and **`src/app/(ops)`** / **`(field)`** / **`(app)`**
- so that **RT.7.6** is **auditable**, **NFR.5.5** is not violated by **live rating** overclaims, and **service type** language stays **aligned** with **corporate shuttle** / **Vestroo** product lines (**NFR.5.4**)

## Acceptance Criteria (ACs)

1. **Canonical matrix subsection — RatingModule mapping:** In **[`docs/capstone-backend-module-matrix.md`](../capstone-backend-module-matrix.md)**, add **`## RT.7.6 / Epic 7 — RatingModule → feedback, ops visibility, Realtime vs batch`** (or equivalent) with anchor **`#rt-7-6`** (match **RT.7.5** style) and a **compact table** (plus short traceability paragraph) covering:
   - **Reference triggers:** Nest **`RatingModule`** / **`rating.controller.ts`** — e.g. **`POST`** **`create-rating`**, **`GET`** **`get-rating-by-trip-id/:tripId`**, **`average-rating`**, **`get-rating-by-query`** (`src/legacy/capstone-reference/backend-modules/rating/*`) — **contrast only** with Vestroo (**FE.5.9** pointer).
   - **Vestroo storage:** **`public.ratings`** — columns per **`20260402131447_vin_shuttle_tickets_and_social.sql`** (initial **`trip_id`**, **`rate`**, **`feedback`**, party ids) and **`20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`** (**`driver_id` → `chauffeur_id`** rename on **`ratings`**).
   - **Mutations / reads:** **Server Actions** or Route Handlers — **TBD** in shipped **`src/actions/`** unless **grep** finds implementations; state **honest gap** if none.
   - **Realtime vs batch:** **`src/lib/supabase/realtime.ts`** exposes **`subscribeVehicleTrackings`** and **`subscribeTripsBoard` only** — **no** **`ratings`** channel builder. **`20260409120000_vst9_realtime_notifications.sql`** — **`alter publication supabase_realtime add table`** for **`vehicle_trackings`** and **`trips` only** — **grep** confirms **`ratings`** **not** published. **Default stance:** **batch** / **on-demand read** after trip completion (or staff refresh) **suffices** until product + **ADR** say otherwise.
   - **Do not** create a **second** matrix file.

2. **Ops visibility — explicit gap + owner:** State clearly that shipped **`/ops/*`** has **no** dedicated **`/ops/rating`** (align with domain table **Notes** for **`RatingModule`** today — **partial**, **TBD** actions). **Re-verify** **[`docs/ops-console.md`](../ops-console.md)** route list and **`OPS_NAV_GROUPS`** / **`ops-nav-config.ts`** for any hidden ratings surface. If product wants manager-style grids later, name **backlog owner** (**product** / **Epic 6** follow-up story **TBD**) — **no** implementation mandate in **7.6**.

3. **Service type mapping (reference hourly vs scenic → Vestroo lines):** In the **RT.7.6** subsection, add a **short table or bullet block**:
   - **Hourly / on-demand style** → **`PricingModule`** quote paths (**`calculateHourlyQuote`**, **`calculateQuote`** — **`src/actions/`**) and **transfer** / **charter** wording in customer copy (**not** “bus product”).
   - **Scenic / corridor / experience style** → **`ScenicRouteModule`** + **`service_routes`** / **`service_points`** narrative; **experiences** packages per **VST-10** / **`experience_packages`** where cited — **[`docs/stories/vst-10.story.md`](vst-10.story.md)** optional cross-link.
   - **Forbidden:** labelling these mappings as a default **bus** **product line** in **Epic 5** UI or shuttle customer copy (**NFR.5.4**).

4. **NFR.5.5 — doc grep for live rating / socket overclaims:** Search **`docs/`** (especially **ops-console**, **field-tools**, **realtime-and-notifications**, **core-traveller-flow-parity**, **front-end** docs) for phrases implying **live** **rating** streams, **socket**-style **rating** delivery, or **Realtime** **subscription** on **`ratings`**. **Patch wording only** where text contradicts **`realtime.ts`** + publication **grep** results. Record **verified — no change** in **Progress Notes** where already aligned.

5. **`techContext.md` align-or-gap:** Add a short block under **RT.7.6**: reference **WebSocket** / **sub-second** / **fan-out** expectations vs Vestroo **Postgres + RLS** path for **`ratings`** — **align** (writes bounded by action/DB), **partial** (if any UI suggests instant cross-role visibility), or **gap** (reference **socket** hub for ratings) with **explicit** mapping to **batch** / **read** pattern — **silent** deviation **not** allowed.

6. **Index / matrix Related / realtime Related for RT.7.6:** **Verify** **[`docs/index.md`](../index.md)** links **[`docs/epic-7.md`](../epic-7.md)**. Extend **[`docs/capstone-backend-module-matrix.md`](../capstone-backend-module-matrix.md)** *Related documentation* **Epic 7** bullet (or equivalent) to include **RT.7.6** + **`#rt-7-6`** when the subsection exists. Add **one** pointer from **[`docs/realtime-and-notifications.md`](../realtime-and-notifications.md)** *Related documentation* **Epic 7** list to **RT.7.6** if missing — **thin** entry (ratings **not** a shipped Realtime topic unless changed).

7. **Domain table `RatingModule` Notes — Story 7.6 / RT.7.6:** Update the main domain table **Notes** for **`RatingModule`** to cite **Story 7.6 / RT.7.6** and **`#rt-7-6`** consistently (same pattern as **Story 7.3** / **7.5**). Short row MUST **not** contradict the **RT.7.6** subsection body (especially **Realtime** and **`/ops/rating`** gap).

8. **Shipped vs reference-only code grep:** **Grep** **`src/app/(ops)`**, **`src/app/(field)`**, **`src/app/(app)`** for **`ratings`**, **`rating`**, and obvious rating UI strings. **Honest record:** e.g. **marketing** testimonials may use **`rating?: number`** unrelated to **`public.ratings`**; **capstone-reference** customer/manager paths are **not** first-party **`(app)`** / **`(ops)`** product routes unless wired — list findings in **Progress Notes**.

## Tasks / Subtasks

- [x] **Task 1 — AC1, AC7:** Add **`## RT.7.6 / Epic 7 — RatingModule → feedback, ops visibility, Realtime vs batch`** to **[`docs/capstone-backend-module-matrix.md`](../capstone-backend-module-matrix.md)** with mapping table (**Nest triggers**, **`public.ratings`**, **Server Actions** **TBD** or paths, **Realtime** **no** **`ratings`** subscriber, **publication grep**); update **`RatingModule`** domain row **Notes** for **Story 7.6 / RT.7.6**. (AC: #1, #7)

- [x] **Task 2 — AC2:** Document **`/ops/rating`** gap, **ops-console** verification, backlog **owner** for future staffed ratings grid. (AC: #2)

- [x] **Task 3 — AC3:** Author **service type** mapping bullets/table (**hourly/transfer** vs **scenic/experiences**) with **`PricingModule`**, **`ScenicRouteModule`**, optional **VST-10**; **NFR.5.4** **forbidden** wording called out. (AC: #3)

- [x] **Task 4 — AC4:** Run **doc grep** for **live rating** / **socket** overclaims; patch **wording only** if needed; log **verified — no change** cases. (AC: #4)

- [x] **Task 5 — AC5:** Read **[`docs/capstone-reference/cline_docs/techContext.md`](../capstone-reference/cline_docs/techContext.md)**; add **ratings-adjacent** **align/partial/gap** under **RT.7.6**. (AC: #5)

- [x] **Task 6 — AC6:** Patch **`docs/index.md`** (if needed), matrix **Related**, **`realtime-and-notifications.md`** **Epic 7** **Related** line for **RT.7.6**. (AC: #6)

- [x] **Task 7 — AC1 / data-models:** **Grep** **`docs/data-models.md`** for **`ratings`**; if narrative is thin, add **one** subsection or epic-table row bullet for **`public.ratings`** (columns + pointer to **RLS** policies in migrations) and link **RT.7.6** — **do not** duplicate the full matrix. (AC: #1, Dependencies)

- [x] **Task 8 — AC8:** **Grep** **`(ops)`**, **`(field)`**, **`(app)`** as specified; paste summary to **Progress Notes**. (AC: #8)

- [x] **Task 9 — AC1 housekeeping:** After substantive matrix edits, bump **`Last updated:`** in **[`docs/capstone-backend-module-matrix.md`](../capstone-backend-module-matrix.md)** to cite **Story 7.6** / **RT.7.6** (preserve prior story citations as in existing line). (AC: #1)

## Dev Technical Guidance

### Primary sources

- **Epic requirement:** [`docs/epic-7.md`](../epic-7.md) — **RT.7.6**
- **Vocabulary / customer copy constraints:** [`docs/epic-5.md`](../epic-5.md), [`docs/epic-6.md`](../epic-6.md) — **NFR.5.4**
- **Canonical matrix (extend only):** [`docs/capstone-backend-module-matrix.md`](../capstone-backend-module-matrix.md)
- **Schema (migrations authoritative if data-models omits detail):** `supabase/migrations/20260402131447_vin_shuttle_tickets_and_social.sql`, `20260402131457_vin_shuttle_rls_enable_and_is_staff.sql`, `20260402133655_vestroo_rls_policies_booking_social.sql`, `20260406103000_vestroo_profile_roles_chauffeur_columns_rls.sql`
- **Shipped Realtime builders + publication:** `src/lib/supabase/realtime.ts`, `supabase/migrations/20260409120000_vst9_realtime_notifications.sql`, [`docs/realtime-and-notifications.md`](../realtime-and-notifications.md)
- **Ops routes / nav:** [`docs/ops-console.md`](../ops-console.md), `src/features/ops/ops-nav-config.ts`
- **Stack contrast (reference):** [`docs/capstone-reference-stack-integration.md`](../capstone-reference-stack-integration.md) (**FE.5.9**)
- **Reference Nest + samples:** `src/legacy/capstone-reference/backend-modules/rating/*`, `src/features/capstone-reference/customer/views/Trips/components/tripRatingView.tsx`, `src/features/capstone-reference/manager/`

### Product line stories (optional)

- [`docs/stories/vst-10.story.md`](vst-10.story.md) — **experiences** / scenic-adjacent packages

## Testing / verification

- **Doc links:** From **`docs/stories/7.6.story.md`**, click-test **`../`** paths to **epic-7**, **epic-5**, **epic-6**, **matrix**, **data-models**, **realtime-and-notifications**, **ops-console**, **driver-to-field-mapping**, **stack-integration**, **techContext**, **vst-10** (if used).
- **Single matrix:** Confirm **no** new **`docs/*matrix*.md`**; only **`capstone-backend-module-matrix.md`** gains **RT.7.6**.
- **Grep bundle:** `ratings`, `rating`, `subscribe`, `postgres_changes`, `supabase_realtime`, `RatingModule`, `realtime.ts`, `create-rating`, `get-rating-by-trip` across **`src/`**, **`supabase/migrations/`**, **`docs/`**.
- **Consistency:** After edits, re-read **`RatingModule`** domain row against **RT.7.6** body and **Epic 7** *Related* footers in matrix + **realtime** doc.

## Story draft self-check

_Checklist source: `bmad-agent/checklists/story-draft-checklist.md`_

| Category | Status | Notes |
| -------- | ------ | ----- |
| 1. Goal & Context Clarity | PASS | **RT.7.6** scoped; **7.1–7.5** **Done**; **Rating** → **`ratings`**, **ops** gap, **Realtime** honesty |
| 2. Technical Implementation Guidance | PASS | **Nest** paths, **migrations**, **`realtime.ts`**, **publication SQL**, **grep** tasks for **`(ops)`**/**`(field)`**/**`(app)`** |
| 3. Reference Effectiveness | PASS | Relative links; **FE.5.9**, **techContext**, optional **VST-10** |
| 4. Self-Containment Assessment | PASS | **No** second matrix; **no** Epic 9 bus product; **default** **batch** stance explicit |
| 5. Testing Guidance | PASS | Link check, grep list, doc-matrix consistency |

**Final Assessment: READY** — Developer agent can add **matrix RT.7.6**, refresh **`RatingModule`** **Notes**, complete **techContext** and **NFR.5.5** sweeps, extend **Related** links, optionally patch **`data-models.md`** for **`public.ratings`**, and record **grep** outcomes — **without** mandating **`ratings`** **Realtime**, **`/ops/rating`** implementation, or **Epic 5** UI redesign.

## Story Progress Notes

### Agent Model Used:

Cursor **dev** agent (documentation implementation pass).

### Completion Notes List

- **AC1 / AC7:** Added **`#rt-7-6`** subsection **[`docs/capstone-backend-module-matrix.md`](../capstone-backend-module-matrix.md)** § **RT.7.6** (mapping table, **Nest** **`rating.controller.ts`** routes, **`public.ratings`** columns + **`driver_id` → `chauffeur_id`**, **`realtime.ts`** + **`20260409120000_vst9_realtime_notifications.sql`** grep posture, **batch** default). Extended **RT.7.4** summary table + per-topic row for **`ratings`**. Updated domain **`RatingModule`** row **Notes**. Bumped matrix **`Last updated:`** to **2026-04-11** + **Story 7.6**.
- **AC2:** **`/ops/rating`** gap and **`OPS_NAV_GROUPS`** verification recorded in matrix **RT.7.6**, **`docs/ops-console.md`** (routes note + **FE.5.11** table row); backlog owner **product / Epic 6** **TBD**.
- **AC3:** Service-type mapping table under **RT.7.6** (**hourly/transfer** vs **scenic/experiences**, **NFR.5.4** forbidden **bus** default).
- **AC4:** `Select-String` across **`docs/*.md`** for `live.*rating`, `rating.*realtime`, `rating.*socket`, `realtime.*rating`, `subscribe.*rating` — hits were **new RT.7.6 / honest negation** lines or **epic-7** requirement text; **no** contradictory “live **`ratings`** stream” doc found — **verified — no wording patch** beyond additive honesty in **realtime-and-notifications** + matrix.
- **AC5:** **`techContext.md`**-referenced **align / gap** table under **RT.7.6** (HTTP vs actions, WebSocket fan-out **gap**, bounded writes **align** posture).
- **AC6:** **`docs/index.md`** already linked **[Epic 7](../epic-7.md)** — **no change**. Matrix **Related documentation** Epic 7 bullet extended with **RT.7.6** + **`#rt-7-6`**. **`docs/realtime-and-notifications.md`** § Supabase Realtime + **Related** Epic 7 list updated with **RT.7.6** thin pointer.
- **AC7:** Domain **`RatingModule`** row aligned with subsection (**Realtime** + **`/ops/rating`** gap).
- **AC8 — shipped code grep (2026-04-11):** **`src/app/(ops)`**, **`(field)`**, **`(app)`** — **no** `ratings` / trip-rating UI in first-party routes. **`(marketing)/page.tsx`** — **`rating?: number`** on **testimonials** only (stars), **not** **`public.ratings`**. **`src/actions/*.ts`** — **no** `ratings` string matches. **Reference-only:** `tripRatingView.tsx` / `tripRatingForm.tsx`, **`src/legacy/capstone-reference/backend-modules/rating/*`**. **`docs/capstone-driver-to-field-mapping.md`:** **no** **`tripRating`** row (not required for **7.6**; honest **absence**).
- **Dependencies / data-models:** Epic mapping table row + **`### public.ratings`** subsection + **RLS** pointers (**[`docs/data-models.md`](../data-models.md)**).

### Change Log

| Date | Change |
| ---- | ------ |
| 2026-04-11 | **Done:** **RT.7.6** matrix subsection, **RatingModule** row, **RT.7.4** **`ratings`** row, **data-models** **`public.ratings`**, **ops-console** + **realtime-and-notifications** updates, **Story 7.6** tasks + progress notes. |
| 2026-04-10 | **Draft** created: **RT.7.6** — **RatingModule** → **`public.ratings`**, **ops** visibility gap, **Realtime vs batch** (**`realtime.ts`** + **VST-9** publication), **service type** → **PricingModule** / **ScenicRouteModule** / optional **VST-10**, **NFR.3.1**, **NFR.1.1/1.2**, **NFR.5.5**, **NFR.5.4**, **techContext** align-or-gap, **Related** link tasks, shipped **`(ops)`**/**`(field)`**/**`(app)`** grep AC. |
