# Story 16.6: `role-display.ts` — ProfileRole → UI labels + guardrail (Epic 16 / L2)

## Status: Done

**Story provenance:** **REMEDIATION** (Chauffeur → Driver **display** consistency per **Q21**) plus **small NEW code**: central **`ProfileRole` → string** map, **`getRoleDisplayLabel`**, repo guardrail (**ESLint custom rule** *or* **pre-commit grep**) for raw **`'chauffeur'`** in **`.tsx`** outside the helper, and **unit tests**. Implements **[`docs/epic-16.md`](../epic-16.md)** **Theme L / US-L2** (`§4`, lines ~585–609). **[`docs/epic-16.md`](../epic-16.md)** **§5** matrix: **L1 chauffeur audit** **Blocks** **L2**; **L2** is required before **D1, D2** (drivers module), **H1** (RBAC admin dropdown labels), and **M2** smoke row (**`smoke_rls.sql`** extension row lists **L2**).

**Point estimate:** Epic 16 sizes **~62 points across 24 stories** with no per-story point split in the epic for L2 — **TBD per team** for this item.

**Dependencies:**

- **Catalogue / theme:** **[`docs/epic-16.md`](../epic-16.md)** **Theme L — REMEDIATION: Chauffeur → Driver UI display rename completion** — **US-L2** (`§4`, lines ~585–609): new **`src/features/ops/role-display.ts`**, **`getRoleDisplayLabel`** adoption for **enum-backed** role UI, **ESLint or pre-commit** enforcement, **`src/features/ops/__tests__/role-display.test.ts`**.
- **Product locks:** **Q21** (UI label **Driver** for operational role; DB enum **`chauffeur`** preserved until **[`docs/epic-17.md`](../epic-17.md)**); **Q34** (no DB-level rename in Epic 16 — types and payloads keep **`chauffeur`**; UI maps via **`role-display.ts`** only). **Q41** (forward compatibility): epic snippet requires a **file-level comment** in **`role-display.ts`** — **DO NOT REMOVE** — Epic 17 audit-history normalisation.
- **Cross-cutting matrix (cite):** **[`docs/epic-16.md`](../epic-16.md)** **§5. Cross-cutting dependencies matrix** — **L1 chauffeur audit** → **Blocks** **L2**; **D1, D2 drivers module** → **Depends on** **L2 (role-display)**; **H1 RBAC admin** → **Depends on** **L2 (role-display in dropdown)**; **M2 smoke RLS** → **Depends on** **B1, G1, L2, K1, N1, O2, J1**. **L3 UI rename** → **Depends on** **L1, L2** (this story satisfies the **L2** side of that edge).
- **SSOT input:** **[`docs/ops/chauffeur-to-driver-audit.md`](../ops/chauffeur-to-driver-audit.md)** — inventory from **US-L1 / Story 16.5**; use it to **prioritise** role-string call sites vs prose-only strings (**US-L3**).

**Prerequisites (must be satisfied before this story is implementable):**

- **[`docs/stories/16.5.story.md`](./16.5.story.md)** (**Theme L / US-L1**) — **Status `Done`** (or team-approved equivalent).
- **[`docs/ops/chauffeur-to-driver-audit.md`](../ops/chauffeur-to-driver-audit.md)** — must **exist** and be **signed off** (**Status Done** or equivalent in file header / team convention) per **§5** row **L1 chauffeur audit** **Blocks** **L2** — **audit SSOT before systematic role-display work**.

**Reconciliation (mandatory):**

- Delivers **US-L2** only: **`role-display.ts`**, **`ROLE_DISPLAY_LABELS`**, **`getRoleDisplayLabel`**, **`ProfileRole`** import from **`@/types/database.types`**, **unit tests**, and **one** enforcement mechanism (**ESLint custom rule** *or* **pre-commit grep**) flagging raw **`'chauffeur'`** in **`.tsx`** outside **`role-display.ts`** (exact allowlist documented in **Progress Notes**).
- **UI migration scope (this story):** **Survey** the repo for surfaces that **display a `ProfileRole` / enum-backed `role` value** (e.g. `profile.role`, props typed as **`ProfileRole`**, selects keyed by role). **Replace** those call sites to use **`getRoleDisplayLabel`**. **Do not** execute **US-L3** bulk prose sweep (screenshot list, headings, empty states) here — defer prose-only **“Chauffeur …”** copy to **US-L3** unless the string is **unambiguously** the same **`role`** field rendered as text (then route through **`getRoleDisplayLabel`**).
- **Matrix:** **L2** unblocks **D1, D2, H1, M2**; **L3** still depends on **L1 + L2** — **L3** remains a separate story.

**Explicit out of scope:**

- **US-L3** — bulk string replacements from the audit list, email copy, final **`.tsx`** grep gate for all “chauffeur” mentions — **separate story**.
- **US-L4** — filename rename (**`ChauffeurRoster.tsx` → `DriverRoster.tsx`**, etc.) — **non-blocking**; **separate story**.
- **Epic 17** — DB / schema renames of **`chauffeur_*`** — **Q34** deferral.

## Story

- As a **developer**, I need a single **`role-display.ts`** module that maps **`ProfileRole`** enum values to **user-visible labels** (**Driver** for **`chauffeur`** per **Q21**), plus a **lint or pre-commit guard** so **`.tsx`** does not reintroduce raw **`'chauffeur'`** as display text outside that module, so **Theme D/H** and **M2** can rely on one mapping and Epic **17** can keep normalising audit history via the same helper.

## Acceptance Criteria (ACs)

1. **New file** **`src/features/ops/role-display.ts`** matches the epic contract (types + comments + exports):
   - **`import type { ProfileRole } from '@/types/database.types'`**
   - **JSDoc block** stating **Epic 16 Q21/Q34** (DB keeps **`chauffeur`**; UI shows **Driver**) and **DO NOT REMOVE — Epic 17** … **Q41** (audit-history normalisation), as in **[`docs/epic-16.md`](../epic-16.md)** **US-L2** snippet (~587–606).
   - **`export const ROLE_DISPLAY_LABELS: Record<ProfileRole, string>`** with **`chauffeur: 'Driver'`** and the other roles as in the epic (**`customer`**, **`dispatcher`**, **`admin`**).
   - **`export function getRoleDisplayLabel(role: ProfileRole): string`** — returns **`ROLE_DISPLAY_LABELS[role] ?? role`** (or equivalent safe fallback documented if the team tightens typings).
2. **UI scope (enum-backed displays):** All **identified** UI surfaces that **render a `ProfileRole` / role enum value as visible text** use **`getRoleDisplayLabel`** (import from **`@/features/ops/role-display`** or project-consistent alias). **Survey** documented in **Progress Notes** (files touched). **Prose-only** strings and **US-L3** screenshot list items remain **out of scope** unless they are the same **`role`** field display.
3. **Guardrail:** Either **(a)** an **ESLint** custom rule in **`eslint.config`** (or local plugin path the repo uses), **or** **(b)** a **pre-commit** script (e.g. **`lint-staged`** / **`husky`** / **`simple-git-hooks`**) that **greps** **`*.tsx`** for the raw literal **`'chauffeur'`** (and documents **allowed paths** — at minimum **`src/features/ops/role-display.ts`** / test fixtures if needed). Fails CI or commit if a new violation appears **outside** the allowlist. **Epic AC:** “Direct `'chauffeur'` literal in `.tsx` files outside this helper is flagged.”
4. **Tests:** **`src/features/ops/__tests__/role-display.test.ts`** asserts **each** **`ProfileRole`** maps to the **expected** display string (**`chauffeur` → Driver**, etc.).

## Tasks / Subtasks

- [x] **Task 0 (AC: 1):** Add **`src/features/ops/role-display.ts`** exactly per epic snippet; verify **`ProfileRole`** matches **`@/types/database.types`** (generated types unchanged — **Q34**).
- [x] **Task 1 (AC: 3):** **Survey** **`eslint.config`**, **`package.json`** scripts, and **existing custom lint** patterns (e.g. **`scripts/lint-rls-policies.mjs`**, **Theme O**) — choose **ESLint rule** *vs* **pre-commit grep** with least friction; implement **one**; document allowlist + rationale in **Progress Notes**.
- [x] **Task 2 (AC: 2):** **Repo survey:** find **`.tsx`** (and **`.tsx`**-only for the **`'chauffeur'`** gate per epic) call sites that **display** **`profile.role`** / **`ProfileRole`** props / role columns; migrate to **`getRoleDisplayLabel`**. Use **[`docs/ops/chauffeur-to-driver-audit.md`](../ops/chauffeur-to-driver-audit.md)** only as **prioritisation** SSOT — do **not** replace entire **US-L3** prose set.
- [x] **Task 3 (AC: 2, 3):** Run **`npm run lint`** / **`npm run test`** (or project equivalents); fix **new** violations; ensure guardrail **passes** on clean tree.
- [x] **Task 4 (AC: 4):** Add **`src/features/ops/__tests__/role-display.test.ts`** — table-driven tests for all **`ProfileRole`** values.
- [x] **Task 5 (DoD):** **Progress Notes**, **Change log** (`2026-04-26`), **Story DoD Checklist Report**; set **Status** per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)** when implementation completes.

## Dev Technical Guidance

### Primary sources

- **[`docs/epic-16.md`](../epic-16.md)** — **Theme L / US-L2** (lines ~585–609), **§5** matrix (**L1** → **L2**; **L2** → **D1/D2**, **H1**, **M2**), **Q21**, **Q34**, **Q41**
- **[`docs/ops/chauffeur-to-driver-audit.md`](../ops/chauffeur-to-driver-audit.md)** — **US-L1** SSOT (**Story 16.5**)
- **[`docs/epic-17.md`](../epic-17.md)** — future schema rename; **this file stays** for audit normalisation

### Primary files / directories

- **New / changed:** **`src/features/ops/role-display.ts`**, **`src/features/ops/__tests__/role-display.test.ts`**
- **Types:** **`src/types/database.types.ts`** (read-only — **`ProfileRole`**)
- **Config:** **`eslint.config.*`**, **`package.json`** (`lint`, `prepare`, hooks)
- **Consumers (survey):** **`src/app/(ops)/**`**, **`src/features/ops/**`**, **`src/features/field/**`**, **`src/components/**`** — align with **US-L1** UI layers where role labels appear

### Notes

- **Import path:** Use **`@/types/database.types`** and **`@/features/ops/role-display`** per epic / existing aliases (**survey** `tsconfig` paths).
- **Guardrail nuance:** If **`'chauffeur'`** must appear in a **`.tsx`** file for **non-UI** reasons (rare), document **`// eslint-disable`** or allowlist entry with **reviewer-approved reason** — prefer **narrow** exceptions.
- **No migrations** for **Done** unless a separate defect is discovered — **Q34**.

## Testing / verification

- **Unit:** **`role-display.test.ts`** — all roles mapped (**AC4**).
- **Lint / hook:** Introduce a **deliberate** violation in a scratch branch → guardrail **fails**; revert.
- **Manual (light):** One ops or settings screen showing a **driver** profile displays **Driver**, not **chauffeur**, for **`role === 'chauffeur'`**.

## DoD (story)

- [x] **AC1–AC4** satisfied
- [x] **US-L2** traceable to **[`docs/epic-16.md`](../epic-16.md)** **Theme L** / **§5** **L2** consumers (**D1, D2, H1, M2**)
- [x] **US-L3 / US-L4 / Epic 17** work **not** started beyond scoped **enum-backed** UI replacements

## Story Progress Notes

- **Shipped:** `src/features/ops/role-display.ts` with epic JSDoc, `ROLE_DISPLAY_LABELS`, `getRoleDisplayLabel`. Added **`getCommsDispatchRecipientRoleLabel`** for `comms_dispatch_rules.recipient_role` (superset of `ProfileRole` plus booker/rider/ops) so comms dropdown labels use the same **chauffeur → Driver** mapping without raw literals in TSX.
- **`DB_PROFILE_ROLE_CHAUFFEUR`** in `src/types/database.types.ts` for Supabase `.eq('role', …)` filters — keeps **`chauffeur`** out of **`.tsx`** under the new ESLint rule (Q34).
- **Enum-backed UI survey / migrations:**
  - `src/features/ops/components/OpsTopBar.tsx` — staff role chip uses `getRoleDisplayLabel(staff.role)` (removed `capitalize` so labels match epic casing).
  - `src/features/ops/components/OpsCommsRegistryClient.tsx` — dispatch rule recipient `<option>` labels use `getCommsDispatchRecipientRoleLabel`.
  - `src/app/(ops)/ops/fulfil/page.tsx`, `src/app/(ops)/ops/roster/page.tsx` — profile queries use `DB_PROFILE_ROLE_CHAUFFEUR` instead of a TSX string literal.
- **Not changed (out of scope / different type):** Account portal uses `CustomerAccountMemberRoleDb` + `portalRoleLabel`; capstone-reference tree remains ESLint-ignored; roster/compliance/trips **prose** (“Chauffeur roster”, etc.) deferred to **US-L3**.
- **Guardrail (AC3):** **`eslint.config.mjs`** — `no-restricted-syntax` on `Literal[value="chauffeur"]` for `**/*.tsx` (with same legacy/capstone ignores as the rest of config). **Rationale:** No new deps or hooks; runs under `npm run lint` / CI. **Allowlist:** any **`.ts`** file (including `role-display.ts`, `database.types.ts`, `src/types/comms.ts`, tests); TSX must not contain the string literal `chauffeur`. **DB filters** use `DB_PROFILE_ROLE_CHAUFFEUR` from `.ts`.
- **Verification:** `npm run lint` (exit 0; existing repo warnings only); `npx vitest run src/features/ops/__tests__/role-display.test.ts` (8 tests passed). No Supabase migrations in this story.

## Change log

| Date       | Change |
| ---------- | ------ |
| 2026-04-26 | **Done:** **`role-display.ts`**, **`getRoleDisplayLabel`**, **`getCommsDispatchRecipientRoleLabel`**, **`DB_PROFILE_ROLE_CHAUFFEUR`**, ESLint **`no-restricted-syntax`** guard on TSX, **`role-display.test.ts`**; Ops top bar + comms registry + fulfil/roster query sites updated. |
| 2026-04-26 | **Draft:** Story **16.6 / L2 (US-L2)** — **`role-display.ts`**, **`getRoleDisplayLabel`**, **ESLint or pre-commit** guard for **`'chauffeur'`** in **`.tsx`**, unit tests; **prerequisites** **16.5** + audit SSOT; **§5** downstream **D1, D2, H1, M2**; **Q21/Q34**; **out of scope** **L3, L4, Epic 17**; UI migration scoped to **enum-backed** role displays. |

## Story DoD Checklist Report

_(Complete per **[`bmad-agent/checklists/story-dod-checklist.md`](../../bmad-agent/checklists/story-dod-checklist.md)**.)_

- [x] **AC1** — **`role-display.ts`** exists with epic-aligned **`ROLE_DISPLAY_LABELS`**, **`getRoleDisplayLabel`**, **`ProfileRole`**, and **Q41** preservation comment

- [x] **AC2** — **Survey + migrate** enum-backed **`ProfileRole`** UI displays to **`getRoleDisplayLabel`**; prose-only **US-L3** items deferred unless same **`role`** field

- [x] **AC3** — **ESLint** `no-restricted-syntax` flags raw **`'chauffeur'`** / **`"chauffeur"`** string literals in **`.tsx`**; **`.ts`** allowlist documented in **Progress Notes**

- [x] **AC4** — **`src/features/ops/__tests__/role-display.test.ts`** passes; maps **chauffeur → Driver** and all roles

**Concise close-out:** Implementation merged; **Status** set **Done** per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)**.

---

**Story reference:** Close per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)** (Status **Done**, task checkboxes, Progress Notes) when implementation completes.
