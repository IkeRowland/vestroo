# Story 15.32: Documentation of algorithm weights and tuning process (Epic 15 / 15D.6)

## Status: Done

**Dependencies:**

- **Catalogue:** **[`docs/epic-15.md`](../epic-15.md)** **Theme E — `15D.6`**: *Documentation of algorithm weights and tuning process* — **15D** — **2** pts; table **Depends: `15D.1`** (per **[`docs/epic-15.md`](../epic-15.md)** Theme E story catalogue).
- **Epic cross-cutting (risk + DoD):** **[`docs/epic-15.md`](../epic-15.md)** **§6** — risk row *“Algorithm weights tuned by someone with incomplete context”* — mitigation: weights in **source control** (**Q26**); **PR review**; **documentation story (`15D.6`)** captures the **calibration process** and **data sources**. **§7 DoD** — *“Docs: Sub-epic README or epic addendum lists flags and rollback”* — **this story** satisfies the **15D** slice: dispatch suggestions **feature flag** name and **rollback** (disable suggestions in prod **without** code revert).
- **Theme D — US-D2 / Q26:** Deterministic formula; weights in **`src/lib/dispatch-suggestions-config.ts`** (not a DB table). **Q25** — suggestions remain **advisory**; **no auto-assign**.

**Prerequisites (must be satisfied before this story is implementable):**

- **[`docs/stories/15.27.story.md`](./15.27.story.md)** (**`15D.1`**) — **`dispatch-suggestions`** module, **`dispatch-suggestions-config.ts`**, locked **0.4 / 0.2 / 0.2 / 0.2** weight mapping and sub-signal semantics (**catalogue** lists **`15D.1`** as the **only** blocking dependency for **`15D.6`**).

**Recommended sequencing (not blocking per catalogue — link for accurate names/paths after ship):**

- **[`docs/stories/15.28.story.md`](./15.28.story.md)** (**`15D.2`**) — **`VehicleSuggestionsPanel`**, Fulfil assign, **Q25** prefill-only; **feature flag** choice (**survey** story + **`.env.example`**).
- **[`docs/stories/15.29.story.md`](./15.29.story.md)** (**`15D.3`**) — **`ops_audit_log`** **`assignment_from_suggestion`** / **`assignment_free_pick`** and payload fields for calibration.
- **[`docs/stories/15.30.story.md`](./15.30.story.md)** (**`15D.4`**) — **`/ops/reports/suggestions`** staff calibration report (rolling window, aggregates).
- **[`docs/stories/15.31.story.md`](./15.31.story.md)** (**`15D.5`**) — E2E spec naming and env gates (**`DISPATCH_SUGGESTIONS_ENABLED`**, **`E2E_15D5_*`** pattern in **`.env.test.example`**).

**Theme E — catalogue `15D.6`:** **“Documentation of algorithm weights and tuning process”** — **2** pts, depends **`15D.1`**.

**Reconciliation (mandatory):** Delivers the **standalone** **15D** operations / engineering doc promised in **§6** mitigation and **`15.27`** header pointer to **`15D.6`**. Aligns **§7** “Docs … flags and rollback” for **dispatch intelligence** without duplicating full **`15D.4`** report spec or **`15D.3`** migration detail — **link** to **[`docs/stories/15.30.story.md`](./15.30.story.md)** / report route and **`ops_audit_log`** as the **source of truth** for SQL shape. **Cross-links** **`15D.2`–`15D.5`** in the doc body are **optional** beyond what ACs require.

**Explicit out of scope (defer to other stories or non-goals):**

- **Changing** weight **values** or **algorithm** implementation in **`src/lib/dispatch-suggestions.ts`** / **`dispatch-suggestions-config.ts`** — **separate PR**; **not** this story unless fixing **documentation-only** typos or broken links.
- **`15D.1`–`15D.5`** **implementation** work (assumed **already shipped** or tracked in their own stories).
- **New** runtime admin UI or **DB**-stored weights (**forbidden** by **Q26** — doc must state **PR-only** changes).

## Story

- As **engineering / ops leadership**, we need a **single markdown document** under **`docs/`** that explains **why** dispatch suggestions exist (**Q25**), the **locked** weighted formula (**Q26** / **US-D2**), **where** weights live, **how** to calibrate using the **report** (**`15D.4`**) and **audit** discrimination (**`15D.3`**), which **feature flag** gates the feature, **how** to **rollback** in production, and **where** tests live — so that **weight changes** and **pilots** happen with **full context**, **PR review**, and **traceability** to **epic** intent.

## Acceptance Criteria (ACs)

1. **Task 0 — Doc path survey:** Before writing the doc, **survey** `docs/*.md` (and `docs/**/*.md` if present) for existing **`dispatch-intelligence`**, **`15D`**, or **`docs/ops/`** documentation patterns. **Choose exactly one** new doc path: **prefer** **`docs/epic-15-15d-dispatch-intelligence.md`** **unless** **`docs/dispatch-intelligence.md`** already exists **or** an established **`docs/ops/*`** pattern applies — in those cases, use the existing convention and **avoid** duplicate topical files. **Record** the **chosen path** and **survey notes** in **Progress Notes**.

2. **New markdown file:** Add **one** new **`docs/*.md`** file at the path from **AC1**. **No** application code changes required for story completion (links may point to **`src/`** paths as text).

3. **Section (1) — Purpose / Q25:** State that suggestions are **advisory** and **dismissable**; dispatcher **must** still use **Assign**; **no auto-assign** (**Q25** / **US-D1**). Briefly tie to **§6** bias risk — suggestions nudge but **free-pick** remains valid.

4. **Section (2) — Locked formula:** Document the **weighted** formula **0.4 × capacity fit + 0.2 × schedule gap + 0.2 × chauffeur familiarity + 0.2 × cost tier alignment** with each sub-signal **normalised 0–100** before weighting (consistent with **[`docs/epic-15.md`](../epic-15.md)** **Q26** and **`15.27`**). **Survey** **`src/lib/dispatch-suggestions-config.ts`** for **exported constant names** and align prose (do not invent alternate names).

5. **Section (3) — Where weights live / how to change:** State that weights live in **`src/lib/dispatch-suggestions-config.ts`**; changes require a **PR** and **code review**; **no** DB or runtime admin tuning (**Q26**).

6. **Section (4) — Calibration process:** Explain **how** staff use **`/ops/reports/suggestions`** (**`15D.4`** / **[`15.30`](./15.30.story.md)**) for rolling-window aggregates. Explain **`ops_audit_log`** actions **`assignment_from_suggestion`** vs **`assignment_free_pick`** (**`15D.3`** / **[`15.29`](./15.29.story.md)**) as the **discrimination** signal for “accepted suggestion” vs “free pick”. Include **example SQL snippets** *or* a **single** clear **link** to the report page path only — **do not** duplicate the full **`15D.4`** functional spec.

7. **Section (5) — Feature flag + rollback:** Document the **primary** env flag gating dispatch suggestions — **survey** **[`docs/stories/15.28.story.md`](./15.28.story.md)** and **[`.env.example`](../../.env.example)** for the **canonical** variable name(s) (**e.g.** server-side **`DISPATCH_SUGGESTIONS_ENABLED`** vs any **`NEXT_PUBLIC_*`** note). Describe **rollback**: set flag **off** / unset in **production** so suggestions **hide** and assign remains **free-pick**-only (**§7** “config change, not a code revert”).

8. **Section (6) — Testing pointers:** Link **`src/lib/__tests__/dispatch-suggestions.test.ts`** (unit). Reference **E2E** pattern **`tests/e2e/epic15-15d5-dispatch-suggestions-e2e.spec.ts`** (**[`15.31`](./15.31.story.md)**) as **regression** coverage for suggestions + audit + report smoke — **filename** may be adjusted if repo survey differs; **Progress Notes** must match reality.

9. **Section (7) — Pilot / retro:** One **short** paragraph per **[`docs/epic-15.md`](../epic-15.md)** **§7** **Pilot** bullet: e.g. validate **15D** with **1–2** pilot accounts/trips, capture **feedback** in retro / changelog habit — **no** new process outside epic intent.

10. **Epic traceability + link hygiene:** Doc **footer** or **“See also”** must link **[`docs/epic-15.md`](../epic-15.md)** (Theme **D/E**, **Q25–Q27**, **§6–§8** as relevant). Optional inline links to **`15.28`–`15.31`** story files for UI, audit, report, E2E context. All **relative** links from the new doc to **`docs/`** stories and **`docs/epic-15.md`** must resolve — verify in **Progress Notes** after add.

## Tasks / Subtasks

- [x] **Task 0 (AC: 1, 10):** Survey `docs/` for naming conflicts; **choose** final markdown path; note in **Progress Notes**; verify links after doc add (**AC10**).

- [x] **Task 1 (AC: 2–10):** Author **`docs/epic-15-15d-dispatch-intelligence.md`** (or path from Task 0) with **all seven** numbered content sections from AC3–AC9 plus **See also** / epic traceability per **AC10**.

- [x] **Task 2 (AC: 5, 7, 8):** Grep / read **`.env.example`**, **`15.28`**, **`src/lib/dispatch-suggestions-env.ts`** (or successor) so flag name(s) and rollback steps match **shipped** behaviour.

- [x] **Task 3 (AC: 4, 6):** Read **`dispatch-suggestions-config.ts`** and **`15.30` / `15.29`** stories (or code pointers) so constant names and audit/report references are accurate.

- [x] **Task 4 (AC: 10, Testing, DoD):** Self-review markdown; **Progress Notes** (path, flag survey, link check); **change log** row **2026-04-26**; **Story DoD Checklist Report**; update **Status** per story-completion rule when implemented.

## Dev Technical Guidance

### Primary sources

- **[`docs/epic-15.md`](../epic-15.md)** — **Theme D** **US-D1–US-D2**, **Q25–Q27**, **§6** (weights risk), **§7** (docs / flags / rollback / pilot), **§8** **15D** paths.
- **[`docs/stories/15.27.story.md`](./15.27.story.md)** — formula, config module, tests file path.
- **[`docs/stories/15.28.story.md`](./15.28.story.md)** — feature flag survey.
- **[`docs/stories/15.29.story.md`](./15.29.story.md)** — audit actions / payload intent.
- **[`docs/stories/15.30.story.md`](./15.30.story.md)** — **`/ops/reports/suggestions`** behaviour (link, do not copy full AC list).
- **[`docs/stories/15.31.story.md`](./15.31.story.md)** — E2E file name and env patterns.

### Likely reference paths (read-only for accuracy)

- **`src/lib/dispatch-suggestions-config.ts`**, **`src/lib/dispatch-suggestions.ts`**, **`src/lib/dispatch-suggestions-env.ts`**
- **`src/lib/__tests__/dispatch-suggestions.test.ts`**
- **`tests/e2e/epic15-15d5-dispatch-suggestions-e2e.spec.ts`** (confirm exists)
- **[`.env.example`](../../.env.example)** — **`DISPATCH_SUGGESTIONS_ENABLED`** (and any related comments)

### Implementation notes

- **Doc-only:** No changes to weight constants or scoring logic in this story.
- **Tone:** Operational — readable by **staff** and **engineers**; avoid duplicating entire epic text.

## Testing / verification

- **Manual:** Open the new **`docs/...md`** in the IDE; click every **relative** link to **`docs/epic-15.md`** and **`docs/stories/15.2*.md`** targets.
- **Optional:** `npm run lint` only if touching linted files (**not** expected for pure doc add).

## DoD (story)

- [x] **AC1–AC10** satisfied.

- [x] **`15D.6`** traceable to **[`docs/epic-15.md`](../epic-15.md)** catalogue and **§6** / **§7** doc intent.

- [x] **No** algorithm or weight **code** changes (unless doc-only typo fix explicitly noted in **Progress Notes**).

## Story Progress Notes

- **AC1 path:** **`docs/epic-15-15d-dispatch-intelligence.md`** (confirmed: no **`docs/dispatch-intelligence.md`**, no competing **`docs/ops/*`** dispatch doc in survey).
- **Shipped:** Single markdown addendum with **§1–§7** per AC3–AC9 plus **See also** (**AC10**): Q25/Q26, **`WEIGHT_CAPACITY` / `WEIGHT_SCHEDULE` / `WEIGHT_CHAUFFEUR` / `WEIGHT_COST`**, PR-only weight changes, **`/ops/reports/suggestions`** + **`ops_audit_log`** actions + example SQL, **`DISPATCH_SUGGESTIONS_ENABLED`** from **`.env.example`** + **`dispatch-suggestions-env.ts`** (no **`NEXT_PUBLIC_*`**), rollback via env off + restart, unit/E2E paths, pilot paragraph from epic **§7**.
- **Link check (relative from new doc):** `epic-15.md`, `stories/15.27–15.31.story.md`, `../src/lib/dispatch-suggestions-config.ts`, `../src/lib/dispatch-suggestions.ts`, `../src/lib/dispatch-suggestions-env.ts`, `../.env.example`, `../src/lib/__tests__/dispatch-suggestions.test.ts`, `../tests/e2e/epic15-15d5-dispatch-suggestions-e2e.spec.ts` — paths intentional for IDE navigation from **`docs/`**.
- **Lint:** Not run (doc-only story; no **`src/`** edits).

## Change log

| Date       | Change |
| ---------- | ------ |
| 2026-04-26 | **Draft:** Story **`15D.6`** — algorithm weights + tuning process **documentation**; **depends** **`15D.1` (`15.27`)**; **2** pts; recommended reads **`15.28`–`15.31`**. |
| 2026-04-26 | **Done:** Added **`docs/epic-15-15d-dispatch-intelligence.md`**; no application code changes. |

## Story DoD Checklist Report

- [x] **AC1** — Doc path survey; **Progress Notes** record choice.

- [x] **AC2** — New **`docs/*.md`** added.

- [x] **AC3** — Purpose / **Q25** / no auto-assign.

- [x] **AC4** — Locked **0.4/0.2/0.2/0.2** formula + sub-signals; config constant names surveyed.

- [x] **AC5** — Weights file path; **PR-only**; **no** DB tuning.

- [x] **AC6** — Calibration: report + audit; SQL **or** link; no full **`15D.4`** duplicate.

- [x] **AC7** — Flag name(s) + rollback.

- [x] **AC8** — Unit + E2E test pointers.

- [x] **AC9** — Pilot / retro short paragraph.

- [x] **AC10** — Epic **See also** / traceability; relative links verified.

**Concise close-out (when done):** Single new doc at agreed path; **no** code changes unless typo; update **Status** per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)**.

---

**Story reference:** Close per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)** (Status **Done**, task checkboxes, Progress Notes) when implementation completes.
