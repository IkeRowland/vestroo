# Story 19.13: Migration / cleanup (Epic 19)

## Status: Done

**Story provenance:** **[`docs/epic-19.md`](../epic-19.md)** — **Suggested child stories** item **13** (lines **266–267**): *19.13 — Migration / cleanup — remove superseded **single-page-only** code paths if any landed; **tests**; **cross-links** in **Epic 1** / **Epic 10**.* **Backward compatibility** (lines **281–285**): **`submitTripRequest`** + payload schema **unchanged**; booking refs + email templates **unchanged**; feature-flagged rollout (**FE.19.10**) may keep a **legacy variant** during A/B — **cleanup must not** break the **legacy flag path** until **PO** retires it.

**Point estimate:** TBD

**Critical numbering (avoid confusion):**

| Label | Meaning |
|-------|--------|
| **Story 19.13** | This repo story — **dead-code / duplicate-path removal**, **test hygiene**, **minimal** pointers in **[`docs/epic-1.md`](../epic-1.md)** / **[`docs/epic-10.md`](../epic-10.md)** to the **four-slide** model and **[Epic 19](../epic-19.md)**. |
| **FE.19.10** | **Variant + analytics** — **preserve** env semantics, event names, and **`getBookingFunnelVariant()`** contract; **do not** remove A/B hooks while rollout is active. |
| **Story 19.14** | **Wholesale documentation** (`ui-ux-specification.md`, **`design/booking-flow-simplified.md`** full alignment, narrative rewrites) — **explicitly out of scope** here per epic **References** (line **289**). |

**Dependencies:**

- **[`docs/epic-19.md`](../epic-19.md)** — **Suggested child 13** (lines **266–267**), **Backward compatibility** (lines **281–285**), **Relationship to other epics** (lines **269–275**).
- **[`docs/stories/19.1.story.md`](19.1.story.md)** through **[`docs/stories/19.12.story.md`](19.12.story.md)** — **Prerequisite:** four-slide shell through confirmation — **Status: Done** expected before implementation.

**Blocks (downstream):**

- **Story 19.14** — full doc alignment may assume **19.13** removed obsolete code/comments and updated **epic-1** / **epic-10** pointers.

## Reconciliation (mandatory)

| Issue | Resolution |
|-------|------------|
| **`submitTripRequest` / payload** | **No** changes to action contract, **Zod** schemas, or server insert semantics — **[`docs/epic-19.md`](../epic-19.md)** backward-compat block. |
| **FE.19.10 / legacy variant** | **`getBookingFunnelVariant()`** and **`funnelAnalyticsBase`** in **`TripRequestBookingShell`** must keep working for **any** PO-approved variant string (e.g. epic examples `v_legacy_slides` vs `v_booking_slides_v2`). Today the **UI** is a **single** four-slide implementation; **variant** is **analytics-only** — do **not** delete env handling or break **`booking_funnel_*`** payloads “because there is no second UI”. |
| **Embedded vs standalone** | **`BookingSearchForm`** (**embedded** + prefill) and **`/book/trip-request`** (**standalone**) are **two supported entry paths** — **not** “duplicate funnels” to collapse without PO/architect sign-off. |
| **Docs scope** | **In scope:** **short** cross-links in **`epic-1.md`** / **`epic-10.md`** (pointer to **Epic 19** + four-slide trip-request). **Out of scope:** **`19.14`** files (`ui-ux-specification.md`, full **`booking-flow-simplified.md`**, **`docs/index.md`**, historical story files, **`docs/trip-request-hardening-10.6.md`**) unless PO expands **19.13**. |

## Story

- As **engineering**, I need **obsolete trip-request code paths and props removed** where safe, so maintenance cost drops and future work does not fork on dead branches.
- As **engineering**, I need **tests and comments aligned** with the **four-slide** shell (**`funnelStep`** **0–2** + confirmation / **`slide_index` 1–4**), so CI and onboarding reflect reality.
- As **product/docs readers**, I need **Epic 1 and Epic 10** to **point at Epic 19** for the **current** public trip-request step model, so dependency tables stay navigable without waiting for **19.14**.

## Acceptance Criteria (ACs)

1. **Discovery-led cleanup:** Run **mandatory discovery greps** (see below); remove or consolidate **confirmed dead** code (unused exports, duplicate helpers, obsolete comments) **without** changing **`submitTripRequest`** behaviour or **booking ref** / email contract.
2. **FE.19.10 preserved:** **`src/lib/booking-funnel-analytics.ts`**, **`.env.example`** **`NEXT_PUBLIC_BOOKING_FUNNEL_*`** comments, and **`TripRequestBookingShell`** instrumentation blocks remain **functional**; **variant** string continues to flow to **`booking_funnel_*`** events.
3. **Entry paths intact:** **`BookingSearchForm`** embed and **`src/app/(app)/book/trip-request/page.tsx`** standalone both still render **`TripRequestBookingShell`** correctly (**`embedded`** / prefill / **`onExit`** semantics unchanged unless PO approves a narrow fix).
4. **Tests:** **`npm run test -- --run`** and **`npm run lint`** green; remove **skipped** / **duplicate** trip-request tests only when superseded; **no** reduction in coverage for **submit success/error**, **slide analytics**, **org/PO**, **vehicle gate**, or **confirmation** without PO approval.
5. **Minimal doc cross-links:** Add **1–2 sentences** (or a **single table row**) in **[`docs/epic-1.md`](../epic-1.md)** and **[`docs/epic-10.md`](../epic-10.md)** linking **Epic 19** and the **four-slide** trip-request funnel — **no** full narrative rewrite (**19.14**).

## Tasks / Subtasks

- [x] **Task 0 (AC: 1, 2, Reconciliation):** Re-run **mandatory discovery** greps; record deltas in **Progress Notes**; confirm with **PO** whether any **planned** second UI branch exists for **`NEXT_PUBLIC_BOOKING_FUNNEL_VARIANT`** before deleting anything tied to **variant**.
- [x] **Task 1 (AC: 1, 3):** Remove **dead** exports / unused deprecated wrappers (e.g. **`consumeTripRequestPrefill`** — grep shows **no** `src/` call sites outside **`trip-request-prefill.ts`**); align **obsolete** comments that still describe **two-step** / **`STEP_COUNT`**-only shells **in touched production files** only — defer broad historical story/doc edits to **19.14** unless trivially in-file.
- [x] **Task 2 (AC: 1, 4):** Test suite hygiene — dedupe overlapping **`TripRequestBookingShell`** tests if any are **fully redundant**; ensure **FE.19.10** and **FE.19.12** files remain asserted; update test descriptions that reference **pre-19.1** **`STEP_COUNT = 2`** if those tests are edited.
- [x] **Task 3 (AC: 5):** **`docs/epic-1.md`** + **`docs/epic-10.md`** — minimal **Epic 19** / **four-slide** cross-links (match tone of existing “Relationship” tables).
- [x] **Task 4 (AC: 1–5):** Final **`npm run test -- --run`** + **`npm run lint`**; short **Progress Notes** summary of removed symbols and **epic** link locations.

## Dev Technical Guidance

### Primary sources

- **[`docs/epic-19.md`](../epic-19.md)** — **Suggested child 13**, **Backward compatibility**, **FE.19.10**.
- **[`docs/stories/19.10.story.md`](19.10.story.md)** — instrumentation boundaries.
- **[`src/features/booking/components/TripRequestBookingShell.tsx`](../../src/features/booking/components/TripRequestBookingShell.tsx)** — **`funnelStep`**, **`showConfirmation`**, analytics base.

### Mandatory discovery (grep-verified — run again at implementation; line anchors approximate)

| Pattern / symbol | Finding (2026-05-01 survey) |
|------------------|-----------------------------|
| **`STEP_COUNT`** in **`TripRequestBookingShell.tsx`** | **No matches** — shell uses **`funnelStep`** **0\|1\|2** + **`showConfirmation`** (**FE.19.1** model). **Stale** **`STEP_COUNT`** narrative remains in **`docs/stories/19.1.story.md`**, **`docs/stories/10.6.story.md`**, **`docs/trip-request-hardening-10.6.md`** → **defer** to **19.14** unless **19.13** scope expanded. |
| **`NEXT_PUBLIC_BOOKING_FUNNEL_*`** | **[`.env.example`](../../.env.example)** ~**131–136**; **`src/lib/booking-funnel-analytics.ts`** — **`isBookingFunnelAnalyticsEnabled`**, **`getBookingFunnelVariant`** (default **`v_booking_slides_v2`**). |
| **`getBookingFunnelVariant` / `funnelAnalyticsBase`** | **`TripRequestBookingShell.tsx`** ~**52**, **~220** — **variant** passed to analytics; **no** separate **legacy UI** branch found in shell (**analytics-only** variant today). |
| **`FE.19.10` comment anchor** | **`TripRequestBookingShell.tsx`** ~**214** (`performance.now()` anchor). |
| **`consumeTripRequestPrefill`** | **Removed (19.13 close-out):** was deprecated alias in **`trip-request-prefill.ts`**; **no** `src/` imports outside that file — superseded by **`getTripRequestPrefillForBootstrap`**. |
| **`rideDateTimeIsInPast`** | **`ride-details-validate.ts`** ~**122–127** — comment “**Legacy name**” (misleading name vs **60-minute** rule); **still used** by **`BookingSearchForm.tsx`** and tests → **rename only** if included as explicit subtask + wide call-site update (**optional**, higher churn). |
| **`TripRequestBookingShell` usage** | **`BookingSearchForm.tsx`** ~**973+** (embedded); **`src/app/(app)/book/trip-request/page.tsx`** (standalone). |
| **`BookingSearchForm` vs shell** | Single shell component; marketing/search card **opens** embedded funnel — **not** dead duplication. |

### Suggested grep commands (implementation)

```bash
rg "STEP_COUNT|funnelStep|showConfirmation|NEXT_PUBLIC_BOOKING_FUNNEL|getBookingFunnelVariant|consumeTripRequestPrefill|deprecated|FE\\.19\\.10" src/features/booking src/lib/booking-funnel-analytics.ts src/app -g"*.{ts,tsx}"
rg "TripRequestBookingShell|BookingSearchForm" src -g"*.{ts,tsx}"
```

### Files to touch (expected — adjust after Task 0)

| Path | Role |
|------|------|
| **`src/features/booking/components/trip-request/trip-request-prefill.ts`** | **`consumeTripRequestPrefill`** removed in **19.13** |
| **`src/features/booking/components/__tests__/*.tsx`** | Test hygiene |
| **`docs/epic-1.md`**, **`docs/epic-10.md`** | Minimal **Epic 19** / four-slide pointers |

### Not in this story

- **Story 19.14** wholesale updates (**`ui-ux-specification.md`**, full **`design/booking-flow-simplified.md`**, **`docs/index.md`**, exhaustive historical story edits).
- **Changing** **`submitTripRequest`**, booking reference generation, or **email** templates.
- **Removing** **FE.19.10** env keys or **analytics** hooks before **PO** retires A/B.
- **Collapsing** embedded vs standalone **routes** into one entry without architecture approval.

## Testing / verification

- **`npm run test -- --run`** and **`npm run lint`**.
- **Spot-check:** embedded path (marketing **`BookingSearchForm`**) and **`/book/trip-request`** — **four slides** + confirmation; **analytics** test sink still receives **variant** on **`booking_funnel_view`** when tests enable the sink.

## Story Progress Notes

**Task 0 — Variant / PO:** **`NEXT_PUBLIC_BOOKING_FUNNEL_VARIANT`** remains **analytics-only**: one **four-slide** UI in **`TripRequestBookingShell`**; **no** second UI branch to delete; **keep** **`.env.example`**, **`booking-funnel-analytics.ts`**, and shell **`useEffect`/handlers** per FE.19.10. No separate PO thread — epic reconciliation already records “no second UI” until PO retires A/B.

**Task 0 — Discovery delta (2026-05-01 implementation pass):** Re-grepped `src/features/booking`, `src/lib/booking-funnel-analytics.ts`, `src/app` for `STEP_COUNT|funnelStep|showConfirmation|NEXT_PUBLIC_BOOKING_FUNNEL|getBookingFunnelVariant|consumeTripRequestPrefill|FE.19.10` — **unchanged** from story survey except **`consumeTripRequestPrefill`** existed only in **`trip-request-prefill.ts`** (now removed). **`STEP_COUNT`**: **no** matches in `*.ts` / `*.tsx` under repo (implementation). **Two-step / `STEP_COUNT` in touched production files:** none found in **`trip-request-prefill.ts`**; no other production files edited for comments-only.

**Task 1:** Removed **`consumeTripRequestPrefill`** export from **`src/features/booking/components/trip-request/trip-request-prefill.ts`**. Fixed accidental tab/space mix in **`getTripRequestPrefillForBootstrap`** `catch` block while editing that file.

**Task 2:** **No** merge of **`TripRequestBookingShell*.test.tsx`** files — **funnel-analytics** vs **confirmation** vs **org-inline** vs **vehicle-gate** remain **distinct** concerns; overlap is intentional (different assertions). **No** test files contained **`STEP_COUNT = 2`** strings.

**Task 3:** **`docs/epic-1.md`** — new paragraph under **Description** pointing to **[`docs/epic-19.md`](../epic-19.md)** (four-slide trip → vehicle → passenger → confirmation). **`docs/epic-10.md`** — new paragraph after Epic 1 relationship table linking **Epic 19** (four-slide shell, **FE.10.2–FE.10.4** + **FE.19.12** confirmation, **FE.19.10** instrumentation).

**Commands:** `npm run test -- --run` — **167** files, **843** tests, **pass**. `npm run lint` — **exit 0** (existing project **warnings** only; no new errors from this story).

**Agent:** Dev subagent (Composer).

## DoD (story)

- [x] **AC1–AC5** satisfied.
- [x] **Lint / test** green.
- [x] **`submitTripRequest`** contract and **FE.19.10** paths verified unchanged (or **explicitly** approved deltas documented).
- [x] **Story file** — **Status** and **Progress Notes** updated when implemented (**Dev** agent).

## Story DoD Checklist Report (docs/checklists/story-dod-checklist.txt)

| Area | Status | Notes |
|------|--------|-------|
| TypeScript strict / no `any` | **Done** | No type widenings. |
| Naming / .cursorrules | **Done** | Matched existing file style. |
| Tests pass | **Done** | `npm run test -- --run` |
| New business logic + JSDoc | **N/A** | Removal + doc pointers only. |
| README / API / schema docs | **N/A** for broad rewrites | **19.14** |
| Security / RLS | **N/A** | |
| Responsive / UX | **Done** | No UI/component behaviour changes beyond dead export removal. |
| Performance | **N/A** | |

## Story Draft Checklist Report

_(Self-review — [`bmad-agent/checklists/story-draft-checklist.md`](../../bmad-agent/checklists/story-draft-checklist.md).)_

| Category | Status | Notes |
| -------- | ------ | ----- |
| 1. Goal & Context Clarity | **Pass** | Epic **13**, backward compat, **19.14** boundary. |
| 2. Technical Implementation Guidance | **Pass** | Discovery table + grep commands + file table. |
| 3. Reference Effectiveness | **Pass** | **epic-19**, **19.10**, shell path anchors. |
| 4. Self-Containment | **Pass** | ACs + tasks + reconciliation. |
| 5. Testing Guidance | **Pass** | CI commands + embedded/standalone spot-check. |

**Final Assessment:** **Draft** — ready for **Dev** after **19.1–19.12** confirmed **Done**.

---

**Story reference:** **Story 19.13** — migration / cleanup; prerequisites **[`19.1.story.md`](19.1.story.md)** … **[`19.12.story.md`](19.12.story.md)**; precedes **[`19.14.story.md`](19.14.story.md)** per **[`docs/epic-19.md`](../epic-19.md)** suggested items (lines **266–268**).
