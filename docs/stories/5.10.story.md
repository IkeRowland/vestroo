# Story 5.10: Traceability matrix — reference backend modules, cline_docs, and `document/` diagrams

## Status: Approved

**Dependencies:** **[Story 5.9](5.9.story.md)** — **Done** — canonical integration appendix **[`docs/capstone-reference-stack-integration.md`](../capstone-reference-stack-integration.md)** (**FE.5.9**); defers module-level enumeration to **FE.5.10** / **BE.6.1**. **[Stories 5.4–5.6](5.6.story.md)** — **Done** — **[`docs/capstone-admin-to-ops-mapping.md`](../capstone-admin-to-ops-mapping.md)**, **[`docs/capstone-manager-to-vestroo-mapping.md`](../capstone-manager-to-vestroo-mapping.md)**, and **[`docs/capstone-driver-to-field-mapping.md`](../capstone-driver-to-field-mapping.md)** supply **screen/route** mapping and partial backlog context; **5.10** adds **Nest module ↔ Vestroo capability** traceability without replacing those artifacts. **[Stories 5.1–5.3](5.3.story.md)** — **Done** — **FE.5.2** stack (**Tailwind + Radix**, no Ant on **`/ops/*`** and authenticated **`/field/*`**) remains binding for how matrix rows describe UI targets.

**Relationship to Story 5.9 (FE.5.9):** **5.9** owns **integration posture** (auth/session, HTTP clients, storage, realtime, UI dependency inventory). **5.10** MUST **not** repeat **5.9** comparison tables; it **cross-links** **`docs/capstone-reference-stack-integration.md`** once per major section or in a **“Related docs”** block and focuses on **module rows**, **cline_docs** indexing, and **`document/`** diagram index.

**Relationship to Story 5.11 (FE.5.11):** **5.11** is the **staff ops capability checklist** (implementation + backlog in **`/ops/*`**). The **5.10** matrix MUST include **explicit rows or a linked subsection** so **FE.5.11** outcomes have **data owners** (Supabase / Server Actions / Route Handlers) per **[`docs/epic-6.md`](../epic-6.md)** **BE.6.7** — without implementing **5.11** in this story.

**Explicit out of scope:** **FE.5.11** ops checklist **implementation**; **new application code** under **`src/`**; **full OpenAPI/Swagger** port (**BE.6.6** remains optional and separate); duplicating **FE.5.9** integration tables; replacing **`docs/capstone-admin-to-ops-mapping.md`** et al. as the **primary** screen-mapping source.

**Constraining NFRs:**

- **NFR.5.3:** The matrix MUST **not** read as endorsement of **JWT in `localStorage`**, **axios-to-Nest** as the default for first-party **`/ops/*` or `/field/*`**, or **socket.io drop-in**; touchpoints MUST point to **Supabase**, **server gates** (**`requireOpsStaffPage`**, **`requireChauffeurPage`**), **Server Actions**, **Route Handlers**, and **[`docs/realtime-and-notifications.md`](../realtime-and-notifications.md)** (**VST-9**) where realtime applies — consistent with **`docs/capstone-reference-stack-integration.md`**.
- **NFR.5.4:** Matrix vocabulary MUST prefer **Epic 4 / VST** terms (**corporate shuttle**, **service routes**, **trips**, **bookings**, **fleet**, **approved pickup/drop-off**, **waypoints**). Vestroo is **corporate shuttle** (**B2B / contracted** transport), **not** **fixed-route public mass transit** or a **public line-network** operator. Reference **transit-demo** naming (including **Nest** class names and **`cline_docs/`** filenames) MUST be **scoped as reference-only** where it appears. **Do not** use **mass-transit network**, **public line service**, or **transit-operator** framing to describe **Vestroo** capabilities, **`/ops`** targets, or user-facing outcomes.
- **NFR.5.5:** Where the matrix mentions **role-based** capabilities or routes, it MUST align with **server-enforced** permissions (nav visibility vs **403/redirect** if deep-linked) — cite gates and RLS expectations at **summary** level, not as client-trusted rules.

## Story

- As a **developer, architect, or PM** tracing capstone **NestJS** domains to Vestroo
- I want a **single canonical module traceability matrix** plus **cline_docs** and **`document/`** diagram indexes
- so that **every** reference **`app.module`** domain module, **every** **`cline_docs/`** topic, and **diagram assets** are **accounted for** with **status**, **Vestroo capability** (epic / VST-* / route or action), and **Supabase / Server Actions / Route Handlers** touchpoints — without forcing **public-transit IA** into **Epic 5** **`/ops`** UI or duplicating **FE.5.9** integration detail

## Epic traceability (source)

**From [`docs/epic-5.md`](../epic-5.md) — FE.5.10:** The traceability matrix (building on **FE.5.4–FE.5.6**; **FE.5.9** covers integration posture only — not module rows) MUST enumerate **every** NestJS **domain** module imported in reference **`docs/capstone-reference/backend/src/app.module.ts`** (**24** domain imports in the vendored tree; **authoritative list = that file**, including capstone **transit-demo** modules under reference **`src/modules/`**). **Row labels** MAY repeat **exact TypeScript module class names** from the reference repo **only** for traceability; **Vestroo capability** and **notes** columns MUST use **corporate shuttle** vocabulary and MUST **not** describe Vestroo using **mass-transit network**, **public line-network**, or **fixed-route public service** product language. Each module: **Vestroo capability** (epic / VST-* / route or action), status **implemented / partial / not started / not applicable / deferred**, and **Supabase / Server Actions / Route Handlers** touchpoints where applicable.

The matrix MUST index **every** **`docs/capstone-reference/cline_docs/`** topic: **productContext**, **systemPatterns**, **techContext**, **ticketingSystem**, **seatTracking**, **driverSchedule**, **projectStructure**, **activeContext**, **progress**, plus **`busOperations`** and **`busTracking`** (**legacy capstone filenames**—**not** Vestroo product terms)—each **linked** to relevant matrix rows or marked **not applicable** with **rationale**.

The matrix MUST include a **diagram index** for **`docs/capstone-reference/document/`** (filename, short purpose). If that folder were **empty** in a checkout, the index MUST state **no diagram assets in repo** and list **elsewhere** paths or tickets. **This repo’s checkout** contains **many** **`.jpg`** diagrams, **`.docx`** / **`.xlsx`**, and **Visual Paradigm** project files — the index MUST use a **pragmatic coverage strategy** (**AC10**).

**Corporate shuttle vocabulary (locked):** Rows tied to reference **transit-demo** domains (capstone modules and docs for **fixed timetable**, **ticketing**, **line-style** routing—see **`app.module.ts`** and **`cline_docs/`**) MUST **not** force **public-transit IA** into **Epic 5** **`/ops`** UI. Each such row MUST state **deferred / not applicable / Epic 9 gate** (see **[`docs/epic-9.md`](../epic-9.md)** **SH.9.1**) or **mapped to corporate shuttle equivalent** (e.g. client run, waypoint sequence, **approved pickup/drop-off**) with **one-line rationale**. Vestroo is **not** **public mass transit**; matrix **narrative** MUST **not** imply **fixed-route public networks**, **scheduled line service** as the core product, or **transit-operator** positioning. **`/ops/*` and authenticated `/field/*` remain Tailwind + Radix per FE.5.2; Ant MUST NOT** be introduced.

**Cross-epic:** **[`docs/epic-6.md`](../epic-6.md)** **BE.6.1** defines the **same** module set at the **backend parity** layer; **5.10** delivers the **documentation artifact** that satisfies **FE.5.10** and **feeds BE.6.1** — **one canonical matrix doc** (see **AC1**).

## Acceptance Criteria (ACs)

1. **Canonical matrix document — path selection:** Before authoring, read **[`docs/epic-6.md`](../epic-6.md)** **BE.6.1** and **`docs/index.md`** to determine whether a **BE.6.1** matrix file already exists. **Default canonical path:** **`docs/capstone-backend-module-matrix.md`** (name aligns reference-backend focus with **FE.5.10** / **BE.6.1**). If an existing **`docs/`** artifact already claims **BE.6.1** ownership (e.g. a differently named matrix), **either** merge **FE.5.10** requirements into that file **or** keep **`capstone-backend-module-matrix.md`** as the matrix and add **prominent cross-links** from the other doc — **one** matrix must be obvious as **canonical** for **module + cline + diagram** traceability. Record the decision in **Deviation Analysis** (one short paragraph).

2. **Domain module enumeration — minimum row set:** The canonical doc MUST include a **master table** (or equivalent structured section) with **≥1 row per** reference domain module import in **`docs/capstone-reference/backend/src/app.module.ts`** (**24** domain modules per **[`docs/epic-5.md`](../epic-5.md)** / **BE.6.1**—verify count against that file on each edit). The **reference module name** column MUST match **Nest** identifiers from the reference repo **for traceability**; **Vestroo capability** and **notes** MUST use **corporate shuttle** language and MUST **not** use **mass-transit**, **public line-network**, or **transit-operator** wording for Vestroo. Each row MUST include: **reference module name**; **Vestroo capability** (epic refs e.g. **Epic 1 / 5 / 6 / 9**, **VST-*** where known, **`/ops/*` or `/field/*` route or Server Action surface** where known); **status** — **implemented / partial / not started / not applicable / deferred**; **Supabase** (tables/views or **none** at summary level), **Server Actions / Route Handlers** (paths or **TBD**), and **notes** for intentional gaps. **RLS summary** SHOULD appear per **BE.6.1** (even if **TBD**) so **BE.6.2** can later attach migration filenames without rewriting row semantics.

3. **Infrastructure vs domain — `app.module` footnote or subsection:** The canonical doc MUST **acknowledge** non-domain **`app.module` imports** verified in **`docs/capstone-reference/backend/src/app.module.ts`**: **ConfigModule**, **MongooseModule**, **MailerModule**, **ScheduleModule.forRoot()** — and **providers** such as **`AppGateway`** (websocket gateway) — as **infrastructure / cross-cutting**, **not** duplicate domain rows **unless** the team documents a deliberate mapping (e.g. mail → notifications). Prefer a **footnote** or **short “Infrastructure” subsection** that points to **FE.5.9** for **transport/auth** posture and **does not** imply Mongo/Mailer are Vestroo targets.

4. **Build on FE.5.4–FE.5.6 — cross-links, not duplication:** The matrix MUST **link** to **[`docs/capstone-admin-to-ops-mapping.md`](../capstone-admin-to-ops-mapping.md)**, **[`docs/capstone-manager-to-vestroo-mapping.md`](../capstone-manager-to-vestroo-mapping.md)**, and **[`docs/capstone-driver-to-field-mapping.md`](../capstone-driver-to-field-mapping.md)** for **screen- and route-level** context. It MUST **not** replace those tables; it adds **backend module** and **doc/diagram** indexing.

5. **FE.5.9 boundary:** The matrix MUST **link once** to **[`docs/capstone-reference-stack-integration.md`](../capstone-reference-stack-integration.md)** and MUST **omit** repeating the **four** integration comparison tables from **5.9** (auth, HTTP, storage, realtime). A **one-sentence** reminder that **integration posture** lives in **5.9** is allowed.

6. **`cline_docs` — full topic index:** The canonical doc MUST include a **dedicated section** listing **every** **`.md`** topic file under **`docs/capstone-reference/cline_docs/`** (currently **11** files: **productContext**, **systemPatterns**, **techContext**, **ticketingSystem**, **seatTracking**, **driverSchedule**, **projectStructure**, **activeContext**, **progress**, **`busOperations`**, **`busTracking`**—the last two names are **legacy capstone files**, **not** Vestroo product vocabulary). For **each** topic: **link** to the file path; **map** to **one or more** matrix rows (module names or subsection IDs) **or** state **not applicable** with **one-line rationale** (e.g. historical capstone-only context).

7. **`document/` — diagram index with test/story mapping:** The canonical doc MUST include a **diagram index** for **`docs/capstone-reference/document/`**. For **each indexed asset** (see **AC10**), provide **filename**, **short purpose**, and **verification mapping**: **automated test** (path or name), **story ID** (e.g. **5.x**, **VST-***), or **`diagram only — no automated test yet`** with **named owner** (role + name or **TBD**). Where diagram type is known (**sequence**, **activity**, **ERD**, etc.), label it; if unknown, state **“type TBD”**.

8. **Epic 9 / corporate shuttle handling for transit-demo reference modules:** For **matrix rows** whose reference **Nest** modules implement capstone **transit-demo** concerns (identify from **`app.module.ts`** and **`src/modules/`**—typically **line-style** schedule, **ticketing**, and **network-style** tracking domains in the reference code), each row MUST include **one-line rationale** choosing among: **deferred**, **not applicable**, **Epic 9 / SH.9.1 gate**, or **corporate shuttle equivalent** (e.g. client run, waypoint sequence, capacity, **approved pickup/drop-off**) — and MUST **not** prescribe **public-transit IA** or **mass-transit / line-network** product language for **Epic 5** **`/ops`** by default.

9. **BE.6.1 / BE.6.7 alignment:** The matrix MUST **cross-link** **[`docs/epic-6.md`](../epic-6.md)** **BE.6.1** and MUST include **visibility** for **FE.5.11** data owners: staff users/roles, corporate clients, vehicles/categories, service routes/areas, bookings/trips/checkout/search — via **rows** or a **subsection** that references **BE.6.7** expectations **without** implementing **5.11**.

10. **`document/` coverage strategy (pragmatic):** Because **`document/`** may contain **many** files (e.g. **`.jpg`**, **`.docx`**, backups), the index MUST follow a **declared strategy**: e.g. **(a)** index **all** non-backup assets alphabetically; **or** **(b)** group by **extension** and **priority** (e.g. **sequence/activity** diagrams first, then **ERD/use-case** images, then **Word** bundles as **single row per docx** with **internal outline TBD**); **or** **(c)** exclude **explicit patterns** (e.g. **`*.vpp.bak`**, temp files) listed in a **“Excluded from index”** table with **rationale**. The strategy MUST be **stated at the top** of the diagram index so readers know **coverage** vs **omissions**.

11. **`docs/index.md`:** Add an entry pointing to the **canonical matrix path** (from **AC1**) with a one-line description (**FE.5.10** / **BE.6.1** — reference backend module traceability, cline_docs, diagrams).

12. **`docs/epic-5.md`:** Under **FE.5.10**, add or update a **single** sentence or bullet identifying the **canonical matrix path** as the **living artifact** (avoid duplicating the full matrix in the epic body).

13. **`docs/epic-6.md`:** Under **BE.6.1**, add or update a **single** sentence or bullet pointing to the **same canonical matrix path** so **Epic 5** and **Epic 6** do not imply **two competing** module matrices.

## Tasks / Subtasks

- [x] **Task 1 — AC1, AC11–AC13:** Confirm **canonical** matrix filename vs any existing **BE.6.1** doc; create or extend the matrix file; update **`docs/index.md`**, **`docs/epic-5.md`** (**FE.5.10**), **`docs/epic-6.md`** (**BE.6.1**). (AC: #1, #11, #12, #13)

- [x] **Task 2 — AC2, AC4, AC5, AC8, AC9:** Author the **domain module** master table with required columns; add **FE.5.4–FE.5.6** links; **FE.5.9** one-liner + link; **transit-demo** / timetable / ticketing **rationale** rows per **AC8** (corporate shuttle wording only for Vestroo); **BE.6.7** visibility subsection or rows. (AC: #2, #4, #5, #8, #9)

- [x] **Task 3 — AC3:** Add **infrastructure** footnote or subsection for **ConfigModule**, **MongooseModule**, **MailerModule**, **ScheduleModule**, **AppGateway** / **AppModule** providers. (AC: #3)

- [x] **Task 4 — AC6:** Build **`cline_docs`** index table: all **11** topics → matrix rows or **N/A** + rationale. (AC: #6)

- [x] **Task 5 — AC7, AC10:** Inventory **`docs/capstone-reference/document/`** per **declared strategy**; produce **diagram index** with **test / story / diagram-only + owner** mapping. (AC: #7, #10)

- [x] **Task 6 — NFR pass:** Grep the new/edited docs for wording that could **endorse** JWT-in-browser, Mongo as Vestroo default, or **Ant** for **`/ops`/`/field`**; fix or add **reference-only** qualifiers. (AC: —, cross-cutting)

## Dev Technical Guidance

### Reference — primary sources

- **Module wiring:** **`docs/capstone-reference/backend/src/app.module.ts`** — authoritative list of **imported** domain modules plus **infrastructure** imports (**AC3**).
- **Module internals:** **`docs/capstone-reference/backend/src/modules/*`** — use **representative** controller/service filenames in matrix **notes** where helpful (not an exhaustive port list).
- **Context docs:** **`docs/capstone-reference/cline_docs/*.md`** — one file per **AC6** topic.
- **Diagrams:** **`docs/capstone-reference/document/*`** — apply **AC10** strategy; prefer listing **human-meaningful** assets over binary noise.

### Vestroo — anchor docs (indicative)

- **Integration (do not duplicate):** **`docs/capstone-reference-stack-integration.md`**, **`docs/front-end-api-interaction.md`**, **`docs/ops-console.md`**, **`docs/field-tools.md`**, **`docs/realtime-and-notifications.md`**.
- **Data model vocabulary:** **`docs/data-models.md`**, **[`docs/epic-4.md`](../epic-4.md)** / VST stories as cited in other docs.
- **Epic 9 deferrals:** **`docs/epic-9.md`**, **SH.9.1**.

### Testing / verification (documentation story)

- **Link check:** All **relative** links from **`docs/`** resolve (index, epics, **5.4–5.6** mappings, **5.9** appendix, matrix, cline paths, sample `document/` paths).
- **Completeness check:** All **24** domain modules in **AC2** each have **≥1 row**; all **11** **cline_docs** topics each have an index row; **diagram index** matches **declared coverage strategy**.
- **Consistency check:** **Transit-demo** reference rows **do not** contradict **Epic 5** corporate shuttle framing (no **mass-transit** or **public line-network** vocabulary for Vestroo) or **Epic 9** gating language.

## Project Structure Notes

- **New file (default):** **`docs/capstone-backend-module-matrix.md`** — canonical **FE.5.10** / **BE.6.1** matrix (unless **AC1** deviation).
- **Edits:** **`docs/index.md`**, **`docs/epic-5.md`**, **`docs/epic-6.md`**; optional **cross-link** from **`docs/capstone-reference-stack-integration.md`** (add **concrete filename** when known).
- **No `src/` changes** unless a **separate** story explicitly tasks code; **5.10** is **docs-first**.

## Deviation Analysis

| Topic | Epic / product expectation | Likely repo reality / risk | Story handling |
| ----- | -------------------------- | --------------------------- | -------------- |
| **Canonical path (AC1)** | Single obvious matrix | **`docs/epic-6.md`** may imply a different filename later | **Check epic-6 + index first**; **Deviation Analysis** paragraph if merged elsewhere |
| **`document/` scale** | Every asset accounted for | **Many** **jpg/docx**; backup files | **AC10** explicit **strategy** + **exclusions** table if needed |
| **`cline_docs` “N/A”** | Every topic mapped | Some files **historical** or **transit-demo** | **N/A + rationale** allowed per **AC6** |
| **Overlap with BE.6.2–BE.6.3** | Matrix stays high-level | Authors may paste **migration** dumps | Keep **migration filenames** to **BE.6.2** stories; matrix **summarises** only |
| **5.9 duplication** | No repeated integration tables | Copy-paste from **5.9** | **AC5** — **link only** |

## Story Progress Notes

### Agent Model Used: Composer dev agent (Cursor) — implementation session

### Completion Notes List

- **AC1:** No pre-existing **BE.6.1** matrix file in **`docs/`**; canonical path **`docs/capstone-backend-module-matrix.md`** (decision recorded in matrix **Deviation analysis**).
- **AC2:** Master table **24 rows** matching **`docs/capstone-reference/backend/src/app.module.ts`** domain imports; columns include **Vestroo capability**, **status**, **Supabase**, **RLS**, **Server Actions / Route Handlers**, **notes**; **NFR.5.4** shuttle vocabulary for Vestroo; transit-demo modules (**Bus***, **Ticket**, etc.) → **not applicable** / **Epic 9** with rationale.
- **AC3:** **Infrastructure** subsection (**ConfigModule**, **MongooseModule**, **MailerModule**, **ScheduleModule**, **AppGateway**) — **reference-only** for Mongo/socket; points to **FE.5.9** / **VST-9**.
- **AC4–AC5:** Links to **FE.5.4–FE.5.6** mapping docs; **FE.5.9** one-liner + link — **no** duplicated integration tables.
- **AC6:** All **11** **`cline_docs/*.md`** topics indexed with matrix mapping or **N/A** + rationale; **busOperations** / **busTracking** flagged as **legacy filenames**.
- **AC7, AC10:** Diagram index lists **all** indexed **`.jpg`**, **`.docx`**, **`.xlsx`**, **`VinShuttle.vpp`**; **excludes** **`VinShuttle.vpp.bak_*`** with rationale; verification **diagram only — Owner TBD** default per asset.
- **AC9:** **FE.5.11 / BE.6.7** data-owner subsection.
- **AC11–AC13:** **`docs/index.md`**, **`docs/epic-5.md`** (**FE.5.10** living artifact), **`docs/epic-6.md`** (**BE.6.1** living artifact), **FE.5.9** appendix cross-link to matrix filename.
- **NFR.5.3–NFR.5.5:** Matrix states **Supabase + gates**; **not** JWT/Mongo/Ant as Vestroo defaults.

### Change Log

| Date | Change |
|------|--------|
| 2026-04-07 | **Draft** created: **FE.5.10** → **`docs/stories/5.10.story.md`**; dependencies **5.9** **Done**, **5.4–5.6** **Done**; scope **docs-only** matrix + cline + diagram index; **out of scope** **5.11** implementation, **`src/`** code, full OpenAPI. |
| 2026-04-07 | **Product vocabulary:** Reinforced **corporate shuttle** vs **public mass-transit** framing; module list delegated to **`app.module.ts`**; planning text avoids public-transit **boarding-network** vocabulary for Vestroo; **`busOperations`/`busTracking`** `cline_docs` names flagged as **legacy filenames only**. |
| 2026-04-07 | **Implementation:** **`docs/capstone-backend-module-matrix.md`**; **index** + **epic-5** + **epic-6** + **FE.5.9** link updates; story **Status → Review**, tasks complete. |

---

## Story Draft Checklist Validation

_Checklist source: `bmad-agent/checklists/story-draft-checklist.md`_

### 1. GOAL & CONTEXT CLARITY

- [x] Story goal/purpose is clearly stated
- [x] Relationship to epic goals is evident
- [x] How the story fits into overall system flow is explained
- [x] Dependencies on previous stories are identified (if applicable)
- [x] Business context and value are clear

### 2. TECHNICAL IMPLEMENTATION GUIDANCE

- [x] Key files to create/modify are identified (not necessarily exhaustive)
- [x] Technologies specifically needed for this story are mentioned
- [x] Critical APIs or interfaces are sufficiently described — **matrix columns**, **gates**, **Supabase/RLS** at summary level
- [x] Necessary data models or structures are referenced — **high-level**; **no** schema dumps required by this story
- [x] Required environment variables are listed (if applicable) — **N/A** for docs-only story
- [x] Any exceptions to standard coding patterns are noted — **canonical path deviation**, **`document/`** strategy

### 3. REFERENCE EFFECTIVENESS

- [x] References to external documents point to specific relevant sections or files
- [x] Critical information from previous stories is summarized (**5.9** boundary, **5.4–5.6** role)
- [x] Context is provided for why references are relevant
- [x] References use consistent format (`../epic-5.md`, `../epic-6.md`, etc.)

### 4. SELF-CONTAINMENT ASSESSMENT

- [x] Core information needed is included (ACs, module list, cline list, diagram rules)
- [x] Implicit assumptions are made explicit (**verified** `app.module` / `cline_docs` / `document/` facts)
- [x] Domain-specific terms or concepts are explained (**infrastructure vs domain**, **corporate shuttle vs transit-demo**)
- [x] Edge cases or error scenarios are addressed — **empty `document/`** clause, **large folder** strategy, **dual epic** pointers

### 5. TESTING GUIDANCE

- [x] Required testing approach is outlined (**link + completeness** verification)
- [x] Key test scenarios are identified (broken links, missing module row, missing cline row)
- [x] Success criteria are defined per ACs
- [x] Special testing considerations are noted — **diagram → test/story/owner** mapping

### VALIDATION RESULT

| Category                             | Status  | Issues |
| ------------------------------------ | ------- | ------ |
| 1. Goal & Context Clarity            | PASS    | —      |
| 2. Technical Implementation Guidance | PASS    | —      |
| 3. Reference Effectiveness           | PASS    | —      |
| 4. Self-Containment Assessment       | PASS    | —      |
| 5. Testing Guidance                  | PASS    | Docs-only verification |

**Final Assessment:** **READY** (for documentation implementation)

The story is sufficient for a developer agent to author the **canonical module + cline_docs + diagram** matrix, update **index** and **epics**, and respect **FE.5.9** / **NFR.5.3–NFR.5.5** — without implementing **FE.5.11**, **`src/`** features, or **full OpenAPI**.
