# Story 8.6: Third-party HTTP clients and idempotency — reference axios / HttpModule vs Vestroo (INT.8.6)

## Status: Done

**Dependencies:** **[`docs/epic-8.md`](../epic-8.md)** — **INT.8.6** source (where the reference uses **axios** or **Nest `HttpModule`** for outbound calls, Vestroo MUST document **Route Handler** or **server-only** **`fetch`** usage, **timeouts**, **retries**, and **idempotency keys** for **payments** and **critical** side effects). **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** (VST-13) — default **canonical home** for **`#int-8-6`** (**`<a id="int-8-6"></a>`** immediately above **`## INT.8.6 — Third-party HTTP clients and idempotency`** — **same anchor pattern** as **`#int-8-1`**–**`#int-8-5`**). **PayFast ITN idempotency** is **already** documented under **[PayFast webhook lifecycle](../integrations-and-payments.md#payfast-webhook-lifecycle)** and **[§ INT.8.3](../integrations-and-payments.md#int-8-3)** — **cite** **`#int-8-3`** / webhook lifecycle only; **do not** duplicate the full lifecycle section in **`#int-8-6`**. **[`docs/front-end-api-interaction.md`](../front-end-api-interaction.md)** — Server Actions, webhooks, and client/server API boundaries (**consolidate** or **cross-link** so **`#int-8-6`** is the integration HTTP contract, not a forked spec). **[`docs/environment-vars.md`](../environment-vars.md)** — server-only keys for outbound integrations (**NFR.3.1**). **[`docs/capstone-reference-stack-integration.md`](../capstone-reference-stack-integration.md)** — **FE.5.9** stack contrast (**Nest** outbound clients vs **Next.js** patterns). **[`docs/stories/8.3.story.md`](8.3.story.md)** — **INT.8.3** PayFast substitution (**thin** pointer: hosted redirect + ITN, not reference **axios** gateways). **[`docs/stories/8.1.story.md`](8.1.story.md)** — **INT.8.1** Resend/email retries (**thin** pointer: **`RETRY_CONFIG`**, **`isRetryableError`** — avoid duplicating **INT.8.1** NFR prose). **[`docs/stories/8.2.story.md`](8.2.story.md)** — **INT.8.2** SMS stub; future **SMS** HTTP client rows in **`#int-8-6`** MUST **align** backlog wording with **`#int-8-2`** (**no** duplicate policy matrix). **[`docs/stories/vst-13.story.md`](vst-13.story.md)** — **optional** reciprocal line to **`#int-8-6`** (same optional pattern as **INT.8.3**–**INT.8.5**). **[`docs/compliance-and-safety.md`](../compliance-and-safety.md)** (VST-12) — when documenting outbound calls that carry **PII** (e.g. future **SMS** HTTP client). **Reference inventory (pattern-only — grep, re-verify):** **`src/legacy/capstone-reference/backend-share/share.module.ts`** — **`HttpModule`** from **`@nestjs/axios`**; **`backend-share/sms.ts`** — **`HttpService`**; **`backend-share/momo.ts`** — **`axios`**; **`backend-share/payos.ts`** — **`@payos/node`** SDK (**not** raw **axios** in prep grep); **`backend-modules/OSR/osr.module.ts`** — **`HttpModule`**; **`backend-modules/OSR/osr.service.ts`** — **`HttpService`**; **`frontend-driver/src/services/apiClient.ts`**, **`userServices.ts`** — **`axios`**. **`src/features/capstone-reference/`** — **customer** / **manager** / **admin** **axios** clients and **`fetch`** (**non-production** reference UI — **do not** conflate with **`(app)`** / **`(ops)`** / **`(field)`** unless wired). **Shipped prep hints (re-verify in AC — paths only):** **`src/lib/maps.ts`** — **`fetch(Distance Matrix URL)`** in **`calculateRouteDistance`** (**prep:** **no** **`AbortSignal`** / explicit **`timeout`** option on that **`fetch`** — document as **gap** if still true after grep). **`src/services/email.ts`** — **Resend** SDK, **`RETRY_CONFIG`**, **`isRetryableError`**, exponential backoff for **`sendBookingConfirmation`** (**cross-link** **INT.8.1** for failure-mode depth). **`src/app/api/payfast/webhook/route.ts`** — idempotent **ITN** handling (**link** VST-13 **`#int-8-3`** / lifecycle). **`src/actions/processPayment.ts`** — hosted **PayFast** redirect via **`initializePayFastModal`** / form POST (**no** first-party **axios** to PayFast for card data). **`src/lib/payfast-client.ts`** — browser **form** submit to **`/eng/process`**, not **`fetch`**.

**Explicit out of scope:** **Globally replacing** first-party **`fetch`** with **axios** across the app. **Implementing** new timeout/retry middleware or shared HTTP wrappers **unless** pulled into a **separate** engineering story. **INT.8.5** secrets / **`share/`** matrix — **one-line** cross-link to **[`#int-8-5`](../integrations-and-payments.md#int-8-5)** only (**no** duplication of the secrets table).

**Constraining NFRs:**

- **NFR.3.1:** Outbound integration **secrets** and signing material remain **server-only**; align with **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** **Security** and **[`docs/environment-vars.md`](../environment-vars.md)**.
- **NFR.1.3:** Document **failure modes** and **operator** / **engineering** mitigations for each **live** outbound path (timeouts, retries, duplicate events) — at minimum **PayFast ITN** + **Resend** slices (**honest TBD** where behaviour is not implemented).
- **NFR.4.1:** Cite **narrow** **TypeScript** types at **HTTP** boundaries (**webhook** payloads, **Distance Matrix** response typing, **`BookingEmailData`**, etc.) — **honest N/A** where the section is docs-only.

## Story

- As a **platform, engineering, or security** owner of **Epic 8** integration traceability
- I want a **grep-backed** canonical **`#int-8-6`** that maps each **material outbound integration** (**Maps** Distance Matrix, **Resend**/email, **PayFast** ITN + checkout surface, **other** first-party **`fetch`** / SDK / Route Handler) to **reference** client style (**axios**, **`HttpModule`**, **`HttpService`**, **`@payos/node`**) vs **Vestroo** implementation (**Route Handler**, **Server Action**, **server-only `fetch`**, **vendor SDK**), with explicit **timeouts** (ms or **TBD**), **retries** (policy or **none**), **idempotency** (DB guards, webhook dedupe — **cite** existing VST-13 text), and **NFR** footnotes
- so that **payments** and **critical** side effects are **auditable** for **timeouts**, **retries**, and **idempotency keys** / guards **without** copy-pasting the **VN reference** HTTP stack, and **gaps** (e.g. **Maps** **`fetch`** without **`AbortSignal`**) are **backlogged** honestly

## Acceptance Criteria (ACs)

1. **Canonical doc home for INT.8.6 (default VST-13):** Add **`## INT.8.6 — Third-party HTTP clients and idempotency`** with stable anchor **`#int-8-6`** to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** — **`<a id="int-8-6"></a>`** immediately above the heading (**same pattern** as **`#int-8-1`**–**`#int-8-5`**). Section MUST include a **matrix** (Markdown table, **no** blank lines between data rows) with columns at minimum: **Integration / outbound call** (e.g. **Maps** Distance Matrix, **Resend** booking email, **PayFast** ITN ingest, **PayFast** hosted checkout handoff, **first-party `fetch`** to own **`/api/*`**, **stub** paths); **Reference client** (**axios** / **`HttpModule`** / **`HttpService`** file paths under **`src/legacy/capstone-reference/`** and, where relevant, **`src/features/capstone-reference/`** — **grep-backed**); **Vestroo implementation** (**Route Handler** path, **Server Action**, **`src/lib/maps.ts` `fetch`**, **Resend** SDK, **browser form POST** — **honest**); **Timeout** (explicit **ms**, platform default, or **TBD** with reason); **Retries** (policy summary or **`none`**); **Idempotency** (keys, conditional DB writes, webhook dedupe — **pointer** to **[PayFast webhook lifecycle](../integrations-and-payments.md#payfast-webhook-lifecycle)** / **`#int-8-3`** for ITN — **no** full duplicate); **NFR notes** (**NFR.3.1** / **NFR.1.3** / **NFR.4.1** one-line each row or footnote). **Optional:** extend the VST-13 **integration matrix** with a row **HTTP clients / idempotency** → **`#int-8-6`**. **Reconcile** existing deferral prose (**prep:** **`#int-8-4`** pointer to **`front-end-api-interaction.md`** for “**INT.8.6**”; **`#int-8-5`** “**INT.8.6 — deferral**” stub) so **`#int-8-6`** is authoritative **without** orphaning readers (**single** canonical subsection).

2. **Reference inventory — axios / HttpModule / HttpService:** **Grep** **`src/legacy/capstone-reference/`** for **`axios`**, **`HttpModule`**, **`@nestjs/axios`**, **`HttpService`**. Narrative MUST summarise **representative** files (**prep:** **`backend-share/share.module.ts`**, **`backend-share/sms.ts`**, **`backend-share/momo.ts`**, **`backend-modules/OSR/osr.module.ts`**, **`backend-modules/OSR/osr.service.ts`**, **`frontend-driver`** axios clients — **re-verify**). MUST note **`payos.ts`** uses **`@payos/node`** (**SDK**, not **axios**) for **contrast** in the **reference** column where applicable.

3. **Shipped inventory — first-party HTTP boundaries:** **Grep** **`src/`** excluding **`src/legacy/`** and **`src/features/capstone-reference/`** for **`fetch(`**, **`axios`**, **`AbortSignal`**, **`signal:`**, **`timeout`** (disambiguate **browser** `setTimeout` / **geolocation** `timeout` from **HTTP** `fetch` options). Report findings in **`#int-8-6`** (**prep:** **`axios`** absent in **`src/app`**, **`src/actions`**, **`src/lib`**, **`src/services`**, **`src/features/ops`**, **`src/features/field`** from targeted greps — **re-verify** whole shipped tree). MUST explicitly state whether **`src/lib/maps.ts`** **`calculateRouteDistance`** still uses **`fetch`** **without** **`AbortSignal`** / explicit **timeout** if true after verification.

4. **Payments + critical side effects — idempotency:** **`#int-8-6`** MUST state that **PayFast** **`COMPLETE`** replay and **booking** **`payment_status`** guards are documented under **`#int-8-3`** / **[PayFast webhook lifecycle](../integrations-and-payments.md#payfast-webhook-lifecycle)** (**cite**, **no** duplicate step list). MUST tie **booking confirmation email** idempotency to **webhook** behaviour + **`src/app/api/payfast/webhook/route.ts`** (**cross-link** **INT.8.1** for **Resend** retry semantics — **no** duplicate **INT.8.1** matrix).

5. **Backlog for gaps:** Any integration row missing a **decided** **timeout**, **retry**, or **idempotency** story (e.g. **Maps** **`fetch`** **TBD** **AbortSignal** / **ms** budget) MUST get a **backlog** line (**P0/P1/P2** or **High/Medium/Low** — **one** scheme, consistent with **INT.8.1**–**INT.8.5**).

6. **Index / Related thin links:** Add **`#int-8-6`** to **[`docs/index.md`](../index.md)** and/or VST-13 **Related** / **Cross-links** (**no** wholesale restructure).

7. **Epic / VST-13 housekeeping:** **Do not** edit **`docs/epic-8.md`** in this story’s implementation pass unless a **separate** maintainer task authorises it; **optional** reciprocal line in **[`docs/stories/vst-13.story.md`](vst-13.story.md)** to **`#int-8-6`**.

## Tasks / Subtasks

- [x] **Task 1 — AC1, AC6:** Add **`<a id="int-8-6"></a>`** + **`## INT.8.6 …`** + matrix to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)**; reconcile **INT.8.4** / **INT.8.5** deferral one-liners; **thin** **[`docs/index.md`](../index.md)** (and VST-13 **Cross-links** if used). (AC: #1, #6)
- [x] **Task 2 — AC2:** Re-**grep** **`src/legacy/capstone-reference/`** for **`axios`**, **`HttpModule`**, **`HttpService`**, **`@nestjs/axios`**; capture **OSR**, **`share`**, **`momo`**, **`sms`**, **`frontend-driver`**; note **`@payos/node`**. (AC: #2)
- [x] **Task 3 — AC2 (reference UI):** **Grep** **`src/features/capstone-reference/`** for **`axios`** / **`fetch`**; classify as **non-product** pattern-only vs any accidental **`(app)`** wiring (**should be none**). (AC: #2)
- [x] **Task 4 — AC3:** Re-**grep** shipped **`src/`** (exclude **`src/legacy/`**, **`src/features/capstone-reference/`**) for **`fetch(`**, **`axios`**, **`AbortSignal`**, **`signal:`**; document **`src/app/(app)/confirmation/page.tsx`** (or current path) **first-party `fetch`** to **`/api/booking-confirmation`** if present. (AC: #3)
- [x] **Task 5 — AC1 matrix rows:** Fill **Maps**, **Resend/email**, **PayFast** (ITN + hosted handoff), **stub/future SMS** pointer, **any** other verified outbound rows — **timeouts** / **retries** / **idempotency** columns **honest**. (AC: #1)
- [x] **Task 6 — AC4:** Add **cites** to **`#int-8-3`** / **PayFast webhook lifecycle**; **`src/app/api/payfast/webhook/route.ts`**; **`src/actions/processPayment.ts`** + **`src/lib/payfast-client.ts`**; **thin** **INT.8.1** for **`src/services/email.ts`**. (AC: #4)
- [x] **Task 7 — AC5:** Add **backlog** rows for **Maps timeout**, **missing** **idempotency keys** on future clients, **SMS** future HTTP client (**align** **INT.8.2** backlog row that references **INT.8.6**). (AC: #5)
- [x] **Task 8 — NFR.3.1 / NFR.1.3 / NFR.4.1:** Footnote **server-only** env usage, **failure modes**, and **boundary types** at cited files (**`maps.ts`**, **`email.ts`**, **`payfast.ts`**, **`webhook/route.ts`** as needed). (AC: **NFRs**)
- [x] **Task 9 — AC1 optional:** Add **integration matrix** row → **`#int-8-6`** if the table is the project’s preferred index. (AC: #1)
- [x] **Task 10 — AC7:** Confirm **`docs/epic-8.md`** unchanged **or** file follow-up for **INT.8.6** deep link parity; optional **`vst-13.story.md`** reciprocal. (AC: #7)
- [x] **Task 11 — AC6:** **Click-test** **`../front-end-api-interaction.md`** from **`#int-8-6`** so deferral readers are not stranded. (AC: #6)

## Dev Technical Guidance

### Primary sources

- **Epic requirement:** [`docs/epic-8.md`](../epic-8.md) — **INT.8.6**
- **Canonical integration doc (default):** [`docs/integrations-and-payments.md`](../integrations-and-payments.md) — **`#int-8-1`**–**`#int-8-5`** anchor pattern; **replace** thin **INT.8.6** deferrals with **`#int-8-6`**
- **FE.5.9:** [`docs/capstone-reference-stack-integration.md`](../capstone-reference-stack-integration.md)
- **API boundaries:** [`docs/front-end-api-interaction.md`](../front-end-api-interaction.md)
- **Env:** [`docs/environment-vars.md`](../environment-vars.md)
- **Sibling stories:** [`docs/stories/8.1.story.md`](8.1.story.md), [`docs/stories/8.2.story.md`](8.2.story.md), [`docs/stories/8.3.story.md`](8.3.story.md), [`docs/stories/8.5.story.md`](8.5.story.md)
- **PII / outbound:** [`docs/compliance-and-safety.md`](../compliance-and-safety.md)

### How to execute the inventory

1. **Reference first:** Run the **legacy** **grep** bundle; capture **Nest** **`HttpModule`** wiring and **direct** **`axios`** calls (**`momo.ts`**, **`sms.ts`**, **OSR**).
2. **Reference UI second:** **grep** **`src/features/capstone-reference/`** — classify **axios**/**`fetch`** as **pattern-only**.
3. **Shipped third:** **grep** first-party trees for **`fetch(`** / **`axios`** / **`AbortSignal`** / **`signal:`**; read **`src/lib/maps.ts`** **`calculateRouteDistance`**, **`src/services/email.ts`**, **`src/app/api/payfast/webhook/route.ts`**, **`src/actions/processPayment.ts`**, **`src/lib/payfast-client.ts`**.
4. **Docs fourth:** Add **`#int-8-6`** matrix + **cross-links**; update **INT.8.4** / **INT.8.5** stubs to point at **`#int-8-6`** instead of implying **`front-end-api-interaction.md`** alone is the full HTTP contract.

## Testing / verification

- **Relative links:** From **`docs/stories/8.6.story.md`**, click-test **`../epic-8.md`**, **`../integrations-and-payments.md#int-8-6`** (after edit), **`../front-end-api-interaction.md`**, **`../environment-vars.md`**, **`../capstone-reference-stack-integration.md`**, **`../index.md`**, siblings **`8.1`**, **`8.2`**, **`8.3`**, **`8.5`**, optional **`vst-13.story.md`**, **`../compliance-and-safety.md`**, **`#int-8-3`**, **`#payfast-webhook-lifecycle`**.
- **Grep bundle (reference):** `axios`, `HttpModule`, `@nestjs/axios`, `HttpService`, `@payos/node`
- **Grep bundle (shipped):** `fetch(`, `axios`, `AbortSignal`, `signal:`, `timeout` (filter **HTTP** vs **non-HTTP** hits)
- **Consistency:** **No** row claims **vendored reference** HTTP clients run in **Vestroo prod**; **first-party** paths are **evidence-backed**.

## Story draft self-check

_Checklist inspiration: `bmad-agent/checklists/story-draft-checklist.md`_

| Category | Status | Notes |
| -------- | ------ | ----- |
| 1. Goal & Context Clarity | PASS | **INT.8.6** scoped; **Epic 8**; default **VST-13** **`#int-8-6`** |
| 2. Technical Implementation Guidance | PASS | **grep** plans, **legacy vs shipped**, **PayFast** cite-only idempotency |
| 3. Reference Effectiveness | PASS | **`HttpModule`**, **`HttpService`**, **`axios`**, **`momo`**, **`sms`**, **OSR** |
| 4. Self-Containment Assessment | PASS | **Out of scope** axios migration, **INT.8.5** one-liner only |
| 5. Testing Guidance | PASS | Link check + **grep** bundles |

**Final Assessment: READY** — Developer agent can add **`#int-8-6`**, **grep-backed** matrix, **NFR.3.1 / NFR.1.3 / NFR.4.1**, **backlog** rows, and **cross-links** **without** implementing global **axios** or new middleware.

## Story Progress Notes

### Agent Model Used:

Implementation (dev agent) — **`docs/integrations-and-payments.md`** **`#int-8-6`**, **`docs/index.md`**, **`docs/stories/vst-13.story.md`**, **`docs/stories/8.6.story.md`**; **`docs/epic-8.md`** not modified (per AC7).

### Completion Notes List

- Added **`<a id="int-8-6"></a>`** + **`## INT.8.6 — Third-party HTTP clients and idempotency`** to **[`integrations-and-payments.md`](../integrations-and-payments.md)** with **grep-backed** reference vs shipped narrative, **matrix** (Maps **`fetch`**, Resend, PayFast ITN + hosted checkout, confirmation **`fetch`**, SMS stub), **NFR** bullets, **P1/P2** backlog, **Cross-links** bullet, and optional **integration matrix** row **HTTP clients / idempotency** → **`#int-8-6`**.
- Reconciled **INT.8.4** thin cross-links and end-of-section forward pointers; **INT.8.5** stub → **`#int-8-6`** + **`front-end-api-interaction.md`** complementary; **INT.8.5** thin siblings + **INT.8.2** SMS backlog row link **`[INT.8.6](#int-8-6)`**.
- **`docs/index.md`**: Epic **8** and **VST-13** bullets include **`integrations-and-payments.md#int-8-6`**.
- **`vst-13.story.md`**: dependencies line adds **INT.8.6** reciprocal to **`#int-8-6`** (**Story 8.6**).

### Change Log

| Date | Change |
| ---- | ------ |
| 2026-04-15 | **Ready** story file created: **INT.8.6** third-party HTTP clients + idempotency — **`#int-8-6`** spec, matrix columns (**timeout** / **retry** / **idempotency** / **NFR**), **grep** verification plan, **PayFast** **cite-only** to **`#int-8-3`**, **Maps** **`fetch`** gap honesty, **docs-only** scope, **epic-8.md** no-edit rule. |
| 2026-04-15 | Added **`8.2.story.md`** dependency + testing link (**SMS** vs future HTTP rows). |
| 2026-04-14 | **Documentation delivered:** **`#int-8-6`** in **`integrations-and-payments.md`**, matrix + backlog + cross-links, **INT.8.2** / **INT.8.4** / **INT.8.5** reconciliation, **`index.md`** / **`vst-13.story.md`** links; story tasks **1–11** marked complete; **`docs/epic-8.md`** unchanged. |
