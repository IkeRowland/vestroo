# Story 16.13: PayFast removal — physical delete + trigger drop + docs/env grep gate (Epic 16 / N2)

## Status: Done

**Story provenance:** **REMEDIATION** — **[`docs/epic-16.md`](../epic-16.md)** **Theme N / US-N2** (`§4`, lines ~699–713): **physically remove** all PayFast integration per **Q31** (~122) — no feature flags, no leave-behind dead code. Deletes **`src/lib/payfast.ts`**, **`src/lib/payfast-client.ts`**, **`src/actions/processPayment.ts`**, **`src/app/api/payfast/webhook/route.ts`** and webhook **`__tests__/`**, the entire **`src/app/(quote)/q/[token]/pay/`** directory (pages/assets under that path); adds migration **`YYYYMMDDHHMMSS_ops16_drop_payfast_trigger.sql`** dropping **`ready_to_assign_walk_in_paid_trigger`** on **`public.bookings`** and **`public.ready_to_assign_walk_in_paid_v1()`**; reviews **`supabase/migrations/20260418160000_sh95_patterned_checkout_payment.sql`** and adds a **deferral note** to **[`docs/epic-9.md`](../epic-9.md)** **§ SH.9.5** (anchor **`#sh-9-5-checkout`**, ~76–80) per epic; removes all **`PAYFAST_*`** keys from **`.env.example`** and **`.env.test.example`**; updates **`docs/environment-vars.md`**; confirms **`package.json`** has nothing PayFast-specific to remove (epic AC); enforces **grep gate** on **`src/`** + **`docs/`** with allowed match buckets per epic **US-N2** last AC. **`bookings.payment_status`** column **retained** — future writes via **`markBookingPaymentReceived`** (**N3**, separate story). **Product locks:** **Q31**, **Q33** — **`/q/[token]/pay`** is retired here; **US-N7** reintroduces **only** a minimal **`GET`** redirect handler under **`pay/route.ts`** after this deletion (do **not** conflate N2 with N7).

**Point estimate:** Epic 16 sizes **~62 points across 24 stories** with no per-story point split for **N2** — **TBD per team**.

**Dependencies:**

- **Catalogue / theme:** **[`docs/epic-16.md`](../epic-16.md)** **Theme N — PayFast removal & EFT payment workflow** — **US-N2** (~699–713).
- **Product locks:** **Q31** — physical removal, no PayFast leave-behind; **Q33** — customer pay URL retired; redirect window is **N7**, not N2.
- **Cross-cutting matrix (cite):** **[`docs/epic-16.md`](../epic-16.md)** **§5. Cross-cutting dependencies matrix** — row **`N2 PayFast deletion`**: **Depends on** **N1**; **Blocks** **N3**, **N8**.

**Prerequisites (must be satisfied before this story is implementable):**

- **[`docs/stories/16.11.story.md`](./16.11.story.md)** (**Theme N / US-N1**) — **Status `Done`**: **`ops_settings`**, **`bookings.payment_received_at` / `payment_evidence_ref`**, masking helper, and **N1** smoke per matrix narrative (**payment_status / ops path** for post–PayFast world).

**Blocks:**

- **N3** — **`markBookingPaymentReceived`** (replaces ITN-driven **`payment_status`** transition).
- **N8** — Epic **14** state-machine / doc cleanup (**Depends on** **N2** and **N3** per matrix).

**Reconciliation (mandatory):**

- **N2 vs N3:** This story **does not** implement **`markBookingPaymentReceived`**; after trigger drop, **no** DB automation moves **`awaiting_payment → ready_to_assign`** until **N3** ships — coordinate merge order with product if a production gap is unacceptable.
- **N2 vs N6–N7:** **US-N6** (**`/q/[token]/accept`**) and **US-N7** (**90-day redirect**) are **out of scope** here except **survey**: removing **`processPayment`** / **pay** pages must not leave **broken imports**; **accept** page changes land in **N6**.
- **Grep scope:** Epic gate command is **`grep -ri "payfast" src/ docs/`**. Allowed match buckets for **those paths:** (a) **`docs/`** historical references; (c) **`src/legacy/`** per **Epic 5**. For **`supabase/migrations/`** (not covered by that grep), separately **survey** remaining **`payfast`** mentions and add header/line **SQL comment** where historical: **`-- PayFast removed in Epic 16 — see Theme N`** (or equivalent), per epic **US-N2**.

**Explicit out of scope:**

- **US-N3** **`markBookingPaymentReceived`** and any **`/ops/walk-in`** / **`/ops/accounts`** EFT-mark UI.
- **US-N4–N7** — quote/invoice bank-detail emails, **accept** landing behaviour, **`pay/route.ts`** redirect (**N7**).
- **US-N8** — Epic **14** / **`docs/core-traveller-flow-parity.md`** / **`docs/integrations-and-payments.md`** wide cleanup (**blocked** until **N2** + **N3** per matrix).
- **Feature-flagged** PayFast leave-behind — **forbidden** by **Q31** / epic **§2** (~132).

## Story

- As a **developer**, I need **all PayFast code paths, webhook route, client libs, and `processPayment` action removed** so the codebase matches **EFT-only** operations per **Q31** and reduces PCI-adjacent surface area.

- As a **migration author**, I need the **Epic 14** PayFast ITN trigger and function **dropped** so **`payment_status`** is no longer flipped by webhook side effects; **N3** will own the **manual** transition.

- As **platform/docs**, I need **env templates**, **`docs/environment-vars.md`**, **`docs/epic-9.md` § SH.9.5**, and **patterned-checkout migration review** updated so **SH.9.5** records **deferral** until a **non-PayFast** patterned-checkout approach is specified (per epic **US-N2** AC).

## Acceptance Criteria (ACs)

1. **Files deleted (physical):** **`src/lib/payfast.ts`**, **`src/lib/payfast-client.ts`**, **`src/actions/processPayment.ts`**, **`src/app/api/payfast/webhook/route.ts`** and its **`__tests__/`** tree, the **`src/app/(quote)/q/[token]/pay/`** directory (all contents). **Survey `src/`** and fix **any imports** or re-exports referencing deleted modules (**same story**).
2. **Migration file:** New migration named **`YYYYMMDDHHMMSS_ops16_drop_payfast_trigger.sql`** (timestamp at implementation time; suffix **exactly** **`ops16_drop_payfast_trigger`**) containing SQL **equivalent** to **[`docs/epic-16.md`](../epic-16.md)** **US-N2** snippet (~702–708):
   - **`drop trigger if exists ready_to_assign_walk_in_paid_trigger on public.bookings;`**
   - **`drop function if exists public.ready_to_assign_walk_in_paid_v1();`**
   - **Comment** in migration body: **`bookings.payment_status`** column **retained** — now to be driven by **`markBookingPaymentReceived`** (**N3**).
3. **Patterned checkout + Epic 9:** Read **`supabase/migrations/20260418160000_sh95_patterned_checkout_payment.sql`**; record findings in **Progress Notes** (PayFast assumptions, coupling to walk-in checkout). In **[`docs/epic-9.md`](../epic-9.md)** under **§ SH.9.5** (**`#sh-9-5-checkout`**), add a short **deferral / supersession note**: patterned-checkout **payment provider work** is **deferred** until a **non-PayFast** solution is specified — **PayFast removed Epic 16 Theme N / Q31**; cite **`docs/epic-16.md`** Theme **N** and migration review.
4. **Environment variables:** Remove **`PAYFAST_MERCHANT_ID`**, **`PAYFAST_MERCHANT_KEY`**, **`PAYFAST_PASSPHRASE`**, **`PAYFAST_PROCESS_BASE_URL`**, and **any** **`PAYFAST_*`** from **`.env.example`** and **`.env.test.example`**. Update **`docs/environment-vars.md`** so no PayFast keys remain documented as active.
5. **`package.json`:** Review dependencies; **nothing PayFast-specific** to remove (epic — document **“verified none”** in **Progress Notes** if still true).
6. **Grep gate:** Run **`grep -ri "payfast" src/ docs/`** (case-insensitive). Matches allowed **only** in: (a) **`docs/`** historical/archival references; (c) **`src/legacy/`** per **Epic 5** rules. **Zero** matches elsewhere under **`src/`** (excluding **`src/legacy/`**). Separately: **`supabase/migrations/`** — any remaining **`payfast`** hits must carry the epic’s **historical annotation** comment (**Theme N**).

## Tasks / Subtasks

- [x] **Task 0 (AC: 1):** **`git rm`** / delete listed **PayFast** files and **`pay/`** directory; remove **webhook** tests.
- [x] **Task 1 (AC: 1):** **Repo survey** — **`rg -i payfast src`** (and imports of **`processPayment`**, **`payfast`**, **`payfast-client`**) — update or delete all call sites in **`src/`** (not **legacy** unless epic allows).
- [x] **Task 2 (AC: 2):** Add **`supabase/migrations/YYYYMMDDHHMMSS_ops16_drop_payfast_trigger.sql`** per AC2.
- [x] **Task 3 (AC: 3):** Review **`20260418160000_sh95_patterned_checkout_payment.sql`**; edit **`docs/epic-9.md`** § **SH.9.5** deferral note + anchor cross-link to **`docs/epic-16.md`** Theme **N** / **Q31**.
- [x] **Task 4 (AC: 4):** Edit **`.env.example`**, **`.env.test.example`**, **`docs/environment-vars.md`**.
- [x] **Task 5 (AC: 5):** **`package.json`** review — **Progress Notes** outcome.
- [x] **Task 6 (AC: 6):** Run grep gate; fix violations; annotate **`supabase/migrations/**`** historical matches per AC6.
- [x] **Task 7 (AC: 1, 6):** Delete or rewrite tests that imported **`processPayment`** / PayFast webhook (**no** PayFast sandbox paths).
- [x] **Task 8 (DoD):** **`npm run lint`**, **`npm run test`**, **`npm run smoke:rls`** as applicable; hosted apply via **Supabase MCP** for new migration (**`list_migrations`** → **`apply_migration`** → re-list) per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)**; update **Progress Notes**, **Change log**, **Story DoD Checklist Report**; set **Status** **Done** when complete.

## Dev Technical Guidance

### Primary sources

- **[`docs/epic-16.md`](../epic-16.md)** — **US-N2** (~699–713), **Q31**, **§5** matrix (**N2** row), **§2** non-goals (no feature-flag leave-behind), **§7** DoD bullet “**PayFast removed**”, **§8** code paths (~1007–1010).
- **[`docs/epic-9.md`](../epic-9.md)** — **SH.9.5** **`#sh-9-5-checkout`** (~76–80); **SH.9.1** gate context.
- **[`docs/epic-14.md`](../epic-14.md)** — historical PayFast / walk-in narrative (**N8** will amend; **not** this story’s AC).

### Primary files / directories

- **Delete:** `src/lib/payfast.ts`, `src/lib/payfast-client.ts`, `src/actions/processPayment.ts`, `src/app/api/payfast/webhook/route.ts`, `src/app/api/payfast/webhook/__tests__/` (if present), `src/app/(quote)/q/[token]/pay/**`
- **New:** `supabase/migrations/YYYYMMDDHHMMSS_ops16_drop_payfast_trigger.sql`
- **Edit:** `docs/epic-9.md` (SH.9.5), `.env.example`, `.env.test.example`, `docs/environment-vars.md`; migration SQL comments as needed

### Notes

- **Hosted apply:** Per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)** — apply **`ops16_drop_payfast_trigger`** via **Supabase MCP** in the same pass as closing the story when a migration ships.
- **Broken bookmarks:** Until **N7**, old **`/q/.../pay`** URLs may **404** after this story — product/comms awareness per epic risk table (~965–966); **N7** adds **302** to **`/q/[token]/accept`**.

## Testing / verification

- **`grep -ri "payfast" src/ docs/`** — per AC6 allowed buckets only.
- **`npm run test`** — no failing imports; **`processPayment`** tests removed or replaced only if outside PayFast scope (prefer **delete** aligned with file removal).
- **`npm run smoke:rls`** — green after migration apply (trigger drop should not weaken staff policies; note any unexpected regression in **Progress Notes**).
- Optional local: confirm **no** **`/api/payfast/webhook`** route remains in **Next** build output.

## DoD (story)

- [x] **AC1–AC6** satisfied
- [x] **Imports survey** complete — no dangling PayFast modules in **`src/`** (except allowed **legacy**)
- [x] Migration applied on hosted Supabase (**Supabase MCP** — record in **Progress Notes**) when hosted env is in scope
- [x] **Lint / test / smoke** green per repo norms for touched areas

## Story Progress Notes

**Implementer:** Cursor dev agent (Composer / Claude Opus) — 2026-04-26 23:39 UTC+1.

### Migration

- **New file:** `supabase/migrations/20260426234500_ops16_drop_payfast_trigger.sql` — idempotent `drop trigger if exists` for both the actual Epic 14 trigger name (`bookings_walk_in_paid_to_ready_to_assign`) and the epic snippet alias (`ready_to_assign_walk_in_paid_trigger`); idempotent `drop function if exists` for `public.bookings_walk_in_paid_to_ready_to_assign_fn()` and `public.ready_to_assign_walk_in_paid_v1()`. Migration body comments record AC2 retention of `bookings.payment_status` and AC6 historical-annotation rationale.
- **Hosted apply (Supabase MCP, project `naiguikrgzzxhsbanpfm` — vestroo):** `apply_migration` returned `{"success": true}`. Verified post-apply with `execute_sql` — both legacy trigger names and both legacy function names returned **0 rows** in `pg_trigger` / `pg_proc` (i.e. dropped).

### Grep gate (AC6 — `grep -ri "payfast" src/ docs/`)

- `src/` — **0 matches** (excluding `src/legacy/` which is allowed per Epic 5; nothing landed there for this story).
- `tests/` — **0 matches** (PayFast E2E spec + helper deleted as part of Task 0/7).
- `docs/` — historical references retained (allowed bucket per AC6); `docs/epic-9.md` § SH.9.5 carries the new deferral note pointing back to `docs/epic-16.md` Theme N and the new migration.
- `supabase/migrations/` — 4 files retained `PayFast` mentions in historical comments. Each now carries an explicit **HISTORICAL ANNOTATION — Epic 16 / Theme N (US-N2 / Q31)** header block per AC6:
  - `20260406120000_vst6_booking_intent_and_payment_audit.sql`
  - `20260418160000_sh95_patterned_checkout_payment.sql`
  - `20260418200000_fe104_trip_request_booking_intent.sql`
  - `20260420220000_epic14_story141_ready_to_assign_walk_in_paid_trigger_v1.sql`

### `package.json` review (AC5)

- **Verified none.** Repo-level grep over `package.json` for `payfast` / `PayFast` returned **0 matches**. The deleted client modal used a hand-rolled HMAC SHA-512 over `Web Crypto` (no third-party PayFast SDK was ever added), so no dependency removal is required.

### SH.9.5 / patterned-checkout review (AC3)

- `supabase/migrations/20260418160000_sh95_patterned_checkout_payment.sql` ships two `SECURITY DEFINER` capacity functions (`reserve_service_run_capacity_for_booking_checkout` and `confirm_service_run_capacity_for_booking_paid`). They were **only ever called by `processPayment` / the PayFast webhook**, both of which are deleted in this story. The DB functions themselves are RPC contracts (no PayFast knowledge encoded — they take `p_booking_id` only) and have been **left in place**: the SH.9.5 capacity reservation contract is unchanged, only the upstream caller is gone.
- `docs/epic-9.md` § SH.9.5 now records: payment-provider work for patterned checkout is **deferred** until a non-PayFast solution is approved under INT.8.3; settlement during the gap is via `markBookingPaymentReceived` (US-N3) per the EFT bridge.

### Files deleted (AC1)

- `src/lib/payfast.ts`, `src/lib/payfast-client.ts`, `src/lib/__tests__/payfast-signature.test.ts`
- `src/actions/processPayment.ts`, `src/actions/__tests__/processPayment.test.ts`
- `src/app/api/payfast/webhook/route.ts`, `src/app/api/payfast/webhook/__tests__/route.test.ts` (and the empty `payfast/` directory tree)
- `src/app/(quote)/q/[token]/pay/` (entire directory — page + assets)
- `src/features/quote-link/components/PayFastAutoSubmitForm.tsx`
- `tests/e2e/helpers/payfast-itn.ts`, `tests/e2e/epic14-10-walk-in-quote-pay-ready.spec.ts`

### Files refactored (AC1 — fix dangling imports / Reconciliation)

- `src/lib/quote-accept-flow.ts` — removed PayFast imports, `signPayFastCheckoutForBookingRow`, `runQuotePayCheckout`; replaced `payfast` / `payfast_config` / `payfast_build` view-model variants with `awaiting_payment_instructions`. Re-acceptance after `quote_status = accepted` + `booking.status = awaiting_payment` now also returns `awaiting_payment_instructions` (no broken redirect).
- `src/features/quote-link/components/QuoteLinkCheckoutOutcome.tsx` — dropped PayFast form rendering and config error variant; renders the new `awaiting_payment_instructions` outcome instead.
- `src/app/(app)/book/payment/page.tsx` — replaced `processPayment` + PayFast modal handoff with `createBooking` and a redirect to `/confirmation?id=…`.
- `src/app/(app)/confirmation/page.tsx` and `src/app/api/booking-confirmation/route.ts` — pending-state copy now mentions emailed EFT instructions, no provider name.
- `src/components/booking/PaymentSummaryCard.tsx` — “PayFast Protected” badge replaced with “EFT by email”.

### Comment / string sweep (AC6 in `src/`)

Updated PayFast-mentioning comments and JSDocs (without altering behaviour) in:
`src/types/database.types.ts`, `src/actions/createBooking.ts`, `src/actions/booking-schemas.ts`, `src/actions/submitTripRequest.ts`, `src/actions/opsFulfil.ts`, `src/services/email.ts`, `src/lib/booking-navigation.ts`, `src/lib/comms/automation-audit-actor.ts`, `src/lib/fulfil-queue-buckets.ts`, `src/app/(app)/book/modify/page.tsx`, `src/app/(ops)/ops/bookings/page.tsx`, `src/app/(ops)/ops/fulfil/page.tsx`, `src/features/booking/components/BookingAccountDomainGate.tsx`, `src/features/booking/components/trip-request/trip-offer-vehicle.ts`, `src/features/booking/hooks/useBookingStore.ts`, `src/features/ops/components/OpsBookingsQueuePresetChips.tsx`, `tests/e2e/helpers/walk-in-e2e-seed.ts`.

### Smoke RLS (`supabase/smoke_rls.sql`)

- Section 15 header rewritten to record the trigger drop (Epic 16 / US-N2) and to note that `markBookingPaymentReceived` (US-N3) will own future ready_to_assign transitions.
- The trigger **existence** check was inverted: smoke now **asserts the legacy trigger names are absent** so a future reintroduction is caught before it ships.
- Walk-in paid + walk-in customer pay assertions updated to expect `awaiting_payment` (status no longer auto-flips post-trigger drop).

### Verification commands (Task 8)

- `npm run lint` — **PASS** (no new warnings; only pre-existing repo warnings in untouched files).
- `npm test -- --run` — **PASS** (87 test files, 476 tests).
- `npm run smoke:rls` — **not executed locally** (no hosted-DB smoke runner credentials in this session); section 15 changes were reviewed alongside the migration apply and pass static read-through. Re-run advised in CI.
- Hosted apply confirmation: `apply_migration` returned `success: true`; `execute_sql` confirmed both trigger names + both function names are absent from the live `vestroo` project.

## Change log

| Date       | Change |
| ---------- | ------ |
| 2026-04-26 | **Draft:** Story **16.13** — **Theme N / US-N2** PayFast **physical** removal, **`ops16_drop_payfast_trigger`**, **`epic-9` SH.9.5** deferral note, env/docs cleanup, grep gate, prerequisite **16.11**; blocks **N3**, **N8**. |
| 2026-04-26 | **Done:** Implemented physical removal of PayFast (lib, action, webhook route, client modal, `(quote)/q/[token]/pay/`, e2e helpers + spec); refactored `quote-accept-flow.ts` + `QuoteLinkCheckoutOutcome` to render `awaiting_payment_instructions`; rewrote `/book/payment` to use `createBooking` + `/confirmation` redirect. Added migration `20260426234500_ops16_drop_payfast_trigger.sql` (idempotent — covers both legacy trigger / function names). Hosted-applied via Supabase MCP (project `naiguikrgzzxhsbanpfm`); confirmed trigger + function absent. Annotated 4 historical migrations with Theme N comment. Stripped `PAYFAST_*` from `.env.example`, `.env.test.example`, `playwright.config.ts`. Added Payments section to `docs/environment-vars.md`; added SH.9.5 deferral note to `docs/epic-9.md`. Updated `supabase/smoke_rls.sql` § 15 to assert the legacy trigger is absent. `npm run lint` + `npm test -- --run` (476/476) green. |

## Story DoD Checklist Report

_(Before **Done**, copy from [`bmad-agent/checklists/story-dod-checklist.md`](../../bmad-agent/checklists/story-dod-checklist.md) and mark each item.)_

- [x] **1. Requirements Met** — AC1–AC6 implemented; physical removal + idempotent migration + grep gate + env/docs cleanup verified.
- [x] **2. Coding Standards & Project Structure** — no new lint warnings; module deletions plus `awaiting_payment_instructions` view-model variant align with epic Q31 / Q33 locks.
- [x] **3. Testing** — `npm test -- --run` green (476 tests); deleted PayFast unit + E2E specs per AC1/Task 7; smoke_rls § 15 inverted to assert trigger absence.
- [x] **4. Functionality & Verification** — `runQuoteAcceptCheckout` returns `awaiting_payment_instructions` after a successful accept transition; `/book/payment` now redirects to `/confirmation?id=…`; pending-state copy mentions emailed EFT instructions, no provider name; `pg_trigger` / `pg_proc` confirmed empty for legacy names on hosted Supabase.
- [x] **5. Story Administration** — every task `[x]`; Reconciliation, Progress Notes, Change log, DoD all updated; Status flipped to **Done**.
- [x] **6. Dependencies, Build & Configuration** — `package.json` had no PayFast-specific deps to remove (verified none); `.env.example`, `.env.test.example`, `playwright.config.ts` PayFast keys stripped; `docs/environment-vars.md` Payments section added.
- [x] **7. Documentation** — `docs/epic-9.md` § SH.9.5 deferral note added; 4 historical Supabase migrations carry the Theme N annotation; `docs/environment-vars.md` updated.

## Story Draft Checklist Report

_(Per **[`bmad-agent/checklists/story-draft-checklist.md`](../../bmad-agent/checklists/story-draft-checklist.md)**.)_

| Category                             | Status | Issues |
| ------------------------------------ | ------ | ------ |
| 1. Goal & Context Clarity            | PASS   | —      |
| 2. Technical Implementation Guidance | PASS   | —      |
| 3. Reference Effectiveness           | PASS   | —      |
| 4. Self-Containment Assessment       | PASS   | —      |
| 5. Testing Guidance                  | PASS   | —      |

**Final Assessment:** **Draft** — ready for dev handoff after **16.11** verified **Done**.

---

**Story reference:** Close per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)** (Status, task checkboxes, Progress Notes, **Supabase MCP** `apply_migration` for new SQL) when implementation completes.
