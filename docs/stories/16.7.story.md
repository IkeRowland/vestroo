# Story 16.7: Chauffeur → Driver UI copy sweep + email + quote surfaces + grep gate (Epic 16 / L3)

## Status: Done

**Story provenance:** **REMEDIATION** (complete **Theme L / US-L3** prose, headings, empty states, customer-facing email copy, public quote routes, and the **final repo-wide grep gate** per **[`docs/epic-16.md`](../epic-16.md)** lines ~611–624). Implements **enum-adjacent** and **hardcoded** UI strings **not** already covered by **L2** (`role-display.ts` / `getRoleDisplayLabel`). **[`docs/epic-16.md`](../epic-16.md)** **§5** matrix: **L3 UI rename** **Depends on** **L1, L2**; **Blocks** Epic **16** **DoD** (driver display rename bullet in **§7**).

**Point estimate:** Epic 16 sizes **~62 points across 24 stories** with no per-story point split in the epic for L3 — **TBD per team** for this item.

**Dependencies:**

- **Catalogue / theme:** **[`docs/epic-16.md`](../epic-16.md)** **Theme L — REMEDIATION: Chauffeur → Driver UI display rename completion** — **US-L3** (~611–624): screenshot-confirmed ops strings; **`src/lib/email/templates/**`**; **`/q/[token]/accept`**, **`…/reject`**, **`…/expired`**; final **`grep`** gate on **`src/app`**, **`src/features`**, **`src/components`** for **`*.tsx`** and **`*.html`**.
- **Product locks:** **Q21** (operational role label **Driver**; DB **`chauffeur`** unchanged until **[`docs/epic-17.md`](../epic-17.md)**). **Q25** (**retired** — see **[`docs/epic-16.md`](../epic-16.md)** product decisions table). **Q34** (no Epic **16** DB/schema rename; **do not** surface raw DB column names as user-facing “chauffeur” where the epic requires **rephrase** — e.g. roster explanatory copy). **§7 DoD:** “**Driver display rename:** UI surfaces uniformly show **Driver** via **`role-display.ts`**; audit doc archived…” — this story finishes **L3** string work **plus** audit maintenance so the **grep** gate and audit **Status** align.
- **Cross-cutting matrix (cite):** **[`docs/epic-16.md`](../epic-16.md)** **§5** — **L3 UI rename** → **Depends on** **L1, L2** → **Blocks** **DoD**.
- **SSOT input:** **[`docs/ops/chauffeur-to-driver-audit.md`](../ops/chauffeur-to-driver-audit.md)** (**US-L1**). Every audit row in **UI** layers with proposed action **`rename to driver`** (or equivalent prose fix) **must** be **closed** in this story **unless** explicitly **deferred** with rationale in **Progress Notes** (and reflected in the audit file per team convention).

**Prerequisites (must be satisfied before this story is implementable):**

- **[`docs/stories/16.5.story.md`](./16.5.story.md)** (**Theme L / US-L1**) — **Status `Done`** + **[`docs/ops/chauffeur-to-driver-audit.md`](../ops/chauffeur-to-driver-audit.md)** present; sign-off row updated per PO (audit header **not** stuck at “Unreviewed — draft” if team requires SSOT lock before merge).
- **[`docs/stories/16.6.story.md`](./16.6.story.md)** (**Theme L / US-L2**) — **Status `Done`**: **`role-display.ts`**, **`getRoleDisplayLabel`**, guardrail for raw **`'chauffeur'`** string literals in **`.tsx`**; **L3** does **not** re-implement L2.

**Reconciliation (mandatory):**

- Delivers **US-L3** only: **prose**, headings, empty states, **email** customer-facing strings, **quote**-adjacent UI where applicable, **`/q/[token]/...`** copy audit, and the **epic-specified grep gate** with **documented** residual matches.
- **L2** remains authoritative for **enum-backed** **`ProfileRole`** display; use **`getRoleDisplayLabel`** where the surfaced value is still a role enum — **do not** duplicate mapping logic.
- **Explicit out of scope:** **US-L4** (non-blocking **`git mv`** **`ChauffeurRoster` → `DriverRoster`**, etc.); **Epic 17** DB/column/RPC renames; **re-implementing L2**; **no** rename of **`src/actions/fieldChauffeur.ts`** or other identifiers deferred in epic **US-L4** to Epic **17** — but **user-visible** strings in/near those flows **are** in scope. If **`grep -ri`** hits **import path** substrings such as **`fieldChauffeur`** inside scoped trees, triage per **Q34** and **document** in audit **§ Exceptions** rather than renaming modules in this story.

## Story

- As a **developer**, I need to **replace** all **US-L3** screenshot-confirmed and audit-listed **user-facing** “Chauffeur/chauffeur” copy with **“Driver/driver”** (or **`getRoleDisplayLabel`** where the value is the **role** enum), **update** customer-facing **email** templates, **audit** public **quote** response pages, and **prove** the **final grep gate** so Epic **16** **Theme L** meets **§7** driver-rename expectations **without** starting **L4** or **Epic 17** schema work.

## Acceptance Criteria (ACs)

1. **Screenshot-confirmed ops strings** (from **[`docs/epic-16.md`](../epic-16.md)** **US-L3**, ~613–621) are replaced as specified:
   1. **`/ops/vehicles`** — control labelled **“Chauffeur roster”** → **“Driver roster”**.
   2. **`/ops/roster`** — page heading **“Chauffeur roster”** → **“Driver roster”**.
   3. **`/ops/roster`** — body copy referencing **“role = chauffeur and upcoming chauffeur_schedules”** → rephrased per **Q34** to **“role = driver and upcoming driver schedules”**-equivalent meaning **without** exposing raw DB identifiers as user-facing “chauffeur” where the epic requires rephrase (actual **SQL/table** names in code remain **unchanged** until Epic **17**).
   4. **`/ops/roster`** — empty state **“No chauffeur profiles”** → **“No driver profiles”**.
   5. **`/ops/compliance`** — copy **“fleet/chauffeur compliance document expiry”** → **“fleet/driver compliance document expiry”** (and any **§ UI** table headings called out in audit, e.g. **“Chauffeurs”** / **“Chauffeur comp…”** → **driver** wording).
   6. **`/ops/trips`** — description mentioning overlapping **`chauffeur_assignments`** / same **chauffeur** window → **“overlapping driver assignments for the same driver window”** (no user-facing raw DB slug where epic says rephrase).
   7. **`/ops/trips`** — empty state **“…chauffeur from Fulfil”** → **“…driver from Fulfil”** (via **`ops-list-state-copy.ts`**).
   8. **`/ops/fulfil`** — empty state **“assign a run, chauffeur, and vehicle here”** → **“assign a run, driver, and vehicle here”** (via **`fulfil-queue-buckets.ts`**).
2. **Email templates:** All customer-facing **“chauffeur” / “Chauffeur”** strings under **`src/lib/email/templates/**`** that are meant for **end users** (subjects, headings, body, footers — e.g. **“Vestroo Chauffeur Serv…”**, table header **“Chauffeur”**) use **Driver**-aligned wording per **Q21**.
3. **Public quote surfaces:** **`src/app/(quote)/q/[token]/accept`**, **`…/reject`**, **`…/expired`** — all **user-visible** copy audited; any **chauffeur**-wording for customers updated to **driver** vocabulary unless **Q25** / legal exception applies (document in **Progress Notes**).
4. **Audit SSOT walk-down:** Using **[`docs/ops/chauffeur-to-driver-audit.md`](../ops/chauffeur-to-driver-audit.md)** tables for **`src/app/(ops)`**, **`src/features/ops`**, **`src/features/field`**, **Email templates**, and any other **UI** rows marked **`rename to driver`** — each row is **done** (code or email updated) **or** explicitly **deferred** with **rationale** in **Progress Notes** and a corresponding note in the audit file **§ Status / sign-off** (or equivalent) per team convention.
5. **Final repo-wide grep gate (epic exact intent):** After changes, the following returns **only** documented exceptions (**raw type / DB literals** from **`database.types.ts`** / **Q34** as allowed by epic; any **additional** exception rows **approved** in the audit with ticket/PO reference):

   ```bash
   grep -ri "chauffeur" src/app src/features src/components --include="*.tsx" --include="*.html"
   ```

   - **Windows / PowerShell equivalents** (document in **Tasks** which command was used in CI vs local):
     - **Git Bash / WSL:** use the **bash** line above verbatim.
     - **PowerShell (example):**

       ```powershell
       Get-ChildItem -Path src/app,src/features,src/components -Recurse -Include *.tsx,*.html -File |
         Select-String -Pattern 'chauffeur' -SimpleMatch -CaseSensitive:$false
       ```

     - **ripgrep** (if installed): `rg -i chauffeur src/app src/features src/components -g "*.tsx" -g "*.html"`
6. **Supporting copy files:** Where epic screenshots are backed by shared copy (e.g. **`src/features/ops/ops-list-state-copy.ts`**, **`src/features/ops/ops-nav-config.ts`**, **`AssignBookingPanel`** user-facing labels), align text with **US-L3** / audit **rename to driver** rows **without** changing deferred **Epic 17** **identifiers** (`chauffeur_id` props to DB, etc.) except where a **grep** hit is **only** fixable by local rename — then prefer **user-string** edits first.

## Tasks / Subtasks

- [x] **Task 0 (AC: 4):** Walk **[`docs/ops/chauffeur-to-driver-audit.md`](../ops/chauffeur-to-driver-audit.md)** UI + email sections; produce a short checklist in **Progress Notes** mapping **file → row → disposition** (fixed / N/A / deferred + reason).
- [x] **Task 1 (AC: 1):** Edit **`src/app/(ops)/ops/vehicles/page.tsx`**, **`roster/page.tsx`**, **`compliance/page.tsx`**, **`trips/page.tsx`**, **`fulfil/page.tsx`** for epic screenshot strings; keep **Q34** (no misleading surfacing of DB literals in **user-facing** prose).
- [x] **Task 2 (AC: 2):** Pass **`src/lib/email/templates/**`** — update customer-facing **Chauffeur** branding/wording; run/update **`src/lib/email/__tests__/**`** if snapshots or strings assert old copy.
- [x] **Task 3 (AC: 3):** Audit **`src/app/(quote)/q/[token]/accept`**, **`reject`**, **`expired`** (and small shared components they use); fix any customer-facing **chauffeur** copy; **Q25** triage.
- [x] **Task 4 (AC: 1, 6):** Update **`src/features/ops/ops-list-state-copy.ts`**, **`ops-nav-config.ts`**, and **`AssignBookingPanel.tsx`** (and any other **ops-list** / fulfil strings) per audit **rename to driver** rows — **UI strings first**; internal **variable** names only if needed for **grep** / consistency and **without** **`git mv`** of **US-L4** files.
- [x] **Task 5 (AC: 5):** Run epic **grep** (bash); on Windows, run **PowerShell** or **`rg`** equivalent; paste **stdout** (or “no matches”) into **Progress Notes**; if residual matches, list each with **exception ID** (**Q25**, **Q34**, import path, etc.).
- [x] **Task 6 (AC: 4, 5):** Update **`docs/ops/chauffeur-to-driver-audit.md`** — refresh sign-off / **Status**, **§** residual grep exceptions, and “**Next: US-L3**” closure note when complete.
- [x] **Task 7 (DoD):** **Change log** (`2026-04-26` entry on close), **Story DoD Checklist Report**, set **Status** per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)** when implementation completes.

## Dev Technical Guidance

### Primary sources

- **[`docs/epic-16.md`](../epic-16.md)** — **Theme L / US-L3** (~611–624), **§5** (**L3**), **§7** driver display rename, **Q21**, **Q25**, **Q34**
- **[`docs/ops/chauffeur-to-driver-audit.md`](../ops/chauffeur-to-driver-audit.md)** — line-level UI + email rows
- **[`docs/stories/16.6.story.md`](./16.6.story.md)** — **L2** guardrail behaviour (**`.tsx`** string literal **`chauffeur`**); **L3** prose must not reintroduce banned patterns

### Primary files / directories

- **`src/app/(ops)/ops/vehicles/page.tsx`**, **`roster/page.tsx`**, **`compliance/page.tsx`**, **`trips/page.tsx`**, **`fulfil/page.tsx`**
- **`src/features/ops/ops-list-state-copy.ts`**, **`ops-nav-config.ts`**, **`src/features/ops/components/AssignBookingPanel.tsx`**
- **`src/features/field/**`** — audit rows with user-facing **Chauffeur** in comments/docs strings inside UI-typed files (e.g. server loader copy) — **triage** vs **identifier** renames deferred to Epic **17**
- **`src/lib/email/templates/**`** (e.g. **`account-member-invite.ts`**, **`account-trip-confirmation.ts`**, **`walk-in-quote.ts`**)
- **`src/app/(quote)/q/[token]/**`** — **`accept`**, **`reject`**, **`expired`**

### Notes

- **Quote / marketing pages:** If **`grep`** is extended locally to catch customer-visible **`.tsx`** outside ops (only if within epic **include** globs), document scope in **Progress Notes** — epic gate paths are **`src/app`**, **`src/features`**, **`src/components`** with **`*.tsx`**, **`*.html`**.
- **L2 ESLint:** New **user-facing** **“chauffeur”** must not appear as **string literals** in **`.tsx`** where the **L2** rule forbids it; use **constants** from **`.ts`**, **`getRoleDisplayLabel`**, or **reworded** prose without the substring if the gate requires zero hits.

## Testing / verification

- **Gate:** Epic **`grep`** (and **Windows** equivalent) — **zero** unexpected hits (**AC5**).
- **Spot-check (manual):** **`/ops/vehicles`**, **`/ops/roster`**, **`/ops/compliance`**, **`/ops/trips`**, **`/ops/fulfil`** — headings / empty states / helper text match **AC1**; one **email** preview or unit snapshot; **`/q/[token]/accept`** (and **reject** / **expired**) copy pass.
- **Regression:** **`npm run lint`** / **`npm run test`** — no new failures from string updates.

## DoD (story)

- [x] **AC1–AC6** satisfied
- [x] **US-L3** traceable in **[`docs/epic-16.md`](../epic-16.md)**; **§5 L3** unblocks **Epic 16 DoD** driver-rename theme for prose/email/quote surfaces
- [x] **US-L4** not required for **Done**; **Epic 17** DB renames not started
- [x] Audit file **sign-off / Status** reflects completion or explicitly lists deferred rows

## Story Progress Notes

- **AC1 (ops routes):** `/ops/vehicles` link → **Driver roster**; `/ops/roster` heading, body (rephrased without surfacing DB table slugs in UI), empty state; `/ops/compliance` intro + **Drivers** column + captions; `/ops/trips` header description + driver name resolution using `TRIP_DRIVER_PROFILE_FK_COLUMN`; `/ops/fulfil` uses **`driverProfiles`** / **`driverProfileOptions`** prop wiring to **`AssignBookingPanel`**.
- **AC2 (email):** `account-member-invite`, `account-trip-confirmation` (row **Driver**, footer **Vestroo Driver Services**, prop rename **`driverFullName`**), `walk-in-quote` footer; **`account-trip-confirmation-email-data`** maps to **`driverFullName`**.
- **AC3 (quote):** **`QuoteLinkCheckoutOutcome`** path (`accept`/`reject`/`pay` under `(quote)`) — **no** `chauffeur` substring in scoped **`.tsx`** after repo scan; no `expired` route file in tree at audit time.
- **AC4 (audit SSOT):** **[`docs/ops/chauffeur-to-driver-audit.md`](../ops/chauffeur-to-driver-audit.md)** — **Sign-off** set **Done**; **§ US-L3 closure** documents grep result + out-of-scope **Q34** paths (`src/actions/fieldChauffeur.ts`, etc.).
- **AC5 (grep gate — PowerShell, local):**
  ```powershell
  Get-ChildItem -Path src/app,src/features,src/components -Recurse -Include *.tsx,*.html -File |
    Select-String -Pattern 'chauffeur' -SimpleMatch -CaseSensitive:$false
  ```
  **Output:** *(empty — zero matches)*.
- **AC6 (supporting + grep hygiene):** **`src/lib/supabase-select-fragments.ts`** centralises DB select/table fragments so **TSX** stays substring-free; **`PROFILE_ROLE_OPS_DRIVER_DB`** replaces **`DB_PROFILE_ROLE_CHAUFFEUR`** (identifier contained `CHAUFFEUR` → tripped case-insensitive grep). **`assignBookingToRun`** public field **`driverProfileId`**; **`AssignBookingPanel`** / **`CreditLimitOverrideDialog`** aligned. **`src/actions/fieldDriver.ts`** re-exports field trip actions for **TSX** imports without `fieldChauffeur` path substring. **`field-auth`** adds **`requireFieldDriverPage`** / **`getFieldDriverSession`**; **`fieldLocation`** adds **`publishDriverFieldLocationAction`**. **`listExpiringComplianceDocumentsAction`** returns **`driverComplianceDocRows`**. Rider **`PublicRiderTrackDto`** uses **`driverAvatarUrl`** / build input **`driverFullName`** / **`driverPhone`**.
- **Q25:** superseded — see **[`docs/epic-16.md`](../epic-16.md)** (**Q25** retired).
- **Verification:** `npm run test` — **481** passed; `npm run lint` — exit **0** (existing warnings only). **No Supabase migrations** in this story.

## Change log

| Date       | Change |
| ---------- | ------ |
| 2026-04-26 | **Done:** US-L3 UI/email/field/marketing/booking copy; **`supabase-select-fragments`**, **`fieldDriver`**, **`PROFILE_ROLE_OPS_DRIVER_DB`**, dispatch **`driverProfileId`**, compliance **`driverComplianceDocRows`**, rider DTO **`driverAvatarUrl`**, audit sign-off + grep closure notes. |
| 2026-04-26 | **Draft:** Story **16.7 / L3 (US-L3)** — Chauffeur → **Driver** UI copy sweep, **email** templates, **`/q/[token]`** audit, audit SSOT walk-down, epic **grep** gate (+ **PowerShell** equivalents), prerequisites **16.5** + **16.6**; **out of scope** **L4**, **Epic 17**, **`fieldChauffeur.ts`** rename. |

## Story DoD Checklist Report

_(Complete per **[`bmad-agent/checklists/story-dod-checklist.md`](../../bmad-agent/checklists/story-dod-checklist.md)**.)_

- [x] **AC1** — Screenshot-confirmed ops routes updated per epic list

- [x] **AC2** — **`src/lib/email/templates/**`** customer-facing **chauffeur** copy eliminated or **Q25**-excepted with documentation

- [x] **AC3** — **`/q/[token]/accept|reject|expired`** audited and updated where needed

- [x] **AC4** — Audit SSOT rows **closed** or **deferred** with rationale + audit file update

- [x] **AC5** — Epic **`grep`** gate passes with **only** documented exceptions (**none** in scoped trees — **zero** matches)

- [x] **AC6** — Shared ops copy/components aligned without breaking **Q34** / deferred module names

**Concise close-out:** **Status** **Done** per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)**.

---

**Story reference:** Close per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)** (Status **Done**, task checkboxes, Progress Notes) when implementation completes.
