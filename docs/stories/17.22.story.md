# Story 17.22: Ops referrer tracking + Finance Referrals

## Status: Done

## Summary

MVP referrer attribution for ops-created bookings and finance visibility into referred volume.

## Tasks

- [x] Remove Booking outcomes overview band from `/ops/bookings`
- [x] Add Referrer column to bookings queue
- [x] Ops New Booking sheet (portal trip-request flow + optional referrer)
- [x] `referrers` table + `bookings.referrer_id` + `trips.referrer_id` (propagate on assign)
- [x] Finance → Referrals page (`/ops/finance/referrals`) with referrer CRUD + referred bookings list
- [x] Hosted migration via Supabase MCP

## Story Progress Notes

- **Migration applied (hosted vestroo):** `referrers_and_booking_referrer_tracking` — `referrers`, `bookings.referrer_id`, `trips.referrer_id`, staff RLS.
- **Referrer on create:** Ops **New booking** → `submitOpsTripRequest` validates active referrer → `insertTripRequestBooking` sets `bookings.referrer_id`. Trip assignment copies `referrer_id` in `assignBookingToRun` (`opsDispatch.ts`).
- **Docs:** `docs/referrals-compensation-mvp.md` (commission/payout recommendations).
- **Tests:** `npm run lint`; targeted tests on touched booking actions/components.
