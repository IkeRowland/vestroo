# Story 16.8: Chauffeur → Driver low-risk component filename rename (`ChauffeurRoster` → `DriverRoster`) (Epic 16 / L4)

## Status: Done

**Story provenance:** **REMEDIATION** — completes **Theme L / US-L4** (optional, **non-blocking** for Epic **16** DoD): a single low-risk **`git mv`** rename **`src/features/ops/components/ChauffeurRoster.tsx` → `DriverRoster.tsx`** **only if that file exists** at implementation time (per **[`docs/epic-16.md`](../epic-16.md)** **US-L4**, ~626–630; **US-L1** audit may show roster UI inline in **`/ops/roster/page.tsx`** with no separate component). **Out of scope:** **US-L3** prose sweep except where a rename **forces** import-path updates only; **Epic 17** module/schema renames; **any** file beyond this **one** epic-named path.

**Point estimate:** Epic **16** sizes **~62 points across 24 stories** with no per-story point split in the epic for **L4** — **TBD per team** for this item.

**Dependencies:**

- **Catalogue / theme:** **[`docs/epic-16.md`](../epic-16.md)** **Theme L — REMEDIATION: Chauffeur → Driver UI display rename completion** — **US-L4** (~626–630): optional filename rename; deferred server/helper identifiers to **[`docs/epic-17.md`](../epic-17.md)**.
- **Product locks:** **Q21** (UI label **Driver**; DB **`chauffeur`** unchanged until Epic **17**). **Q34** (no Epic **16** DB/schema rename). **§7 DoD:** “**Driver display rename:** … **deferred filename renames listed for Epic 17**” — this story either performs the **single** in-scope **`git mv`** (if present) **or** records **N/A** in **Progress Notes**, and **always** extends the audit doc **§ Future Work** (or equivalent) per **AC** below.
- **Cross-cutting matrix (cite):** **[`docs/epic-16.md`](../epic-16.md)** **§5** — **L4 filename rename (non-blocking)** → **Depends on** **L1** → **Blocks** nothing; **L3 UI rename** → **Depends on** **L1, L2** → **Blocks** DoD — **L4** is **not** on the **L3 → DoD** blocking edge; epic Phase **7** lists **L4** as parallel “filename rename pass” but explicitly **non-blocking** (see **§6** risk row: *L4 is non-blocking and explicitly deferred to clean merge windows*).
- **SSOT input:** **[`docs/ops/chauffeur-to-driver-audit.md`](../ops/chauffeur-to-driver-audit.md)** (**US-L1** / L3 closure) — confirm whether **`ChauffeurRoster.tsx`** exists before rename; if absent, close story as **N/A** without inventing a new component file.

**Prerequisites (must be satisfied before this story is implementable):**

- **[`docs/stories/16.5.story.md`](./16.5.story.md)** (**Theme L / US-L1**) — **Status `Done`**: audit matrix present; **§5** row **L4 filename rename** **Depends on** **L1** satisfied.

**Recommended sequencing (not a hard matrix dependency):**

- **[`docs/stories/16.7.story.md`](./16.7.story.md)** (**Theme L / US-L3**) — **Status `Done`** before starting **L4** is **recommended** to reduce merge churn on the same **`src/app/(ops)`** / **`src/features/ops`** paths; epic **§5** does **not** list **L3** as a dependency of **L4**.

**Reconciliation (mandatory):**

- Delivers **US-L4** only: **one** optional **`git mv`** + import updates **if** **`ChauffeurRoster.tsx`** exists; otherwise **N/A** closure documented.
- **Explicit out of scope (epic + story):** **`src/actions/fieldChauffeur.ts`**, **`src/lib/chauffeur-trip-transitions.ts`**, **`src/lib/resolve-chauffeur-assignment.ts`**, **`getChauffeurForAction`** — **Epic 17**; do **not** rename these in this story.
- **Explicit out of scope:** **US-L3** customer-facing prose sweep **unless** the only edits are **import paths / default export names** strictly required by the **`DriverRoster`** rename.
- **No** broad **`grep`-driven** renames beyond the **single** component file named in the epic.

## Story

- As a **developer**, I need to **optionally** rename **`ChauffeurRoster.tsx`** → **`DriverRoster.tsx`** with **`git mv`** (preserving history), **update all imports** and any default export references, run **lint/tests**, and **document** deferred **Epic 17** renames in **`docs/ops/chauffeur-to-driver-audit.md`** so Epic **16** **Theme L** **L4** is satisfied or explicitly **N/A** without starting schema or server-action renames.

## Acceptance Criteria (ACs)

1. **Pre-check (branching):** Before any code move, verify whether **`src/features/ops/components/ChauffeurRoster.tsx`** exists (e.g. `git ls-files` / filesystem). **If the file is absent** → story **closes as N/A**: no new **`DriverRoster.tsx`** invented solely for this story; **Progress Notes** state audit/outcome (“inline roster” or equivalent) and **Tasks** checklist reflects skip.
2. **If the file exists:** Rename **`src/features/ops/components/ChauffeurRoster.tsx`** → **`src/features/ops/components/DriverRoster.tsx`** using **`git mv`** (not a delete+add) so **`git blame`** history follows the file.
3. **If the file exists:** All **imports**, **dynamic imports**, **re-export** sites, and **test** references that pointed at **`ChauffeurRoster`** / old path are updated to **`DriverRoster`** / new path; **default export** / **named export** convention matches existing project pattern after rename.
4. **Audit documentation:** Add a **§ Future Work** (or equivalent heading consistent with the audit file) in **`docs/ops/chauffeur-to-driver-audit.md`** listing **deferred** identifier renames explicitly called out in epic **US-L4** (**`fieldChauffeur.ts`**, **`chauffeur-trip-transitions.ts`**, **`resolve-chauffeur-assignment.ts`**, **`getChauffeurForAction`**, and any other epic-named deferrals the implementer finds) with a **pointer to [`docs/epic-17.md`](../epic-17.md)**. If **§ Future Work** already exists, **merge** without duplicating bullets.
5. **Quality:** **`npm run lint`** and **`npm run test`** pass after changes (or **N/A** branch: still run and confirm **no** diff-related failures — document in **Progress Notes** if baseline unchanged).

## Tasks / Subtasks

- [x] **Task 0 (AC: 1):** Confirm **`src/features/ops/components/ChauffeurRoster.tsx`** exists. **If no** → skip Tasks 1–3; complete **Task 4** + **Task 5**; set **Progress Notes** to **N/A (file absent)**; leave **Status** per dev-agent completion rules.
- [x] **Task 1 (AC: 2, 3):** **`git mv`** — **Skipped (N/A):** path not in `git ls-files` and not on disk; no rename performed.
- [x] **Task 2 (AC: 3):** **Skipped (N/A):** no `ChauffeurRoster` references in tree (`rg`); no import updates.
- [x] **Task 3 (AC: 5):** **`npm run lint`** (exit **0**; existing warnings only); **`npm run test`** (**481** passed, **89** files) — no code diff from this story.
- [x] **Task 4 (AC: 4):** Edit **`docs/ops/chauffeur-to-driver-audit.md`** — **§ Future Work** (or equivalent) with **Epic 17** pointer and deferred module/helper list per epic **US-L4**.
- [x] **Task 5 (DoD):** **Change log** (`2026-04-26` entry on close), **Story DoD Checklist Report**, set **Status** per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)** when implementation completes.

## Dev Technical Guidance

### Primary sources

- **[`docs/epic-16.md`](../epic-16.md)** — **Theme L / US-L4** (~626–630), **§5** (**L4** row), **§6** (L4 merge-churn risk), **§7** DoD driver-rename + deferred filenames, **§8** likely paths (**`DriverRoster.tsx`** as rename target)
- **[`docs/ops/chauffeur-to-driver-audit.md`](../ops/chauffeur-to-driver-audit.md)** — whether a **UI** row referenced **`ChauffeurRoster.tsx`**
- **[`docs/epic-17.md`](../epic-17.md)** — downstream consumer of deferred renames

### Primary files / directories

- **`src/features/ops/components/ChauffeurRoster.tsx`** → **`DriverRoster.tsx`** (conditional)
- **`src/app/(ops)/ops/roster/page.tsx`** — likely consumer **if** a standalone component existed; may be **inline-only** today
- **`docs/ops/chauffeur-to-driver-audit.md`** — **§ Future Work** addition

### Notes

- **Current repo snapshot (story authoring):** no **`ChauffeurRoster`** string hits in tree — implementer **must** still run **Task 0** at implementation time (branch may differ).
- **Do not** expand scope to create **`DriverRoster.tsx`** when no **`ChauffeurRoster.tsx`** exists.

## Testing / verification

- **If rename performed:** Typecheck/lint clean; **`npm run test`** green; quick manual **`/ops/roster`** smoke if that route imports the component.
- **If N/A:** Confirm **Task 4** audit update and **Progress Notes**; lint/test baseline unchanged or explained.

## DoD (story)

- [x] **AC1–AC5** satisfied (including **N/A** branch documented)
- [x] **US-L4** traceable in **[`docs/epic-16.md`](../epic-16.md)**; **Epic 17** boundaries respected
- [x] No **Epic 17** server-action / lib renames in this PR
- [x] Audit **§ Future Work** lists deferred items with **Epic 17** pointer

## Story Progress Notes

- **US-L4 / L4 outcome: N/A (file absent).** **Task 0:** `git ls-files src/features/ops/components/ChauffeurRoster.tsx` → empty; `Test-Path` → false. Roster UI is **inline** in `src/app/(ops)/ops/roster/page.tsx` (per audit **US-L1** layer table); no standalone `ChauffeurRoster.tsx` / `DriverRoster.tsx` to rename — per story, **no** new `DriverRoster.tsx` invented.
- **Task 2:** `rg ChauffeurRoster` → no matches.
- **Task 3:** `npm run lint` → exit **0** (pre-existing warnings in compliance/trips pages, tabs, VehicleOptionCard, sms.test); `npm run test` (vitest) → **481** tests passed.
- **Task 4:** Added **`## Future work (Epic 17)`** to **`docs/ops/chauffeur-to-driver-audit.md`** with US-L4 deferrals + **16.8 N/A** note (merged with existing **Next: follow-on**; no duplicate epic bullets).
- **Agent:** Composer (Cursor) implementation pass, 2026-04-26.

## Change log

| Date       | Change |
| ---------- | ------ |
| 2026-04-26 | **Draft:** Story **16.8 / L4 (US-L4)** — optional **`ChauffeurRoster` → `DriverRoster`** **`git mv`**, import updates, lint/test, audit **§ Future Work** + **Epic 17** deferrals; prerequisites **16.5**; recommended after **16.7**; **N/A** if file absent. |
| 2026-04-26 | **Done:** **N/A** — `ChauffeurRoster.tsx` absent; audit **`## Future work (Epic 17)`** added; lint/test verified. |

## Story DoD Checklist Report

_(Complete per **[`bmad-agent/checklists/story-dod-checklist.md`](../../bmad-agent/checklists/story-dod-checklist.md)** when closing.)_

- [x] **AC1** — File existence checked; **N/A** branch documented if absent

- [x] **AC2** — **`git mv`** used when file exists (**N/A:** file absent)

- [x] **AC3** — Imports/tests updated when rename performed (**N/A:** no rename)

- [x] **AC4** — **`docs/ops/chauffeur-to-driver-audit.md`** **§ Future Work** (or equivalent) lists **Epic 17** deferred renames

- [x] **AC5** — **`npm run lint`** / **`npm run test`** verified

**Concise close-out:** **Status `Done`** per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)** (2026-04-26).

### Story DoD checklist (bmad `story-dod-checklist.md`)

1. **Requirements:** [x] Story ACs met (**N/A** path for L4 rename; audit + verification).
2. **Coding standards:** [x] No app code changed; no new lint issues from this story.
3. **Testing:** [x] Full vitest suite green; no new tests required for doc-only / skip path.
4. **Verification:** [x] Pre-check + grep + lint/test executed.
5. **Story administration:** [x] Tasks, Progress Notes, changelog updated.
6. **Dependencies / build:** [x] Lint exit 0; tests pass; no new deps.
7. **Documentation:** [x] Audit **Future work** updated per AC4.

## Story Draft Checklist Report

_(Per **[`bmad-agent/checklists/story-draft-checklist.md`](../../bmad-agent/checklists/story-draft-checklist.md)**.)_

| Category                             | Status | Issues |
| ------------------------------------ | ------ | ------ |
| 1. Goal & Context Clarity            | PASS   | —      |
| 2. Technical Implementation Guidance | PASS   | —      |
| 3. Reference Effectiveness           | PASS   | —      |
| 4. Self-Containment Assessment       | PASS   | —      |
| 5. Testing Guidance                  | PASS   | —      |

**Final Assessment:** **READY** — story is actionable for Dev Agent (including **N/A** closure path).

---

**Story reference:** Close per **[`.cursor/rules/story-completion-dev-agent.mdc`](../../.cursor/rules/story-completion-dev-agent.mdc)** (Status, task checkboxes, Progress Notes) when implementation completes.
