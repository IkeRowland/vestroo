# Story 8.1: Email template parity — Mailer / Handlebars → Vestroo (INT.8.1)

## Status: Done

**Dependencies:** **[`docs/epic-8.md`](../epic-8.md)** — **INT.8.1** source (inventory reference **email templates** + **send triggers**; map to **Vestroo** template, **trigger site**, **status**; **PII** per **VST-12**; missing items → **backlog** + **priority**). **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** (VST-13) — existing **Resend** matrix row and **per-environment** email notes (**grep** **`Resend`** / **`src/services/email`**). **[`docs/environment-vars.md`](../environment-vars.md)** — **`RESEND_API_KEY`**, **`RESEND_FROM_EMAIL`** (server-only; align names with **`.env.example`** — **verify**). **[`docs/compliance-and-safety.md`](../compliance-and-safety.md)** (**VST-12**) — **minimisation**, **third parties**, **DSR** boundaries; **do not** duplicate full DSR text in email inventory — **cross-link**. **[`docs/stories/vst-12.story.md`](vst-12.story.md)** — story-level AC context where cited by index. **[`docs/capstone-reference-stack-integration.md`](../capstone-reference-stack-integration.md)** — **FE.5.9** stack contrast (Nest / reference vs Vestroo); **email** may be thin today — **grep** before claiming coverage. **Reference code (traceability):** **`src/legacy/capstone-reference/backend-modules/auth/auth.service.ts`** — **`MailerService`** / **`sendMail`** with **`template: 'password-reset'`** (**verify** additional **`sendMail`** / **`@nestjs-modules/mailer`** usages under **`src/legacy/capstone-reference/`** — initial **grep** showed **one** callsite). **Shipped email path (verify, extend inventory):** **`src/services/email.ts`**, **`src/services/email-templates.ts`**, **`src/app/api/payfast/webhook/route.ts`** (booking confirmation send — **grep** **`sendBookingConfirmation`**). **`docs/index.md`** — Epic 8 + VST-13/VST-12 entry points. **`share/`** (Epic 8 narrative) — **verify** presence in this repo; if **absent**, document **“not vendored”** and rely on **`src/legacy/capstone-reference/`** + external capstone repos **TBD**. **`docs/capstone-backend-module-matrix.md`** — optional **Epic 8** cross-link once **INT.8.1** subsection exists (**grep** **`INT.8`** / **`email`** — **verify** no duplicate matrix file policy from prior epics).

**Explicit out of scope:** **INT.8.2** SMS policy/matrix, **INT.8.3** PayFast checkout/webhook **design** beyond what is needed to **cite** the **booking-paid → email** trigger and **idempotency** pointer (single cross-link to VST-13 webhook section). **INT.8.4** cron / scheduled jobs (named-owner automation) except **one** line if a trigger is **cron-like** **TBD**. **INT.8.5** **`share/`** secrets/JWT/Redis mapping as a **full** story (this story may **mention** **`share/`** only if inventory proves email helpers live there — **verify**). **No** requirement to **implement** new providers or templates unless a backlog row is later pulled into a product story — **INT.8.1** is **inventory + documentation parity**.

**Constraining NFRs:**

- **NFR.3.1:** **`RESEND_*`** and any future mail API keys remain **server-only**; **no** secrets in client bundles (**align** with **[`docs/environment-vars.md`](../environment-vars.md)** and **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** § security audit). Webhook-driven email MUST remain on **verified** PayFast ITN path (**signature** story stays in VST-13).
- **NFR.1.3:** Email provider **failure modes** and **operator** / **engineering** next steps MUST be **documented** for each **live** trigger (at minimum: **Resend** down / rate limit / invalid API key — **grep** **`src/services/email.ts`** retry and error strings; align prose with **local-dev** / **staging** expectations).
- **NFR.4.1:** Boundary types for payloads (**`BookingEmailData`**, template render inputs) stay **TypeScript**-narrowed — inventory MUST reference **types** + **tests** (`src/services/__tests__/email*.test.ts` — **verify** paths).

## Story

- As a **platform or engineering team member** owning **Epic 8** integration traceability
- I want a **grep-backed inventory** of reference **Nest Mailer / Handlebars** (and any other reference **email** sends) **mapped** to **Vestroo** templates (**Resend** + in-repo HTML/TS templates or **TBD** provider), **trigger sites** (**Server Action** / **`src/app/api/**/route.ts`** / **Supabase DB trigger** / **none**), and **status** (**implemented / stub / gap / N/A**), with **missing** rows as **prioritised backlog**
- so that **transactional email** behaviour is **auditable**, **staging vs production** is explicit, **PII** in messages follows **VST-12**, and we do **not** silently assume **capstone** template filenames exist in this repo

## Acceptance Criteria (ACs)

1. **Canonical doc home for INT.8.1:** Add **`## INT.8.1 — Email template parity (reference Mailer / Handlebars → Vestroo)`** (or equivalent title) with stable anchor **`#int-8-1`** to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** **unless** a **short** ADR or separate **`docs/email-*.md`** is justified in **Progress Notes** — default is **extend VST-13** so the **integration matrix** and email inventory stay co-located. Section MUST include a **compact matrix** (sortable-by-eye table is fine in Markdown) with columns at minimum: **Business event** (e.g. booking paid confirmation, password reset), **Reference template / trigger** (file:line or **Nest** controller/service — **grep**), **Vestroo template / renderer** (`src/services/email-templates.ts` **TBD** rows, **Resend** raw HTML **TBD**, etc.), **Trigger site** (**Route Handler** path, **Server Action** path, **DB trigger** migration name, **none**), **Env / secrets** (pointer to **`RESEND_*`** rows in **[`docs/environment-vars.md`](../environment-vars.md)**), **Staging vs prod** behaviour (**one** sub-row or footnote per provider), **Status** (**implemented / stub / gap / N/A**), **Backlog ID** (GitHub issue **TBD** or **Story TBD** — **honest** if not filed).

2. **Reference inventory — Mailer / templates:** **Grep** **`src/legacy/capstone-reference/`** for **`MailerService`**, **`sendMail`**, **`@nestjs-modules/mailer`**, **`template:`**, and **`.hbs`** (if **zero** `.hbs` in-repo, state **explicitly** — templates may live only in upstream capstone assets). Summarise **each** distinct **template name** and **call path** (e.g. **`password-reset`** from **`auth.service.ts`**). For **OTP**: reference stack may use **SMS** for OTP delivery (**grep** **`auth.service.ts`** / **OTP** module) — matrix row MUST **not** claim **email OTP** without evidence.

3. **Vestroo inventory — implemented sends:** **Grep** **`src/`** for **`sendBookingConfirmation`**, **`Resend`**, **`renderBooking`**, **`getResendClient`**, **`from:`** / **`RESEND_`**. Document **at least** the **PayFast ITN → booking paid → confirmation email** chain with **file:line** pointers (**[`src/app/api/payfast/webhook/route.ts`](../../src/app/api/payfast/webhook/route.ts)** and **`src/services/email.ts`** — **verify** line drift). Note **idempotency** interaction (**no duplicate email** on replayed **COMPLETE** — cite existing VST-13 bullet; **grep** webhook tests).

4. **PII / compliance (VST-12):** In the **INT.8.1** section, add a **short** checklist (bullet list) tying **email body fields** to **VST-12** themes: **minimisation** (only fields needed for the business event), **avoid** unnecessary duplication of **special-category** or **high-risk** data in **subject/preview** text, **retention** awareness (email copies **not** a second database — **operator** guidance **TBD** if product needs archive policy). **Link** **[`docs/compliance-and-safety.md`](../compliance-and-safety.md)** and, where useful, **[`docs/stories/vst-12.story.md`](vst-12.story.md)** — **no** new legal claims.

5. **Backlog rows for gaps:** Every **reference** business event that **lacks** a Vestroo row after inventory MUST appear as **gap** with **priority** (**P0/P1/P2** or **High/Medium/Low** — pick **one** scheme and define it in a **one-line** legend) and **suggested owner** (**product** / **platform** / **ops** — **TBD** allowed). Example class: **password-reset email** if reference **Mailer** exists but Vestroo uses **Supabase Auth** only — state **N/A** vs **gap** **honestly** after **grep** **`(auth)`** / **Supabase** reset flows (**verify**).

6. **NFR.1.3 failure modes — email slice:** Extend or add a **subsection** under **INT.8.1** (or tight pointer to **`src/services/email.ts`** behaviour doc) listing **operator-visible** symptoms and **engineering** mitigations for: missing **`RESEND_API_KEY`**, **429**/rate limit, **network** errors — aligned with **retry** logic **if present** (**grep** **`RETRY_CONFIG`**, **`isRetryableError`**). **Staging clarity:** what happens in **dev** when keys are unset (**silent skip** vs **logged error** — **verify** code, **do not** invent).

7. **NFR.4.1 — TypeScript boundaries:** Matrix or footnote MUST cite **`BookingEmailData`** / **`EmailResult`** (or successors) from **`src/services/email.ts`** and template render exports from **`src/services/email-templates.ts`**; note **tests** under **`src/services/__tests__/`** as **verification** harness (**grep** **`email-templates.test`**).

8. **Index / Related links:** Update **[`docs/index.md`](../index.md)** or **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** *Related* / cross-links so **Epic 8** / **INT.8.1** discovery leads to **`#int-8-1`** (**thin** edit — **no** wholesale index restructure). If **`docs/capstone-backend-module-matrix.md`** already lists **Epic 8**, add **one** bullet for **INT.8.1** + anchor (**verify** matrix § **Epic 8** exists — if **not**, skip or add minimal **Related** line only after PM agrees matrix scope).

## Tasks / Subtasks

- [x] **Task 1 — AC1, AC8:** Add **`#int-8-1`** subsection to **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** with matrix skeleton + staging/prod column; patch **Related** / **[`docs/index.md`](../index.md)** cross-links; optional matrix **Epic 8** bullet after **grep**. (AC: #1, #8)

- [x] **Task 2 — AC2:** **Grep** **`src/legacy/capstone-reference/`** for Mailer / template usage; record **template names**, **callsites**, **`.hbs`** presence or absence. (AC: #2)

- [x] **Task 3 — AC3, AC7:** **Grep** **`src/`** for Resend / booking confirmation; document **trigger chain** with **types** + **tests** pointers. (AC: #3, #7)

- [x] **Task 4 — AC4:** Author **VST-12** minimisation / PII bullets for transactional email; link **compliance** docs. (AC: #4)

- [x] **Task 5 — AC5:** Fill **gap** rows + **priority** legend; include **password-reset** vs **Supabase Auth** **verify** note. (AC: #5)

- [x] **Task 6 — AC6:** Document **failure modes** + **dev/staging** behaviour for **Resend** path; align with **`email.ts`** implementation (**grep**). (AC: #6)

- [x] **Task 7 — AC1 housekeeping:** Bump **“Last updated”** / changelog-style footer in **`integrations-and-payments.md`** if that file uses one (**grep** **`Last updated`**); else add **single** line at end of **INT.8.1** section: **Story 8.1** traceability. (AC: #1)

## Dev Technical Guidance

### Primary sources

- **Epic requirement:** [`docs/epic-8.md`](../epic-8.md) — **INT.8.1**
- **Integration + env:** [`docs/integrations-and-payments.md`](../integrations-and-payments.md), [`docs/environment-vars.md`](../environment-vars.md)
- **Compliance:** [`docs/compliance-and-safety.md`](../compliance-and-safety.md) (**VST-12**), [`docs/stories/vst-12.story.md`](vst-12.story.md)
- **Stack contrast (grep first):** [`docs/capstone-reference-stack-integration.md`](../capstone-reference-stack-integration.md)
- **Shipped implementation:** `src/services/email.ts`, `src/services/email-templates.ts`, `src/app/api/payfast/webhook/route.ts`
- **Reference Mailer sample:** `src/legacy/capstone-reference/backend-modules/auth/auth.service.ts`

### How to inventory (reference vs Vestroo)

1. **Reference first:** automate discovery with **`sendMail`**, **`MailerService`**, **`template:`**, **`hbs`**, **`@nestjs-modules/mailer`** under **`src/legacy/capstone-reference/`**. Treat **OTP** as **channel-specific** — confirm **SMS vs email** per file before mapping.
2. **Vestroo second:** **`Resend`** client creation, **`emails.send`**, **React/HTML render** functions, and **only** production code under **`src/app/`**, **`src/actions/`**, **`supabase/migrations/`** (for **DB-triggered** mail — **likely none**; **grep** **`pg_net`**, **`http`**, **`cron`**, **`edge`** — **TBD**).
3. **Map by business event, not provider name** (Epic **design goals**): e.g. “**Booking payment confirmed**” → **Vestroo** **Resend** + template **`renderBookingConfirmationEmail`** (**verify**) vs **reference** **TBD** additional events.
4. **Staging clarity:** reuse VST-13 per-environment table language; add **email-specific** footnotes (sandbox domain, skip send, log-only **TBD**).

## Testing / verification

- **Relative links:** From **`docs/stories/8.1.story.md`**, click-test **`../epic-8.md`**, **`../integrations-and-payments.md#int-8-1`**, **`../environment-vars.md`**, **`../compliance-and-safety.md`**, **`vst-12.story.md`**, **`../capstone-reference-stack-integration.md`**, **`../index.md`**.
- **Grep bundle:** `MailerService`, `sendMail`, `template:`, `@nestjs-modules/mailer`, `Resend`, `sendBookingConfirmation`, `renderBooking`, `RESEND_`, `password-reset`, `otp` (narrow to **`auth.service`** when classifying **channel**).
- **Consistency:** After edits, re-read **INT.8.1** matrix against **`email.ts`** + webhook route — **no** row claiming **implemented** without a **callsites** line.

## Story draft self-check

_Checklist inspiration: `bmad-agent/checklists/story-draft-checklist.md`_

| Category | Status | Notes |
| -------- | ------ | ----- |
| 1. Goal & Context Clarity | PASS | **INT.8.1** scoped; **Epic 8** outcomes; **VST-12** / **NFR** called out |
| 2. Technical Implementation Guidance | PASS | **Resend** paths, **grep** plan, **webhook** idempotency pointer |
| 3. Reference Effectiveness | PASS | Known **`auth.service`** Mailer path; **`.hbs`** / **`share/`** marked **verify** |
| 4. Self-Containment Assessment | PASS | **Out of scope** for **8.2–8.5**; **no** mandatory new provider |
| 5. Testing Guidance | PASS | Link check + **grep** bundle |

**Final Assessment: READY** — Developer agent can add **`#int-8-1`**, complete **grep-backed** rows, **VST-12** minimisation bullets, **failure modes**, and **backlog** priorities **without** implementing **SMS**, **PayFast** scope beyond **email trigger**, or **cron** automation.

## Story Progress Notes

### Agent Model Used:

Story preparation (**sm** subagent) — **documentation requirements only** (**no** product code changes in this step).

### Completion Notes List

- **INT.8.1** canonical home: **[`docs/integrations-and-payments.md`](../integrations-and-payments.md)** (VST-13), anchor **`#int-8-1`**.
- **`docs/index.md`** / VST-13 **Related** cross-links point to the INT.8.1 section.
- **`share/`** traceability: not vendored at repo root; inventory uses **`src/legacy/capstone-reference/`** only; upstream **`share/`** email helpers documented as out of scope unless vendored later.
- **`docs/epic-8.md`** links to the canonical inventory **[`integrations-and-payments.md#int-8-1`](../integrations-and-payments.md#int-8-1)**; thin **FE.5.9** stack link from INT.8.1 to **`capstone-reference-stack-integration.md`**.

### Change Log

| Date | Change |
| ---- | ------ |
| 2026-04-14 | **Ready** story file created: **INT.8.1** email template parity inventory — **Mailer/Handlebars** reference vs **Resend** / **`email-templates`**, triggers (**Route Handler** / actions / DB), **VST-12** PII, **NFR.3.1 / 1.3 / 4.1**, **`#int-8-1`** in **VST-13** default home. |
