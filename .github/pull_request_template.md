## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] **Migrations:** Schema changes include matching files under `supabase/migrations/` (or a linked prior PR).
- [ ] **Secrets:** No real API keys, `.env`, or `.env.local` committed; placeholders only in `.env.example` where needed.
- [ ] **Docs:** Updated developer docs if behaviour, env vars, or setup changed.
- [ ] **Checks:** `npm run lint`, `npm run test`, and `npm run build` pass locally (see `CONTRIBUTING.md`).

See `CONTRIBUTING.md` and `docs/staging-and-promotion.md` for branching and promotion expectations.
